# Codex prompts

Use the relevant subbundle as the source of truth for each feature. The prompts below assume the work happens inside the uploaded CanDoItAll solution and that any AgentFramework paths are treated as reference seams only unless the feature explicitly says otherwise.

## PRM-F01 — Process module foundation and shell integration

Implement feature PRM-F01 (Process module foundation and shell integration) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F01-process-module-foundation-and-shell-integration` as the source of truth. Respect dependencies: none. Primary acceptance goals: A top-level /processes route exists and renders without breaking current shell navigation.; A project-scoped route exists for process work and can be opened from project UX.; The module is registered through the same composition pattern as existing CanDoItAll modules.; SQLite and PostgreSQL migrations compile with the new module present.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F02 — Process definition language and versioning

Implement feature PRM-F02 (Process definition language and versioning) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F02-process-definition-language-and-versioning` as the source of truth. Respect dependencies: PRM-F01. Primary acceptance goals: Process definitions support draft, published, and archived versions.; Published versions are immutable and draft edits produce a new working version.; Definitions can be scoped as workspace templates or project-owned processes.; The canonical graph is stored outside Workbench metadata.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F03 — Actor roles, responsibilities, and CRM-HR bindings

Implement feature PRM-F03 (Actor roles, responsibilities, and CRM-HR bindings) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F03-actor-roles-responsibilities-and-crmhr-bindings` as the source of truth. Respect dependencies: PRM-F01, PRM-F02. Primary acceptance goals: A process step can reference responsible, consulted, approver, and observer roles.; Roles can bind to CRM-HR parties and AI-agent profiles without duplicating durable identity.; Actor rebinding preserves auditability of earlier runs.; Future AI execution metadata can be attached without introducing a runtime dependency on AgentFramework.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F16 — Role and agent templates, staffing briefs, and sourcing handoffs

Implement feature PRM-F16 (Role and agent templates, staffing briefs, and sourcing handoffs) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F16-role-and-agent-templates-staffing-briefs-and-sourcing-handoffs` as the source of truth. Respect dependencies: PRM-F03. Primary acceptance goals: A process actor role can optionally reference a reusable manager-approved role/agent template instead of free-text only.; Templates capture modality, required skills/capabilities, allocation intent, and fallback/supervisory expectations.; HR can open staffing, recruiting, or AI-agent sourcing work from unresolved process role gaps without losing process context.; Role/agent templates support governed lifecycle states (draft/active/retired) without rewriting history.; Published process versions snapshot the selected template version and key requirement summary.; Runs snapshot the resolved assignee, eligible pool/fallback metadata, and rebind reasons.; AI-oriented templates still resolve through CRM-HR identities and future execution bridges rather than direct runtime coupling.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F04 — Step contracts, inputs, outputs, and evidence

Implement feature PRM-F04 (Step contracts, inputs, outputs, and evidence) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F04-step-contracts-inputs-outputs-and-evidence` as the source of truth. Respect dependencies: PRM-F02, PRM-F03. Primary acceptance goals: Each step can declare entry criteria, exit criteria, expected artifacts, and evidence requirements.; Steps can declare reusable input and output contracts with type, cardinality, and notes.; Reviewers can see required evidence before completion is allowed.; Contract data is queryable separately from the diagram layout.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F17 — Process ownership, interfaces, customer, and value alignment

Implement feature PRM-F17 (Process ownership, interfaces, customer, and value alignment) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F17-process-ownership-interfaces-and-value-alignment` as the source of truth. Respect dependencies: PRM-F02, PRM-F03, PRM-F04. Primary acceptance goals: A process definition cannot be published without a process owner, primary customer, criticality tier, and value statement.; Process definitions can declare sponsor, stewarding managers, strategic objective links, and upstream/downstream interface contracts.; Interface contracts capture sender, receiver, required inputs/outputs, definition of done, and handoff expectation metadata.; Actor assignments remain separate from org hierarchy; the model does not force the process graph to mirror reporting lines.; Shared-project or shared-library processes preserve explicit ownership instead of being duplicated as shadow copies.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F09 — Canvas modeler and interactive diagrams

Implement feature PRM-F09 (Canvas modeler and interactive diagrams) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F09-canvas-modeler-and-interactive-diagrams` as the source of truth. Respect dependencies: PRM-F01, PRM-F02, PRM-F03. Primary acceptance goals: Users can create and edit process nodes and transitions from an interactive canvas surface.; Diagram layout persists independently from the canonical graph semantics.; Phase grouping and actor grouping can be represented without forcing Workbench to be the source of truth.; Wave 1 designer delivery is not blocked by later handoff-label chrome; handoff visuals can deepen in Wave 2.; The design leaves room for labeled transitions and swimlane extensions where CanvasLib needs them.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F15 — Storage, migrations, and performance hardening

Implement feature PRM-F15 (Storage, migrations, and performance hardening) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F15-storage-migrations-and-performance-hardening` as the source of truth. Respect dependencies: PRM-F01, PRM-F02, PRM-F07, PRM-F08. Primary acceptance goals: Process tables live in the main app database with consistent naming and indexing conventions.; SQLite remains supported for local users without extra setup.; PostgreSQL migrations exist and stay in lockstep with SQLite.; The journal and runtime tables have a defined retention/extraction seam for future scale.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F05 — Transition rules, decisions, and explicit handoffs

Implement feature PRM-F05 (Transition rules, decisions, and explicit handoffs) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F05-transition-rules-decisions-and-explicit-handoffs` as the source of truth. Respect dependencies: PRM-F02, PRM-F03, PRM-F04. Primary acceptance goals: Decision paths can carry condition text, default-path markers, and branch priority.; Handoffs record source actor, target actor, payload summary, and completion reason.; The engine rejects invalid graphs such as unreachable end states or orphaned transitions.; Sequential specialized handoffs are first-class even before AgentFramework runtime integration.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F06 — Approval policies, escalations, and governance gates

Implement feature PRM-F06 (Approval policies, escalations, and governance gates) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F06-approval-policies-escalations-and-governance-gates` as the source of truth. Respect dependencies: PRM-F03, PRM-F04, PRM-F05. Primary acceptance goals: A process run can pause awaiting approval and resume with an auditable decision.; Escalation routes can target a human party or supervisory role.; Policy metadata is explicit and not hidden inside runtime-only configuration.; Approval policies can prevent self-approval or conflicting role combinations unless an explicit override path is configured.; The model can later map to agent external-call approvals and collaboration rights.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F07 — Runtime execution state machine and assignments

Implement feature PRM-F07 (Runtime execution state machine and assignments) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F07-runtime-execution-state-machine-and-assignments` as the source of truth. Respect dependencies: PRM-F02, PRM-F03, PRM-F04, PRM-F05, PRM-F06. Primary acceptance goals: A process run can start from a published definition version and keep that version immutable for the run lifetime.; Only valid state transitions are allowed for runs and steps.; Conflicting claims and double completions are rejected deterministically.; Assignment resolution can consider eligible pools, capacity/validation state, and fallback routes before work is claimed or rebound.; Manual, human-approved, and AI-backed executors all fit the same state machine.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F08 — Execution timeline, audit journal, and replay

Implement feature PRM-F08 (Execution timeline, audit journal, and replay) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F08-execution-timeline-audit-journal-and-replay` as the source of truth. Respect dependencies: PRM-F07. Primary acceptance goals: Every run change emits a durable process event with actor and reason metadata.; High-level process events appear on the shared activity stream.; A replay API can reconstruct step order and handoff decisions from journaled events.; Journal writes are separated from mutable current-state rows.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F18 — Variants, exceptions, input quality, and decision rights

Implement feature PRM-F18 (Variants, exceptions, input quality, and decision rights) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F18-variants-exceptions-input-quality-and-decision-rights` as the source of truth. Respect dependencies: PRM-F04, PRM-F05, PRM-F06, PRM-F07, PRM-F17. Primary acceptance goals: Steps can define mandatory inputs, completeness checks, and structured rejection/rework reasons before execution continues.; The model distinguishes normal path, approved variant, and exception path metadata with escalation or override requirements.; Decision rights are explicit: who can decide, under what threshold or rule, with what evidence, and through which override route.; Controls can be tagged as mandatory, conditional, or optional based on risk tier so low-risk work is not over-approved.; Runtime journals capture exception reasons, overrides, and input-quality failures separately from generic failure states.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F22 — Process-native work briefs, baton handoffs, and governed triage routing

Implement feature PRM-F22 (Process-native work briefs, baton handoffs, and governed triage routing) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F22-process-native-work-briefs-baton-handoffs-and-governed-triage` as the source of truth. Respect dependencies: PRM-F04, PRM-F05, PRM-F07, PRM-F16, PRM-F17. Primary acceptance goals: Each executable step can materialize a normalized work brief from process, step, template, customer, and governance context.; Baton handoffs are persisted as first-class runtime artifacts with source role, target role, brief snapshot, and completion context.; Triage or dispatcher behavior is modeled as a process role, step, or governed routing decision record rather than hidden out-of-band agent topology.; Direct production agent-to-agent wiring outside the process requires an explicit override path with journal evidence.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F12 — Import/export, templates, Mermaid, and prompt-flow seeding

Implement feature PRM-F12 (Import/export, templates, Mermaid, and prompt-flow seeding) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F12-import-export-templates-mermaid-and-promptflow-seeding` as the source of truth. Respect dependencies: PRM-F02, PRM-F09. Primary acceptance goals: Mermaid mindmap and flowchart can be imported into a draft process with explicit limitations recorded.; Published processes can be exported as Mermaid and JSON packages.; Starter templates can reference prompt-flow patterns without making Prompt Factory the canonical process store.; Import warnings are explicit whenever semantics do not round-trip perfectly.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F10 — Project, Workbench, and shell projections

Implement feature PRM-F10 (Project, Workbench, and shell projections) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F10-project-workbench-and-shell-projections` as the source of truth. Respect dependencies: PRM-F01, PRM-F02, PRM-F07, PRM-F09. Primary acceptance goals: Project surfaces expose a clear entry point into processes.; If Workbench projection is enabled, it shows references and summaries rather than acting as the canonical store.; Process-related project object types and routes remain explicit and typed.; Shared-project processes are navigated through project ownership rather than duplicated shadow copies.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F11 — Activity, Automation, Validation, and TestLab hooks

Implement feature PRM-F11 (Activity, Automation, Validation, and TestLab hooks) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F11-activity-automation-validation-and-testlab-hooks` as the source of truth. Respect dependencies: PRM-F04, PRM-F06, PRM-F07, PRM-F08. Primary acceptance goals: Runs can emit activity entries and automation signals without tight module coupling.; Validation and TestLab references can be attached to steps and gates.; Overdue steps and blocked approvals become visible in automation/operations surfaces.; The hook design does not require the intelligence lake to exist first.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F13 — Future AgentFramework adapter and AI executor seam

Implement feature PRM-F13 (Future AgentFramework adapter and AI executor seam) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F13-future-agentframework-adapter-and-ai-executor-seam` as the source of truth. Respect dependencies: PRM-F03, PRM-F05, PRM-F06, PRM-F07. Primary acceptance goals: The process runtime can distinguish manual, AI, and hybrid executor modes.; The process module compiles and works without referencing AgentFramework projects.; A bridge contract exists for future AI execution and handoff orchestration adapters.; CRM-HR remains the durable owner of AI agent identity and staffing.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F24 — Live process execution canvas overlays and baton visibility

Implement feature PRM-F24 (Live process execution canvas overlays and baton visibility) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F24-live-process-execution-canvas-overlays-and-baton-visibility` as the source of truth. Respect dependencies: PRM-F07, PRM-F08, PRM-F09, PRM-F22. Primary acceptance goals: A live run can be viewed on the authored process canvas with active, waiting, blocked, and completed step overlays.; Canvas overlays show current assignee or executor, wait reason, approval state, and last baton movement where relevant.; Timeline and canvas views link to the same underlying run and journal without duplicate state mutation paths.; Runtime overlay projection is explicitly separated from canonical definition data and mutable runtime state.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F23 — AgentFramework convergence: process-bound context, rights, and shared registries

Implement feature PRM-F23 (AgentFramework convergence: process-bound context, rights, and shared registries) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F23-agentframework-convergence-process-bound-context-and-shared-registries` as the source of truth. Respect dependencies: PRM-F13, PRM-F16, PRM-F22. Primary acceptance goals: Future external executor correlations can link ProcessRun, ProcessStepRun, and assignment records to runtime session, log, and metric identifiers.; CRM-HR remains canonical for business role and agent templates plus durable AI identities even if runtime-level templates exist elsewhere.; Shared provider and capability ownership is explicitly converged so the process bridge does not introduce a second canonical registry.; Process step governance can narrow or require approvals for future AgentFramework permissions and external-call behavior.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F20 — Change governance, prioritization, literacy, and management adoption

Implement feature PRM-F20 (Change governance, prioritization, literacy, and management adoption) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F20-change-governance-prioritization-literacy-and-management-adoption` as the source of truth. Respect dependencies: PRM-F02, PRM-F06, PRM-F10, PRM-F17. Primary acceptance goals: Change proposals capture reason, impacted processes and roles, expected outcomes, risk, and rollout plan.; Publish, retire, and critical-change operations can require governance approval based on criticality and impact.; Affected owners, stewards, approvers, and participants receive communication and acknowledgement tasks when governed versions change.; The process portfolio can classify criticality and prioritization tiers so not every process is modeled to the same depth.; UI surfaces provide role-based guidance and glossary/help so middle management and operators can understand the process model.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F19 — Outcome metrics, capacity, wait-state telemetry, and customer-value measures

Implement feature PRM-F19 (Outcome metrics, capacity, wait-state telemetry, and customer-value measures) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F19-outcome-metrics-capacity-and-wait-state-telemetry` as the source of truth. Respect dependencies: PRM-F07, PRM-F08, PRM-F11, PRM-F17, PRM-F18. Primary acceptance goals: The runtime distinguishes active work time, waiting time, approval wait, blocked time, and rework loops.; Metrics include lead time, touch time, queue time, first-time-right, rework rate, bottleneck steps, capacity load, and SLA attainment.; Dashboards can segment by process, owner, customer, project, interface, and criticality tier.; Raw activity counters are not presented as success KPIs without outcome context.; Customer or internal-customer feedback signals can be attached to completed runs or outputs.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F14 — Operational intelligence, improvement backlog, and training-opportunity loop

Implement feature PRM-F14 (Operational intelligence, improvement backlog, and training-opportunity loop) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F14-kpis-improvement-backlog-and-training-opportunity-loop` as the source of truth. Respect dependencies: PRM-F08, PRM-F19, PRM-F21. Primary acceptance goals: The module can turn outcome telemetry and conformance signals into process-level improvement candidates.; Improvement requests are separated from live execution state and can be routed to owner/governance review.; Training-opportunity markers can be generated without contaminating normal execution queries.; The design remains compatible with a later intelligence-lake layer.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.

## PRM-F21 — Conformance, field observation, and reality-alignment reviews

Implement feature PRM-F21 (Conformance, field observation, and reality-alignment reviews) in the CanDoItAll repository. Use subbundle `03-subbundles/PRM-F21-conformance-field-observation-and-reality-alignment` as the source of truth. Respect dependencies: PRM-F08, PRM-F11, PRM-F19, PRM-F20. Primary acceptance goals: Reviewers can record conformance observations against runs or process versions with structured deviation reasons.; The system can cluster repeated unofficial loops, extra handoffs, and bypass patterns from journals for owner review.; Observation notes support restricted visibility and privacy-safe governance handling; there is no unmanaged rumor registry.; Process owners can convert deviation clusters into approved variants, fixes, or policy-breach investigations.; Conformance reporting can show paper-versus-reality deltas by step, interface, owner, customer segment, or project.. Keep comments in English, maintain SQLite/PostgreSQL parity where persistence changes occur, and add tests described in the verification plan.
