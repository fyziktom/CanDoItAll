# Implementation prompt

```text
Implement only the selected subbundle.

Respect these non-negotiables:
- keep the universal WebGL library free of Processes references,
- keep the frame loop, hit-testing, and preview interactions in JavaScript,
- keep labels and automation discoverability in a DOM/HTML mirror layer,
- keep all sandbox edits in-memory only,
- use real process-template projection for representative scenarios,
- stop immediately if a review gate or stop rule fails,
- update tests and proof artifacts exactly as the subbundle requires,
- do not widen scope into production `ProcessWorkspace` replacement.
```
