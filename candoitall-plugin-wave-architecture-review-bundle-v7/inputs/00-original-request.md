The user asked for a detailed re-review after the phase6 refactor and requested a new execution-grade bundle if anything important is still insufficient. If the same problems keep repeating, the bundle structure must be improved so Codex is forced to actually resolve them.

The user also clarified several architectural intent constraints:
- node should remain the universal carrier
- brainstorming often starts as simple notes and later becomes richer blocks
- stable node identity and historical evolution matter
- node X/Y and markers are canonical semantics, not just cosmetic canvas state
- the next big wave is plugin-heavy: email, LinkedIn, and custom APIs
