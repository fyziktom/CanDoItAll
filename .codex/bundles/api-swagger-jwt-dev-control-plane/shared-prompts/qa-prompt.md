# QA Prompt

Validate the selected subbundle against its acceptance checklist and proof rules.

- Verify JWT disabled/enabled behavior separately.
- Verify OpenAPI/Swagger metadata is reachable.
- Verify process run detail filtering returns focused data.
- Verify Settings API access UI only issues tokens when authorization is active.
- For UI proof, inspect screenshots for readability, clipping, overlap, layout consistency, and alignment with the existing Settings page.
