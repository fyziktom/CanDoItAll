# QA Prompt

Validate that every default workflow example is file-backed, YAML parsed, converted into a valid `WorkflowDefinition`, and seeded without compiled graph builders. Confirm tests exercise all shipped templates and that failures include actionable file/key context.
