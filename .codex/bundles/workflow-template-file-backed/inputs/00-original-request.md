# Original Request

The user asked to use the `candoitall-bundle-workflow` skill to improve CanDoItAll's workflow template system.

Raw request:

> Improve our system of templates of workflows. MAF has system of how to load/store them with use of yaml. Source of MAF for references: `C:\repositories\agent-framework\dotnet\samples\03-workflows`. We must have those default workflow templates as some file and not compiled in code. Later we can provide some workflows catalogue, people can share workflows, etc. But they must be as text files. Analyze how to do it, prepare bundle, and then implement it and validate it.
