# Original Request

The user backed up the current branch and asked for cleanup:

1. Analyze the ProjectStructure and Processes MCP servers for API gaps. Preserve their agent/tool instructions in the bundle and later add that guidance to skills for the new API.
2. Remove the actual ProjectStructure and Processes MCP servers from the solution and code.
3. Fix `/tools/` MCP reinstall scripts and repo skills so those two MCPs are no longer installed or used. Remove them from this Codex user's `config.toml`.
4. Remove settings for those MCPs from the UI.
5. Create and install skills explaining how to use the new APIs to create, control, and edit projects, processes, and agents. Include those skills in the repo and reinstall flow so other machines receive them.

The user explicitly requested the `candoitall-bundle-workflow` skill for this cleanup.
