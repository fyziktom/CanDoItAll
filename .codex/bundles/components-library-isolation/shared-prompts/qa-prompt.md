# QA Prompt

Use this prompt for validation:

```text
Review the implementation against the raw inputs and normalized requirements. Confirm every requested behavior has proof, every browser-visible change has browser evidence or a blocker, every subbundle gate is updated, and every residual risk has an explicit follow-up or blocker. Specifically audit moved source absence, package presence, project-reference removal, Tailwind output links, slnx membership, and package README/version metadata.
```
