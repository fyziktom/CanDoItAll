SB06 analytics UI failing-first evidence

Initial command:
dotnet test tests/Components/CanDoItAll.Tests.Components/CanDoItAll.Tests.Components.csproj --no-restore -p:BuildProjectReferences=false --filter "FullyQualifiedName~WorkflowAnalyticsPanelTests" --logger "console;verbosity=minimal"

Initial result:
- Failed because `WorkflowAnalyticsPanel` did not exist.
- Concurrent executor-schema test drift also appeared during that run and was left to its owning slice; no trust/renderer files were modified by the analytics slice.

First implementation result:
- Passed 2, failed 3.
- Aggregate rendering and the no-event-parsing source gate passed.
- The three failures proved that BaseLib `EmptyState` and `LoadingState` reject arbitrary `data-testid` parameters.
- Test identifiers were moved to BaseLib `Stack` wrappers instead of adding raw markup or changing shared components.

The final command passes all five focused tests.
