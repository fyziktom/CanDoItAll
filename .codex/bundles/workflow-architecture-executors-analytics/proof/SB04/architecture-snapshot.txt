Runtime lifecycle ownership snapshot

- Workflows.Abstractions owns typed cancellation/response results, the optional resume port, and mandatory atomic store contracts.
- Workflows.Runtime owns transition coordination, TimeProvider use, safe incremental progress, and the singleton active-run registry.
- MafAdapter advertises external-request production, external-response resume, and cancellation independently. InProcess produces requests and supports active cancellation but does not claim resume.
- InMemoryWorkflowRunStore implements atomic behavior under one mutation lock.
- PersistentWorkflowRunStore implements initial creation and terminal event persistence in transactions, conditional state transitions with ExecuteUpdateAsync, and exactly-once response acceptance with a conditional update.
- Existing UI/API callers retain compile compatibility through legacy adapters while typed methods expose honest outcomes for migrated callers.

No runtime dependency was added from Workflows.Runtime to application, module, process, plugin, or web projects.

Caller migration ownership snapshot

- Workflows.Core owns authoritative definition selection, validation, runtime-policy resolution, input normalization, and runtime launch behind IWorkflowLaunchService.
- WorkflowsApi owns only HTTP authorization/origin construction and typed result-to-status mapping. Its focused run-control route mapper isolates the production boundary for integration testing.
- WorkflowTestRunner owns preview request orchestration and result projection; it no longer validates and starts independently.
- SchedulerPlanner owns persisted plan-run scheduling context and retry-stable idempotency construction; it no longer resolves workflow definitions or calls StartAsync directly.
- Workbench owns project/node/agent/session authorization context. ProjectStructureWorkflowLaunchIntentFactory is directly testable, and ProjectStructureWorkflowNodeService no longer fabricates a Running status before an authoritative run exists.
- Scoped CodeAnalytics snapshot: snap-20260712193548-7d7ce356. Six projects and 238 documents were analyzed; no project reference or dependency-cycle change was introduced by this slice.
