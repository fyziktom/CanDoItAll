# QA Prompt

Review the docs refresh for:

- stale claims that instruct users to install or call removed `candoitall_processes` or `candoitall_projectstructure` MCP servers
- Mermaid blocks that use `architecture-beta` or complex labels likely to fail on GitHub
- customer-facing statements that overclaim the external `CanDoItAll.Economy` work
- missing image files or broken local image links
- bundle rows left pending at final closure

Required proof: prepared/completed bundle validators, `git diff --check`, stale-reference searches, and confirmation that all four infographic assets are under `docs/images`.
