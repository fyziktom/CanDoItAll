# Implementation Prompt

Implement the current subbundle only.

Use the repository as source of truth. Keep documentation direct, current, and explicit about boundaries. Do not describe removed Processes or ProjectStructure MCP servers as active. Use the HTTP API and repo-managed API skills as the current replacement path. For Mermaid, use simple GitHub-safe diagram types and keep complex wording outside labels.

After changes, update `.codex/bundles/docs-enterprise-refresh/reviews/01-execution-report.md` with proof and gate status before moving to the next subbundle.
