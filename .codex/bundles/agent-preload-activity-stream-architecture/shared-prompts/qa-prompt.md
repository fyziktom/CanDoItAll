# QA Prompt

Review the named subbundle against its observable success criteria, architecture contract, exact covered requirements, and progression gate.

Verify prerequisite proof, source/test wiring, dependency direction, DI lifecycle, canonical-state ownership, failure/cancellation/disposal behavior, and required semantic/adversarial tests. Reject placeholder, no-op, string-routed, mutable-snapshot, live-resource-pool, shared-DbContext-parallelism, selected-run-only UI, or mocked-zero-delay performance proof.

For UI work, replay the logged Playwright actions at the named large desktop viewport, inspect normal and relevant overlay/busy/error/approval screenshots, and record scroll owner, first-viewport, clipping, stale-status, accessibility, and console findings. For closure, verify SharedInfo alignment, the explicit `gpt-5.4-mini` run, builds/tests/validators, architecture snapshots, and rebuilt port-5032 health.

Update the execution report and raw-note closure only for evidence actually observed. Mark the gate blocked and reopen the earliest owning subbundle when proof is incomplete or contradictory.
