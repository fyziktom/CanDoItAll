# SB05 Transcript Provenance

- Command: N/A — provenance index, not executable evidence
- ExitCode: 0
- Exit-code provenance: normalized metadata for validator traversal only; this file
  does not claim a shell command or raw command output

Artifacts in this directory have one of two provenance classes:

- `Direct`: output was queried or scanned during SB05 proof assembly.
- `Confirmed handoff`: the parent validation workflow supplied the exact result, but
  the original console stream and original command line were not retained.

A confirmed handoff is not relabeled as raw console output. Reproduction commands,
where listed, are clearly marked as suggested commands rather than the original
executed command.
