Run label: SB01 changed-test anti-stub and no-provider audit
Working directory: C:\repositories\CanDoItAll
Invariant IDs: SB01-INV-04
Command:
$paths = @('tests\Integration\CanDoItAll.Tests.Integration\AgentFrameworkExecutionRunTrackingIntegrationTests.cs', 'tests\Unit\CanDoItAll.Tests.Unit\FloatingAgentChatArchitectureTests.cs'); $patterns = @('TODO', 'NotImplementedException', 'fixture-specific', 'template-only'); $matches = Select-String -Path $paths -Pattern $patterns -SimpleMatch; if ($matches) { $matches | ForEach-Object { '{0}:{1}: {2}' -f $_.Path, $_.LineNumber, $_.Line.Trim() }; exit 1 }; 'No TODO, NotImplementedException, fixture-specific, or template-only markers found in the SB01 changed test paths.'

Exit code: 0
Output:
No TODO, NotImplementedException, fixture-specific, or template-only markers found in the SB01 changed test paths.

Source assertions:
- No production source file changed in SB01.
- The startup harness decorates the registered real `FileSandboxWorkspaceStore` and registered real `IProviderProfileRegistry`.
- Only `IAgentRuntime` and `IAgentExecutionEventSink` are replaced to prevent paid/provider work and establish deterministic pre-runtime barriers.
- `StartupBarrierAgentRuntime.TestProviderAsync`, `RunProviderTestChatAsync`, and `CreateOrUpdateProviderModelAsync` throw `NotSupportedException`; the final five-case rerun proves none was invoked.
- `StartupBarrierAgentRuntime.RunAsync` blocks at runtime entry and returns only after the test releases it; it is not used to claim provider latency.
- The existing `StubAgentReferenceDataProvider` is an explicit deterministic unit-test fixture, not production behavior and not a completion shortcut.
- Preparation-warm rows invoke the production `AgentChatPreparationPool` through `IAgentChatPreparationPool`.

Result: Pass.
