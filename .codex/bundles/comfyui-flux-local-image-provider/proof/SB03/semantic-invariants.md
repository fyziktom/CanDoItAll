# SB03 Semantic Invariants

## Invariant SB03-PROJECT-STRUCTURE-ASSET

- Invariant ID: `SB03-PROJECT-STRUCTURE-ASSET`
- Source raw notes: `N004` and `N009`.
- Expected behavior: project-structure image generation selects the seeded local ComfyUI Flux image provider, generates bytes through `IAgentImageGenerationService`, creates an `ImageAsset`, and reads back non-empty `image/png` content with the same SHA-256 hash.
- Disallowed shallow implementation: uploading fixture bytes, direct ComfyUI calls that bypass `IAgentImageGenerationService`, a provider health-only check, or metadata-only proof without content readback.
- Failing-first test: `bundle://proof/SB03/transcripts/failing-first-project-structure-proof.txt` records that pre-change tests did not prove live ComfyUI Flux project-structure asset storage.
- Passing test: `bundle://proof/SB03/transcripts/project-structure-comfyui-image-asset.txt` runs the opt-in live integration test with `CANDOITALL_RUN_LIVE_COMFYUI_FLUX_PROOF=1`.
- Content proof: `bundle://proof/SB03/transcripts/asset-content-readback.txt` recomputes the PNG hash and verifies the proof summary length/hash.
- Changed source files: hashes are recorded in `bundle://proof/SB03/manifest.md`.
- Production assertions: the project-structure page remains behind `IAgentImageGenerationService`; the proof test exercises that runtime service and project-structure asset readback.
- Red-team negative case: a manual storage-only proof fails this invariant because the readback summary must show `ProviderName: Local ComfyUI Flux`, model `flux1-dev.safetensors`, and hash-matched generated bytes.
- Downstream dependency check: final bundle closure may proceed because SB01, SB02, and SB03 are complete.
