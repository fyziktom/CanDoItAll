# Requirement Traceability

| Input or requirement | Owning subbundle | Delivered behavior | Behavioral proof | Closure |
| --- | --- | --- | --- | --- |
| `N001`, `R001`, `R002` mixed assignments open | `SB01`, `SB02` | A Person + Agent direct-assignment set opens in Gantt/canvas. Its direct-assignment selector is read-only; ordinary fields remain editable and the full set is retained. | Focused Project Structure suite `107/107`; `ProjectStructureTaskApplicationServiceTests` proves mixed scalar edit succeeds and direct assignment change is rejected; PostgreSQL-backed browser dialog inspected. | `Solved` |
| `N002`, `R004`, `R006` CRM price on create/update | `SB02` | New and explicit `NotStarted` task mutations apply the authoritative person CRM quote; `Unknown` and historical states preserve their stored price. | Application/CRM revision tests `6/6`; details tests `8/8`; unit policy/metadata suite `48/48`. | `Solved` |
| `N003`, `R003` estimator strategies | `SB01` | The dispatcher resolves one typed Person, Agent, Workflow, or Process strategy; Agent uses its history estimator rather than a CRM workforce rate. | Focused suite `107/107` includes strategy/registration negatives and source-specific quote tests. | `Solved` |
| `N004`, `R005` missing CRM rate cannot leave stale price | `SB02` | An unavailable quote clears amount and currency on an unstarted task and exposes unavailable feedback. | Focused suite `107/107`; direct application saga test covers cleared post-replacement state and compensation. | `Solved` |
| `N005`, `R007` improve partial architecture | `SB01`, `SB03` | Shared task application saga, revision service, pricing strategies, and canvas dialog coordinator own the extracted slice; no new page partial was introduced. | Partial cluster remains 23 files and shrank from 11,431 to 11,112 lines; `ComponentAdapters` shrank from 426 to 107 lines; Workbench/Web builds clean; independent architecture gate passed. | `Solved` |
| `N006`, `R008` preserve behavior | `SB03` | Existing issue/work-item direct-assignee behavior, compensation, graph/attachment mutations, page creation/mutation/move/database-switch behavior, Gantt dialog composition, and current-source HTTP behavior remain covered. | Focused `107/107`; split page-preservation suites `35/35` across four invocations; repaired modal regression `1/1`; graph/attachment `9/9`; serializable lock `1/1`; HTTP boundary `1/1`; affected builds, browser smoke, and independent architecture gate. | `Solved` |
