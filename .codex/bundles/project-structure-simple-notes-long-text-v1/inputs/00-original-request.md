# Original Request

User request captured on 2026-06-21:

> Use [$candoitall-bundle-workflow](C:\\Users\\lucys\\.codex\\skills\\candoitall-bundle-workflow\\SKILL.md) to create new bundle to fix/improve this:
> In project structure we have some bug around simple notes. When I add some longer simple note, it sometimes does not store it correctly. It did not happened for shorter, but for longer multiple time.
> I was not able to find out exact cause of it. Only that it happened with longer simple notes. You must explore and test it more in deep.
> Also "short" simple notes can be little longer than now. It cuts it too short. On UI on the node you can see it has enough space in the visual representation of the node, it just does not use available space effective way. Analyze it too and repair it. you will have to analyze it from the screenshot of the canvas with those simple notes otherwise you will not see it. Same after changes.
> based on that analysis prepare whole bundle and execute it to implement and test changes.

## Raw Note IDs

- `N001`: Longer simple notes sometimes are not stored correctly; shorter notes did not show the problem.
- `N002`: Explore and test the longer-note behavior deeply because the exact cause is unknown.
- `N003`: Short simple notes are cut too aggressively; the canvas node has visible room that is not used effectively.
- `N004`: Analyze the canvas screenshot before changing layout and capture screenshot proof again after changes.
