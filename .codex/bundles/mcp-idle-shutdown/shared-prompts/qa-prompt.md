# QA Prompt

Validate that Components MCP and SSH Ops MCP now have explicit idle shutdown behavior.

Check that no shutdown happens during an active tool invocation, shutdown happens after inactivity, defaults are different for Components and SSH Ops, and both projects still build. Browser proof is not applicable.
