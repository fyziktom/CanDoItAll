# Original Request

Source: User request on 2026-05-08 to improve automatic process canvas node placement.

```text
Use [$candoitall-bundle-workflow](C:\Users\lucys\.codex\skills\candoitall-bundle-workflow\SKILL.md) to analyze our composition algorithms for layout of processes "mindmap/flow diagram" in our canvas. Because processes are complicated (and they will be even more), we must find some good algorithms to compose "nodes" in processes canvas more clear. If you will check the screenshot of how some of the complicated processes looks like in canvas you will see that for human it is chaotic. we must show more clear what is main path, place roles/agents/executors more close to steps that are related to and maybe add more spacing between steps so connection lines are more clear where they go to.
you can try to generate some images [$imagegen](C:\Users\lucys\.codex\skills\imagegen\SKILL.md) based on our processes to see how it should like "user friendly way". Our canvas in general is great. you must tune just automatic positions of nodes in canvas.
```

## Raw Notes

| Note | Exact wording |
| --- | --- |
| `N001` | `processes are complicated (and they will be even more)` |
| `N002` | `find some good algorithms to compose "nodes" in processes canvas more clear` |
| `N003` | `for human it is chaotic` |
| `N004` | `show more clear what is main path` |
| `N005` | `place roles/agents/executors more close to steps that are related to` |
| `N006` | `maybe add more spacing between steps so connection lines are more clear where they go to` |
| `N007` | `Our canvas in general is great. you must tune just automatic positions of nodes in canvas.` |
| `N008` | `We will have to use multiple nodes for same role if it plays role in multiple steps. Otherwise lines are too much crossing each other.` |
| `N009` | `Even if each node will have own position, it still can represent same role.` |
| `N010` | `Improve it and repair also exiting default processes so their canvas representation will be more clear.` |
