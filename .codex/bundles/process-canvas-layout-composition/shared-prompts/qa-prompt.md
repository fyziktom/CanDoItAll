# QA Prompt

Validate the process canvas layout-composition bundle.

Check:
- The default/main route is visually and geometrically stable.
- Branches fan away from the main lane instead of all routes receiving equal placement.
- Role nodes sit near related step anchors and no longer form one remote left-side column for all processes.
- Connection lines have more readable spacing and no obvious node overlap.
- Existing manual canvas movement and save behavior were not changed.

Required records:
- Targeted test command and outcome.
- Browser route, viewport, actions, screenshot paths, and visual review notes.
- Raw-note closure status for `N001` through `N007`.
