# Original Request

Received on 2026-05-15:

> Use `candoitall-bundle-workflow` to solve this:
> Lots of our forms where peolpe enter some values are not proplely stretched to use available space. Especially for fields like textarea that expect larger text must have proper default sizes so text is readable. If there is too many fields sometimes helps to split them to subtabs based on their similar topics.
> Analyze all our forms across the app. analyze their screenshots to identify what needs improvements. For each of those screenshots of forms (not whole page, just area with those forms) create with `imagegen` proposal of layout for it (again only for that form part, not whole page). And based on those propsals improve layout. Add icons or esthetics elements to make it clear and enterprise looking.
> It will be lots of smaller changes, so make proper xlsx checklist with references to files, so you will not get lost during this task. Each change must be validated with screenshot and coparison with proposals. if it does not match you must improve it.

Literal scope words preserved:

- "Lots of our forms"
- "Analyze all our forms across the app"
- "For each of those screenshots of forms"
- "proper xlsx checklist"
- "Each change must be validated with screenshot and comparison with proposals"
