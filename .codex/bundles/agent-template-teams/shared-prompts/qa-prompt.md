# QA Prompt

Review the bundle outcome against the raw notes. Confirm every default agent has editable template files, seeded teams are produced from those files, hardcoded default-agent definitions are gone from production seed code, targeted .NET tests pass, and browser validation shows expected agents/teams in the app. Reject closure if any proof row is pending, weak, or missing an artifact path.
