# Implementation Prompt

Implement the bundle with the smallest correct change. Keep pricing typed, reject missing manual override prices explicitly, calculate costs from token counts and provider pricing, and render private-provider badges through shared agent card surfaces. Do not introduce silent zero-cost fallbacks for unknown models.
