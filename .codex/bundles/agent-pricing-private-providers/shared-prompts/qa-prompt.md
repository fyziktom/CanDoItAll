# QA Prompt

Verify that provider pricing rows survive save/load, manual model overrides require a matching price row, token cost math handles cached input separately, private-provider agent cards render `Private`, and process analytics use usage cost where metrics exist.
