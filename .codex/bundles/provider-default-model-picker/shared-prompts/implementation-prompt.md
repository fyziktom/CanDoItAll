# Implementation Prompt

Implement the active subbundle only. Preserve the raw request's provider-default linkage requirement: in Agents Runtime, provider default must save as an empty agent model, while explicit dropdown selections or override text save concrete model strings. Use the shared AgentFramework component for provider model choice and BaseLib form controls. Before editing, read the subbundle README, exact source references, and traceability table. After editing, run the subbundle's targeted tests and record proof in `reviews/01-execution-report.md`.
