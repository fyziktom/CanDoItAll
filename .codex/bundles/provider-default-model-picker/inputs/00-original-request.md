# Original Request

Use [$candoitall-bundle-workflow](C:\Users\dell\.codex\skills\candoitall-bundle-workflow\SKILL.md) to solve this.

In agents settings in runtime tab when I select some provider it must offer its default model. Model field must be splited to some dropdown where default option is provider default model (so I can easilly change for multiple agensts model by changing it on one place in provider setting) or options that specified model allows (like for openai we know names of models, same as ollama can fetch the available models), but there must be also checkbox "override" model name with standard text field as we have now. This is better to do as some generic component because providers are used also on other places (in workflows, memory, etc).
