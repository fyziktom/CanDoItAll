# Original Request

Use `[$candoitall-bundle-workflow](C:\Users\lucys\.codex\skills\candoitall-bundle-workflow\SKILL.md)` to solve this.

The file `canvasWorkbenchInterop.js` is three times in our repo. We must keep just that is part of CanvasLib. It is super long file, so I need you to split it into logical parts. Aslo split CanvasLib (including js and css resources in wwwroot) into folders so it is better managable. We did similar thing in BaseLibs (splitting into folders).

At the end you must validate as senior QA C# specialist that the code of CanvasLib has logical structure, there are no more super long files that exceeeds 2000 of lines. otherwise the task is not complete.
