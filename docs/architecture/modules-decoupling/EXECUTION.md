# Module Decoupling Execution

## Active authorization

Execution mode: **LIGHTWEIGHT_INCREMENTAL_E2E**.

The operator's attached task, accepted on 2026-09-10, authorizes the complete existing-product module-boundary refactor in coherent, verified slices. This replaces the Foundation's procedural requirement for a separately approved implementation request for each slice, including the older restrictions in `START_HERE_FOR_ASTRA.md` and chapter 12. Legacy bundle/subbundle generation is waived. No new bundle framework, manifests, or per-phase proof packets are required.

The architectural reference remains [CanDoItAll Architecture Foundation](../../../codex/bundles/CanDoItAll_Architecture_Foundation/). Its ownership, product preservation, security, compatibility, and evidence requirements remain binding, together with repository engineering rules, truthful validation, portability checks, and platform permissions. Keep discoveries and justified refinements in these task records without rewriting the reference snapshot.

Work only on **modules-decoupling**. No merge, rebase, squash, history rewrite, pull, other-branch implementation, pull request, or auto-merge is authorized. Keep signed checkpoints local; pushing requires separate authorization and must target this branch. Preserve the accepted Agents UI checkpoint and existing product capabilities. Sibling repositories are read-only source dependencies. Preserve user data, secrets, live hosts, and unrelated work.

All task artifacts and reports are in English. Preserve product localization and compatible identifiers. Maintain only this record, `COVERAGE.md`, and `VALIDATION.md` as task coordination records; update maintained product documentation when implementation changes its claims.

## Starting state

| Item | Observed state |
| --- | --- |
| Repository | Current repository root (`fyziktom/CanDoItAll`) |
| Starting branch | `modules-decoupling` |
| Starting HEAD | `3da508996e46e68d811f5ffb6926df8b876878cf` |
| Working tree before task | Clean; no staged or unstaged user changes |
| Instructions inspected | Root `AGENTS.md`, `.github/copilot-instructions.md`, current testing guidance and CI workflow, operator task |
| Initial implementation status | Production code unchanged; focused baseline discovery in progress |
| Initial validation status | Signing reuse, isolated Release product build, documentation and portability-static PASS; focused runtime baseline pending |

## Current checkpoint and next action

Signed checkpoints: authorization `49ef77a3dcc4bc5b4d39126e7770e4706553098d`, Collaboration `d96ef27796cf7648cb42cfd42c2b832c0b432180`, Memory `f9908080ebe38f342ab4d1799fc7cef8481e3ce8`, catalog owners/migration authority `c0950854f44c674aafd474c64532fe08fe2801b3`, History/SimpleChats coordination `52c477f51591018762b3d0a12c68657f1379a733`, and Security/Providers/Search/Storage `0cb2d519edd549afcf79a61c0db758871fba74db`, all verified with the expected identity. The frozen Collaboration stable gate passed 10827 cases with no failures or skips; seven runtime-expanded theories explain the 55 additional rows over discovery. That gate predates subsequent owners and is not final-refactor proof.

Simple Chats receipts `5264ba2a34db61bf3f1b1a391202f81e29e617fa`, Projects/deletion/Agent claims `e64d3c95b263b607bea41832f1fc63dea469eb4a` and Prompts/Scheduler/connector owners `3e12e99024b5891fd0a65c0e224eb5ab0b0a3dd6` are signed and verified. This checkpoint adds persisted project lifetimes, retained retirements and lifetime-specific Agent cleanup, with the complete canonical migration. Applied product build, 41 unit, 67 integration and 24 component cases passed. Static enforcement passed at 14429 reviewed findings. Complete implementation and journeys A-H remain open.

The HR definition adapter, durable admission/recovery and Prompt policy catalog are signed at `7bafbc9e9a8e0c1692f8948c6f99f0b4301f8753`, verified with the expected signing identity. Normal product/test builds, 574 unit cases and 67 component cases pass. The 75-case integration run passed 74 before a fixture-only seed-customization correction; all three affected theory cases then passed without production changes. Current-access checks protect completed results and cached receipts while retaining committed evidence. Portability, documentation and final evidence scans pass. See VALIDATION.md for exact runs; real-provider/browser and final broad closure remain open.

The CRM/Provider checkpoint is signed at `d1e5e5cf11a80ef54df488371dff3926c087a01d`, verified with the expected identity. Normal applied Web/test builds, 114 unit, 179 PostgreSQL and 31 component cases pass without skips; final portability enforcement passes at 14435 reviewed findings, documentation and evidence scans pass.

The Workflow/Workbench/Storage checkpoint is signed at `4ad34331afed3e0921b36db0ba5d7a781acebfba`, verified with the expected identity. Its 167-path source delta is applied over CRM. It gives Workflow its actual seventeen-entity model and Workbench its complete twelve-entity model, preserves delivery manifests and owner receipts, and adds the complete 155-entity canonical migration. Candidate proof: 183 unit PASS; initial PostgreSQL 104/106 followed by all five affected cases PASS after fixture-only canonical-readback/latest-migration corrections; 95 components PASS. Normal applied builds and all 183 unit, 106 PostgreSQL and 95 component cases pass without failures or skips. The complete static scan passes final enforcement at 14442 reviewed findings; final focused/build evidence scans are clean. Documentation evidence and canonical validation also pass. Continue automatically with the next owner cut.

The 55-path Work/AgentHistory/WorkspaceSettings delta is applied over `4ad34331afed3e0921b36db0ba5d7a781acebfba`, with source tree `41abd3744b785cb1c0fcd7bae1d9cf6f973f8369` before records/baseline. All 268 cumulative candidate paths match. Candidate proof passes all four builds, 77 unit and 45 component cases; PostgreSQL passes 103/104 followed by the corrected one-case fixture PASS. All nine populated migration/rollback cases pass. All four normal builds and all 77 unit, 104 PostgreSQL and 45 component cases pass without failures or skips. The final evidence scans cover thirteen text logs/TRX files and all sixteen files for token-shaped values, with no findings. Final static enforcement passes at 14446 reviewed findings after four existing mapping-property repetitions in the new Designer were inspected. Documentation evidence tests pass nine cases and canonical validation passes 209 maintained Markdown files. This checkpoint is signed at `9578422d4ca4fc68733e3d4da05953939088e7f3` and verified with the expected identity. Continue automatically with source-bound execution admissions.

The source-authority checkpoint composes Projects reservations/native admission, the PostgreSQL row-lock fence, Scheduler authority/source leases and Process prepared launch/catalog/operator producers. Its 378 cumulative paths include five actual Razor inputs with verified logical paths/CSS scope and root registrations. Complete canonical migration `20260911010404_AddSourceBoundExecutionAdmissions` has 159 entities; all 156 previous mappings are preserved except the intended nullable Scheduler/Process authority columns. No authority is backfilled. Down locks the affected tables and refuses retained evidence. Candidate Unit passes 196 after two fixture corrections. PostgreSQL passes 129/136, then all seven corrected fixture cases pass; the nine migration/rollback cases passed in the initial run. Components initially pass 83/92: seven manager-chat fixtures lack the policy service (one exact failure is reproduced on the preceding 957 normal binary), one launch fixture bypasses the user-event render, and one real Structure deferred-render key omits new preparation state. The root fixes preserve assertions: all manager and Structure cases pass in the twenty-case rerun; the remaining shell fixture passes both launch cases after using the actual button event instead of a private handler/parameter render. The reviewed 136-path delta is applied and staged; all 378 cumulative paths match, with source tree `41cc34ba3f78d832ae20c326341618a3f30590b7` before records/static baseline. All four normal builds pass (Web/Unit/Integration/Components warnings: 0/1/6/3; no errors). Normal focused proof passes all 196 unit, 136 PostgreSQL and 92 component cases without failures or skips. Final portability enforcement passes at 14491 reviewed findings; documentation and final evidence scans pass. The forced eight-project dependency review has no blocking error or project cycle, with analyzer limitations recorded. This checkpoint is signed at `5065db4ec07f305435d1ad8789d0e8a8bdf4a33b` and verified with the expected identity; its final tree is `ef7027495690575da1554dfa4a3a2bd95d7c2869`. The normal worktree was clean immediately after signing. The policy-owner cut is now applied and staged: 165 logical paths (45 new, seven deleted) from 166 effective candidate paths. Its reviewed activation patch is `4360e8e55babf3c127c216e76be3055e834dbc8699ff71e50f9f366f24775977`, with source tree `0e2be5836a5860f5bf944668d72bb53477b5b81b` before records/static baseline. Product policies, Storage attachment and Process context/invocation/workspace/capability checks now live with their owners. All 213 classification vectors retain their earlier values. No SQL schema changes. The final candidate fixture repair passes all eleven host checkpoint cases and all 42 component cases; original failed inputs and corrected proof remain recorded in VALIDATION.md. All four normal builds pass (0/1/6/3 existing warnings), and normal Unit discovery/execution is 1220/1240, all passing without skips. All 154 mixed PostgreSQL/file/SDK integration cases and all 42 component cases pass without failures or skips (21:07 and 30 seconds). Portability scans all 5900 eligible files without truncation; all 219 added/228 stale findings were reviewed against the original source, and final no-write enforcement passes at 14482 reviewed findings. Normal proof, evidence review and the forced twelve-project dependency review are complete; no project cycle or Error finding is reported. Documentation evidence passes nine cases and canonical validation passes 210 files. All current policy checkpoint gates are satisfied; sign locally and continue automatically with native recovery. Next validate the schema-neutral native/background/disclosure cut: source preflight currently composes 138 paths, with original-state-aware retry denial and persisted-completion acknowledgment guards under new regression coverage. Then combine captured lifetimes, selected transfer, Process asset receipts and their complete canonical migration. Required real journeys A-H and the final broad gate remain open.

Resume by reading this file, checking branch/HEAD/dirty files, and reconciling committed work and tested revisions with `COVERAGE.md` and `VALIDATION.md`.

## Decisions and blockers

- Work Management's remaining assignment writer is the `WorkItemAssignee` subset of CRM's mixed assignment table, including non-task WorkItem subtypes. The existing Workbench ProjectObject remains the native work identity. Move those assignment rows with their exact IDs, full payload, duplicates and retained orphan history into Workbench ownership; keep other participation roles and workforce/rate facts in CRM. Mixed cleanup/move and hidden Party/affiliation merge writes must enlist the assignment owner and native revision/pricing update in their actual transaction. Combined reports must order/count/page the combined facts rather than concatenate separately paged results. The ownership and exact data move are applied; captured lifetime admission and retained cleanup/replay remain required.

- The mode note is task-scoped; it does not change application dependencies, global Codex instructions, or shared skills.
- Git uses `C:\Program Files\Git\usr\bin\gpg.exe` (GnuPG 2.4.9), matching `gpgconf`/agent, and home `%USERPROFILE%\.gnupg` (resolved and checked through that installation's `cygpath`). Expected signing fingerprint: `96E836FAA8854EE98ABC10903C206549E1D7EAD6`. Initial policy had no configuration file and used defaults of 600 seconds idle / 7200 seconds absolute. It expired before the first real commit. After explaining the change, apply the operator-authorized finite task target: 14400 idle / 28800 absolute seconds. `gpgconf --check-options` and `--reload gpg-agent` succeeded; no agent was killed/restarted. Trusted pinentry then unlocked the first signed checkpoint.
- **Restore at closure:** the task created `%USERPROFILE%\.gnupg\gpg-agent.conf` from an absent file. SHA-256 is `89E963D5696BE60087BFA127381DECACB857FB470CC5A2DCAF7A03252B6DDC5E`. It contains only the task comment and `default-cache-ttl 14400` / `max-cache-ttl 28800`. Remove only if the hash still matches, then reload the same agent; preserve any concurrent user edits and report them. Cache expiry/hardware policy can still require another unlock. Do not leave the temporary policy unreported.
- Existing application PID 1836 serves `http://localhost:5032` from the main checkout's Release output. It predates this task and is not owned by the current DotNetWatch backend. Preserve it. Its active PostgreSQL override profile is `e5df9ad6-33db-c697-4a06-78a74976013c`; no pending profile activation was observed. Baseline default Release copying encounters its locked DLLs; use isolated build output/fixtures rather than stop the host blindly.
- All inspected sibling worktrees were clean. Components matches CI's source commit; FileTools is clean at a newer detached commit than CI's pin. Keep the actual source graph stable (revisions in `VALIDATION.md`).
- Source-grounded findings: runtime `AppDbContext` scans registered mappings globally; separate Simple Chats, Memory and provider-history stores do not yet imply bounded EF models. Processes has its own context. Preserve complete design-time migrations and immutable per-host database profile binding.
- Existing HR CRM party/affiliation operations already call the owner. HR has 14 tool descriptors and Scheduler has three workflow-only descriptors, with managed identity and InteractiveChat guards. HR Simple Chat definition administration is absent and is required new integration.
- Structure workflow status reads write projections, and result detection uses a project-wide new-node set difference. Structure replay currently uses process-local locks and metadata keys without an atomic durable receipt/fingerprint. Process launch UI links after admission using mutable page scope. These are required investigation/repair areas, not runtime test results.
- Projects deletion/participation and direct-assignment contracts initially exposed `AppDbContext`; their applied replacements preserve the shared transaction and cleanup outcomes through data contracts and explicit owner enlistment. Plugin and Scheduler bootstrap no longer issue separate schema DDL after migrations; CRM lookup seeding is retained.
- Transfer scope follows the selected shipped handler. The Projects dataset owns twelve Projects/Workbench collections; Agent catalog transfer is a separate selection. Unrelated CRM organization cursors and local Process launches must survive a project-only import unchanged. New project receipt/history fields must travel with their owner data, but this task does not invent whole Workflow/Process/Scheduler journal export. The necessary IPFS package seam adopts a fresh target reference while retaining source placement provenance as history. The ignored scope review supersedes the earlier all-five-owner-history draft requirement.

- Confirmed remaining Workflow output invariant: `WorkflowStructureAuthority`, `WorkflowStructureOutputPlan` and the saved native contribution retain profile/project IDs without the original lifetime. Request admission is JSON-ignored; retrying against current ProjectId cannot prove the original incarnation. The following owner cut must capture and persist original lifetime at actual launch/output preparation and hold source/lifetime checks through native commit. Existing incomplete records remain explicit reconciliation; never bind them to today's reused ID. The Storage operator route may identify these records safely but cannot authorize recovery until that evidence exists. Ordinary project creation/transfer compensation also needs the original create receipt plus exact unchanged owner snapshot under its delete transaction, preserving intervening human edits.

## Ordered milestones

| Order | Outcome | State |
| --- | --- | --- |
| 0 | Signed execution checkpoint, current owner/caller map, isolated build/test/provider baseline | In progress |
| 1 | Bounded runtime persistence through a small complete owner operation; explicit model membership, unchanged complete schema/migrations and stamping; extend to remaining owners according to dependencies | Collaboration, Memory, TestLab, Resources, Plugins, History, SimpleChats, Security, Providers, Search, Storage, Projects, Agent access, Prompts, Scheduler and connector command contexts implemented with focused proof. Workbench context serves assignment/deletion paths; its other paths remain open. Collaboration intermediate broad gate passed; remaining owners and foreign access remain open |
| 2 | Agents/Providers technical/pricing authority and CRM projections/enrichment; Work Management assignments; owner contracts replacing foreign EF and hidden writers | Assignment bridge no longer accepts a foreign context and preserves CRM/Workbench atomicity. Technical/pricing projections, CRM model and complete Work Management meaning remain open |
| 3 | Projects/Structure/Resources/Storage lifecycle, shared transaction coordination, imports/exports, single migration authority and restart | Project deletion now stages four owner participants in one actual transaction. Agent revocation claims are generation-fenced with database-time leases. Complete migration authority and receipt-preserving chat transfer have focused proof; project lifetime/admission and twelve-owner transfer remain unfinished |
| 4 | Durable Structure contributions and execution origin/result recovery for Workflow/Process; pure queries, separate projection delivery, trusted scope and replay receipts | Ignored Workflow/Storage/Workbench candidate passes focused proof; Scheduler/Process authority, final native lifetime checks and operator reconciliation remain in progress |
| 5 | Required HR Simple Chat definition adapter with owner-atomic create receipt; preserved curators/Scheduler and permitted direct CRM/Resource automation | Owner receipt, canonical migration and transfer implemented with focused proof; HR adapter and durable runtime admission/recovery applied, final applied checkpoint validation in progress |
| 6 | Remaining module/runtime contract reconciliation, cumulative architect/QA review, final frozen product/stable/browser/live/PostgreSQL/static gates and journeys A-H | Not started |

This order is adaptive: finish one coherent production path and its proof before expanding. Future estimates in the Foundation are not automatic implementation scope. The complete required scope remains open until coverage and mandatory journeys are closed.

## Implemented ownership decisions

`CollaborationService` uses the explicit four-entity `CollaborationDbContext` and a per-operation factory bound to immutable `ICanonicalRuntimeDatabase.Profile`. Its unused public context-accepting command overloads are removed. Notifications, activity mirroring, routes, IDs and stored shapes are preserved.

Share only the stateless GUID stamping algorithm with `AppDbContext`; do not create a generic foreign repository or new context inheritance hierarchy. Existing complete migration composition continues to apply the same configurations. No schema migration or data copy is intended. The transfer-residue participant remains an explicit maintenance read under the current complete-schema target lock until the coordinated transfer slice replaces that shared contract; it is not a business writer.

Acceptance: actual owner model excludes every foreign entity, mappings match the complete model, synchronous/asynchronous saves retain GUID stamping, canonical legacy records survive restart/readback, profile factories stay isolated, and existing Collaboration integration plus shell-badge behavior remains. Do not add optimistic-concurrency enforcement that the current Collaboration mappings do not have as an incidental refactor. Shared stamping/composition changes are a named broad-gate invalidation trigger, with focused proof first.

Memory now follows the same boundary with its seven existing mappings. All Memory ledger/retention/lease stores use `MemoryDbContext`; generic module registration no longer mutates the global model registry. Application composition supplies the pooled owner factory; standalone consumers register that typed factory explicitly. GUID profile stamping remains distinct from worker lease ownership tokens, and generic workers remain disabled by default. PostgreSQL mapping/readback/restart/retention/profile and lease proof plus the existing API/UI paths passed. The two lease providers share a process; this does not claim a simultaneous distributed race.

TestLab uses its four existing aggregate mappings, Resources its single metadata mapping, and Plugins its six installation/grant/connection/OAuth/log mappings. Canonical pooled factories preserve profile binding. Resources composes project references through a data contract instead of querying Projects entities; existing catalog limits and missing-project behavior remain explicit. Workbench foreign reads and transfer maintenance are separate unfinished cutovers.

Migration cleanup removes the Plugin/Scheduler post-migration DDL while retaining CRM lookup seeding and canonical provider bootstrap. The complete design-time factory fails explicitly when its composition catalog cannot load; partial reflection discovery no longer silently omits mappings. Applied PostgreSQL proof covers exact fresh physical mappings, repeated bootstrap, and populated baseline upgrade/restart with historical extra indexes/defaults preserved. This removes opportunistic repair of manually corrupted, already-baselined schemas; it does not claim to repair such corruption. Static baseline review removes only the two allowances belonging to the deleted initializers.

History has an explicit eleven-entity model; SimpleChats has its nine existing entities. Public History staging contracts accept owner data, without a caller's context. Infrastructure coordination requires the actual active owner connection/transaction, checks canonical physical profile identity, creates fresh enlisted contexts, and rejects retired or mismatched scopes. Independent factories do not switch behavior based on ambient state. Simple Chats transcript/product writes retain their same scoped owner and append-only evidence rules; callbacks run after the owning commit. GUID stamping remains separate from numeric Simple Chats versions. The explicit InMemory test path checks store identity and makes no atomicity claim.

Shared relay retention uses an explicit read-only integration SQL join so both owners' expiry predicates and deadline ordering apply before the batch limit. History outbox, workflow usage/resume, file/agent history and chat maintenance participate in the original owner transaction. The transfer adapter uses an explicit target-profile History session; the global target-transfer orchestration is still an unfinished maintenance boundary.

Security maps its two existing entities without introducing token stamping. Provider Management maps its six entities and retains the complete schema's source-to-secret FK while omitting Security entities from the runtime model. Security existence reads use a bulk owner query for ordinary reads and explicit enlistment during mutations. Secret-reference policies no longer accept a caller context. Catalog reads use a fixed number of calls; mutation callbacks run after the transaction coordination frame is released. Technical catalog/CRM projection and remaining bootstrap/maintenance readers are subsequent boundaries.

Search owns its one index entity; Storage owns its catalog and routing entities. Normal services now use these contexts, preserving bootstrap/path migration and search behavior. Typed Storage planning facts carry host/path classification and parse only the referenced FTP configurations; they do not perform provider I/O. Structure reads Resource/TestLab projection facts through their owners, with explicit enlisted variants retaining the original mutation read set. Project deletion and Prompt search projection have not yet switched to the new staged operations.

Simple Chats now maps ten entities, including an immutable definition-create receipt keyed by producer, actor, history namespace and a server-owned intent. The same definition writer atomically reserves the intent, creates revision one and commits its receipt. Replays return that original identity without resolving the provider or overwriting later revisions; changed semantic input conflicts. Nested unit-of-work failure makes the transaction rollback-only even when its caller catches the error. No LLM call or approval occurs inside this transaction.

Migration `20260910163827_AddLlmChatDefinitionCreateReceipts` is generated from the canonical 149-entity model. The receipt-to-original-revision FK is deferred; the unique intent key remains immediate. The internal Simple Chats transfer document advances from version 8 to 9 and preserves receipt fingerprints and identities. Replacement refuses a target that already holds receipts. Downgrade is supported only while the receipt table is empty; once receipts exist, retain the schema or restore a reviewed pre-admission backup without replaying effects. The owner contract adds no tools, context or transcript access to ordinary Simple Chats.

Projects maps its four existing record types, Workbench its ten, and Agent access its single revocation record. Normal Projects operations use the bounded factory; mutation reads explicitly enlist while ordinary queries stay independent. CRM's direct-assignment bridge sends final typed facts to Workbench, which saves its native revision/bindings in the same physical transaction. Existing task IDs, assignment multiplicity, unknown metadata and pricing invalidation remain; this does not yet relocate all Work Management semantics.

Project deletion stages Workbench, Agent access, Search and Storage on Projects' actual connection/transaction, then commits and releases preparation locks before postcommit cleanup. Storage supplies typed catalog/path facts and retains host binding, containment, reparse and survivor checks; bytes remain under the existing separate managed-binding gate after authoritative deletion. Revocation uses existing AttemptCount as an exact generation with short row-lock transactions and one database-time sample after lock acquisition. Lost leases cannot finalize a successor's claim. No schema change belongs to this slice. Mixed old workers do not honor the new finalization fence; lifetime-bearing grants and retired-ID admission remain required follow-on work.

Prompts maps its ten existing record types, Scheduler its two and connector commands/audits their two. Canonical pooled factories retain the complete schema and profile identity. The same Prompt draft writer supports ordinary saves and explicitly enlisted creation; the latter returns a preparation whose activity/projection completion runs after the outer commit and scope release. It does not provide durable retry identity. Search owns index mutations, retaining its advisory lock and atomic batch rebuild; incremental Prompt state reads lock the current owner row before writing. No schema or transfer-format change belongs to this slice. Scheduler dispatch authority, connector effect reconciliation and remaining Workbench callers are dependent work.

Projects now maps five records: the existing four plus retained retirements. Admission binds canonical profile, public project ID and immutable lifetime. Deletion records retirement in the same transaction; a recreated public ID receives a new lifetime. Editors retain their loaded lifetime, and delayed Project search updates revalidate the original lifetime under the project gate. Legacy callers without a lifetime remain compatible pending their explicit admission cutover.

Migration `20260910183745_AddProjectLifetimes` is generated from the complete 150-entity model. It assigns distinct live lifetimes, records upgrade-only legacy-grant eligibility and defaults new creations/restores to ineligible. Both fields are omitted from compatible v2 project JSON. Agent recovery adds paired nullable profile/lifetime fields and filtered legacy plus per-lifetime uniqueness; historical rows remain null rather than being attributed to today's project. Exact catalog cleanup preserves another lifetime's grant under the existing file lock. Bare-ID grant writers, controlled bootstrap binding, reservations and final runtime admission are still required. Retained retirement or typed recovery evidence blocks downgrade; empty-evidence rollback requires stopped writers and discarded admission handles.
