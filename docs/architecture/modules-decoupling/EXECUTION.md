# Module Decoupling Execution

## Active authorization

Execution mode: **LIGHTWEIGHT_INCREMENTAL_E2E**.

The operator's attached task, accepted on 2026-09-10, authorizes the complete existing-product module-boundary refactor in coherent, verified slices. This replaces the Foundation's procedural requirement for a separately approved implementation request for each slice, including the older restrictions in `START_HERE_FOR_ASTRA.md` and chapter 12. Legacy bundle/subbundle generation is waived. No new bundle framework, manifests, or per-phase proof packets are required.

The architectural reference remains [CanDoItAll Architecture Foundation](../../../codex/bundles/CanDoItAll_Architecture_Foundation/). Its ownership, product preservation, security, compatibility, and evidence requirements remain binding, together with repository engineering rules, truthful validation, portability checks, and platform permissions. Keep discoveries and justified refinements in these task records without rewriting the reference snapshot.

Work only on **modules-decoupling**. No merge, rebase, squash, history rewrite, pull, other-branch implementation, pull request, or auto-merge is authorized. Keep signed checkpoints local; pushing requires separate authorization and must target this branch. Preserve the accepted Agents UI checkpoint and existing product capabilities. Sibling repositories are read-only source dependencies. Preserve user data, secrets, live hosts, and unrelated work.

All task artifacts and reports are in English. Preserve product localization and compatible identifiers. Maintain only this record, `COVERAGE.md`, and `VALIDATION.md` as task coordination records; update maintained product documentation when implementation changes its claims.

## Starting state

| Item | Observed state |
| --- | --- |
| Repository | Current repository root (`fyziktom/CanDoItAll`) |
| Starting branch | `modules-decoupling` |
| Starting HEAD | `3da508996e46e68d811f5ffb6926df8b876878cf` |
| Working tree before task | Clean; no staged or unstaged user changes |
| Instructions inspected | Root `AGENTS.md`, `.github/copilot-instructions.md`, current testing guidance and CI workflow, operator task |
| Initial implementation status | Production code unchanged; focused baseline discovery in progress |
| Initial validation status | Signing reuse, isolated Release product build, documentation and portability-static PASS; focused runtime baseline pending |

## Current checkpoint and next action

Latest executable checkpoint is signed **`8edd80826ddf7ae9244b56ea3d5dd508830f7333`** on `modules-decoupling`. The subsequent update is reporting-only; its exact HEAD and unchanged executable-source reconciliation are recorded in `artifacts/modules-decoupling/final-frozen-v11/reporting-reconciliation.json`. The repaired Hosting/Process slice passes direct production builds, owning43 and runtime429, with no weakened architecture checks. Final v11 product/Stable/Playwright builds pass (0/11/0 existing warnings), and one 1,946-file image is published. Source SHA `bff97e319e2a0716ab5c49487509b3f5930ca4fc3f97b4603ecef333183a3d96`; build-summary SHA `3c9fa6ef422c0dad3e97ed7b1cbaa5d57346625e7ea1b285826ea5d5b14be172`; image manifest SHA `e3efd19054f0f2daf14b851bf3817383b593a1aae893482ce5a1d691a210ddab`. All six test assemblies, source, runtime and three sibling guards match. FileTools' local and CI commits still have the identical source tree; no sibling or CI pin was changed.

Fresh v11 execution passes **Memory225/225**, **Windows core Unit140/140** and **runtime Unit429/429**, zero failed/skipped. Stable discovery is **13,720**, exactly four added Facts and no removals; reviewed data expansion expects13,775 executions. That full execution is **BLOCKED**, not passed. Browser discovery is72 active cases; Windows headless browser **1/1 PASS** and two actual sequential WindowsHeadless host lifetimes pass at18:47:42 UTC. Both report Healthy/Ready and seven ready purpose roots. Windows uses the canonical script's process termination, not graceful shutdown. This InMemory lane does not prove PostgreSQL retention. Its first attempt failed before Web startup because the long temporary path exceeded Windows'260-character template-copy limit; the preserved retry uses fresh short root `C:/h11-cdd717ba`. Both hosts stopped, and all eight retained log/TRX/summary files pass the log secret scan. Evidence: `final-frozen-v11/offline/`, `stable/`, `headless-windows-retry1/results/`. Remaining browser/provider/LiveProcess/Linux and final A-H acceptance are incomplete.

Docker Desktop still reports **stopped** after its17:14 UTC WSL memory failure. Restart approval remains pending because operator containers are protected. The old Windows Stable attempt has no final TRX counts, and Linux aborted in restore; neither is PASS. The refreshed repository secret Fact is **1/1 executed, FAIL**, on exactly the same ten synthetic-key files; all file/archive hashes match the protected inventory, with no new findings. Published-image scanning retains two findings for the known public development-password fingerprint `9fa1a3076674cff5`. Automatic cleanup review previously rejected the ten-file deletion as "blocked by policy"; no bypass or deletion occurred. This is a second independent closure blocker.

No build, test or task Web host remains active. Root retains H lease **PID39788**, started15:46:41.0640956 UTC, session62320, run`99fd6a81-36e1-4224-980c-358194c3edac`. H database`cditall_h_retained_v10_7e6c94daedb24784805f05ed43162f12`, its baseline dump/43 environment files/152 database fingerprints, and the first upgraded100-file snapshot remain preserved; **no database drop or restore has run**. E's original run remains recovered Completed with its confirmed activation and approvals retained. Operator5032/PID1836, unrelated5040/PID6152 and Ollama11434/PID47836 are untouched. The two stopped headless attempts retain their attributed directories. After explicit Docker approval, the next environment action is `docker desktop restart`; then revalidate owned containers and PostgreSQL before the already reviewed H upgraded-state backup, same-identity baseline restore and two fully quarantined host lifetimes. Rebind E/H and Linux successors to v11 plus the reporting reconciliation; their old v10 guards must not be activated as current. Final replay/readbacks, full Stable and the remaining browser/provider/platform lanes still require execution. Signing-cache restoration and final process/dependency disposition are recorded in `final-frozen-v11/final-report.json` after the reporting checkpoint. No merge, push or PR occurred.

## Retained v10 incident and repair history

Current HEAD is signed `e3c2a9a5e9b289dae6285a290c41617d8957d85a`. The v10 product, Stable and Playwright builds passed with source SHA `68a8aec259b14bc293dd7b2f06a498a46ebb1bd69fe2f3df3690051611eb789a` and one 1,946-file published image. Actual E UI recovered original run `05022a19-039d-4b7d-a47d-604d8b10f09a` to Completed, preserving its draft, original input/context, activation checkpoint, original intents and approval/restart checkpoints. Final replay and remaining host readbacks are still open. Evidence: `final-frozen-v10/e-host-lifetime-1/original-run-recovery-result-v2.json`.

At 17:14 UTC on 2026-09-12, Docker Desktop's WSL engine stopped with insufficient-memory error `Wsl/0x8007000e`. PostgreSQL became unavailable; E's background-service failure triggered normal StopHost shutdown. The independently reviewed Windows Stable log first failed at the same time, then repeatedly failed database availability checks. Root verified and stopped only this attempt's three test processes at 17:49:03 UTC; its wrapper closed FAIL/incomplete, without final TRX counts. The separate Linux build aborted during restore with a Docker engine error. Preserve both original attempts. Docker restart approval is pending because operator containers are protected. No Docker restart was performed.

Independent offline proof on the same frozen source: Memory **225/225 PASS**, Windows core portability Unit **140/140 PASS**, runtime portability Unit **425/429 PASS, four FAIL**, zero skips. Those four failures exposed a generic Hosting singleton capturing scoped runtime tool providers, the Process executor's partial admission file, its 613-line size and a duplicated tool-name literal. The repair scopes all six runtime/port/diagnostics registrations, keeps shared activity identity singleton, extracts admission into a top-level type, moves result construction to its existing factory, and reuses the canonical tool identifier without changing denial text. Existing architecture assertions remain intact; the executor is now 577 lines. Direct Hosting/Processes builds pass. The first Unit build retained a missing test namespace import; corrected Unit build passes with one existing warning. Confirmed discovery/execution is **43/43 owning PASS** and **429/429 runtime PASS**, zero failed/skipped, at 18:24:40 UTC. These are 467 distinct cases because Hosting5 overlaps. Complete source, 352 assembly/configuration files and sibling guards pass; evidence is `continuation-v10/runtime-architecture-repair/`, source SHA `48222e8696a375c819f1dd8c661861a719dab48b59afecd17fa350222699e821`. Complete portability scans 6,291 files / 30,282 findings. Its sole reviewed delta replaces the singleton-line fingerprint with the scoped-line fingerprint; normalized workspace-path handling is unchanged. Inspected baseline refresh and final no-write enforcement pass 14,667 protected findings; enforcer6/scanner4 pass. Independent architecture and QA review found no material blocker. Next: finish documentation checks, sign this small repair, then build a successor frozen candidate and run the independently available Windows headless lane. Previous v10 evidence remains tied to its actual source.

H lease PID39788/session62320 remains held for run `99fd6a81-36e1-4224-980c-358194c3edac`; no H Web host is active. The first canonical upgrade retained C1/R1, current R2, Collaboration and historical pricing, but normal startup changed the disabled Scheduler plan's next-fire projection and invoked the pre-existing Process run-summary worker. The Scheduler difference is exactly two expected fields; all seven other Plugins/Scheduler row hashes match. This first launcher did not fully quarantine dispatch and is not complete H proof. Preserve its upgraded 100-file environment snapshot (manifest SHA `05c366761f7db2fc2837f23bf1556e4a3e75193a642028483a0b0e81010b1de5`), original 43-file baseline and database dump. Its upgraded database backup failed on the outage before restore; **no database drop or restore occurred**. Reviewed ignored successors prepare endpoint-pinned backup/restore and disable Process summaries/provider bootstrap/Memory workers, with durable-queue preflight SQL before both clean host lifetimes. Do not activate their old-image guards after this source repair without a new reviewed candidate. The separate ten-file security cleanup approval also remains pending.

## Earlier continuation checkpoints

The checkpoint after `2fb6941e3af95a9b5e7c8494a012252a8732fa97` (historical price representation and H seed) changed two Workflow test files and was subsequently signed as `e3c2a9a5`. The new terminal-status theory observes actual node/admission updates on one PostgreSQL connection/transaction, injects before/after commit, reconstructs the service provider over the same profile, and requires unchanged completed run/events/artifacts/output identities with zero relaunches. Its first build retained one wrong test-entity property name; first execution retained two assertions comparing pre-authorization origin with the legitimately authorized launch origin. Corrected assertions resolve the real expected authorization before launch and preserve the entire origin; **2/2** pass at 16:11:04 UTC, then all **96/96** Workflow delivery/downgrade cases pass at 16:17:51 UTC, zero skipped, with source/DLL guards. Source SHA `ef47d405b5768d4ae4ce8240de334d5b5092fa99824e3770a30d9eecf54ac732`, Integration DLL `43f687d6d121128734490257e52f84204b31284fda047c5c878c9b70c8af6490`. Final build has seven existing warnings, zero errors. Complete portability 6,290 files / 30,282 findings passes final no-write enforcement at 14,667 unchanged protected findings; enforcer6/scanner4 pass, no baseline edit. Independent review found no material blocker. Evidence: `continuation-v10/g-terminal-*`. This is actual database fault proof and DI reconstruction, not an OS-host or provider-transport failure claim.

The held H runner and backups below remain unchanged. An additional baseline read-only snapshot now covers all 152 table/schema/sequence fingerprints, SHA `6a99432bab4c7047d56a4174db63f2c16e7140cc9420cc8a97c5961340c4272b`. Use `Capture-HDatabase.ps1 -Name restored` only after actual same-identity restore, then compare every fingerprint plus all baseline environment files. Both Process automatic recovery workers read `Processes:RuntimeDispatchQueue:EnableRecovery`; the H launcher must set it false and verify existing queue eligibility before first host startup. This preserves explicit new dispatch while quarantining old recovery. Next: sign this test slice after doc checks, build the final current-source product/test assemblies, publish one immutable application image, exercise actual retained E recovery and both H Web lifetimes, then close all frozen/platform gates. No test/build command is active. Root still holds only H lease PID39788/session62320; no new task Web host is active. The explicitly scoped obsolete-artifact cleanup approval remains pending.

Current local signed checkpoint is `72ffdb6488ad02d59a5010f6a890a22aac80d4d9` (bounded Projects summary), with the verified H history slice ready for signing. H now includes actual owner-seeded historical chat R1/C1, current R2, synthetic immutable usage/pricing and Collaboration history. Its first old-schema seed failed before projection/host startup at the queued-versus-reloaded history hash assertion; numeric price equality passed. The holder's input/source/DLL guards passed (`continuation-v10/h-seed-first-{inputs,result}.json`). No H host started. Port5040 belongs to an unrelated process; H reserves the independently verified free5041 instead. Operator5032 and Ollama11434 remain untouched.

Six new actual-PostgreSQL owner cases reproduced the same decimal-scale defect for both provider-reported/calculated prices and fresh/historical aggregate/attempt-backed envelopes: **0/6 PASS**, zero skipped, with source/DLL guards at15:36:59 UTC. Replacing only the reloaded Amount representation restores the complete original hash in every case. The source repair canonicalizes only the Simple Chat aggregate price representation; legacy equal-version replay must match the complete original SHA256 using an exactly equivalent decimal scale, retaining every historical row/hash and all conflict/version/tombstone guards. The broader attempt-backed case cannot recover the old scale from a separate aggregate row because that row is intentionally absent. The first repaired run retained7/9, with two new fixture assertions sampling incomplete initial canonical promotion. Projecting both historical envelopes before the snapshot corrected that fixture, then **9/9** passed at15:44:38 UTC and the full owning history/migration selection **38/38** at15:50:24 UTC. Both have zero skipped and unchanged source/DLL guards. Exact initial-row preservation, changed aggregate/attempt rejection and zero/scale28/maximum decimal cases pass. Production builds are0-warning; Integration retains seven existing warnings, and the isolated holder builds0-warning. Complete portability6,289 files/30,280 findings passes final no-write enforcement at14,667 unchanged protected findings; enforcer6/scanner4 pass, no baseline edit. Independent actual-diff review found no material issue.

The corrected H seed is READY with guarded unchanged inputs (`h-seed-second-{inputs,result}.json`). The only held process is root's lease runner PID39788, session62320, run`99fd6a81-36e1-4224-980c-358194c3edac`, exact database`cditall_h_retained_v10_7e6c94daedb24784805f05ed43162f12`; evidence root is `continuation-v10/h-owned-host-99fd6a8136e14224980c358194c3edac/`. This is seed readiness, not actual application-host proof. A premature `verify` command correctly failed its current-migration requirement before upgrade; do not repeat until the first canonical host upgrade. Initial backup preflight was refused before DB work because the environment handoff deliberately has no direct connection and PowerShell's dictionary adapter required an explicit connection-string setter. Corrected root backup uses the same seed test-server connection and exact owned database. PostgreSQL16.13/pg_dump16.3 backup passes with26 migrations headed by the exact original baseline migration; dump419,677 bytes, SHA`c07b0606cabaa08ef6a1ec7ef8651087c703110c3720bff17f4f64c0982fbe23`. All43 matching environment files are snapshotted and individually verified; manifest SHA`0886398f8db508b39505574409aa2894fb930564071fd43a2014c13502ecda00`. No new task Web host is active. Read-only review confirms all52 original migration/Designer files match baseline, with eight later migrations added. Retain the exact profile/connection identity for rollback after both host lifetimes; a renamed database would be a clone with another runtime identity. Next: sign this slice after doc checks, add the narrowly identified real-Workflow terminal-status commit/ack fault theory, then freeze/build/run final gates and both actual H lifetimes. The existing downgrade-guard tests cover synthetic private Kind12 events; the H seed does not contain that evidence and cannot alone prove downgrade refusal. Cleanup approval remains pending.

The task is **incomplete**. Fresh-task preflight on 2026-09-12 found a clean `modules-decoupling` checkout at signed HEAD `79126f9faefe99b259ed1caa16263cec66b5d951`; `git verify-commit HEAD` confirms fingerprint `96E836FAA8854EE98ABC10903C206549E1D7EAD6`. The v7-v9 corrections are committed. The previous implementation task is idle. Root is the sole tracked editor and build/test/host owner; three bounded independent reviews are read-only. This continuation has performed no publication, merge, rebase, reset, stash, pull or other-branch change.

Retained `final-frozen-v9/` evidence is newer than the historical summaries below. All 6,351 recorded source fingerprints and all six recorded test DLL hashes matched the starting checkout before continuation repairs. Product, Stable and Playwright builds passed at that revision. The full Stable run completed at 09:58:44 UTC: **13,681 expected/executed, 13,626 passed, 55 failed, zero skipped or unexecuted**. Original TRX counters independently agree with its summary: Components 1,979/1,979; Integration 2,888/2,942; AgentFramework Memory 22/22; Memory 203/203; Unit 8,534/8,535. The 54 Integration failures require owning-path triage; the remaining Unit failure is the repository secret scan. This is FAIL, not a final gate PASS. Evidence: `artifacts/modules-decoupling/final-frozen-v9/stable/execution/`.

All ten inspected sibling checkouts are clean. Components remains `1c939033cb427b507086d1f7f2381c43992175ee`, FileTools `7c7453c6583365ae5bd63f8fc6efc4a776e15818`, and Mcp `abeaea463aa906bca2d83a1b608ff432dbc29d8d`. Independent read-only review confirms FileTools local HEAD and CI pin `498b36825bd5a5222429972af120b04becf4b3f6` have the identical source tree `6bc360281b6ad13ddec5e813f0a00e26b5bc7d6d`; their diff is empty. Keep the pin and record the actual local commit: build provenance can differ, but there is no source/API variance. Port 5038 has no listener; no prior task host or test process is being reused. Operator host 5032/PID1836 and Ollama11434/PID47836 remain running and untouched. Retained task database/workspace/vault and Linux ownership records remain present; identity and manifest checks are required before their next use. The task GPG configuration hash still matches its recorded finite-cache policy.

First continuation repair: provider recovery reproduced 14 failures in 19 discovered/executed cases. Its fault injector still replaced the complete schema factory although provider operations now use `ProvidersDbContext`. The fixture now decorates the provider owner factory; independent canonical readback and all existing commit, lost-response, concurrency, sanitization and non-replay assertions remain. Integration rebuild passed with seven existing warnings and zero errors. Refreshed discovery/execution passed **19/19**, zero failed/skipped, with unchanged source/binary guards at 12:06:00 UTC. Evidence: `artifacts/modules-decoupling/continuation-v10/provider-recovery-{before,after}*.{log,trx,json}`. Production behavior is unchanged by this first repair. Documentation211 and evidence9 passed. Trusted local pinentry refreshed signing access and the detached starting-evidence signature verifies with the expected key.

The provider repair is signed in local checkpoint `13886856f9c55c935b7ea7fb8155711f7ee0affe`. The next correction adds caller-displayed project admission to fresh single/batch HTTP deletion, retaining existing positional constructors, routes, invalid-disposition errors and exact durable cleanup. Both actual PostgreSQL delete/recreate cases prove missing/stale refusal and replacement preservation before fresh success. Workflow admission status now retains the existing union of runtime files and native contribution paths. Independent review found no material blocker; no new persistence boundary, migration or UI extraction is involved.

Deletion reproduced its original 500 before repair, then passed 3/3 HTTP cases. The refreshed agent-tool, task-admission and Workflow selection passed 15/15 at 12:28:07 UTC; both selections have unchanged source/DLL guards. Direct Workbench/Web builds and Integration rebuild pass. Complete portability scan 6,279 files / 30,271 findings passes final no-write enforcement with all 14,667 protected findings unchanged; no baseline edit. Evidence stays in `artifacts/modules-decoupling/continuation-v10/`.

Deletion/Workflow checkpoint `506106b6f29ff1874b196a7f27ace42e45f958f9` is signed locally. The subsequent CRM correction preserves historical allocation display and computes capacity/next availability only from the report's current, unretired lifetime. It retains legacy labels while preventing an older lifetime from borrowing a recreated project's name. The private projection adds no owner query, public DTO or schema. Current-lifetime reporting fixtures and caller-captured staffing admissions are corrected. The six-row PostgreSQL regression covers legacy, retired, recreated, missing and empty references before and after removing the only current allocation. Independent review found no blocker. The first selection retained 12/13 PASS with one further missing staffing admission; the corrected selection discovered/executed **13/13 PASS**, zero failed/skipped, with source/DLL guards at 12:42:58 UTC. CRM builds without warnings; Integration rebuild has seven existing warnings and zero errors. Final complete portability scan 6,279 files / 30,273 findings passes no-write enforcement at 14,667 unchanged protected findings.

Root's read-only inspection of the original HR run `05022a19-039d-4b7d-a47d-604d8b10f09a` confirms the exact retained profile and unchanged file SHA `59609a4ba527e0e9fe3d4ca6950f58a822e04f8c791d9e9e43bb70e897336653`. The settings activation has a Completed/Committed result; the sole unresolved proposal is a later Read/RevalidateAndRead CRM summary, with no result and zero pending approvals. Sanitized metadata is in `continuation-v10/e-original-run-journal-status.json`. This permits investigation of exact read recovery without inventing a mutation receipt; no recovery or replay has been performed.

CRM checkpoint `9d5bc9f4a7b77c49622c70b0388f1a37ee7b1564` is signed locally. The uncommitted owner-fixture slice preserves actual Project/lifetime admissions, owner save observation, persisted agent grants and canonical readbacks. Its first selection closed 98 executed, 75 PASS / 23 FAIL; corrected verification closed 27 executed, 13 PASS / 14 FAIL at 13:12:49 UTC. Both have zero skipped and unchanged source/DLL guards. Cleanup, migration upgrade/replay and asset effects now pass. Thirteen generated-image cases need their explicit model's price row and one admitted tool-integrity case still returns an unexpected generic failure. Original results are retained in `continuation-v10/owner-fixtures-{first,corrected}*`. The latest price-row and bounded logger corrections await rebuild and a 17-case run. One diagnostic-helper scope compile failure is retained before the successful seven-warning rebuild. Complete portability 6,279 files / 30,274 findings passes final no-write enforcement at 14,667 unchanged protected findings.

Final fixture verification passes **17/17**, zero failed/skipped, at 13:23:42 UTC with source/DLL guards intact. The retained preceding 17-case diagnostic had 16 PASS / 1 FAIL; its safe stack proved the test provider factory omitted production's `MafToolAdmissionChatClient`, so the contextual run had no admitted batch before dispatch. Restoring that wrapper fixes the case with every original assertion intact; temporary diagnostics are removed. Generated-image providers retain the explicit model and its existing repository price row. Independent review of all 13 test-file diffs found no material blocker. Final build passes with seven existing warnings; complete portability 6,279 files / 30,274 findings passes no-write enforcement at 14,667 unchanged protected findings. No production source changed in this fixture slice.

Read-only original E provenance also passes envelope digests and skill intent/prepared-digest/source/candidate structural matches, with original input and runtime context present, recoverable support and zero current approvals. The exact original run SHA is unchanged. `continuation-v10/e-original-run-provenance-status-v2.json` supersedes the first diagnostic's mistaken text-as-envelope projection; it does not claim current authority or SDK restoration. No recovery or effect was dispatched.

Fixture checkpoint `b5b3bdfa8518674397c05692c62589e3fa43a552` is signed locally. Exact original test names reconcile all 54 failed v9 Integration cases to later focused PASS in `continuation-v10/v9-failure-focused-reconciliation.json`; this does not relabel the original broad failure.

The Process slice adds `IProcessLaunchVariablePreparer` in the existing Application owner, updates callers/registrations and resolves only Workbench surface and definition-catalog dependencies for variable preparation. Actual launch paths retain their existing dependency scope and behavior. The unused AgentFramework-to-Collaboration project reference is removed. Three real-PostgreSQL tests prove no launch-service activation, derived values, input immutability, missing-node refusal and propagated contributor failure. The DI test covers four combinations and forces real production workspace activation before later Workbench resolution. All five changed production projects build without warnings; Integration/Unit builds pass with seven/one existing warnings after retaining one missing-import compile failure. Fresh guarded verification passes **10/10 Integration** at 13:38:59 UTC and **1/1 Unit**, zero failed/skipped. Source manifest SHA `472deeb4577b802f09de3aa91f1e2602359d770e8f8d32f492d3fed62e1f8120`; evidence `continuation-v10/process-boundary*`. Final complete portability 6,279 files / 30,274 findings passes no-write enforcement at 14,667 unchanged protected findings. Independent review found no material blocker.

Process checkpoint `bac65924e062b919fc8da5fc2f02a32732495982` is signed locally. E backend now maps single recognized CRM query errors to bounded typed no-effect failures. A previously dispatched read can complete only after current authorization, under its original Read/RevalidateAndRead contract, with no saved outcome or committed/predispatch evidence. Mutation uncertainty remains protected. Typed disclosure comes from the saved checkpoint discriminator and rechecks current HR authority; unsupported, altered and incompatible failures are refused. Independent review also found and closed adjacent returned-Unknown read normalization and typed-NotCommitted disclosure gaps.

All three changed production projects build with zero warnings/errors. Unit discovery/execution passes **42/42**. Integration first closes **79/80**, with only the new Core fixture failing before dispatch because its managed model and provider setup differed; original failure and two preceding fixture compile failures are retained. Correcting the actual reloaded model and preserving both old/new attempt receipts passes the Core case, then added approval-history and acknowledged-activation assertions pass **1/1** at 14:23:03 UTC. The other 79 include all 26 new read-recovery cases and existing mutation, privacy and restart cases. Each run has zero skipped and unchanged source/DLL guards. The actual Core/MAF fixture preserves original run/chat/input/context/session, proposal intents, approved activation, historical approvals and prior receipts; recovery records the real owner's missing-record result, and repeated completed recovery makes zero provider/query calls. This is fixture proof, not recovery of the retained live E run. Final complete portability 6,282 files / 30,278 findings passes no-write enforcement at 14,667 unchanged protected findings; no baseline edit. Evidence: `continuation-v10/e-backend-*` and `e-core-recovery-*`.

E backend is signed locally as `3b9af6d4ff967381b746e0783350bca55796e5ed`. The explicit UI recovery action now calls only the recovery owner with the original run/chat and captured profile/activity scope. It reads saved state before clearing the warning, preserves draft/attachments, exposes existing pending approvals and rejects detached/profile-stale effects. Current profile generation also fences catalog/thread/detail acceptance and late send refusal. Real floating-handle cleanup remains mandatory after detachment. Independent review found the stale-refusal gap and a deterministic test synchronization race; both are corrected.

Focused orchestrator proof passes **15/15**. Components first retains **156/165**, all nine failures exposing the disabled recovery button after a refused send; its last render observed an unfinished tracked task. The corrected render predicate retains click-time overlap admission and enables recovery without another refresh. The same discovered **165/165** cases pass with zero skipped and unchanged source/DLL guards at 14:58:58 UTC. Core/module builds pass without warnings; Unit/Components builds retain one/three existing warnings. The browser fixture's missing isolated assets file was restored and its build passes with zero warnings/errors. Evidence: `continuation-v10/e-ui-*`. No actual retained E recovery or new host has been started.

E UI complete portability covers **6,284 files / 30,278 findings** and final no-write enforcement passes at **14,667 unchanged protected findings**. Enforcer6/scanner4 tests pass; no baseline edit. Next: sign this UI checkpoint after doc checks, implement the bounded Projects summary query for the three audited point lookups, then extend H retained chat/pricing/Collaboration proof before the final source freeze. H's old prepared fixture never started; its corrected ignored holder is not yet built or launched. Linux ownership and read-only mounts were freshly reverified; operator5032 and Ollama11434 remain untouched, with no5038 listener. Final browser/platform/G/H and B/E recovery obligations remain open. All ten obsolete plaintext copies and ZIP entries were independently hash-verified; specifically scoped cleanup approval remains pending and no cleanup has occurred.

E UI is signed locally as `104837e44f2b26f5ee55cc2c16786c7df489f145`; documentation211/evidence9 also pass. The focused architecture audit reconciles 24/24 owner rows and the seven retained AgentFramework module references, with no ordinary foreign EF writer found in the reviewed paths. Three exact-project reads still loaded the whole portfolio; they now use `IProjectSummaryQueryService`, an alias of the existing scoped Projects service. Filtered project/phase/hierarchy reads and exact-ID CRM lookup reuse the complete existing summary mapper. This avoids the basic project-query/CRM constructor cycle and preserves editor overloads and existing list behavior.

Projects/Workbench production builds pass without warnings; Integration/Components builds pass with seven/three existing warnings. Fresh guarded Integration **8/8** passes at 15:11:12 UTC, including the new PostgreSQL query/aggregate/CRM proof, existing owner/profile checks, two 21-scenario Workflow harnesses and two operator recovery cases. Components **1/1** passes at 15:11:38 UTC with rendered CRM labels and filters preserved. No skipped cases or source/DLL drift. Independent review found no blocker. Complete portability covers 6,286 files / 30,280 findings; final no-write enforcement passes with 14,667 unchanged protected findings and no baseline edit. Enforcer6/scanner4 pass. Next: sign after doc closure, then install/build the prepared H chat/history/Collaboration fixture extension from `continuation-v10/MigrationAuthorityIntegrationTests.OwnedHostHistory.cs`; that ignored draft was prepared while the guarded tests ran and is not yet compiled or executed. No new host/runtime action has occurred.

### Historical v7-v9 checkpoint evidence

V7 Stable compilation passed in 1:23.17 with eleven warnings and zero errors. Exact focused checks passed: DI 2/2, Unit 511/511 and Components 126/126, with zero failed/skipped cases and source/binary guards passing. Narrow canonical Agent/capability catalog leases resolved the second DI cycle while retaining current-profile and revoked-capability checks. Complete owner policies and stale owner fixtures were repaired.

Integration discovery reconciled 276 cases, including forty existing MAF partial-file cases missing from the initial expectation. All 276 executed in 39.9667 minutes: **260 passed, 16 failed, zero skipped**, with source/binary guards intact. Evidence: `reviewed-v7-integration-focused-expanded/`. Twelve failures came from two fixtures passing unassigned MCP/native capabilities to MAF; two Agent-access fixtures invented projects; one deletion probe conflated precommit owner saves with subsequent cleanup; one Structure fixture captured only four of its fourteen required tools. Original TRX remains unchanged. Reviewed fixture corrections preserve current grants, native uncertainty barriers, owner effects, exact transactions and restart assertions.

V8 Quartz-focused verification closed 140 expected/discovered/executed: 139 PASS, one HR affiliation FAIL, zero skipped, in 11.2461 minutes; source/binary guards PASS. Its legacy-history, historical Simple Chat settings and Quartz cases passed. V9 exposed the remaining fixture error as InvalidToolArguments: the synthetic CLR request omitted required affiliationId:null. The corrected fixture preserves the actual AIFunction wire options and explicit null, exact owner effects, approval and replay. The new blocked-thread fixture also needed its acquired journal lease bound before seeding a committed receipt; no production guard changed. Original failures are retained. V9 corrected owning proof passes 18/18 Integration in 1.7366 minutes with source/binary guards, alongside 3/3 Unit and 55/55 Components. Evidence: `reviewed-v9-chat-start/`; corrected source1522 SHA `eb6b7d8fe2b2ffe44fd68166a6a715b615d9abcf8cefbecd6b928783de214543`. Stable build passed in 1:34.36 with eleven existing warnings; final Integration rebuild passed in 28.53 seconds with seven existing warnings, zero errors.

The v6 full Stable gate remains historical **FAIL**: 13,480 expected, 10,937 completed, 10,813 passed, 124 failed and 2,543 Integration cases unexecuted after a diagnosed constructor-cycle hang. Original TRX and corrected aggregate reconciliation remain in `combined-owner-disclosure-normal-v6/stable/execution/`. V6 Windows/Linux builds and v7 focused passes are not final broad/platform evidence.

Current portability-static PASS: complete untruncated scan of 6,279 files / 30,266 findings includes new protected files. All eight ADDED/five STALE entries exactly match the already inspected Process file-owner changes; the previous two reviewed MCP deltas remain. Baseline refresh was inspected, including count2 to1 rather than deleting the retained absolute-path allowance. Final enforcement without --write-baseline passes with 14,667 reviewed protected findings. Enforcer6 and scanner4 tool tests, documentation211 and evidence9 pass. Evidence: `reviewed-v9-static-final/`. Ten obsolete synthetic-key plaintext copies remain after automatic approval review rejected cleanup; their verified archive and pending specific user confirmation are unchanged. Artifact secret scanning is not green. The task browser was closed and its four exact cache DBs moved, with verified identical hashes, from the literal source-root directory to ignored `browser-cache-preserved-20260912/`; no unrelated process or data was touched.

The authorization-enabled isolated host is `http://127.0.0.1:5038`, PID71800/parent58268, process started 05:10:12.931232 UTC from `live-e2e/app-final-v9/`. Manifest SHA `597b187e0a0f0b04b5db9e43b95e57b82780d418cfb3606550192ed1a6f2a1b5` covers 1,946 files and seven matching compiled owner/Web binaries. Exact v8 process exited and port release was reverified before replacement. Database `cditall_modules_e2e_3d9c82d394b3`, profile `9bf37898-cf12-7578-0112-39f40d55747c`, vault and workspace are retained; user5032/PID1836 and Ollama11434 remain untouched. Two image-scan findings are the same unchanged public development-password fingerprint, with original scanner FAIL retained and explicit source-byte review. Final frozen build/runtime gates are still pending.

A/C results remain retained: OpenAI floating run `796c945b-d97a-40d0-9c0e-a681d3570268` read 150 actual file bytes, a separate Resource and authorized CRM data, then created two individually approved Alpha tasks. Beta navigation/hide/reopen preserved scope. Gantt dependency was an operator drag. Ollama gpt-oss:20b completed scoped tools as run `5a052f60-fe31-4e5d-983c-2868919104e7`. C recovered original UI admission `16fb8104-e032-432b-a8af-b139a730c283` into the same run `d5cc4be3-a24a-4302-82a6-83e115670354`, one provider history row, one 1,574-byte asset and Applied delivery. B denial/stale-load closure remains open.

D original process `dfa774ef-9dca-445b-b2b9-96b71188f69a` accepted UI rework at 03:13:44 UTC and completed all three actual Agent-backed steps at 03:16:23 UTC without another Start. Three produced files match their recorded hashes, and native run/summary/output projections persisted. Evidence: `final-D-completed-owner-v7.json` and `final-D-structure-projection-v7.json`. Browse files exposed a real mismatch: the producer uses the original organization workspace while Structure/native Process browsing used another root. The applied correction reuses `IProcessRunFileScopeProvider`, validates accepted receipt/profile/current project lifetime and resolves the exact managed root. Native ephemeral scope keys become `run:v2`; old views must reopen. On v8, all three files were downloaded through Structure and exactly match the original producer hashes; evidence `final-D-ui-artifacts-v8.json/png`. Owning Unit/Components pass; two new deletion/recreation PostgreSQL cases hit the shared Quartz failure. No authority is backfilled and no schema changes.

E retained activation, CRM entities/affiliation, Active Simple Chat R2 and ordinary streaming remain verified, with C1 pinned to R1 and both conversations retaining four messages. The older HR run `05022a19-039d-4b7d-a47d-604d8b10f09a` correctly refuses new Send because unresolved effects remain. V9 now emits a typed safe reason, restores the unsent draft and shows a persistent Alert. Actual UI Send at05:13:00 UTC and same-thread Refresh retain the warning, draft, original two messages and byte-identical original run; the run inventory is unchanged. Evidence `live-e2e/evidence/final-E-original-thread-refusal-v9.json` and screenshots. All twelve new regression rows pass within the owning Unit/Component/Integration selections; Active work receives wait guidance, terminal uncertainty receives reconciliation guidance, and stale/foreign/ABA completions cannot replace the current composer. No prior activation, mutation, approval or journal uncertainty was replayed or cleared.

F actual managed Scheduler UI run `8b301779-bd91-4ab5-976c-7b6d182d6743` received one exact operator approval and created plan `3b39bcff-0985-41d4-bc60-f6a94a85c2fe`, `F-v8-20260912-0430`. Real Quartz fired at04:30:00.008644 UTC: fire `41e61372-fef2-44c6-a8d8-14ba0fcf7497`, Scheduler/admission `4e02ac44-86c9-48c0-a595-65dcef2b07a1`, Workflow `98a99d36-e46a-4de4-a1e3-dba1deb8c689`, Completed04:30:00.956421. Actual UI History showed one completed run. Root paused the exact plan through UI and reloaded; canonical04:33:41 readback confirms one plan/fire/admission/workflow, Observed generation1, no active claim, no next firing and IsEnabled=false. Plan/history remain for review. Evidence `live-e2e/evidence/final-F-completed-v8.json`. Scheduler assistant used actual OpenAI/tools; the small echo Workflow has no provider step or notification. Prior v7 refused save and first v8 target-search miss remain retained.

Browser 72, LiveProcess 1, final Stable, Linux runtime lanes, G/H reconciliation and the old-schema two-lifetime migration/restart remain open. Reviewed native shared-provider seed/launchers and H published-image launcher are ready for root execution after the final source is frozen. Canonical marker `20260911194528_BindWorkflowProviderDisclosureHistory` retains the 161-entity schema with zero Up DDL; old readers cannot consume new private evidence. Code rollback is not data rollback. Restore the finite task GPG cache policy only at closure and only if unchanged.

Next: inspect/sign the coherent local checkpoint, freeze current source and sibling identities, then execute final product/Stable/browser/platform/migration gates and remaining A-H reconciliation. Linux SDK/PostgreSQL ownership and read-only mounts are pinned by guard `db40692937daa6040194ed2237c6a89b1ad29c55fd00b30cd1d7776c406a7d6e`; actual mounted FileTools7c7453 and Components1c9390 match clean Windows siblings. The corrected ignored runner preserves canonical source/stamp guards. Python3.12.3, Node18.19.1 and npm9.2.0 are installed only in that disposable SDK container; runtime lanes remain NOT_RUN. Continue automatically.

## Ordered milestones

| Order | Outcome | Current state |
| --- | --- | --- |
| 0 | Signed authorization, current product baseline and real-provider connectivity | Complete; initial HEAD preserved, signed checkpoints verified, OpenAI/Ollama actual tool calls observed |
| 1 | Bounded owner persistence, immutable profiles and one complete migration authority | Applied across the24 coverage rows; current owner continuation validation/signing pending |
| 2 | Agents/Providers technical/pricing ownership, CRM projections and Work assignment ownership | Applied with focused proof; final retained-operation proof pending |
| 3 | Projects/Structure/Resources/Storage lifetimes, native receipts, cleanup and selected transfer | Applied with focused proof; current correction and final upgrade/restart/transfer proof pending |
| 4 | Workflow/Process/Scheduler source authority, durable admission, native result delivery and recovery | Applied including retained provider disclosure; actual C workflow complete, Process/Scheduler and final gates pending |
| 5 | HR Simple Chat definition administration with owner-atomic creation; retained curators/CRM/Scheduler | Applied with focused proof; final managed-HR, ordinary chat and Scheduler journeys pending |
| 6 | Remaining disclosure contracts, cumulative architecture review, frozen broad/platform gates and A-H | In progress; actual A/C complete within recorded limits, B reconciliation and D-H/final gates open |

The order is adaptive. Existing functionality and mandatory evidence remain in scope; speculative future integrations do not. Root is the sole tracked editor, composition/migration owner and executor of builds, tests, database operations and live hosts. Delegated work is limited to frozen ignored drafts and bounded source reviews.

## Decisions and operational constraints

- Runtime business owners expose typed detached contracts and explicitly enlist in a shared transaction when atomicity spans owners. Complete `AppDbContext` composition remains for canonical migrations, profile/schema administration and selected transfer coordination. It is not a generic business repository. Process keeps its separate19-record context; canonical composition is161 records.
- Work Management owns WorkItemAssignee rows, including non-task WorkItem subtypes, within Workbench. CRM owns other participation and workforce/rate facts. Preserve exact IDs, duplicates, orphan history and original lifetimes; mixed changes keep native revision/pricing and assignments in the actual transaction. Combined reports filter, order and page the combined result.
- Projects admission binds canonical profile, public project ID and immutable lifetime. Original source/grants are rechecked at native commit; a recreated ID never inherits old authority. Existing missing-lifetime history stays readable but cannot be reminted into recovery authority. Compensation requires the original create receipt and unchanged owner snapshot; human edits survive.
- Transfer follows shipped selections: Projects owns its twelve Projects/Workbench collections; Agent catalog is separate. Project-only import preserves unrelated CRM cursors and local Process launches. Receipt fields travel with their owner dataset. No new universal Workflow/Process/Scheduler journal export is required. IPFS transfer acquires a fresh target reference while retaining original placement provenance as history.
- Admission, confirmed mutation, execution completion, projection delivery and business acceptance are distinct. Confirmed writes retain typed committed-result failures; lost acknowledgement remains uncertain without owner proof. Native receipt and local effect must share their transaction. External transport uncertainty is not exactly-once delivery.
- Preserve accepted Agents UI, routes/tool names/managed identities, serial tool invocation and current approval defaults. Ordinary Simple Chats remain text-only with no tools, implicit context or HR transcript access. HR definition administration and existing owner-backed CRM operations remain separate capabilities.
- Workflow provider disclosure retains private owner evidence across transformations and actual SDK wait/resume. Original/current source, project or path grants must be checked before provider I/O. The compatible producer, persistence codec, provider gate, reader filters and protocol marker deploy together. No authority comes from prompt text or public diagnostics. Genuine undeclared v1 history is preserved without fabricating protected-read proof.
- Git uses `C:/Program Files/Git/usr/bin/gpg.exe` and its matching GnuPG2.4.9 agent. Expected fingerprint is `96E836FAA8854EE98ABC10903C206549E1D7EAD6`. Trusted pinentry reuse was verified in separate shells at15:34:56/15:37:44 UTC on2026-09-11. Never handle passphrases in scripts or fall back to unsigned commits.
- **Restore at closure:** `%USERPROFILE%/.gnupg/gpg-agent.conf` was originally absent. The task file SHA is `89E963D5696BE60087BFA127381DECACB857FB470CC5A2DCAF7A03252B6DDC5E`, with finite14400/28800-second idle/absolute cache values. Remove only if unchanged, then reload the same agent. Preserve concurrent edits; never kill the agent or leave restoration unreported.
- Preserve existing user host PID1836 on5032. Root owns final host5038/PID76136 only; preserve its evidence before shutdown. Windows long paths are disabled, so use short owned outputs without changing the registry. Verify exact process/fixture identities before shutdown or cleanup; do not delete unattributed databases.
- Components and FileTools sibling sources remain unchanged. Record their actual commits and the FileTools CI-pin variance; do not hide sibling changes. Linux resources are recorded in `artifacts/modules-decoupling/linux-final/ownership.json`; verify exact labels/IDs before cleanup. The isolated UiSandbox output override changes only build destinations under the owned mount.

On resume, verify branch/HEAD/dirty files, read this current state and reconcile proof with COVERAGE/VALIDATION before continuing. Keep actual source/build inputs unchanged while their guarded checks run. Final closure requires a clean signed worktree, owned cleanup, restored task signing policy and a truthful implemented-versus-E2E report.
