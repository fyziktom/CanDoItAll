# Module Decoupling Merge Readiness

One compact verdict for the operator's merge decision of `modules-decoupling` into `components-decoupling`. It is the practical handoff, not an implementation bundle; the dated history stays in [EXECUTION.md](EXECUTION.md), [VALIDATION.md](VALIDATION.md), [COVERAGE.md](COVERAGE.md) and the [manual test handover](MANUAL_TEST_HANDOVER.md). Written on 2026-09-15 for the signed head `160616c8256594257d00612b4e7dbadd567c024e`.

## Verdict

| Axis | Status | Basis |
| --- | --- | --- |
| MERGE_READINESS | READY_FOR_OPERATOR_DECISION | Every gate executable on this machine ran on the signed head `160616c8` and its `build-fix13` binaries: the broad Stable gate, the browser partition, the LiveProcess lane, the generated-app scenario and the static gate. The only failures are three retained raw findings that predate this closure and are documented below (the local secret scan on git-ignored drafts; two timing-bound browser flows, one of which passes in isolation). Architecture, API compatibility, dependencies, documentation and merge topology carry no blocker. What remains is the operator's own decision on the drafts, on the four lanes that cannot run here, and on the blocked Garden run; none of them changes the product source. |
| ARCHITECTURE_REVIEW | PASS_WITH_DOCUMENTED_ADAPTERS | Audit of the 24 Foundation owner cards against the current callers, DI registrations, contexts, contracts and routes (read-only, then repaired in `160616c8`): no foreign `DbContext` or `Set<T>`, no second authoritative writer in the composed host, no EF/Blazor/`IServiceProvider` type in a published contract, reusable UI references abstractions only. The three medium findings (unguarded global-context factory, order-dependent owner bridge registrations, live legacy CRM technical writer) are closed by a guard, by `Replace` registrations with order tests, and by an order test that proves the Agents bridge wins in every composition order; the low findings are recorded below as intentional adapters or deferred presentation coupling. |
| AUTOMATED_VALIDATION | EXECUTED_ON_HEAD_WITH_RETAINED_RAW_FAILURES | On `build-fix13` (source `617f8a86…`, head `160616c8`): Stable gate `stable-fix13` **13,872 executed / 13,871 passed / 1 failed** (Components 2,023/2,023, Integration 3,009/3,009, AgentFramework.Memory 22/22, Memory 203/203, Unit 8,614/8,615; the failure is `SecretScanningTests` on the git-ignored drafts); browser discovery 72, ordinary partition **56/58**, dashboard **1/1**, headless core **1/1**, isolated rerun of the two failures 1/2; LiveProcess **1/1** against the local Ollama; Scenario04 **1/1** against the owned generated app; static gate `RESULT: PASS (14672 reviewed executable-source findings unchanged)` on the committed head; focused repair lanes 86/86, 171/171, 4/4, 57/57, 100/100, 272/272, 104/104 on the working tree that became the head. Not executed: Linux824, synthetic3, real8, sibling DotNetWatch (reasons below). Details and hashes in [VALIDATION.md](VALIDATION.md). |
| API_COMPATIBILITY | ADDITIVE | Generated OpenAPI of the head (289 paths, 321 operations, 518 schemas, SHA-256 `4A72044B…`) against the 2026-08-31 snapshot (276 / 308 / 486): 13 added operations, 32 added schemas, 15 schemas with added fields, 20 operations changed only through those schemas; no route, method, operation id, tool name, enum member or authorization scope removed or renamed. |
| DEPENDENCIES | REMOTELY_REPRODUCIBLE | Components CI pin `7b618cdac5570e806832e3f5ff65ddf1439eb868` is the `development` branch head on GitHub (parents `bd1eb103` and the repair `780e9a30`; tree `35a4d090…` equals the repaired worktree the guarded builds consume); FileTools pin `498b36825bd5a5222429972af120b04becf4b3f6` is on GitHub and equals the sibling tree; both verified 2026-09-15 through the GitHub API. A fresh clone can build CI's graph. |
| DOCUMENTATION_AND_SHAREDINFO | REFRESHED | `Test-Documentation.ps1` passed for 213 maintained Markdown files and `Test-DocumentationEvidence.ps1` passed 9 evidence tests on this record set; `docs/architecture/agent-tool-failure-recovery-boundary.md` and `docs/architecture/modules.md` carry the new contracts. SharedInfo: local branch `modules-decoupling-api-refresh` over `main` `00e6fe63`, signed commit `032149cb` (OpenAPI snapshot 289 / 321 / 518 captured on the canonical port from an isolated capture host of the audited publication, manifest, README, partner migration matrix, agents and processes skill guidance, regenerated route appendices, the new owner-contracts architecture page); `Test-CanDoItAllWebOpenApi.ps1` Passed and `Test-SharedInfo.ps1` 0 failures (45 skills, 403 markdown files); not pushed, not merged, no installed skill overwritten. |
| SECURITY_GATES | RAW_FAIL_RETAINED | `SecretScanningTests` walks the working tree and fails on ten synthetic-key draft copies under the git-ignored `artifacts/modules-decoupling/drafts/**`; they are not tracked, so a clean clone and CI do not see them. The published-image scan flags the development defaults of `appsettings.Development.json` inside the publish output. No exemption, relocation, exclusion or scanner change was made; both stay operator decisions. |
| MERGE_TOPOLOGY | FAST_FORWARD_POSSIBLE | `components-decoupling` (`cb10029523ec67aaff7fb24ebff3294fc45ef26b` locally and on GitHub) is the merge base of `modules-decoupling`; the source is 56 commits ahead and 0 behind once the two closing record commits above the product head (`b2166187` and the topology correction that follows it) are counted, so integration is a fast-forward in history terms. The product head `160616c8` and those record commits are not yet on `origin/modules-decoupling` (`0374d773`). |
| OPERATOR_BASIC_MANUAL_QA | USER_REPORTED_PASS (scope: basic, not exhaustively enumerated) | The operator reported on 2026-09-15 that the application works after basic manual testing on the private candidate. The exact scenarios, build identity and results were not recorded by the operator and are not inferred here; the per-scenario operator results in the handover stay `NOT_RUN`. |
| MERGE | NOT_PERFORMED | No push, pull request, merge, rebase, squash or target-branch move was made; the SharedInfo refresh is a local branch as well. |

## What `160616c8` closed

The audit findings were repaired in their owners with regression proof in the same commit (40 files, +1586 / −92, signed with key `96E836FAA8854EE98ABC10903C206549E1D7EAD6`):

1. **Agent chat context scope lifetime** (`AgentFramework.Core`, Workbench, CrmHr, Projects, AgentFramework pages). Scope and publication leases report `IsActive`; a provider whose lease was superseded by a newer scope, or that is disposing, stands down instead of publishing or synchronizing navigation; an execution-completed notification is dispatched only when the lease that admitted it is still the active one at consumption; fire-and-forget reloads log their faults instead of dropping them. Deterministic bUnit proof admits the notification from a foreign thread while the renderer is busy (`ProjectStructureAgentChatContextProviderTests`, `CrmAgentChatContextProviderTests`, `FloatingAgentChatArchitectureTests`). This is the class of race behind the one timing-bound Components failure of the `stable-fix11` gate.
2. **Process pre-dispatch tool inventory versus executable authority** (Workbench). An inventory context composes the declared task and project tools as inert `ProjectStructureInventoryOnlyTool` wrappers that refuse invocation with `403 ProcessToolInventoryNotExecutable`; the inventory access state allows no project, not even `Guid.Empty`, and grants nothing; dispatch still composes its own tools from the saved execution, and a governed path without the inventory flag still fails closed (`409 ProcessExecutionReconciliationRequired`). Proof: `ProjectStructureAgentRuntimeToolProviderTests`, `ProjectStructureAgentRuntimeToolRoundTripIntegrationTests`.
3. **No-effect and denial semantics** (Workbench, `AgentFramework.Maf`). Rejected asset content is a typed `ProjectAssetContentValidationException` mapped to `ProjectAssetContentInvalid` (400, effect `None`) instead of a framework exception; a saved pre-dispatch denial is restored on replay only when its journal outcome is `NotCommitted`, and a tampered checkpoint is refused without consulting the owner or dispatching; effect evidence distinguishes the same target reached by different tools and path spelling variants; the completion contract stays truthful for the eight audited scenarios (`AgentToolFailureEffectEvidenceTests`, `ToolOutcomeCompletionIntegrationTests`, `MafPolicyDenialCheckpointTests`, `ProjectAssetStorageServiceTests`).
4. **Plan truth and costing** (Workbench). One helper, `ProjectTaskExecutionStatePolicy.ResolveExecutionBackedProgress`, is used by the plan summary, the manager forecast and the Gantt task metrics; a recorded `NotStarted` or `Started` state beats the status text and the canvas hint, `Cancelled` is excluded from the progress aggregates, and a zero quote stays distinguishable from a missing one. The person quote summary no longer discloses the hourly rate to the agent (`ProjectPlanSummaryCalculatorTests`, `ProjectStructureGanttProjectionAdapterTests`, `ProjectStructureTaskResourceCostServiceTests`).
5. **Gantt Mermaid export contract** (Workbench). The export is a schedule snapshot: a task line carries the projected UTC start and duration, and is written as `after <ids>` only when the projection synthesized the whole interval, every predecessor precedes it in row order and its start equals the latest predecessor end. The exporter now uses its own filtered predecessor set; multi-predecessor (`after task1 task2`) and pinned-successor cases are covered.
6. **Composition hardening** (Projects, Workbench, CrmHr, AgentFramework). Projects registers its no-op bridge defaults with `TryAddScoped`; the owners install their implementations with `services.Replace`; `OwnerBridgeRegistrationOrderTests` proves the real bridges win in every registration order; `DatabaseCanonicalityArchitectureTests` also refuses any product-module use of the global `AppDbContext` factory.
7. **Documentation.** `docs/architecture/agent-tool-failure-recovery-boundary.md` gained the denial-consistency, typed asset validation, tool inventory and context lifetime sections; `docs/architecture/modules.md` gained the owner contracts and adapters section; one reviewed portability baseline fingerprint was refreshed for the restructured case-policy line.

## What the next UI decoupling consumes

Stable owner contracts (see [docs/architecture/modules.md](../modules.md#owner-contracts-and-adapters) and the SharedInfo architecture page `candoitall-owner-contracts.md`):

- Agents / Providers: `IAgentFrameworkWorkspaceService` through `ICanDoItAllAgentWorkspaceFactory`; `/api/agents`, `/api/shared-providers`; same-run recovery and cancellation reconciliation routes.
- CRM/HR: `CrmHrServices`, `/api/crm-hr`, the `crm_planning_*` tools; the Projects-owned party bridges (`IProjectPartyIntegrationBridge`, `IProjectPartyCostRateBridge`, `IProjectWorkAssignmentPartyFacts`) implemented by CRM/HR and replacing the Projects no-op defaults whatever the registration order.
- Projects: `ProjectsService`, `ProjectWriteAdmissionService`, `/api/projects`; creation receipts and fingerprinted compensation for multi-owner journeys.
- Project Structure / Workbench: `ProjectWorkbenchService`, `ProjectStructureAgentService`, `/api/project-structure`, the `project_structure_*` and `project_task_*` tools; plan analytics read the recorded task execution state on every surface.
- Workflows and Processes: `IProcessRuntimeUnitOfWork`, `IProcessPreparedLaunchStore`, `/api/processes` (including `GET /api/processes/launch/{admissionId}`), `/api/workflows`; the pre-dispatch preflight receives inert tool inventories.
- Resources / Storage: `StorageStablePlacementService`, `/storage`, `/api/storage-placement-recovery`, the `storage_*` tools.
- Simple Chats: `LlmChatDefinitionApplicationService`, `/api/llm-chats`, `/api/llm-conversations`, `/api/llm-chat-operations`.
- Agent chat context: `IAgentChatContextRegistry` with one active scope per circuit and `IsActive` leases; `IAgentRuntimeToolProvider` composition at the owning module boundary.

Presentation-only coupling intentionally deferred to that work: the Agents module Razor pages still import CRM, Security, Prompts and Workspace types (DEFERRED_UI in the coverage table); `Modules.Workbench` keeps an unused project reference to `Modules.Security`; `ProjectStructureWorkflowAuthorityService` remains a seven-file partial class. None of these adds a writer or a persistence dependency.

## Remaining intentional adapters and bounded maintenance

- `AgentFrameworkAiTechnicalAgentBridge` is the only caller of the CRM technical-agent projection store; the CRM `LegacyAiTechnicalAgentBridge` is a fallback for hosts without the Agents module and is replaced whenever that module is registered (guarded by `OwnerBridgeRegistrationOrderTests`). CRM's party merge still copies the legacy technical columns that only the fallback reads.
- `Modules.Processes` reaches Agents only through the `Hosting` API in its `Services/RuntimeIntegration` folder.
- The complete `AppDbContext` model exists for migrations, transfer and schema health through `IProfileAppDbContextFactory`; product modules never inject the global context or its pooled factory (guarded by `DatabaseCanonicalityArchitectureTests`). The pooled factory registration itself remains for that maintenance composition.
- Workbench reads Process persistence store interfaces read-only for node and workflow authority projections (low, documented seam).
- Deleting and recreating a technical agent with the same party hint yields a new CRM party while the old one keeps a `Missing` binding; no manual field is overwritten and no grant is projected (documented reconciliation).
- Six owner contexts (Process, TestLab, Security, Prompts, Resources, Simple Chats) do not stamp concurrency tokens because they own no such entity; the Process context scans its own assembly instead of listing mappings; the host does not enable `ValidateScopes` outside Development. Informational only.

## Blockers, denied operations and platform limitations

- **Security findings.** The repository secret scan keeps its raw FAIL on the ten git-ignored draft copies, and the published-image scan on the development defaults inside the publish output. Deleting the drafts or accepting the local-only failure is the operator's decision; CI does not see the drafts.
- **Denied housekeeping stays denied.** The protected synthetic artifacts were not cleaned and the GPG agent cache settings were not changed or restored (`gpg-agent.conf` SHA `89E963D5…` unchanged); all commits of this task were signed through the configured agent and verified.
- **Lanes not executed on this head, with reasons.** Linux824 (its owned containers have exited and its activation inputs are pinned to the `5a8aff09` lineage; re-pinning them is a new reviewed input set), synthetic3 and real8 (their `Build-SyntheticImage` inputs are pinned to an older image fingerprint; the real pair also needs the approved OpenAI credential), the isolated sibling DotNetWatch lane (it needs a sibling patch, and the siblings are read-only for this task). Two browser cases keep a raw FAIL on this head: `AppSmokeTests.Project_structure_meeting_and_send_composers_keep_static_select_options` (the floating standard-blocks toolbox window intercepted the outline click for 30 s in the ordinary lane; it passed in 11 s in the isolated rerun and in the fix2 lane) and `AiAgentFlowTests.Agentframework_catalog_projects_agents_into_crm_hr_directory` (the CRM-HR agent-item flow that has failed at four different timing-bound steps across the head2, fix1, fix2 and fix13 lanes; a known observation, exercised manually on 2026-09-14).
- **Operator-owned data left as found.** The blocked Garden process run `d4a6edde…` was not recovered, cancelled or replayed; the two prepared launch admissions left by non-mutating preflights (`82b66b97…` for Customer onboarding, `4f88822b…` for the Garden note writeback on this head) stay `Prepared` and unconsumed.
- **Product observations for triage (not defects of this refactor).** The typed same-run recovery denial surfaces through the API as a generic `agents.run-failed` 500; the shared dialog component (Components sibling) can drop the circuit when a task-create dialog is submitted about 100 ms after it opens; a CRM-HR agent item click issued immediately after the filtered match count appears can be lost; a process run blocked before dispatch keeps its persisted blocker classification after the defect is repaired and waits for an operator decision; the example Mermaid workflow started on a Note parent reports that no source document was loaded; the privacy-filtered CRM planning summary does not disclose a person's rate to the agent, so the agent may search for it in vain while task pricing still applies it through the Projects bridge.

## Regression scenarios to repeat after any new change

Automated (all on the head's build, see VALIDATION.md for the lane identities):

- `ProjectStructureAgentChatContextProviderTests.Provider_stands_down_when_a_newer_provider_supersedes_its_scope` and `…_dispatches_an_execution_notification_only_to_the_scope_that_admitted_it`; `CrmAgentChatContextProviderTests.Provider_stands_down_when_a_newer_scope_supersedes_its_selection`; `FloatingAgentChatArchitectureTests.Leases_report_inactive_after_supersession_or_disposal`.
- `ProjectStructureAgentRuntimeToolProviderTests` inventory cases (no project incl. `Guid.Empty`, configured task and project tools reported without admission, inert tools refuse invocation) and `ProjectStructureAgentRuntimeToolRoundTripIntegrationTests.Process_tool_inventory_composes_declared_tools_that_cannot_read_or_mutate_any_project`.
- `MafPolicyDenialCheckpointTests.Tampered_denial_checkpoint_is_refused_without_consulting_the_owner_or_dispatching`; the three truthfulness cases of `ToolOutcomeCompletionIntegrationTests`; `AgentToolFailureEffectEvidenceTests` (same target, different tools; path spelling variants); `ProjectAssetStorageServiceTests` typed rejection.
- `ProjectPlanSummaryCalculatorTests` (cross-surface agreement, cancelled excluded, started stays running, not-started stays planned or ready, zero versus missing quote); `ProjectStructureGanttProjectionAdapterTests` (multi-predecessor export, pinned successor, execution-state metrics); `ProjectStructureTaskResourceCostServiceTests.Person_quote_summary_does_not_disclose_the_hourly_rate`.
- `OwnerBridgeRegistrationOrderTests`, the `ProjectWorkbenchServiceArchitectureTests` bridge theory and `DatabaseCanonicalityArchitectureTests.Product_modules_do_not_reach_the_global_app_db_context`.
- The portability static gate (`tools/Validation/Portability`, no `--write-baseline` at the end) and the documentation validators.

Manual, on the private candidate (handover scenarios): 17 (CRM planning in the structure: Gantt chain, Manager Summary `FUTURE PLAN REMAINING` equal to the sum of the quotes, API `plan/summary` agreement), 16 (simple process from the Garden structure with writeback: preflight ready, run Completed 2/2), 13 (structure analysis with SVG assets: a `File`/`text` request for an SVG is a correctable `ProjectAssetContentInvalid` failure inside the same run), 4 and 5 (pending approval survives navigation; current denial with zero effects), and 11 (restart with retained data through the identity-checked helpers).

## How the integration would be performed (not performed here)

1. Verify the head: `git log -1 --show-signature 160616c8` must show a good signature from `96E836FAA8854EE98ABC10903C206549E1D7EAD6`; `git status` must be clean.
2. Confirm the topology has not moved: `git merge-base modules-decoupling components-decoupling` must still be `cb100295…` and `git rev-list --count components-decoupling..modules-decoupling` must be 56.
3. Fast-forward `components-decoupling` to the closing records head of `modules-decoupling` (the product source is `160616c8`; the two record commits above it change documentation only; no merge commit is needed); push `modules-decoupling` first so the CI Components pin `7b618cda` and the head are both public.
4. In SharedInfo, review and merge the local branch `modules-decoupling-api-refresh` (`032149cb`, base `main` `00e6fe63`) after the product integration, then reinstall the API skills; the OpenAPI snapshot names the branch contract and the commit, so it stays correct after a fast-forward.
5. Re-run the CI workflow on the target; the two required security gates keep their known status until the operator decides on the drafts and the development defaults.
