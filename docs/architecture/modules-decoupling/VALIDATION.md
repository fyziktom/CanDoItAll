# Module Decoupling Validation

## Environment and revision

Baseline code: `3da508996e46e68d811f5ffb6926df8b876878cf`, branch `modules-decoupling`, initially clean. Windows 10.0.26200 x64; SDK 10.0.303 selected by the existing 10.0.302/latestPatch policy; runtime 10.0.11. No SDK/package changes. Ignored evidence root: `artifacts/modules-decoupling/20260910-baseline/`.

| Source dependency | HEAD | Initial state |
| --- | --- | --- |
| Components | `1c939033cb427b507086d1f7f2381c43992175ee` | Clean; branch **codex/original-ui-refactoring-release**; matches CI pin |
| FileTools | `7c7453c6583365ae5bd63f8fc6efc4a776e15818` | Clean; detached; differs from CI pin `498b36825bd5a5222429972af120b04becf4b3f6` |
| Mcp | `abeaea463aa906bca2d83a1b608ff432dbc29d8d` | Clean; main |
| SharedInfo | `00e6fe6389eba9a6bc3b2bb78e27fb10b7597292` | Clean; main |
| AgentFramework.Rag | `3cc37118461437f1b798af804b0b047a1558be24` | Clean; main |
| AgentFramework.SemanticCompletion | `fef6ab6ec84e04b4edb3f94ffb9d3ca81e79e32e` | Clean; main |
| CodeAnalysis | `b023500eee239a56358843f1380adb4610f72850` | Clean; main |
| IPFS | `1869a78e45ea6591a7c5acb24ac83f7401685711` | Clean; main |
| Ledger | `317e452d3cce9fd67752b9e41b33fff07486088a` | Clean; main |
| Demos | `974a6dc9fa0bcbcaa57c70f2b6ed7e065dea5465` | Clean; main |

All sibling sources remain read-only. Shared standards inspected: documentation and tooling, with the standards-map invariants; no templates copied.

## Baseline checks

| Check | Reproduction / observation | Result |
| --- | --- | --- |
| Repository | `git status --short`, `git branch --show-current`, `git rev-parse HEAD` | PASS: clean expected branch and actual HEAD recorded |
| Signing reuse | Matching Git GnuPG `gpgconf --launch gpg-agent`; two detached signatures of disposable non-secret input in separate shell invocations, each verified | PASS: expected fingerprint `96E836FAA8854EE98ABC10903C206549E1D7EAD6`; second reused cache without pinentry. No settings changed during the probes. Initial default idle cache later expired; finite task policy and restoration details are in EXECUTION. |
| First checkpoint | `git commit -S`, `git verify-commit HEAD`, `git log -1 --format='%H %G? %GF'` | PASS: `49ef77a3dcc4bc5b4d39126e7770e4706553098d`, status G, expected fingerprint. Local only. |
| Product build | `dotnet build ./CanDoItAll.slnx --configuration Release /m:1` | FAIL: output copy locks held by existing PID 1836; 1000 warnings / 200 errors, elapsed 6:23. Log: `product-build.log`. Isolated output rerun passed below; not classified as a source defect. |
| Isolated product build | `dotnet build ./CanDoItAll.slnx --configuration Release --artifacts-path ./artifacts/modules-decoupling/build /m:1` | PASS: 0 warnings / 0 errors, elapsed 3:20.81. Log: `product-build-isolated.log`. Same source/dependency graph; existing sandbox retains its explicit Parity output. |
| Established host | Read-only `GET http://localhost:5032/_dev/runtime` and `/_dev/database/selection`, PID/path inspection | Ready, PID 1836, main checkout Release executable, predates task. Runtime override profile `e5df9ad6-33db-c697-4a06-78a74976013c`; no pending activation. Build freshness for task not proven. |
| Ollama availability | `GET http://127.0.0.1:11434/api/tags` | PASS for discovery only: `gpt-oss:20b` installed. Real tool invocation NOT_RUN. |
| OpenAI | Configured provider discovery, `GET /_dev/agentframework/credential`, bounded synthetic `POST /api/agents/providers/{id}/test-chat` | Connectivity PASS on baseline host: provider `c1c103db-707e-3f52-8809-8d804fc171d1`, model `gpt-5.4-mini`, response OK, input 29/output 32 tokens, 40-second deadline/no retry. No credential printed. Sanitized `openai-connectivity.json`; real tool proof NOT_RUN. |
| Ollama connectivity | Same synthetic test-chat route, configured local provider `bd2bffbb-23d5-d152-82f6-e1d37908b169`, installed `gpt-oss:20b` | FAIL: client deadline at 60 seconds, TaskCanceledException, no automatic retry; real tool proof NOT_RUN. Availability alone does not prove usable completion. |
| Documentation | `./tools/Validation/Test-Documentation.ps1` | PASS: 208 maintained Markdown files after correcting two task-note path findings and five missing historical evidence references reproduced in unchanged Governance fixture / UI sandbox READMEs. |
| Portability-static | Two documented Python tooling test commands; `scan_portability.py --repo-root . --output ./artifacts/modules-decoupling/20260910-baseline/portability-scan.json --tracked-only`; enforcement with the canonical baseline and no write flag | PASS: tooling 6 + 4 tests; 5627 files / 29249 findings scanned; 14403 reviewed executable-source findings unchanged. No baseline rewrite. |
| Runtime analysis | CodeAnalytics `snap-20260910124606-06a9d455`, five scoped projects, 443 documents, 1067 types, 9528 members | Source evidence; no blocking load error. Generated attribute warnings and partial factory-DI interpretation; not runtime PASS. |
| Persistence analysis | CodeAnalytics `snap-20260910124744-63054d66`, 11 scoped projects, 493 documents, 1467 types, 9427 members | Source evidence; no blocking load error. EF broad assembly interpretation/out-of-scope entities means reported entity count is not actual model membership. |

## Selected baseline proof to discover

Commands run from the repository root. Build the owning test assembly first and use `--list-tests --filter "FullyQualifiedName~<class>"`; confirm the counts below before executing the same filter with refreshed binaries. These are source-based expectations, not executed results.

| Suite / class | Expected cases | Actual / result |
| --- | --- | --- |
| Integration / CollaborationIntegrationTests | 2 | PASS: expected/actual 2, 2 passed / 0 failed / 0 skipped, 17 seconds; `collaboration-baseline.trx` / `.log` |
| Integration / ProjectStructureAgentRuntimeToolRoundTripIntegrationTests | 12 | NOT_RUN |
| Integration / ProjectStructureAssetEffectIntegrationTests | 6 | NOT_RUN |
| Integration / ProjectStructureWorkflowScenarioHarnessTests | 2 (each covers 21 scenarios internally) | NOT_RUN |
| Integration / ProjectStructureProcessRunRecordIntegrationTests | 5 | NOT_RUN |
| Integration / ProjectDeletionIntegrationTests | 16 | NOT_RUN |
| Integration / CrmHrApiIntegrationTests | 2 | NOT_RUN |
| Integration / LlmChatsApiPostgreSqlIntegrationTests | 10 | NOT_RUN |
| Integration / MigrationBootstrapIntegrationTests | 6 | NOT_RUN |
| Unit / HrAgentRuntimeToolProviderTests | 13 | NOT_RUN |
| Unit / SchedulerAgentRuntimeToolProviderTests | 3 | NOT_RUN |

## Gate and safety requirements

Follow [Testing](../../testing.md) and current CI exactly, with any isolated-output adjustment stated. During slices use affected production builds, focused discovery/execution and real PostgreSQL when persistence is affected. Protected-source closure requires the complete portability scan, reviewed ADDED/STALE deltas, and final enforcement without `--write-baseline`. Maintained documentation changes require `tools/Validation/Test-Documentation.ps1`.

Final cross-cutting closure explicitly requires the frozen product build, broad stable gate with its documented exclusions, separate browser and relevant LiveProcess/host gates, migration/upgrade/restart, portability, documentation, and real journeys A-H. Windows proof cannot claim Linux/macOS execution. Missing real providers or critical journeys prevents end-to-end success.

Use disposable database/storage/profile fixtures. Browser default fixtures isolate these; attaching through `CANDOITALL_PLAYWRIGHT_BASEURL` does not own the existing host's data. Scheduler firing requires a separate test-owned host with workers enabled because the default browser fixture uses `McpToolHost`. Do not expose copied user records to providers, touch existing schedules, or delete user data/volumes.

Build/test isolation uses the same Release configuration with `--artifacts-path ./artifacts/modules-decoupling/build`. During child-host test execution, set process-scoped `ArtifactsPath` to that absolute root and `CANDOITALL_TEST_CONFIGURATION=Release`; the existing Playwright child `dotnet run` then resolves the isolated Web executable. Keep those settings scoped to the tool process. SDK property evaluation confirmed this path without modifying projects; runtime verification is still required.

## Collaboration checkpoint and intermediate broad gate

Source base: signed documentation checkpoint `49ef77a3d` plus the staged Collaboration context/stamping change, source tree `a8e5ed489c4c86e1fdbd26d8d0b56781c5a36669` (index before evidence-text updates). Both changed production projects built directly in Release with isolated artifacts: Infrastructure and Collaboration each report 0 warnings / 0 errors. Evidence: `artifacts/modules-decoupling/collaboration/`. xUnit 2.9.3 runs through VSTest on the existing SDK; no test-platform change.

| Check | Discovery and result |
| --- | --- |
| Integration, `FullyQualifiedName~CollaborationIntegrationTests` | Expected/actual 4; PASS 4, failed 0, skipped 0, 28 seconds. Preserves two baseline cases and proves complete-schema mapping parity, historical records/restart and profile isolation on disposable PostgreSQL. `collaboration-integration.trx`. |
| Unit, `FullyQualifiedName~CollaborationDbContextTests` | Expected/actual 5; PASS 5, failed 0, skipped 0, 1 second. Exact bounded model/foreign-query rejection and save-overload GUID stamping matrix. `collaboration-unit.trx`. |
| Components, `FullyQualifiedName~MainLayoutCollaborationTests` | Expected/actual 1; PASS 1, failed 0, skipped 0, 15 seconds. Existing shell unread badge preserved. `collaboration-components.trx`. |
| Portability-static | PASS: tooling 6 + 4; complete staged-source scan 5630 files / 29250 findings; final no-write enforcement reports 14403 reviewed executable findings unchanged. No baseline rewrite. |
| Evidence secret scan | PASS: 13 text artifacts scanned, no oversized/unreadable files or findings, before broad execution. |
| Documentation | PASS: 208 maintained Markdown files after slice and evidence updates. |
| Broad stable gate | Trigger: shared persistence stamping and owner composition. Frozen revision d96ef27796cf7648cb42cfd42c2b832c0b432180. Stable aggregate build PASS, 0 errors / 3 existing component warnings. Discovery 10772; execution PASS 10827, failed 0, skipped 0: Components 1896, Integration 1412, Memory 196, AgentFramework Memory 22, Unit 7301. Run 2026-09-10 13:19:41-15:22:07 UTC, exact documented exclusions. Evidence: collaboration/stable-tests.log and five TRX files under collaboration/stable. This predates Memory and does not close the final gate. |
| Independent architecture/QA source review | No actionable findings. Explicit model membership, inherited save dispatch, complete mappings, profile binding and removed overload callers checked; runtime closure is evaluated separately. |

No schema migration or data copy is introduced by this slice. Complete migration mapping remains unchanged. Migration rehearsal, cumulative review, remaining owner changes and final gates are NOT_RUN. Complete implementation and end-to-end validation are **not complete**.

The Collaboration slice preserves current local-operator behavior. It does not establish a new membership/private-thread authorization contract: the current service stores participant identities but has no member-removal operation or per-thread authorization policy. Foundation QA-070's broader scenario therefore cannot be claimed PASS from these persistence and shell tests.

Runtime result names reconcile the complete discovery/execution difference: two MemoryOperationHandler theories expand 1 to 5 each; MemoryOperationAccessAuthorizer ownership dimensions expand 1 to 9; PluginCatalog preview expands 1 to 6; FloatingAgentChatSettingsValidator and MafFinalizerToolFactorySchemaCharacterization each expand 1 to 8; ProcessRuntimeToolPreflight routes expand 1 to 21. No discovered method is missing. Ignored reconciliation: collaboration/stable-discovery-expansion.json.

Captured broad-test output required artifact sanitation: synthetic secret-scanner/redaction samples and a disposable authorization fixture token in an intentionally rejected query-string request appeared in discovery/TRX text. Nineteen fragments in three files were redacted, preserving XML validity and every test ID/outcome, with value-free scan/fingerprint metadata retained. The initial scan's oversized integration TRX was subsequently scanned in full at a 100 MB limit. Final scan PASS: 31 text artifacts, no oversized/unreadable files or findings. This is artifact hygiene, not a source-test change or a claim about real-provider credentials.

## Memory applied-source checkpoint

Base: signed Collaboration d96ef27796cf7648cb42cfd42c2b832c0b432180. Applied source tree before this record update: `19f0cc204b15ac71c3e40ba4f8b170899558413c`. Every check below uses actual working-tree files and the normal isolated build root, without the candidate compiler overlay. Evidence root: `artifacts/modules-decoupling/memory/`.

| Check | Discovery and result |
| --- | --- |
| Direct builds | Memory.Persistence and Composition PASS, 0 warnings / 0 errors. Refreshed Memory test assembly PASS 0/0; Integration PASS with five preexisting analyzer warnings; Components PASS with three preexisting warnings. No SDK/package change. |
| Memory focused | memory.filter selects the 11 changed stable fixture classes, MemoryWorkerHostingTests and new MemoryDbContextTests. Discovery 77, execution PASS 85 / failed 0 / skipped 0, 5 seconds; two CrossCallerRoutes theories add eight runtime rows. Existing 392 assertions retained in migrated fixtures. memory.trx. |
| PostgreSQL/API | FullyQualifiedName~MemoryOwnerPersistenceTests\|FullyQualifiedName~MemoryProvidersApiIntegrationTests: discovery/execution 19, PASS 19 / failed 0 / skipped 0, one minute. New owner tests 3 plus existing API tests 16. memory-integration.trx. |
| Components | FullyQualifiedName~MemoryProvidersPageTests\|FullyQualifiedName~MemoryProviderOperationsPageTests\|FullyQualifiedName~MemoryProviderUiSurfacePageTests: discovery/execution 17, PASS 17 / failed 0 / skipped 0, 3 seconds. memory-components.trx. |
| Portability-static | PASS: tooling 6 + 4; complete staged-source scan 5633 files / 29250 findings; final no-write enforcement 14403 reviewed executable-source findings unchanged. No baseline rewrite. |
| Evidence secret scan | Memory PASS: 17 text artifacts, no oversized/unreadable files or findings. |
| Documentation | PASS: 208 maintained Markdown files after checkpoint/evidence updates. |
| Architecture/QA review | Independent review found no material issue or missed callers. Root reviewed the staged source/fixture diff. Exact model membership, all six save entry points, canonical mapping/JSON retention/readback and profile binding covered. |

Reproduce builds with `dotnet build <owning-project> --configuration Release --artifacts-path ./artifacts/modules-decoupling/build /m:1`. After refreshing each owning assembly, run `dotnet test <owning-project> --configuration Release --artifacts-path ./artifacts/modules-decoupling/build --no-build --no-restore --list-tests --filter <filter> /m:1`, then the same filter without `--list-tests`, adding a TRX logger/results directory. Set the scoped configuration/artifact environment above and the explicit disposable PostgreSQL fixture connection; each test leases its own database. The Memory filter file is reproducible from the listed changed fixture classes and the two named additions.

The exact Memory class selection is AgentMemoryProviderEndToEndTests, ManualMemorySourceIngestionTests, MemoryAsyncWorkerTests, MemoryContextPackValidationTests, MemoryEndToEndObservabilityProofTests, MemoryFeedbackHandleSecurityTests, MemoryOperationHandlerTests, MemoryProviderRuntimeContractTests, MemoryRuntimeCheckpointTests, MemoryRuntimePersistenceTests, MemoryWorkerLeaseTests, MemoryWorkerHostingTests and MemoryDbContextTests. Join `FullyQualifiedName~<class>` clauses with `|`.

No schema migration or data copy. The PostgreSQL lease test uses two independent service providers/connections in one process; it proves restart/expiry/forged ownership behavior, not a simultaneous distributed race. ExternalCognitiveMemoryLiveConformanceTests was adapted but is an opt-in LiveProcess test and was not selected. Final frozen product/stable/browser/live/provider/upgrade journeys remain open.

## Catalog owners and migration authority applied-source checkpoint

Base: signed Memory `f9908080ebe38f342ab4d1799fc7cef8481e3ce8`. Applied source tree before README corrections and evidence/baseline updates: `77f484cf2339fb57283a65d8f044bffc70d23d02`. Builds and tests use actual source files and the normal isolated build root without a compiler overlay. Evidence: `artifacts/modules-decoupling/catalog-owners/`. Siblings, host preservation, process-scoped Release/artifact configuration and disposable PostgreSQL connection remain as recorded above.

| Check | Discovery and result |
| --- | --- |
| Product build | Complete product solution PASS, 0 warnings / 0 errors, 2 minutes 29 seconds. Refreshed Unit assembly 0/0; Integration five existing warnings; Components three existing warnings. |
| Unit | Expected/discovered/executed 62, PASS 62 / failed 0 / skipped 0, 9 seconds. New Plugins mapping/stamping and migration-authority tests plus existing registry, database configuration, Resource connectors/catalog, CRM gateway and Plugin executor contracts. results/unit.trx. |
| PostgreSQL/integration | Expected/discovered 110, executed 115, PASS 115 / failed 0 / skipped 0, 8 minutes 12 seconds. The existing packaged-plugin preview theory expands one discovery to six cases. New TestLab, Resources, Plugins and migration-authority classes contribute three each; retained bootstrap, CRM/Structure, Plugin catalog and Resource Storage integration paths also pass. results/integration.trx. |
| Components | PluginsPageTests expected/discovered/executed 7, PASS 7 / failed 0 / skipped 0, 44 seconds. results/components.trx. |
| Portability-static | PASS: tooling 6 + 4, final complete scan 5640 files / 29260 findings, no-write enforcement 14401 reviewed executable-source findings unchanged. Initial two STALE findings were the deleted PluginSchemaInitializer and SchedulerPlannerSchemaInitializer case-policy allowances; removed those only (14403 to 14401), with no added executable finding. |
| Documentation and artifacts | Documentation PASS for 208 maintained Markdown files. Initial validation caught a TestLab README relative-link error, repaired and rechecked. Final evidence secret scan PASS: 23 text artifacts, no oversized/unreadable files or findings at a 100 MB limit. |
| Independent review | No material finding in migration authority cleanup or its PostgreSQL fixtures; root reviewed all applied owner/test changes and the baseline diff. |

The exact unit class selection is StorageObjectResourceConnectorTests, ResourceFileSourceCatalogTests, CrmHrResourceSourceGatewayAdapterTests, PluginsDbContextTests, AppDbContextMigrationAuthorityTests, AppDbContextModelRegistryTests, DatabaseConfigurationTests, BundledPluginWorkflowExecutorTests and PluginWorkflowExecutorBoundaryTests. Integration selects TestLabOwnerPersistenceTests, ResourcesOwnerPersistenceTests, PluginsOwnerPersistenceTests, MigrationAuthorityIntegrationTests, MigrationBootstrapIntegrationTests, CrmHrCrossModuleIntegrationTests, ProjectStructureAgentIntegrationTests, ProjectStructureAutomaticPlacementIntegrationTests, PluginCatalogIntegrationTests and ResourceStorageObjectIntegrationTests. Saved `unit.filter`, `integration.filter` and `components.filter` combine these classes with the exact documented stable exclusions. Reproduce with the Memory command pattern above against the owning Unit/Integration/Components project.

Fresh-schema checks verify the eight affected Plugin/Scheduler tables' columns, types, defaults, nullability, primary/foreign keys and indexes. Populated starting-migration fixtures retain eight representative records and historical extra indexes/defaults through two bootstraps/restarts; CRM lookup seeding preserves edited existing records and fills missing entries. OAuth invalid-callback/replay behavior and existing session/connection save boundaries remain. No new migration/data copy or physical mapping change is introduced. Removal of opportunistic runtime DDL repair does not establish support for manually corrupted, already-baselined databases.

Broader persistence/composition proof remains the final frozen gate; the earlier Collaboration gate is explicitly intermediate. These focused fixtures do not close provider/browser, complete transfer/lifecycle or journeys A-H.

## History, Simple Chats and shared transaction checkpoint

Base: signed catalog owners `c0950854f44c674aafd474c64532fe08fe2801b3`. Applied source tree before README/evidence/baseline updates: `cab9bff1c90b4cdf34d4e32c0a32fe8e50cc2d14`. Evidence: `artifacts/modules-decoupling/history-simplechats/`. Actual working-tree sources, the normal isolated Release build root and the same frozen sibling graph were used; no compiler overlay. The user's host/profile stayed untouched and PostgreSQL fixtures leased disposable databases.

| Check | Discovery and result |
| --- | --- |
| Product and owning assemblies | Product solution PASS 0 warnings / 0 errors, 1 minute 50 seconds. Refreshed Unit 0/0, Integration five existing warnings / 0 errors, modified Playwright assembly 0/0. Compiling Playwright is not browser execution. |
| Unit | Expected/discovered/executed 35, PASS 35 / failed 0 / skipped 0, 2 seconds. SimpleChatsDbContextTests 8, LlmChatBackendCompositionTests 4, PersistentWorkflowResumeBoundaryStoreInMemoryTests 1, WorkflowHitlProductionConstructorGuardTests 13 and WorkflowUsageAnalyticsTests 9. |
| Integration | Expected/discovered/executed 210, PASS 210 / failed 0 / skipped 0, 11 minutes 52 seconds. Includes new transaction coordinator 10, History owner 9 and SimpleChats owner 5; retained History, workflow/file audit, relay retention, Simple Chats HTTP/SSE/concurrency/recovery and database transfer paths. results/integration.trx. |
| Portability review | PASS: tooling 6 + 4; final complete scan 5649 files / 29263 findings; no-write enforcement 14402 reviewed executable-source findings unchanged. Reviewed one ADDED path-api finding: Path.IsPathRooted preserves case for socket/path endpoints while DNS names normalize case; no fixed host path or platform-specific shell was added. Baseline 14401 to 14402, exact one-entry diff reviewed. |
| Documentation/artifacts | Documentation PASS for 208 maintained Markdown files. Two disposable authorization-fixture token appearances in rejected query-string requests were redacted from assembly stdout, preserving XML and every test ID/name/outcome. Final sanitized artifact scan PASS: 22 text files, no oversized/unreadable files or findings at a 100 MB limit; value-free metadata retained. |
| Review | Independent reviews of the unchanged candidate inputs found no material issue; root verified applied patch hashes and reviewed the integrated diff. Exact model membership, mapping parity, profile separation, actual enlisted connection/transaction identity, rollback and postcommit callbacks have focused proof. |

The integration filter is the union of ProviderHistoryCaptureIntegrationTests, ProviderHistoryPersistenceIntegrationTests, ProviderHistoryQueryIntegrationTests, ProviderHistoryRuntimeIntegrationTests, ProviderHistorySourceProjectionIntegrationTests, FileSandboxWorkspaceUsageProjectionIntegrationTests, WorkflowUsagePersistenceIntegrationTests, WorkflowHitlRecoveryPersistenceIntegrationTests, LlmChatsDatabaseTransferIntegrationTests, SharedProviderPremergeUpgradeTests, every `LlmChat`/`EfLlmConversation` topic case, and the three new classes above. Relay cases select `SharedProviderPersistenceIntegrationTests.Actual_relay_`, `Invocation_audit_is_metadata_only_and_finalization_is_idempotent`, and `Relay_retention_filters_both_owners_before_limiting_and_keeps_deadline_order`. The exact saved integration.filter and unit.filter include all documented stable exclusions. Use the owning project and reproduction commands recorded above after refreshing each binary.

The coordinator's ten cases include seven PostgreSQL and three explicit InMemory checks; InMemory proves only binding/guard behavior. No schema migration or data copy belongs to this checkpoint. The read-only relay retention SQL join preserves the before-limit cross-owner predicate and ordering; it remains an explicit integration read. Complete transfer orchestration, the receipt schema, durable HR admission, final frozen broad stable/browser/live gates and journeys A-H remain open.

## Security, Providers, owner projections, Search and Storage checkpoint

Base: signed History/SimpleChats `52c477f51591018762b3d0a12c68657f1379a733`. Applied source tree before Provider README and evidence/baseline updates: `22eba807e2ffb03c793b0fe00430a46106d13ada`. Evidence: `artifacts/modules-decoupling/security-providers-storage/`. These are actual working-tree sources with the normal isolated Release build root; no compiler overlay. Sibling revisions, preserved user host/profile and disposable PostgreSQL fixture configuration remain as recorded above.

| Check | Discovery and result |
| --- | --- |
| Product and owning assemblies | Product solution PASS 0 warnings / 0 errors, 1 minute 31 seconds. Refreshed Unit 0/0, Integration five existing analyzer warnings / 0 errors, Components three existing warnings / 0 errors. |
| Unit | Expected/discovered/executed 135, PASS 135 / failed 0 / skipped 0, 5 seconds. Initial expectation 136 stopped at discovery: the unchanged concurrent Storage bootstrap test has RequiresHostDocker=true and is excluded by this lane. All 123 prior expanded candidate cases remain, plus two Search/Storage model cases and ten Storage catalog cases. |
| PostgreSQL/integration | Expected/discovered/executed 67, PASS 67 / failed 0 / skipped 0, 3 minutes 30 seconds. Includes owner mapping/readback/restart/profile tests, explicit shared-transaction reads/rollback, secret deletion/reference policy, provider publication/source/relay behavior, Resource/TestLab projection scope and batch limits, Search/Storage facts and migration. |
| Components | Expected/discovered/executed 19, PASS 19 / failed 0 / skipped 0, 37 seconds. Provider mutation regression 3, source/import 4, publication panel 3, secret selection 1 and Storage catalog selection 8. |
| Portability-static | PASS: tooling 6 + 4; complete staged-source scan 5663 files / 29282 findings, untruncated; final no-write enforcement 14406 reviewed executable-source findings unchanged. Four added findings are the typed RootPathSyntax/RootPathState fields and their mapping from existing host-binding metadata. They introduce no machine path, reinterpretation or platform assumption. Exact four-entry baseline diff reviewed; no stale entries. |
| Review | Earlier independent reviews of the unchanged Security/Providers and owner-projection candidate inputs found no material issue. Root verified all seven patch hashes and reviewed applied owner/fixture changes, new planning facts and the baseline diff. The final cumulative architect/QA review remains open. |
| Documentation/artifacts | Documentation PASS for 208 maintained Markdown files. Evidence scan PASS: 25 text files, no oversized/unreadable files or findings at a 100 MB limit; separate token-shaped artifact check also found no JWT. No evidence redaction was needed. |

Use the command pattern above with `unit.filter`, `integration.filter` and `components.filter`; all include the documented stable exclusions. Unit selects StorageObjectResourceConnectorTests, ResourceFileSourceCatalogTests, CrmHrResourceSourceGatewayAdapterTests, ProjectStructureFileScopeResolverTests, ProjectStructureFileActionCoordinatorTests, SecretMigrationTests, SecretRuntimeAuthorizationTests, SecretVaultTests, ProviderCatalogProjectionFailureTests, SharedProviderPublicationAndCatalogTests, ProvidersDbContextTests, PluginsDbContextTests, SearchStorageDbContextTests and StorageCatalogServiceTests. Integration selects ResourcesOwnerPersistenceTests, TestLabOwnerPersistenceTests, PluginsOwnerPersistenceTests, WorkbenchOwnerProjectionIntegrationTests, SecurityOwnerPersistenceTests, ProvidersOwnershipIntegrationTests, SharedProviderPersistenceIntegrationTests, SharedProviderSourceSyncIntegrationTests, SearchStorageOwnerPersistenceTests, StorageMigrationIntegrationTests and the two named SecretPortability restart/migration methods. Source counts and TRX outcomes are reconciled in test-result-summary.json and discovery-reconciliation.json.

No migration or data copy is introduced. Current-host secret tests exercised Windows DPAPI and headless wrapping-key restart; they do not prove execution on Linux/macOS. LongRunning provider performance and Docker-tagged Storage cases were not selected. The Search/Storage staged deletion methods are a prerequisite for the next lifecycle slice. Technical-agent/CRM projections, remaining foreign/maintenance access, full lifecycle/admission and all final browser/live/provider journeys remain open.

## Simple Chats creation receipt and canonical migration checkpoint

Base: signed Security/Providers/Search/Storage `0cb2d519edd549afcf79a61c0db758871fba74db`. Applied source tree before task records and baseline updates: `89ee44aafb3f2d0b44e464cbd1739c2ae24324f6`. Evidence: `artifacts/modules-decoupling/simplechats-receipts/`. Actual applied source and normal isolated Release output were used, without a compiler overlay. Sibling revisions and the preserved user host/profile remain as recorded above; PostgreSQL fixtures use disposable databases.

| Check | Discovery and result |
| --- | --- |
| Product and owning assemblies | Product solution PASS 0 warnings / 0 errors, 1 minute 15 seconds. Refreshed Unit 0/0 and Integration five existing analyzer warnings / 0 errors. |
| Unit | Expected/discovered/executed 32, PASS 32 / failed 0 / skipped 0, 1 second. Definition and conversation services, fingerprints, backend composition and bounded model checks. |
| PostgreSQL/integration | Expected/discovered/executed 56, PASS 56 / failed 0 / skipped 0, 3 minutes 53 seconds. Receipt 15, migration/transfer 8, transactional concurrency 8, Simple Chats ownership 5, Search/Storage ownership 6, Storage migration 1, retained Simple Chats transfer 7 and bootstrap 6. |
| Migration and restart | Complete generated Designer and snapshot contain the same 149 entity mappings after brace-format normalization. Upgrade from the prior last migration preserves legacy definitions across restart. The original-revision FK is deferred and intent uniqueness immediate; a missing original revision fails commit and rolls back. Empty-receipt downgrade/re-upgrade passes, while retained receipts explicitly block downgrade. |
| Recovery and transfer | Concurrent identical creates converge on one definition/receipt; different semantic input conflicts. Faults before commit roll back; faults after commit retain the receipt. Receipt replay survives restart, later human revision/provider changes and transfer without resolving the provider. Target replacement with receipts and corrupt source metadata are rejected before destructive target work. |
| Portability-static | PASS: tooling 6 + 4; complete scan 5673 files / 29288 findings, untruncated; final no-write enforcement 14410 reviewed executable-source findings. Four added allowances reproduce existing RootPathSyntax, RootPathState, RaisesEscalation and EscalationCount metadata in the new canonical Designer. No new path or process behavior; exact baseline delta reviewed. |
| Review | Root reviewed the applied writer/unit-of-work/transfer/migration diff and patch hashes. The canonical snapshot changes only for the receipt. Model, transaction, replay, rollback and transfer assertions are exercised against PostgreSQL. This is self-review, not an independent final architecture review. |
| Documentation/artifacts | Documentation PASS for 208 maintained Markdown files. Final evidence scan PASS: 20 text files, no oversized/unreadable files or findings at a 100 MB limit. A separate token-shaped artifact check found no JWT; no redaction was required. |

The exact saved `unit.filter` and `integration.filter` contain the documented stable exclusions: Playwright, LiveProcess, LongRunning, Quarantined, UnixRuntimePortability and RequiresHostDocker=true. `run-focused.ps1` discovers and checks counts before execution; `source-state.json` and `test-result-summary.json` identify inputs and TRX outcomes. No real provider or browser path is claimed by these fixtures.

The migration is additive for legacy binaries, but binaries that do not understand retained receipts must not replace chat data after receipt admission. The supported rollback point is before the first receipt: Down then succeeds without removing legacy definitions. Later rollback requires keeping the schema or a reviewed pre-admission restore that cannot replay completed effects under fresh identities. The internal transfer format advances to 9; public chat DTOs and ordinary turn versions are unchanged. HR registration, durable SDK/tool admission and actual run recovery remain open, together with final cumulative proof and journeys A-H.

## Projects, assignment coordination, deletion and Agent revocation checkpoint

Base: signed receipt checkpoint `5264ba2a34db61bf3f1b1a391202f81e29e617fa`. Applied source tree before final README/task-record/baseline corrections: `f023bf09ba1025973203ec4d1a26a947f612a4b9`. Evidence: `artifacts/modules-decoupling/project-owners/`. Six patches were hash-checked and applied in dependency order. Tests below use the actual applied source, normal isolated Release output and disposable PostgreSQL; the earlier compiler-overlay candidate is separately labeled and is not substituted for this proof.

| Check | Discovery and result |
| --- | --- |
| Product and owning assemblies | Product PASS 0 warnings / 0 errors, 1 minute 50 seconds. Refreshed Unit 0/0, Integration five existing analyzer warnings / 0 errors, Components three existing warnings / 0 errors. |
| Unit | Expected/discovered/executed 51, PASS 51 / failed 0 / skipped 0, 1 second. Projects/Workbench model invariants, dashboard and file-source callers, managed Storage deletion and Agent participant behavior. |
| PostgreSQL/integration | Expected/discovered/executed 68, PASS 68 / failed 0 / skipped 0, 3 minutes 43 seconds. Owner mapping/restart/profile, uncapped bulk queries, CRM assignment revisions, same-transaction final-save rollback, residual recovery, deletion HTTP/API and shared-Storage races, plus Agent claim races/expiry. |
| Components | Expected/discovered/executed 25, PASS 25 / failed 0 / skipped 0, 1 minute 24 seconds. Projects page, assignment revision/pricing and Serializable scope behavior. |
| Portability-static | Tooling 6 + 4 PASS; complete scan 5683 files / 29306 findings, untruncated. Initial 30 ADDED and 15 STALE entries were individually reviewed; one stale entry reduced a count by two. The exact 45-entry delta preserves host-bound path metadata, native-path/reparse containment and FTP scheme case handling while moving logic to shared owner helpers. Baseline 14410 to 14424; final no-write enforcement PASS. |
| Review | Root reviewed applied ownership, coordinator lifetime, final-assignment read set, claim SQL, path policy and baseline changes. Review caught a pre-lock timestamp problem in the draft; each claim transition now locks its row before sampling database time. Source self-review does not replace the final independent cumulative architecture/QA gate. |
| Documentation/artifacts | Documentation PASS for 208 maintained Markdown files. Evidence scan PASS: 25 text files, no oversized/unreadable files or findings at a 100 MB limit. Separate token-shaped check found no JWT; no redaction was needed. |

Use `run-applied.ps1` and the three saved filters. Unit classes: ProjectsWorkbenchDbContextTests, DashboardQueryServicesTests, ResourceFileSourceCatalogTests, StorageObjectResourceConnectorTests, ProjectManagedStorageDeletionTests and AgentProjectStructureAccessDeletionParticipantTests. Components: ProjectsPageTests, ProjectPartyAssignmentRevisionPricingTests and SerializableMutationScopeTests. The saved integration filter lists fifteen owning classes, including ProjectsOwnerPersistenceTests, WorkbenchAssignmentOwnerPersistenceTests, ProjectLifecycleOwnerPersistenceTests and AgentProjectAccessClaimIntegrationTests, alongside all retained relevant deletion/assignment/HTTP cases. All use the exact documented stable exclusions recorded above. TRX counters and hashes are in test-result-summary.json; all selected counts match.

The seven new Agent claim cases use separate PostgreSQL contexts and include three blocked-update expiry cases: renewal, completion and failure wait on a row lock until the lease expires, then refuse the stale transition. Heartbeat loss cancels workspace dispatch; generation/profile/restart checks preserve the exact recovery identity. The lifecycle proof checks the actual shared connection and raw transaction across owners and rollback when the final Projects save fails. No database migration or data copy is introduced.

Earlier candidate compilation exposed a missing test namespace and malformed FTP test locators; both were corrected without changing their assertions before applied validation. Historical model/transfer/schema tests remain, and obsolete no-op schema initializer calls were removed from two fixtures. Old workers do not honor the new generation fence and must be drained for cutover. This slice does not establish lifetime-safe grants, all project writer admission, complete twelve-owner transfer, full Work Management ownership, browser/provider stories or final frozen gates.

## Prompts, Scheduler and connector owner checkpoint

Base: signed Projects checkpoint `e64d3c95b263b607bea41832f1fc63dea469eb4a`. Applied source tree before task records: `f480c9573324156487e3586c89d3fd95d52c8407`. Evidence: `artifacts/modules-decoupling/prompts-scheduler-workspace/`. Frozen Prompts patch `12197e29429037b149c439f7916dc3aedce7dcb8a1390a8a41fc4696b6831327` and Scheduler/Workspace patch `151a689352825d83f73f08039a555ea02a332cd8b343105429aa9f4f3f82bb45` were applied after input checks. Results below use normal applied source and isolated Release output, not a compiler overlay. The user host/profile and sibling sources remain preserved.

| Check | Discovery and result |
| --- | --- |
| Product and owning assemblies | Product PASS 0 warnings / 0 errors, 1 minute 15 seconds. Refreshed Unit 0/0, Integration five existing analyzer warnings / 0 errors and Components three existing warnings / 0 errors. |
| Unit | Expected/discovered/executed 81, PASS 81 / failed 0 / skipped 0, 2 seconds. Prompt services/curator, workflow callers/dashboard, Scheduler tools/launchers and bounded model checks. |
| PostgreSQL/integration | Expected/discovered/executed 21, PASS 21 / failed 0 / skipped 0, 1 minute 2 seconds. Exact owner model parity, all historical Prompt/Scheduler/connector record shapes, two-provider restart/profile readback, staged draft rollback/commit, API/search concurrency, repeated Scheduler dispatch and connector handlers. |
| Components | Expected/discovered/executed 23, PASS 23 / failed 0 / skipped 0, 56 seconds. Prompt Gallery and Scheduler Planner pages. |
| Transaction/projection | Enlisted Prompt creation uses the actual shared connection/raw transaction and is invisible to an independent reader before commit. Rollback leaves no draft/activity; completion preserves original activity fields and later human edits. Delayed search updates cannot overwrite newer state or revive archived records. |
| Portability-static | Tooling 6 + 4 PASS; complete scan 5693 files / 29306 findings, untruncated; final no-write enforcement PASS with 14424 reviewed executable-source findings unchanged. No baseline edit. |
| Review | Root reviewed the applied owner mappings, DI, draft writer, transaction lifetime, Search state lock/rebuild and fixture changes. Candidate build caught shared-factory fixture assumptions; fixtures now explicitly share an InMemory root or canonical PostgreSQL profile without weakening assertions. Self-review does not replace final independent cumulative review. |
| Documentation/artifacts | Documentation PASS for 208 maintained Markdown files. Evidence scan PASS: 23 text files, no oversized/unreadable files or findings at 100 MB. Separate token-shaped check found no JWT; no redaction was required. |

`run-applied.ps1` contains the exact three filters and expected counts, with the documented stable exclusions. Saved discovery logs and `test-result-summary.json` reconcile all selected cases and TRX hashes. The earlier isolated Prompts candidate passed 50 unit, 11 integration and eight component cases; it is supporting evidence only. No migration or data copy is introduced. These fixtures do not prove real provider effects: Scheduler uses a typed recording launcher, and connector provider recovery remains unfinished. Full Workbench caller cutover, product policy removal from Core, project lifetime admission, complete transfer and final browser/live/provider journeys A-H remain open.

## Project lifetime, retirement and exact Agent cleanup checkpoint

Base: signed owner checkpoint `3e12e99024b5891fd0a65c0e224eb5ab0b0a3dd6`. Applied source tree before task-record/baseline updates: `c8925e3588afcba4037ed90d0d0520b745defac2`. Evidence: `artifacts/modules-decoupling/project-lifetimes/`. Projects admission patch `dc21e8b79ff4d95c64e4741b6ef430fc04cbae51f2c26350a99ff513a1010fb5` and Agent cleanup patch `f1216acab09d944dfefc06080852200a9a557092744e8a8e32489e6b25e215f9` were applied with a narrow README reconciliation. Root added the migration provenance marker, canonical migration and six migration cases. Normal applied source, isolated Release output and disposable PostgreSQL were used; no compiler overlay or user-host mutation.

| Check | Discovery and result |
| --- | --- |
| Product and owning assemblies | Product PASS 0 warnings / 0 errors, 36 seconds. Refreshed Unit 0/0, Integration five existing analyzer warnings / 0 errors, Components three existing warnings / 0 errors. |
| Unit | Expected/discovered/executed 41, PASS 41 / failed 0 / skipped 0, 1 second. Model membership, unchanged project JSON, lifetime metadata and retained access/Storage callers. |
| PostgreSQL/integration | Final expected/discovered/executed 67, PASS 67 / failed 0 / skipped 0, 4 minutes 21 seconds. Projects admission 5, owner mapping/restart 3, Projects services 8, lifecycle 6, migration 6, Agent lifetime 3, claim 7, access deletion 5, bootstrap 6, profile transfer 3, retained chat transfer 7 and chat receipt migration/transfer 8. |
| Components | Expected/discovered/executed 24, PASS 24 / failed 0 / skipped 0, 1 minute 29 seconds. Actual Projects editor clone retains its loaded lifetime and rejects same-ID recreation; all existing page cases retained. |
| Canonical schema/upgrade | Complete generated Designer and snapshot have identical 150-entity bodies after whitespace normalization. Fresh and starting-schema upgrade tests preserve project/phases/options/hierarchy and legacy Agent recovery fields through two restarts. Each live project gets a distinct nonempty lifetime; only pre-upgrade rows get legacy-grant eligibility. New inserts remain ineligible. |
| Transactions/recovery | Final deletion-save failure rolls back the retirement with every participant. Delayed Search cannot resurrect an old lifetime. Delayed real Agent cleanup preserves a recreated project's exact bound grant; its later deletion creates a different recovery. PostgreSQL enforces paired metadata, one legacy recovery and one per lifetime. Retained retirement or typed recovery blocks Down atomically; empty-evidence Down/re-upgrade preserves business rows and invalidates former lifetime handles. |
| Portability-static | Tooling 6 + 4 PASS; complete scan 5703 files / 29312 findings, untruncated. Reviewed six ADDED and one STALE allowance: four repeated Designer properties and catalog name ordering moved/used by exact grant/revocation. Baseline 14424 to 14429; final no-write enforcement PASS. |
| Review | Root reviewed applied admission, actual transaction usage, compatibility marker, exact cleanup, canonical migration and seven-entry baseline delta. Legacy recovery metadata is deliberately not inferred from the current project. This is source self-review, not the final independent architecture/QA gate. |
| Documentation/artifacts | Documentation PASS for 208 maintained Markdown files. Evidence scan PASS: 29 text files, no oversized/unreadable files or findings at 100 MB. Separate token-shaped check found no JWT; no redaction required. |

The exact filters and standard stable exclusions are in `run-applied.ps1`. Initial discovery stopped at 61 against an estimate of 57: lifecycle has six cases, the transfer suffix also selects seven retained Simple Chats cases, and the bootstrap class is named MigrationBootstrapIntegrationTests. The corrected selection explicitly includes bootstrap and retains all 67 cases; `discovery-reconciliation.json`, `run-remaining.ps1` and TRX hashes document this correction without repeating the already-passed unit lane.

Projects now has five mapped entities and Agent access remains one. New lifetime/provenance fields are omitted from existing project-package JSON. Downgrade requires drained writers and discarded admission handles, and is blocked once retirement or typed access-recovery evidence exists. Compatible old binaries do not enforce the new metadata and cannot participate in the final lifetime-aware cutover. Bare-ID grant writers, creation reservations, controlled bootstrap binding, effective runtime admission, all remaining project-attributed writers, twelve-owner transfer and final journeys A-H remain open.

Separate ignored HR candidate evidence is under `artifacts/modules-decoupling/drafts/hr-candidate/`. It compiled and passed 58 unit cases after namespace corrections. Native SDK/file-journal tests initially passed 15/19; a first protocol correction plus two streaming-prefix tests passed 17/21. The second correction passed all 21 discovered cases with no failures or skips. It snapshots provider updates before the installed history decorator mutates them, configures the actual admitted history serializer, and recognizes public nested installed Ollama types. Exact inputs are in `native-fix-inputs-second.json`; the build/discovery/test logs and `results/hr-candidate-integration-native-second.trx` preserve the result. These scripted native SDK tests are not real-provider proof. The complete recovery layer and product registration still need combined validation and application.

The separate ignored Workbench/Workflow/Storage candidate combines frozen patches `ab153aac3aa749527226bf9f0c1b4ba312fad445266e1b46184bf25e78bf0981`, `b6b49f1e222875a1eedfa971f484276f5a91f2ff95d1867e19b78b362d1f774c` and `48f24d510d9b2d8602cd18e3bb5e98e7dfee574d04681cab7b46072a0394dd14`, with explicit root composition and compiler corrections. Evidence is under `artifacts/modules-decoupling/drafts/workflow-candidate/`; `source-state.json` identifies the exact overlay inputs. Expected/discovered/executed counts agree: Unit 117 PASS (2 seconds), PostgreSQL 64 PASS (5 minutes 15 seconds), Components 38 PASS (4 seconds), all with zero failed or skipped cases. The first unit execution had two incorrect fixture assumptions about DurableFileWriter's retained lock file; the correction asserts the exact payload and lock filenames while retaining occupied-target and content checks. The PostgreSQL cases include a complete 154-entity canonical candidate migration with four additive evidence tables, unchanged old mappings, upgrade/restart readback, empty-evidence Down/re-upgrade, and refusal to drop any retained evidence. Production and test dependencies were refreshed in the isolated candidate output; the final Integration build had five existing warnings and no errors, and Components had three existing warnings and no errors. These are candidate results, not an applied-source or final broad gate. Scheduler/Process authority, all lifetime fences, transfer and journeys A-H remain open.

## HR admission/recovery and Prompt policy applied checkpoint

Signed checkpoint: `7bafbc9e9a8e0c1692f8948c6f99f0b4301f8753`; good signature from the expected key `96E836FAA8854EE98ABC10903C206549E1D7EAD6`.

Base: signed `9006d9dcd60c816f927c99328796f24ad321903a`. The ignored composite under `drafts/hr-recovery-candidate` combined the frozen HR adapter, native admission, recovery, current-authority and Prompt policy layers with the applied lifetime hunks. The first complete 244-case unit run exposed two test scope assertions; corrected authorization uses the authenticated original governance scope. The final corrected candidate discovered and passed 246 unit cases in 10 seconds, including two real receipt JSON round-trips, and all 54 PostgreSQL/native cases in 6 minutes 36 seconds, with no skips. Exact proof: `unit-scope-third-tests.log`, `integration-scope-third-tests.log`, and corresponding `results/hr-recovery-candidate-*-scope-third.trx` files.

The preceding 54-case run passed 49 and failed five. Root fixed the runtime-only transient-context deserialization defect with a journal-owned plain context snapshot; original digest validation still precedes scope recovery. Normal and cancelled recovery now use that original Project scope for current authority resolution. Cancellation reconciliation reports its PersistingResult phase before completion. The revoked-read fixture no longer grants mutation-only authority. Three validated receipt value constructors now explicitly participate in JSON deserialization, retaining original intent, definition ID and revision after a committed file checkpoint. The independent runtime reviewer found no blocking issue in the seven-file context/scope/activity correction.

Candidate Components discovered 38 and passed 37; the remaining failure was the exact managed HR seed inventory gaining its seven expected capabilities. Root updated that inventory and verified that an unadmitted context still exposes no new adapter tools. Candidate Razor inputs were not included by the C# overlay, so that run is not proof of the changed ChatWorkspacePanel. Root added four actual rendered Prompt catalog cases and will validate the Razor source through the normal applied build. A separate two-line policy correction restores the historical CanReadExternalTarget values of the four Prompt read policies; its full-record contract theory remains intact.

The reviewed 108-file composite was applied with root package, typed UI, HTTP and disclosure corrections, now spanning 126 paths. `drafts/hr-activation/manifest.json` records the original composite hashes. Normal builds use `artifacts/modules-decoupling/build`, not an overlay. The package fence removes executable admission state and private provider checkpoints from exports and rejects an alternate importer carrying privileged admission. Three package regressions remain in the focused unit cohort. Four rendered Prompt approval cases use the actual Razor file, typed catalog and required active session. Recovery HTTP routes enforce the general API scope; their explicit cancellation projection excludes raw owner receipts. HR authorization now reads one catalog snapshot through the narrow catalog store, removing the direct workspace-service dependency cycle.

Evidence before the final disclosure correction: `drafts/hr-activation/final-catalog/` passed all 574 unit cases and 63/64 integration cases. The failed public-read fixture tried to attach admission to a persisted legacy run and correctly hit the anti-fabrication guard. Its replacement creates a new admitted run at initial persistence and checks public read projections. The resulting `http-projection-fixture/` run discovered/executed/passed 11 HTTP cases and 67 components, no failures or skips. This supplements the prior 54/54 owner/native result; it is not a combined final 75-case result.

Independent runtime review found two disclosure defects: completed recovery bypassed current access checks, and repeated cancellation reconciliation returned cached receipts after revocation. Reviewed patch `e1f17e825e075c86fb47f32bab721a422e4326ebaa1f8952ee54e43acd0b00f4` now checks original/current source, profile/generation and read ceilings before completed-result disclosure, and reauthorizes/re-reads cached receipts through the existing read-only owner reconciler. Denial/unavailability retains positive committed evidence. Ten new integration cases cover read/capability/profile/scope revocation, restoration, no runtime replay and real PostgreSQL receipts. Root corrected one fixture local-name collision and restored two pre-existing UTF-8 comments.

Normal final builds passed. `final-disclosure/` discovered/executed 574 unit cases, all PASS in 8 seconds, and 75 integration cases, 74 PASS / one FAIL / zero skips in 7 minutes 18 seconds. The failed capability-revocation fixture omitted the same managed-seed customization marker used by the real operator factory, so seed normalization restored its removed capability. The fixture now marks the persisted edit as customized and independently reloads the catalog to assert the capability is absent before testing denial. Production authorization and seeding are unchanged by this correction. `fixture-confirmation/` rebuilds Integration, discovers/executes all three cached-receipt revocation theory cases and passes 3/3 in 2 minutes 44 seconds; its normal Components build and all 67 discovered cases pass in 1 minute 44 seconds. These supplemental results close the fixture failure; they are not presented as a single 75/75 run.

The earlier reviewed baseline delta was 12 ADDED and eight STALE entries (14429 to 14433), covering moved policy comparisons and explicit journal/workspace path metadata. Final portability scan: 5741 tracked files, 29339 findings, complete/untruncated; no-write enforcement PASS with 14433 reviewed executable-source findings unchanged. Tooling tests pass 6 + 4. Documentation evidence tests pass 9, and the canonical validator passes 209 maintained Markdown files. Final evidence secret scan passes 32 text files at 100 MB with zero oversized/unreadable files or findings; the separate final token-shaped scan also passes. Earlier broader artifact inspection found only the known local fixture password in ignored scripts and synthetic secret arguments in existing audit-redaction test names. The final evidence folder redacts six such test-name fragments while verifying original TRX IDs and outcomes; this does not conceal configured credentials or test failures. Root reviewed the applied diff, disclosure fixes and final fixture correction; the sealed Foundation remains unchanged. No real-provider or browser journey is claimed, and final broad closure remains open.

## CRM and Provider candidate review

The ignored `drafts/crm-candidate/source-state.json` identifies a 67-file composite with the 30-entity CRM owner, technical projection provenance/cursor, committed-identity wrappers, Provider runtime/usage ownership and the complete 151-entity canonical migration `20260910213323_AddCrmTechnicalProjectionProvenance`. Web and all three test builds passed. Before review corrections, expected/discovered/executed counts were 114 unit PASS and 173 integration with 170 PASS / 3 FAIL / 0 skipped; all three failures were enum materialization in nonempty assignment reports. The four canonical migration cases passed within that cohort. Evidence is preserved under `focused-before-review/`.

Independent persistence review also identified possible reassignment to a superseded duplicate after an empty catalog and empty historical project IDs changing ordinary label-query behavior. Reviewed correction `ccc9c7de690f1f7aab743b99fc06cb03d7f0db10224041962ca639011de06d48` fixes all three causes, preserves the existing explicit opportunity missing-project diagnostic and adds six PostgreSQL cases. Corrected candidate expected/discovered/executed: 114 unit PASS in 5 seconds and 179 integration PASS in 6 minutes 10 seconds, zero failed/skipped. Component discovery also includes three retained ProjectStructurePartyPicker cases, so the explicit source-derived expectation is 31. Execution passed 29/31 with two timeouts: both race fixtures delay the old AppDbContext factory after production switched to CrmHrDbContext. The owner-factory fixture correction and successful rerun are recorded below. These are candidate checks, not applied-source proof. Work assignment ownership, selected transfer integration, cumulative schema and final journeys remain open.

CRM activation: candidate Components now discovers/executes/passes 31/31 in 1 minute 28 seconds after changing only the race fixture factory to CrmHrDbContext; assertions and deadlines are unchanged. The failed run is preserved as `component-owner-gate/before-fixture.trx`. The reviewed 68-file patch `3bf02170f6b61a1ffe8391681ce485772ce6e49602dcc9d5ca43904f8ea32948` was applied over signed HR; every resulting source matches the tested candidate after line-ending normalization. Applied source tree before task records: `1627e95b37d6febd31861302d7806c0f05ddcf8e`. Normal applied Web, Unit, Integration and Components builds pass under `drafts/crm-activation/first-validation/` (0/0/5/3 existing warnings respectively, no errors). `focused/` expected/discovered/executed counts agree: 114 unit PASS in 6 seconds, 179 PostgreSQL PASS in 10 minutes 39 seconds, and 31 components PASS in 2 minutes 17 seconds, all with zero failures or skips. `closure-summary.json` records TRX hashes and counters. The candidate evidence is supporting proof only; no provider or user host was changed.

The complete applied CRM portability scan covers 5753 files and 29343 findings. Eight ADDED and four STALE entries were reviewed: four canonical Designer repetitions, moved Provider comparisons/hosted-MCP policy, and consolidated CRM tag ordering replace the removed bridge/loader findings. No portability defect was identified; the inspected baseline changes from 14433 to 14435 and final enforcement without `--write-baseline` passes. Tooling tests pass 6 + 4. Evidence scanners pass nine focused text files and four build logs at 100 MB with no oversized/unreadable files or findings; the separate token-shaped scan covers all sixteen files including the three filter files and finds none.

Forced CodeAnalytics snapshot `snap-20260910231033-baeb1b65` covers five selected projects, 570 documents and 1894 types, with no blocking errors or project cycles. Its partial DI/namespace findings are review input; zero recognized entities means it is not persistence-model proof. The actual PostgreSQL mapping/migration/restart tests supply that proof. Root reviewed owner routing, canonical old-mapping parity, Provider assembly moves, the staged diff and the independent persistence findings. Documentation evidence tests pass nine cases and canonical validation passes 209 maintained Markdown files. Work assignment ownership, remaining transfer/authority work, final broad validation and all real journeys remain open.

## Workflow, Workbench and Storage applied validation

CRM is signed at `d1e5e5cf11a80ef54df488371dff3926c087a01d`, verified with key `96E836FAA8854EE98ABC10903C206549E1D7EAD6`. The next reviewed 167-path patch `053adbe741795f87f5186ff5ba44276526b1ea594318f869d1e9afb59d16663c` applies Workflow17, Workbench12, durable delivery and Storage placement reconciliation. Source tree before records/baseline updates: `61456c9ab755ac2adb2d9e2aab31eda72433a7b5`. Canonical migration `20260910225242_AddWorkflowStructureReceipts` contains 155 entities; all 151 preceding entity mappings match, and only four receipt tables/indexes are added. Populated CRM/native evidence survives upgrade/restart and empty-receipt Down/re-upgrade; each retained receipt blocks Down.

The isolated cumulative candidate passed all four builds, 183 unit cases and 95 components. Its 106-case PostgreSQL run passed 104 and failed two fixtures: the old latest-migration constant and serialization of pre-materialized canonical rows. The first correction passed four of five affected cases; EF decimal equality retained the pre-read scale, so the final fixture clears tracking and reads every canonical row anew before comparing exact owner payloads. All five affected cases then passed in 44 seconds, without production changes or removed assertions. Candidate evidence is under `drafts/workflow-cumulative-candidate/`, with each failed run retained separately. These supplemental passes are not represented as one 106/106 run.

Normal applied proof is under `drafts/workflow-activation/`. Web and all three test builds pass (0/0/5/3 warnings, no errors); expected/discovered/executed unit count is 183, all PASS in four seconds. PostgreSQL expected/discovered/executed 106, all PASS in 8 minutes 42 seconds; Components expected/discovered/executed 95, all PASS in 3 minutes 18 seconds. All three lanes have zero failures or skips. `closure-summary.json` records counters, TRX hashes and equality of all 233 cumulative candidate source paths with the applied source. User host PID 1836, its profile and live providers remain untouched; PostgreSQL proof uses disposable fixture databases.

The complete applied static scan covers 5788 tracked files and 29381 findings. Nine ADDED and two STALE baseline entries were reviewed: existing path-policy/root resolution and hashed Storage binding metadata, four repeated canonical Designer properties, the FileTools namespace, moved WorkflowLaunchMode classification and removal of the former inferred-node ordering. No platform defect was identified. The inspected baseline moves 14435 to 14442; final enforcement without `--write-baseline` passes, and tooling tests pass 6 + 4. Final artifact scans pass nine focused text files and four build logs at 100 MB, with no oversized/unreadable files or findings; a separate token-shaped scan covers all sixteen files including filters, with no findings. Documentation evidence tests pass nine cases and canonical validation passes 209 maintained Markdown files; the applied diff and complete migration were reviewed before signing. Selected transfer/source authority, real journeys and the final broad gate are still open.

Workflow/Workbench/Storage signed checkpoint: `4ad34331afed3e0921b36db0ba5d7a781acebfba`; the expected key signature was verified.

## Work assignments, AgentHistory and WorkspaceSettings applied validation

The 55-path owner delta is based on signed `4ad34331afed3e0921b36db0ba5d7a781acebfba`. Full canonical migration `20260911000227_MoveWorkItemAssignments` has 156 entities; all 155 earlier mappings are preserved except the CRM participation-role check. The Work copy and populated Down verify all thirteen fields and row counts under exclusive locks; Down refuses cross-owner ID collisions before any mutation. Workbench's runtime model has thirteen types; AgentHistory and WorkspaceSettings each have one.

Evidence is under `artifacts/modules-decoupling/drafts/owner-maintenance-candidate/`. All four isolated candidate builds pass (Web/Unit 0 warnings, Integration 5, Components 3; zero errors). Initial integration compilation needed a missing SharedKernel import in the new migration fixture. Expected-count discovery rejected three selector mistakes before execution: 50 class-level UnixRuntimePortability exclusions were counted, one filename contained three differently named classes, and a short substring matched another transfer class. Final filters use the actual fully qualified class plus a trailing dot. Unit expected/discovered/executed 77, all PASS in 21 seconds; 50 declared special-lane cases are excluded, not PASS. Integration expected/discovered/executed 104, with 103 PASS and one fixture failure in 13 minutes 29 seconds. Components expected/discovered/executed 45, all PASS in 5 minutes 14 seconds. No runtime case is skipped. All nine Work migration cases pass. The only failure is the expected-data query combining a varchar enum column and an integer literal; the fixture now compares textual discriminators while retaining database ordering and every page assertion. The corrected one-case confirmation passes in 28 seconds after a refreshed integration build (5 existing warnings, no errors). Failed discovery/build/test evidence is retained; this is not a claimed 104/104 run.

The reviewed owner patch `9ec1aece547cedf6a60a6194f49bd386ddd02d40b901b490700b9b3f13dff3b1` is applied; pre-records/baseline source tree is `41abd3744b785cb1c0fcd7bae1d9cf6f973f8369`. Normal evidence is under `drafts/owner-maintenance-activation/`: all four builds pass (Web/Unit 0 warnings, Integration 5, Components 3; zero errors). Expected/discovered/executed counts are 77 unit PASS in 39 seconds, 104 PostgreSQL PASS in 12 minutes 9 seconds and 45 components PASS in 4 minutes 3 seconds, with no failures or skips. `closure-summary.json` records TRX counters/hashes and normalized equality of all 268 cumulative source paths with the applied code. The complete tracked static scan covers 5807 files and 29387 findings. Four ADDED/no STALE entries repeat RootPathState, RootPathSyntax, RaisesEscalation and EscalationCount in the new canonical Designer, whose existing mappings were already verified unchanged. The inspected baseline moves 14442 to 14446; final enforcement without `--write-baseline` passes. Tooling tests pass 6 + 4. CodeAnalytics forced snapshot `snap-20260911005437-ec8ee977` covers six projects, 611 documents and 2130 types with no blocking diagnostics or project dependency cycle. Its eight context names but only one recognized entity do not prove EF membership; actual model/PostgreSQL tests provide that proof. Final artifact scans pass nine focused text files and four build logs at 100 MB with no findings, unreadable or oversized files; the separate token-shaped scan covers all sixteen files including filters and finds none. Documentation evidence tests pass nine cases and canonical validation passes 209 maintained Markdown files. Signed checkpoint `9578422d4ca4fc68733e3d4da05953939088e7f3` is verified with the expected key; final tree is `148ee92ad8948d04810612bbb3ab0f1d191a4221`. The complete staged source/migration diff and actual owner/transaction routing were reviewed; no final product/journey claim is made.

## Source-bound execution admission candidate

Base: signed `9578422d4ca4fc68733e3d4da05953939088e7f3`. The ignored source-authority candidate composes 378 paths; its reviewed activation delta is 136 paths (55 new, no deletions), patch SHA-256 `612b49606f0fd018bae1158627b4338026b8ce68daaacee2f7d1445590c25b00`. Candidate output is `artifacts/modules-decoupling/source-authority-build`; source maps, exact source-derived filter counts, builds, discovery and TRX are under `drafts/source-authority-candidate/`. The delta is applied and staged; all 378 candidate paths match. Source tree before records/static baseline: `41cc34ba3f78d832ae20c326341618a3f30590b7`. The same Windows SDK, immutable sibling graph and leased PostgreSQL environment apply; the user host/profile are unchanged.

All four candidate builds pass. Unit initially passes 194/196, then all 196 pass after preserving the complete thirteen-entity Workbench expectation and restoring trusted JSON-ignored fields in the concurrent producer fixture. PostgreSQL discovery/execution agrees at 136: 129 pass, seven fail, no skips, 16 minutes 59 seconds. Three replay assertions expected Duplicate where the preserved UoW returns its stored Applied outcome; the API fault inspected a cleared change tracker instead of the committed run/accepted receipt; the Scheduler independent-read fixture awaited an update blocked by the new required shared row lock; both retirement/recreation races correctly returned PostgreSQL 40001 wrapped by EF. The corrected six methods/seven cases all pass in 1 minute 18 seconds, retaining receipt identity, exact exception, no-duplicate and revoked-new-intent checks. All nine new canonical migration/rollback/maintenance cases pass in the initial run. The full normal PostgreSQL result follows below.

Components discovers and executes 92, with 83 pass/nine fail/no skips in 5 minutes 1 second. Seven manager-chat failures share a missing AgentToolPolicyCatalog fixture registration; the exact representative also fails against the preceding 957 normal binary (1/1 discovery), which proves it predates this candidate, not that it existed at the original starting commit. The shell private-method fixture bypasses the user-event render, while a parameter render reloads and clears its error; it now enables the two fixture launch commands through the existing projection transform and clicks the actual button. The Structure failure exposes a production deferred-render key that omits the new dialog identity/preparation/authority-presence fields; the correction adds only the consumed state facts and does not serialize grants. The twenty-case correction run passes 19, including all seven manager and eleven Structure cases, with only the shell private-handler fixture still failing. Both shell launch cases then pass through the actual button after a fresh build (2/2 discovery, 858 ms), with unchanged persistence/authority assertions. Original failures and their source maps remain under `focused/integration-first-failure` and `focused/components-first-failure`; `fixture-confirmation` contains the seven passing PostgreSQL cases, and `component-confirmation` contains the nineteen passing cases and retained one-case failure, and `shell-confirmation` contains the final two passing cases. All affected candidate cases now have passing proof; full normal checkout results under `drafts/source-authority-activation/` follow below. These results are not an end-to-end completion claim.


Normal applied source-authority proof: Web and all three test builds pass under `drafts/source-authority-activation/first-validation/`, using `artifacts/modules-decoupling/build` without an overlay. Warning counts are 0/1/6/3, with no errors. Expected/discovered/executed counts agree: 196 unit PASS in 21 seconds, 136 PostgreSQL PASS in 18 minutes 15 seconds, and 92 components PASS in 4 minutes 6 seconds; all have zero failures or skips. The complete 159-entity migration and nine new populated upgrade/restart/rollback/maintenance cases are included. `closure-summary.json` records actual TRX counters/hashes and normalized equality of all 378 cumulative candidate paths. The exact staged source patch remains `612b49606f0fd018bae1158627b4338026b8ce68daaacee2f7d1445590c25b00`; the tested source tree before records/baseline is `41cc34ba3f78d832ae20c326341618a3f30590b7`.

The complete tracked portability scan covers 5862 files and 29486 findings. All 49 ADDED and four STALE entries were inspected: existing configured workspace-root flow, four repeated canonical Designer mappings, native graph-link terms, preserved governance comparisons, and domain Process/Workflow wording. No new portability defect was found. The baseline diff moves 14446 to 14491, with final no-write enforcement PASS; tooling tests pass 6 + 4. Forced CodeAnalytics snapshot `snap-20260911022749-2fde3fbd` covers eight projects, 833 documents, 2448 types and 182 registrations with no blocking error or project-reference cycle. Its internal owner namespace/type cycles and partial factory interpretation are recorded, not reported as a clean complete architecture. It recognizes nineteen Process entities but incompletely recognizes explicit mappings elsewhere; actual owner/model and PostgreSQL tests provide persistence proof.

Documentation evidence tests pass nine cases and canonical validation passes 209 maintained Markdown files. Strict final evidence scans pass all nine focused text files and four build logs, with no oversized/unreadable files or findings; the separate token-shaped scan covers all sixteen files, including filters, with no findings. The broader frozen candidate evidence scan covers sixty text files and reports only eight occurrences of the same public local PostgreSQL fixture password in four ignored reproduction scripts; that exact fingerprint is reviewed in `candidate-secret-review.json`. Failed candidate evidence remains retained. The source, composition, migration and staged diff were reviewed; the sealed Foundation is unchanged. No user host or provider was altered. Runtime policy/native/disclosure, captured-lifetime/transfer work, final broad gates and real journeys A-H remain open.

Source-authority signed checkpoint: `5065db4ec07f305435d1ad8789d0e8a8bdf4a33b`, tree `ef7027495690575da1554dfa4a3a2bd95d7c2869`; `git verify-commit HEAD` and the expected fingerprint both pass. The worktree was clean after commit. No merge, rebase, other-branch change, or push occurred.


## Product and Process policy owner candidate

The next ignored candidate is based on signed `5065db4ec07f305435d1ad8789d0e8a8bdf4a33b`. `drafts/policy-owner-candidate/source-state.json` identifies 153 effective paths across sixteen projects, ten frozen layers, twenty reviewed three-way hunk compositions and two explicit Scheduler resolutions. The new Storage integration project is referenced through an isolated candidate project with absolute source dependencies; the eventual normal solution entry and README are prepared. The actual contextual Razor input preserves its logical target path and CSS scope. No schema change is intended. Root preserves all Source-authority admission/context/registration hunks.

The first candidate Web build fails with exactly three removed Core policy-constant references in the newer Process/Workflow authority files (zero warnings, 5 minutes 6 seconds). Root changes only those references to the existing Workbench owner policy constants. The failed log and source state remain under `compiler-first-failure/`; the rebuilt 153-path candidate is now running. Output is `artifacts/modules-decoupling/policy-owner-build`. Source-derived discovery targets are 1214 Unit, 132 Integration and 42 Components. Standard stable exclusions remain; the 257-case class marked UnixRuntimePortability stays in the separate actual-host gate, and one opt-in LiveComfyUi case is explicitly excluded from this focused run. No candidate pass or applied-source proof is claimed yet.


The second policy Web build fails on the moved scope policy's Process artifact parser reference (one error, zero warnings, 2 minutes 36 seconds). Source inspection finds the parser/result still in Core.Execution, with production callers exclusively in Processes after this cut. Root moves `ManagedProcessArtifactOutcomeReader` and its result into the Process owner, changes only namespace/brace formatting, and verifies normalized source-body parity. The three existing parser tests change only the imported namespace. The deleted Core source and exact move are recorded in `policy-owner-root/artifact-parser-review.json`; the second failed build/input state is retained under `compiler-second-failure/`. The candidate now has 156 paths and source-derived expectations of 1217 Unit, 132 Integration and 42 Components. The third candidate build is running; no policy checkpoint is applied yet.

The third candidate Web build passes (zero warnings, 3 minutes 2 seconds); Unit compilation identifies only a missing Processes.Application import and the renamed owner ReadTools collection. Root corrects those two references without changing assertions and retains both logs and source state under `compiler-third-failure/`. The fourth complete candidate build passes Web/Unit/Integration/Components with 0/1/6/3 warnings, zero errors, and elapsed times 1:28.59 / 1:27.88 / 1:16.47 / 1:14.35.

Initial Unit discovery correctly stops at 1186 against the initial 1217 source estimate. Inspection accounts for all 31: eleven cases belong to the existing class-level UnixRuntimePortability lane, and a 21-row strongly typed ProcessHostCapabilityId theory is one non-pre-enumerated xUnit discovery item. The corrected selection therefore requires 1186 discovery items and 1206 executed cases; both counts are enforced independently, preserving every selected data row. The two excluded host classes contain 268 cases in total. No test code or assertion changes for this correction. `discovery-first-mismatch/` retains the original evidence. Integration and Components still require 132/42 discovery and execution counts, with one existing opt-in LiveComfyUi case excluded. Focused candidate execution is running. The reviewed normal-checkout delta has 155 changed paths (41 new, seven deleted) from 156 effective candidate paths; its ignored patch SHA256 is `43c43f3da739fc526fb41b807416c4f98444b2516afcb762c8917cb45b75eee7`. No policy source is applied or signed yet.

The initial policy Unit execution discovers 1186 items and runs all 1206 cases: 1199 pass, seven fail, zero skip (9 seconds). Two curator fixtures still consult the built-in known-tool list; the zero-provider fixture now acquires configured Storage; two newly drafted Process Storage cases assume writes despite the preserved operation-contract denial; two actual MAF streaming/nonstreaming cases expose a trusted policy failure being overwritten by generic successful-Read assessment. Root retains all approval/redaction and denied-tool checks, moves the positive Storage write assertions into an added ordinary-Agent case, and adds trusted pre-dispatch failure capture/replay while preserving the exact visible denial text. The failure evidence remains under `focused/unit-first-failure/`; `policy-owner-root/pre-dispatch-failure-review.json` records every disposition and compatibility limits. A new private result kind makes older readers reject the new denial evidence instead of ignoring it. No SQL schema change is required. The first repair build passes Web (4:15.57, zero warnings), then fails Unit on a diagnostic property name (1:06.72, one existing warning); that exact input/log is retained under `compiler-after-unit-repair-failure/`. The corrected 162-path candidate is building with expectations of 1203 discovered/1223 executed Unit cases, 143 Integration and 42 Components. Native SDK replay regressions and two independent file-store denial restart cases are included. Source remains unapplied.

The repaired four builds pass with 0/1/6/3 warnings and no errors (2:01.67 / 1:03.16 / 0:56.41 / 0:53.89). Unit discovery is 1203 and execution is 1223/1223 PASS, zero skips, eight seconds. Integration discovers/executes 143: 141 PASS, two FAIL, zero skips, 14 minutes 26 seconds. This is a mixed PostgreSQL/file-journal/SDK cohort, not 143 PostgreSQL cases. Both failures are the historical roundtrip fixture's unsaved Agent lacking the required current lifetime grant; both exact failures reproduce on the preceding signed 5065 normal binary (2/2 discovery/execution, 26 seconds). The replacement fixture saves/reloads the same scoped Agent through its owner and asserts the captured lifetime, preserving all metadata, managed-file and no-mutation assertions. The original 162 effective inputs and completed proof are retained under `focused/integration-first-failure/`.

Components discovers/executes 42: 41 PASS, one FAIL, no skips, 24 seconds. The direct HR approval-decision fixture omits the owner policy catalog. All three production calls already pass the injected catalog. The fixture now passes `ProductToolPolicies` without changing deterministic hash, privacy or approval assertions; original input/results remain in `focused/components-first-failure/`. Independent persistence review finds the new trusted policy-denial checkpoint correct by source inspection and identifies an adjacent pre-existing loss of trusted unavailable/argument-failure details when JSON is restored. A bounded correction and admitted first-call/restart regressions are in preparation. The remaining Core target/effect metadata interpretation is also under owner-boundary review. The current candidate remains unapplied; these findings and new code require refreshed proof before signing.

The next reviewed candidate has 166 effective paths across seventeen projects. It adds the frozen host-failure checkpoint (`2baa401d…`), typed body-failure codec (`b9bb7d6a…`) and explicit owner capability effects (`7a32b9ab…`) over the retained root corrections. All 213 source-derived metadata vectors, runtime descriptors and configured descriptors match the pre-cut values, cross-checked against the independent 106/88 existing golden rows; CLR assertions remain pending. Body failures preserve their actual None/NotCommitted distinction without claiming pre-dispatch provenance. New private result kinds reject older readers explicitly while legacy/null/ordinary JSON stays unchanged. Maintained failure-boundary documentation records these compatibility limits. The fresh selection is 1220 discovered /1240 executed Unit, 154 Integration and 42 Components. All four builds and the selection are running. The guarded unapplied normal delta has 165 paths (45 new, seven deleted), patch SHA256 `5ed35f9a226ea61f52d4460def347840cb105f2ef1a6063add55b265d7de3e68`; it must be regenerated after any candidate correction.


The expanded candidate Web/Unit builds pass (0/1 warnings, 2:52.26 / 1:05.31). Integration first rejects a Unit-only test helper; after replacing it with the real path-policy factory, it rejects the initially wrong namespace. Both original inputs/logs are retained under `compiler-host-failure-fixture/` and `compiler-host-failure-fixture-namespace/`. The corrected Integration build passes with six existing warnings in 1:14.06; Components passes with three existing warnings in 0:51.49. Unit discovery/execution is 1220 /1240, all PASS, zero skip, eight seconds. The full mixed Integration cohort discovers/executes 154: 143 PASS, eleven FAIL, zero skip, 15:53. All eleven failures are the new host-result fixture's observation/marshaling assumptions; previous roundtrip failures now pass. The complete source map, fixture, log and TRX are retained under `focused/host-observer-first-failure/`.

Root moves the fixture observer from middleware inside the factory to the SDK's actual FunctionResultContent, retaining payload, trace, atomic checkpoint, independent restart and exact dispatch-count assertions. Only the two explicit trusted CLR-return cases use the installed SDK's identity result marshaler; arbitrary/default-marshaled JSON remains untrusted. A first repair compile catches that this SDK's marshaler accepts three arguments and returns ValueTask; its exact source/log/map is preserved under `focused/host-observer-repair/compiler-marshaler/`. The corrected Integration rebuild passes with six existing warnings, no errors, 1:17.73. The eleven fixture cases pass in 1:02 and all 42 Components pass in 24 seconds, without failures or skips. This selected repair proof is not represented as a fresh 154/154 cohort run or normal-checkout proof.

Independent source-only preflight composes fifteen frozen native/background/disclosure layers into 132 effective paths with 26 reviewed hunk merges. Manual resolutions preserve Workflow and Process admissions together, retain both Process authority interfaces, keep trusted pre-dispatch capture alongside explicit owner uncertainty, and retain the scope-reviewed project-reference predicate with child-launch provenance. Root adds a guarded checkpoint for explicit authorization denial between claim and dispatch, twelve actual SDK/file-restart cases, current disclosure callbacks for the two prior denial fixtures, and production registrations. Unexpected authorization errors or conflicting committed evidence remain unresolved. This following candidate is not compiled/applied. Source review verifies actual governed Process execution and recovery both select the organization store; scoped floating/runtime tools require a separate active-journal source observation seam, now being prepared. Neither source review is browser/provider proof.


### Applied policy-owner checkpoint validation

The 165-path reviewed policy delta is applied and staged over signed `5065db4ec07f305435d1ad8789d0e8a8bdf4a33b`. All 166 effective candidate paths match the checkout; one trailing blank-line cleanup was mirrored into the reviewed candidate and source hashes without changing C# tokens. Final activation patch SHA256 is `4360e8e55babf3c127c216e76be3055e834dbc8699ff71e50f9f366f24775977`; source tree before records/baseline is `0e2be5836a5860f5bf944668d72bb53477b5b81b`. `policy-owner-activation/run-applied.ps1` builds normal repository projects without an overlay into `artifacts/modules-decoupling/build`. Web, Unit, Integration and Components all pass (0/1/6/3 existing warnings; 2:47.20 / 1:22.82 / 1:13.37 / 1:02.59). Unit discovers 1220 and executes 1240, all PASS, zero skip, nine seconds. Integration discovers and executes exactly 154, all PASS in 21:07; Components discovers and executes exactly 42, all PASS in 30 seconds. Both have zero failures/skips. This mixed Integration selection includes PostgreSQL, actual MAF SDK and independent file-journal cases. `closure-summary.json` records exact counters, times, source equality and TRX hashes.

The complete tracked-source portability scan covers 5900 files (5923 candidates, 23 binary/large exclusions), 29504 findings and no truncation. Both tooling self-tests pass. Initial enforcement reports 219 added and 228 stale findings; all 426 distinct fingerprint deltas were reviewed with original/current source contexts. The changes are moved owner policies, existing platform syntax/inspection checks, protocol case rules and explicit immutable tool metadata. No new portability defect remains. The baseline refresh changes exactly those reviewed counts, from 14491 to 14482; scanner/scope metadata is unchanged. Final enforcement without `--write-baseline` passes. Ignored `portability-source-review.txt` and `portability-reviewed-dispositions.json` retain the per-fingerprint review and the scan hash.

Native recovery remains a following, unapplied cut. Independent review found that an owner-receipt retry denial could replace earlier uncertain/committed evidence, and that rethrowing a fresh authorization denial made the actual factory trace differ from replay. Root now permits trusted non-commit completion only for original Prepared proposals without current committed capture, returns fresh sanitized denials through normal assessment, and keeps prior result/effect/disclosure evidence on retry. Same-claim persisted Completed outcomes are an explicit no-op during uncertainty preservation, including lost acknowledgments. Three actual factory first-call/restart cases and eighteen real file-journal retry/lost-ack cases supplement the twelve streaming/nonstreaming authorization cases. The 138-path source preflight passes; this is not compiled/runtime proof or closure of the later lifetime/transfer/operator work.


Final normal policy evidence review scans nine focused text artifacts and four build logs at a 100 MB limit, with no unreadable/oversized files. Build logs have zero findings. The raw focused scanner reports six secret-assignment matches: the two existing synthetic `api_key` privacy-test inputs appear once each in discovery and twice each in TRX metadata. All six exact fingerprints and their source locations are reviewed; they are test literals, not credentials. No scanner or test was weakened and the raw FAIL report is retained beside `evidence-secret-review.json`. A separate token-shaped/private-key scan covers all sixteen files, including the three filter files, with zero findings. No real provider secret or copied user content is present in these proof artifacts.


Forced CodeAnalytics snapshot `snap-20260911052857-4477ff94` covers twelve projects and 1176 documents. The initial 85 MB response exceeds the tool frame, but the fresh completed snapshot is confirmed through recent-snapshot lookup and bounded inventory/dependency/findings/services/persistence queries. It has no project cycle or Error finding, 167 recognized DI registrations, 237 large-file warnings and 995 type-complexity notices. The six SCCs are two existing internal namespace groups, Core journal/claim ownership, two nested-type relationships and the existing Workbench package/importer pair. The latter stays in the following selected-transfer cut. DI recognition diagnostics are 31 DI0001 and eleven DI0002; static EF extraction recognizes six contexts but only one entity and is not model-completeness proof. Actual normal builds/focused PostgreSQL proof remain authoritative; this policy cut has no schema change. The compact query review is retained in `codeanalytics-review.json`. Final cumulative architecture and browser/provider gates remain open.


Documentation evidence validation passes all nine cases; canonical validation passes all 210 maintained Markdown files. The complete normal policy checkpoint has passed its focused build/test/static/evidence/documentation gates. Native/background/disclosure and later lifetime/transfer/operator work remain separate unfinished implementation; journeys A-H and the final cumulative broad gate are still required.


Policy checkpoint `0f1330f78540e8449fd9b132ba28b057e68cea1a` is signed and verified (`G`, key `96E836FAA8854EE98ABC10903C206549E1D7EAD6`), with final tree `d5945c9696f565e366f7b134cc221825208fea4a`. There were no unstaged tracked changes at signing. The following native/background/disclosure candidate composes 138 paths across eighteen projects and 26 reviewed hunk merges. `native-recovery-candidate/build-and-run.ps1` now builds into its isolated `native-recovery-build` output, with exact source-derived expectations of 310 Unit, 269 mixed Integration and 88 Components. The two existing class-level UnixRuntimePortability cohorts remain excluded for the separate actual-host gate. The native candidate is unapplied and its first compilation is running.


### Native candidate compiler integration

The first Web compiler attempt fails at the new execution-authority query because a LINQ range variable shadows a later local. Renaming only the range variable preserves the query. Original log/source/map are retained in `native-recovery-candidate/compiler-first-failure/`. The next Web build passes with zero warnings/errors in 1:51.59. Unit then reports six errors in four new fixtures: two missing owner imports, tuple switch target typing and three mandatory invocation-context arguments. The retained original evidence is `compiler-second-failure/`; all assertions are unchanged. Corrected Unit passes with one existing warning and no errors in 2:02.56.

The first Integration build reports fourteen errors: missing owner imports, the target-query constructor now accepts the assembly service, two fixture revocations use the actual Write case, and the transaction-end probe must obtain the physical connection from its actual owner context. Original log/map and all affected sources are retained in `compiler-third-failure/`. Root also corrects eight new nullable/assertion analyzer warnings without reducing coverage. Integration rebuild passes with six existing warnings and zero errors in 3:21.66. Adding the affected existing prepared-launch fixture expands the independent integration selection from 269 to 287; Unit remains 310 and Components 88. Component compilation and all runtime execution are still pending. The candidate now contains 139 logical paths and remains unapplied.

The following source-only owner continuation preflight composes Resources/TestLab lifetimes, Work admissions, Process asset receipts, ordinary Agent native/Projects fences and Process Projects source authority. Every frozen patch reconstructs against its recorded original bytes. Six layers touch 139 paths with 24 hunk merges; five strict hash-bound resolutions preserve both independent documentation additions, the existing deletion delegate, the original-lifetime deletion guard, receipt-based creation results, test friends and tracked WorkflowAuthority. This map overlays the native candidate and is not a standalone compile input. New early context returns still require the dependent Workflow authority/lifetime cut. No schema migration, compilation, runtime or browser pass is claimed for this continuation.


The first native runtime cohort uses the unchanged 139-path candidate. Components build passes with three existing warnings, zero errors, 1:12.86. Unit discovers and executes 310/310 PASS, zero failures/skips, seven seconds. Integration discovers and executes 287: 192 PASS and 95 FAIL, zero skipped, 36:40 (2026-09-11 06:06:45–06:43:28 UTC). Exact source/map/selection, discovery, logs and TRX are retained under `native-recovery-candidate/focused/first-integration-failure/`; this failed run is not superseded by an unexecuted correction. Failure review identifies managed seed identities replacing fixture grants, missing assigned executors in file catalogs, disabled master capability switches in four disclosure fixtures, two background tests expecting a returned failure rather than the existing typed exception, a synthetic hosted local read missing its explicit disclosure callback, and a missing production Process tool-admission registration. Root corrections preserve the affected behavior assertions, add canonical readback/intended-fault assertions, and remain uncompiled. The separate Components run discovers and executes 88/88 PASS, zero failures/skips, 1:47.

The following selected-transfer preflight now reconstructs eighteen frozen layers and 202 touched files over the native candidate, with 46 hunk merges. `owner-continuation-transfer-snapshot/` freezes those exact input bytes; every source hash matches. Resolutions retain original Process source/lifetime facts, selected-handler scope, imported history and existing owner registrations. This is source-only preparation, not applied source, migration or runtime proof.


Native correction compilation: Web and Unit pass (0/1 existing warnings, 3:53.46 / 1:24.59). Integration catches two missing fixture imports; its exact failed 140-path inputs and log remain under `focused/first-integration-repair/`. Independent review additionally catches that a configured fixture Agent must be persisted before saving its first journal. Root repairs both imports and that catalog setup while preserving ordinary recovery-input behavior. The subsequent Integration and Components builds pass (6/3 existing warnings, 2:42.29 / 2:31.03). `native-recovery-diagnostic/` retains the exact three-file correction, source map and 21-case filter. Actual discovery/execution is 21/21: 7 PASS, 14 FAIL, zero skips, 8:07. The failures expose an unannotated ProcessRunId JSON constructor, a fixture expecting a CLR result rather than the SDK JSON result, a reused activity operation ID in a completed background retry, three fixtures whose intended provider fault was not reached, and a Storage replay request digest mismatch after restoring access. A second preserved diagnostic uses the constructor/result/activity corrections and full exception-chain assertions; its expected Unit/Integration selections are 9/6. These are corrections under test, not passes. No correction has been applied to the tracked source.

The following owner/runtime preflight now composes 32 frozen layers and 307 touched files, with 68 hunk merges, captured immutably in `owner-runtime-snapshot/`. It includes selected-transfer Work/Process asset history, compensation safety, Structure/image/configured-workspace disclosure, actual SDK context-tool attachment and Storage recovery/discovery/continuation. Root has prepared nine composition/model files to register the real owner adapters, map the explicit HTTP surface and add the fifteenth Workbench history entity. This remains uncompiled; complete canonical migration, Workflow lifetime/Process producer, original SDK skill approval source binding and import lock-order correction remain required. Independent review found an imported Workflow receipt catch-path bypass (being closed in the Workflow cut), five transfer fixtures with empty mutation scopes, and a serializable snapshot taken before import exclusion locks. The latter requires an import-specific ordering and real waiter/contention proof; it is not a passing race test.


### Native replay diagnostics and continuation compiler

`native-recovery-diagnostic2/` preserves the141-path input: Unit/Integration build PASS (1/6 existing warnings;4:05.59/1:09.13), Unit9/9 PASS, Integration0/6 PASS. Its failures identify inherited unsupported thinking-effort settings in three synthetic provider fixtures. A diagnostic-only transport trace then reaches ten original owner checks: eight saved-result replays differ only in the private per-call provider-history RequestId, while two HR cases attempt unrelated local MCP startup. That instrumentation is confined to ignored evidence and is absent from candidate/production source. Root limits HR fixtures to their actual tool grants and uses the existing `ProviderHistoryChatContext.ForTransport` normalization for hashing. Four new Unit cases preserve sensitivity to actual provider options/messages/tools and verify source options remain unchanged.

`native-recovery-diagnostic3/` preserves all142 inputs. All four builds PASS (Web/Unit/Integration/Components warnings0/1/6/3;2:35.98/0:59.89/0:51.89/0:47.71). Unit315/315 PASS; Integration20/21 PASS, zero skips,3:57. All Storage, gallery, Memory and HR saved-result cases pass. The remaining Process result uses SDK string enums. Root adds a separate compatible result reader; canonical numeric proposal serialization remains unchanged. Numeric/string result round trips retain the original nonempty ProcessRunId.

`native-recovery-diagnostic4/` freezes the corrected142 inputs. Unit/Integration builds PASS (1/6 existing warnings;1:55.42/1:03.83). Unit discovers and executes316/316 PASS, zero skips,7s. Integration discovers/executes3: two cached-result cases PASS and the actual background node-start producer FAIL, zero skips,1:12. Valid deserialization exposes a precise receipt mismatch: the generic Structure wrapper overwrites `process-launch-admission` with a project marker. Diagnostic5 preserves the original failed inputs and tests a narrow typed result-to-effect selector; expected Integration discovery/execution3. It is running, not passing proof. The full applied316/287/88 cohort remains pending.

The source-only continuation compiler contains493 effective paths across37 projects and103 reconstructed/explicitly reviewed hunk merges. Its first467-path Web preview fails with one forbidden reverse-owner dependency in Storage transfer; all467 inputs are frozen under `owner-continuation-candidate/compiler-preview1/src/`. Root replaces that dependency with a mandatory Infrastructure provenance port implemented by the original Workbench policy, preserving all validation/stamping behavior. Preview2 is running. The38-layer continuation includes original Workflow lifetimes, the Process Workflow tool producer, complete import lock ordering, SDK source/disclosure and operator Storage recovery UI. Neither the continuation nor its canonical migration is applied; generated model, PostgreSQL, component, browser and provider proof remain required.


### Native review closure and owner compiler continuation

Diagnostic5 freezes 142 inputs (state SHA `3838a0f68ed5634c03d0221a9abe7855a89b6293287ca9b3b205de9395683421`). Integration build passes with six existing warnings, zero errors, 1:37.16. All three expected Process producer/disclosure cases are discovered and pass, zero skips, 1:11. The typed selector retains `process-launch-admission/<admission ID>` instead of replacing it with a generic Structure marker. Diagnostic6 adds the cancelled-read uncertainty guard: Integration build passes with six existing warnings, zero errors, 3:49.29; all nine expected cases are discovered and pass, zero skips, 1:18. Cancellation after dispatch does not establish rollback for a reconciliation-required read.

Independent review checks the immutable diagnostic5 sources and both frozen SDK layers. Its other confirmed finding is stale Storage/Memory grants in retained runtime callbacks. The separately frozen correction `9b1b6f9882254249212c4e6eeeda5b705d4f780e08355bfbe71fb09163a38416` rechecks the current canonical Agent while preserving the original grant limit, session and exact saved result. Diagnostic7 freezes all 144 composed inputs (state SHA `77e75a3bb8cd39c7c39413bf9bfe6d879a1767bcfc4393323c5021732ff5b728`); 23 Memory Unit and six actual retained-runtime integration cases are building. No native source is applied yet. The final full focused expectations are 318 Unit, 296 Integration and 88 Components; those applied gates are NOT_RUN.

The following owner continuation preserves compiler previews1-5 and exact source copies. Preview5 contains 495 paths and Web passes with zero warnings/errors in 3:22.11. Components fails with four compiler errors caused by two stale Gantt fixture calls (missing BunitContext and mandatory failure argument), with three existing warnings, 1:32.14. Root changes only those calls. Preview6 contains 496 paths after the native grant correction, frozen at state SHA `76a73fe00edb492e15f96927b7602cc0bb135659770b56d892376963752eb0be`; Components/Unit/Integration builds are running. No continuation runtime, migration or browser pass is claimed. Migration review additionally requires guards for both PascalCase and camelCase cleanup JSON and for pending mapped Workflow origins retained before run admission.


Diagnostic7 completes with Unit build PASS (one existing warning, 6:15.11), Integration build PASS (six existing warnings, 2:21.11), Unit 23/23 PASS, and Integration 2/6 PASS with four initial Storage setup failures (2:27, zero skips). Canonical normalization replaces the seeded fixture because constructing its grant JSON from an empty object removes managed-seed metadata; MarkCustomized then intentionally does nothing. The one-file correction preserves the original configuration and adds customization readback assertions without changing production callbacks or grant checks. Diagnostic8 freezes the 144 code inputs (state SHA `6c16affb598847fc1ddb7fca017609d967788174f43a31d5854d12a464581e56`), builds Integration with six existing warnings/zero errors in 2:29.44, discovers four cases and passes all four with zero skips in 2:15.

The 147-path native activation adds only three reviewed documentation files to those diagnostic8 code inputs. `native-recovery-activation3/review.patch` SHA is `f005088b9b29a73d792182c8466cde1e5cd203bd179f22d6fb2adda4ed7da307`; guarded application verifies the exact signed predecessor, empty index, two expected dirty records and all before/after hashes. All 147 paths are applied and staged, diff-check passes. Normal Web passes with zero warnings/errors in 4:03.36; normal focused suites are running. Portability tooling self-tests pass (6 enforcer, 4 scanner), and the complete scan contains 5967 files/29593 findings with no truncation. Initial enforcement has a delta under independent review; final no-write enforcement is NOT_RUN.

Continuation preview6 Components build passes (three existing warnings, 5:41.12), discovers six Storage recovery dialog tests and passes six, zero skips, 1s. Unit fails with 19 errors in stale test DTO names, explicit target typing, missing SharedKernel imports and SDK AIContext construction (one existing warning, 2:22.83); all inputs/logs remain frozen. Root keeps the three moved Storage snapshot behavior tests on their actual Storage-owner implementation and changes transfer setup to the published owner data contracts, retaining byte/provenance assertions and explicitly checking cleared live authority. Preview7 freezes 515 paths, state SHA `327ea7d64e2004339c95f6a887e5702872d9efb7e3b5d3e349fea5601d9c12a3`: Web PASS 0/0 in 4:12.25; Unit FAIL three MAAI001 diagnostics/one existing warning in 1:44.85. The SDK marks direct InvokingContext construction experimental; the repository already acknowledges that diagnostic at equivalent test-only calls. Root applies three call-local pragmas, preserving SDK behavior assertions.

Continuation preview8 freezes 524 paths, state SHA `4298fe3c05cc7fdf47d7dc9782142859f4ad9eea7652a84b975edc48055f3be1`, and is building Unit/Integration. It includes frozen mapped Process claim/receipt correction `37e8a2222f2573f29e69be932abc5f4a1489e1b7edbb471850889d8a3f76c061` and migration test patch `0f1ffe7747525c0ec8ed7c567c34d2dd0053f6639263bbb654ad9bc8ddaea8e0`. Migration tests contribute 97 source cases (103 with coherence); no migration or SQL pass is claimed. Complete future source selections are 664 Unit, 957 Integration and 174 Components, with the existing actual-host exclusions retained. The mapped parent-adapter case remains a separate explicit actual-host selection.

Applied native validation advances to Web PASS 0/0 (4:03.36), Unit PASS 1/0 (1:46.36), Integration PASS 6/0 (1:43.68), Components PASS 3/0 (1:36.98); Unit discovery/execution 318/318 PASS, zero skips, 13s. PostgreSQL 296 and Components 88 remain running/pending. All 42 portability fingerprint dispositions cover 43 occurrences and 17 exact source hashes; scan SHA `beecd8c80fe1853fd36b8b23f7d87a3a6c770797a9f00c3388b1aeaaa1d5b169`, review manifest SHA `1ed5ca7813ea06887b6199e5d2d963bc7e8081563b243a2bf93a8c97c9af2090`. No genuine portability defect; reviewed baseline count 14482 → 14519, final no-write enforcement PASS. CodeAnalytics `snap-20260911093657-40cd4c1d` covers Core, MAF, Process Application/Persistence: 1223 types, 9568 members, 19 Process entities, zero diagnostics or blocking errors; 380 complexity observations and two intra-Core type cycles remain review observations, with no project cycle. The journal/lease/reconciliation cycle reflects scoped claim callbacks; the other is the existing nested reference cache.

Continuation preview8 Unit build PASS 1/0 (5:07.07), Integration build FAIL two fixture errors (1:50.16), SDK discovery/execution 33/33: 31 PASS, two FAIL, zero skips, 2s. Direct SDK resource invocation omitted required AIFunctionArguments.Services. Root supplies a disposable service provider while retaining actual SDK/schema/approval assertions, adds the runtime response namespace, and exposes the internal actual Process executor to the existing Integration assembly. Preview9 state SHA `c0555ad32a30065386a94f4d5efd0ad307e398891ceac26074007e9d4ae36ba1`, 525 paths; Unit build PASS 1/0 (6:22.33); Integration build and SDK retry remain running.


### Native persisted-state correction and complete continuation preview

The 147-path normal native gate finishes Unit 318/318 PASS and Components 88/88 PASS (3:57), zero skips. Every changed production project (17 total) builds directly with zero warnings/errors. Integration discovers/executes all 296: 287 PASS, nine FAIL, zero skips, 56:27. The failed TRX and a read-only testhost stack sample are retained under `native-recovery-activation3/focused/`; the stack sample showed migration metadata construction during the quiet interval; it did not establish completion or deadlock.

`native-recovery-precision.patch` (`070e6e9b01fb1a81f44b9a8a40b1c61c550dd9a7a68bc03dab4f1ac225ec9cb6`) returns the actual saved Process snapshot before transaction commit. Direct Persistence build PASS 0/0 (1:10.07), refreshed Unit PASS 1/0 (6:05.51), Integration PASS 6/0 (1:31.09). Source/discovery/execution: 77 store tests PASS (10.13s), seven PostgreSQL cases PASS (2:47), no skips; these include all six original timestamp failures and an explicit 100ns/microsecond, GUID-token and stale-state regression. No concurrency comparison was weakened.

`native-recovery-fixture-parity.patch` (`ceb451c7a69ea98e8111d276d0212a15ab2ad043ce9e139dd90d59c3ef853971`) corrects the other three fixtures, preserving exact identity/effect/exception checks. Integration rebuild PASS 6/0 (1:53.41); all three cases discover and pass (1:29). The broader refreshed Process selection expects, discovers and executes 127: all PASS, zero skips, 19:13. The exact applied 148-path successor is frozen at state SHA `68d1dfcad83f156b43253db1a35d362c0b39259536ba0df18edd2fe5c815275e`; the original candidate/failed evidence remain immutable. Final regenerated no-write portability enforcement PASS: all 14519 reviewed findings unchanged, complete 5967-file/29593-finding scan, tooling self-tests 6 + 4 PASS.

Continuation preview9: Integration compile FAIL 26 errors/15 warnings (1:47.10), SDK 33/33 PASS (7s), zero skips. Preview10 composes reviewed root fixture repairs, six new owner/postcommit/lifetime layers and the complete 161-entity migration; 558 paths/38 projects, state SHA `421ff5a42bcf2edd19742d4fb7e94db640ce51ccb96a190f8c538950a282ae7b`. Integration and Components builds are running. New ignored proof sources add 12 bootstrap, six tracker, 17 Workflow Storage, 16 owner postcommit, 18 postcommit component, 16 action-lifetime component and 22 JSON migration cases; these are not execution results.

Canonical generation independently preserves all 1859 old property mappings, 63 FKs (39 Restrict), 12 defaults and 310 unchanged indexes. Intended delta: 20 nullable columns, two tables, four checks, eight new indexes and one changed reservation filter. Prepared migration SHA `4395e84f46864c2b03c0321df41c5d5fde951cba08e02a3f2a7d5682a018aa5e`; Designer/Snapshot parity survives preparation. PostgreSQL 16 session-local helper probe PASS with transaction rollback: inspection-only active escaped NUL replacement, doubled backslashes/quotes, all 25 .NET whitespace characters, large numeric JSON, malformed JSON and invalid critical UUID refusal. Original stored TEXT is never replaced by the inspection copy. Actual canonical upgrade/down/restart and 125 migration/coherence cases remain NOT_RUN.

The native successor retains the original 296-case failed run and proves every corrected failure plus the full 127-case Process owner selection. The original 318 Unit/88 Components proof predates the one-file persistence correction; refreshed 77 store Unit and 127 PostgreSQL owner cases cover that correction. This is a focused checkpoint, not a rerun of the entire 297-case augmented selection or the final broad gate. All 148 actual source hashes match `native-recovery-successor/source-state.json`. Artifact scan review covers the original activation, precision correction, fixture correction and successor folders: six findings are the same public local test credential in three scripts; no user/provider credential was found in the scanned text. Original scanner FAIL reports and the explicit disposition are retained in `native-recovery-successor/secret-scan-review.json`; configured non-text exclusions remain outside that claim.

Continuation preview10 Integration FAIL one missing Core import, zero warnings (3:34.13). Preview11 state `7d238a0db396fffebfe43da7d33c4bbbca7d077bae510a3750b9647b26093ef1`, 563 paths, Integration FAIL one SharedKernel import/two nullable UI warnings (1:59.26); Components did not run after either failure. Frozen UI compiler correction `1416ede929e9e8f7393b1f4fca65eee5b0de0a326cc9da3ba1c0756f9e564da9` returns the actual nonnullable feedback to the two dialogs, with no suppression or fallback. Historical fixture delta `eae7c54a112bdcf8c50108c8222dcc74c431afbb6129fde41408d4ad913b56b0` retains 25 cases and adds two populated-downgrade refusals; combined migration/coherence/historical source count is 152. CRM diagnostics delta `374d26f019b7c5d6bf2ea28ed15eecd6b5126337dad8ce721c0c14413d9f447e` moves four development-only global reads into the existing owner service and adds six PostgreSQL cases. Current continuation source-derived selections: 664 Unit, 1076 Integration, 208 Components; execution is not yet claimed. Preview12 builds are running.

Preview12 state SHA `db82e2699b2710bf705aa18a8c07a2b18ab2de6905114d4c73a40abd84ae095d`: Integration build PASS, 16 warnings/zero errors (1:43.71), Components build PASS, five warnings/zero errors (1:01.81). New test-only analyzer warnings are under review before executing the PostgreSQL selection; they are not classified as pre-existing.
