# Implementation Prompt

Implement only the current subbundle for `C:\repositories\CanDoItAll\units-converter-agent-delivery-bundle`.

Read the subbundle README first, then obey its prerequisites, scope limits, proof rules, browser-validation logging rules, and progression gate exactly. Keep AgentFramework as the canonical AI-agent owner, keep CRM-HR as a projection and bridge only, and prefer reusable process templates over scenario-specific hardcoding. Use the smallest correct code changes, but do not preserve a broken ownership model or showcase-specific orchestration just because it already exists.

When the subbundle affects runtime or browser-visible behavior, capture proof immediately with targeted tests, Playwright MCP actions, screenshots, and execution-report updates. If the live run exposes architecture or maintainability pressure, record the concrete weakness before refactoring, then split code only where the evidence justifies the boundary.
