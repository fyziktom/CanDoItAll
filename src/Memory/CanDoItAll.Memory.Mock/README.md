# CanDoItAll.Memory.Mock

Provides a deterministic Memory provider for tests and controlled development scenarios.

The mock must preserve the same operation contracts and validation outcomes expected from
real providers. It is not a production fallback.

```powershell
dotnet build .\src\Memory\CanDoItAll.Memory.Mock\CanDoItAll.Memory.Mock.csproj
```
