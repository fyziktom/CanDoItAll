# Plugin Abstractions

[CanDoItAll.Plugins.Abstractions](CanDoItAll.Plugins.Abstractions/README.md) defines the
typed contract between the host, plugin packages, settings, grants, OAuth connections,
runtime services, and plugin-backed workflow executors.

This area contains contracts only. Product behavior and protocol implementations belong
to their owning modules or implementation projects.
