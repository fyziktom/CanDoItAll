Create or improve Blazor SSR applications with maintainable, strongly typed C# and explicit validation. Start with the real scaffolded project files, especially the solution file, `.csproj`, `Program.cs`, routed page, and test project, before making substantial edits.

Use existing repository conventions and the shared component library where available. Preserve the scaffolded target framework, Blazor SSR hosting shape, and chosen test framework unless the approved scope explicitly changes them. Prefer predictable component state and explicit service boundaries over hidden lifecycle side effects. Do not introduce unnecessary JavaScript, fallback mechanisms that hide failures, or speculative abstractions.

If the project structure or attached step materials name a concrete output directory, treat that directory as the real application root. When it is outside the managed workspace, use the mapped alias `external-target/<drive>/...` with workspace file and dotnet tools; do not scaffold a parallel copy under `artifacts/...`, `output/...`, or another generated implementation folder. If the named directory already is the app folder, scaffold directly into it instead of adding an extra nested child directory unless the requirement explicitly asks for that extra layer. Treat durable artifact paths as proof destinations only, not as substitute Blazor project roots.

Avoid same-name collisions between Razor components and domain types. If the domain already defines `LengthConverter`, `MassConverter`, or unit enums, do not create UI components that rely on ambiguous type resolution. Rename the UI surface or qualify the domain type explicitly so the generated build stays deterministic.

Do not bind Blazor inputs or selects to `object`, `dynamic`, or other weakly typed holders that depend on runtime conversion. Keep form state concrete and explicit so route rendering fails at compile time instead of throwing `TypeConverter` or ambiguous binding errors at runtime.

When the scaffolded tests come from `dotnet new mstest` or the generated project references the `MSTest` meta-package, respect that API surface. Use the modern assertion methods exposed by the generated package, such as `Assert.Throws<T>` or `Assert.ThrowsExactly<T>` when present, and avoid injecting legacy `[ExpectedException]` or `Assert.ThrowsException(...)` patterns unless the scaffold already uses them.

Keep automated test projects outside the Blazor host directory. For a host at `HostApp/HostApp.csproj`, the test project belongs beside it, for example `HostApp.Tests/HostApp.Tests.csproj`, not under `HostApp/HostApp.Tests`. If a host build reports missing `Xunit`, `Fact`, `MSTest`, or test attribute namespaces, inspect for misplaced test files inside the host and use `workspace_delete_path` with `recursive: true` on the stale nested `*.Tests` folder; do not add test packages to the production host. Do not recreate test files under the host after deleting them. After any test-layout repair, rerun the same host build that failed and then run the relevant test project.

Do not rerun `workspace_dotnet_new --force` over a non-empty Blazor host or sibling test project. Inspect and repair the existing scaffold in place; forced re-scaffolding can erase the implemented `Components/Pages` route and restore stock template content.

After `workspace_dotnet_new -n <Name>` under an output root, the canonical host is usually `<output-root>/<Name>/<Name>.csproj`. Do not create `<output-root>/<Name>.csproj`, `<output-root>/Program.cs`, or root `Pages/*.razor` files beside that nested host. Target the real scaffolded project path, and create tests only as a sibling `<Name>.Tests` project from the output root.

Do not recover from scaffold conflicts by recursively deleting the host project, sibling test project, target root, or any directory that contains a `.csproj`, `.fsproj`, `.vbproj`, `.sln`, or `.slnx`. Repair project directories in place. Do not delete scaffold core files one by one to make re-scaffolding succeed; preserve and edit `.csproj`, `Program.cs`, `Components/App.razor`, `Components/Routes.razor`, `_Imports.razor`, `Components/Pages/Home.razor`, layout files, `appsettings*.json`, and `wwwroot/app.css`. The only exception is a misplaced nested `*.Tests` folder directly inside a host project directory.

If a previous attempt only scaffolded a stock app or wrote markdown artifacts, the next recovery attempt must mutate concrete source or project files before writing any artifacts or rerunning validation. For Blazor work, edit files such as `Components/Pages/Home.razor`, a domain or application service under the host project, the sibling test `.csproj`, and test source. Do not write implementation change-set or rollout artifacts until after the source changes build and tests pass in the same attempt.

Clean scaffolded test projects before retries. Delete or replace stale compiled sources such as `UnitTest1.cs`, `<Project>.Tests.cs`, old `.bak` files that are still included, and duplicate test classes before rerunning `workspace_dotnet_test` against the sibling test `.csproj`. If the same test command fails repeatedly after rewriting one file, inspect the whole test project instead of repeating that same edit loop.

Put testable business logic in a public domain or application class and test that class from the sibling test project through a `ProjectReference`. Use a distinct concrete type such as `<Feature>Service` under `<RootNamespace>.Domain` or `<RootNamespace>.Application`; do not write tests that instantiate the root namespace, project namespace, or a Razor component as the domain service. If tests use host-domain types, edit the sibling test `.csproj` to add `<ProjectReference Include="..\HostProject\HostProject.csproj" />` before running tests.

If a test compile fails with `CS0118` or text such as `'<ProjectName>' is a namespace but is used like a type`, do not rerun the same tests or create a type whose simple name equals the root namespace. Create or inspect a distinct concrete type such as `<RootNamespace>.Domain.<Feature>Service`, add the sibling test project's `ProjectReference`, update tests to instantiate that type, and rerun both host build and test validation.

Avoid root namespace/type collisions in generated Blazor apps. Do not create a domain type whose simple name equals the project or root namespace; use a distinct type such as `<Feature>Service` under `<RootNamespace>.Domain` and import that concrete namespace in `_Imports.razor`. A `.razor` component file also generates a C# type, so do not create `Components/<ProjectName>.razor` in a matching project/root namespace; put the main route in `Components/Pages/Home.razor` or name the component `<Feature>Page.razor`.

For Blazor Web App scaffolds from `dotnet new blazor`, inspect the generated `.csproj`, `Program.cs`, and route files before writing UI. Do not create legacy root `Pages/*.razor` routes such as `Pages/Home.razor` or `Pages/Index.razor`. Put routed UI under `Components/Pages`, usually by replacing the stock `Components/Pages/Home.razor` content for `/` when it exists. If a stale root `Pages/*.razor` file duplicates or replaces `@page "/"`, delete or move that legacy route before validation; duplicate routes can pass compile and fail when the app starts. If inherited architecture notes say `Blazor Server`, `Blazor Server-Side`, or `Razor Pages` while the scaffold or project structure says Blazor SSR/Web App, treat those notes as stale shorthand and keep the current Blazor Web App hosting shape.

Do not convert a `dotnet new blazor` Blazor Web App into older Blazor Server/Razor Pages hosting. Do not add `Pages/_Host.cshtml`, `Startup.cs`, `UseStartup<Startup>()`, `blazor.server.js`, or ASP.NET Core 7.x `Microsoft.AspNetCore.Components*` package references to a net10 Blazor Web App. If a bad repair already added those files or packages, delete `Pages/_Host.cshtml`, `Startup.cs`, legacy root `Pages/*.cshtml`, and stale root `Pages/*.razor` routes, remove obsolete package references, restore the generated minimal `Program.cs`, keep `Components/App.razor` and `Components/Routes.razor`, and put the UI in `Components/Pages/Home.razor` before rebuilding.

For UI work, validate the rendered result with Playwright and screenshots, not only by reading Razor files. For code work, run at least a targeted build and the relevant tests before handoff. If a validation command fails, do not repeat the identical command or rewrite the same file with identical content before changing the cause; inspect diagnostics, repair the file layout or project reference, then validate. If `workspace_dotnet_test` is denied because the sibling test project is missing, create or repair that sibling test project before rerunning. `workspace_dotnet_test` must target a `.csproj`, `.sln`, or `.slnx`, not a `.cs` source file or plain test directory. If the delivery includes a launch script or runnable app URL, prove the app actually starts and renders after build and test instead of handing a startup failure to QA. Remove scaffold leftovers such as default placeholder classes or tests instead of leaving conflicting templates in place. Treat untouched default navigation, default Bootstrap-looking home pages, and placeholder template sections as unfinished work for a serious user-facing app. Keep the generated `MainLayout` type/file unless you update every reference to it in `Routes.razor`, `NotFound.razor`, and `_Imports.razor` in the same change. Keep the change minimal and explain any residual risk clearly in the produced artifact.

If the step contract names explicit code-edit, build, test, and artifact paths, do not stop after scaffolding or reading files. The delivery slice is still incomplete until those edits, validations, and durable artifact writes have all happened for the real scaffolded project.
