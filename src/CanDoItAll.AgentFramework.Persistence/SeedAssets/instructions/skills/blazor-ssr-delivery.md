Use this skill only for Blazor app delivery and component-driven ASP.NET Core UI work, as a Blazor specialization of the concrete deliverable delivery skill. It is generic across app domains and must not inject sample topics, old output paths, or prior-run acceptance criteria. Create, repair, run, and validate Blazor Web App, Blazor SSR, interactive server, and component-library UI deliverables with maintainable, strongly typed C# plus small JavaScript interop only when the requested interaction genuinely needs browser APIs. Start with the real scaffolded project files, especially the solution file, `.csproj`, `Program.cs`, routed page, and test project, before making substantial edits.

Use existing repository conventions and the shared component library where available. Preserve the scaffolded target framework, Blazor SSR hosting shape, and chosen test framework unless the approved scope explicitly changes them. Prefer predictable component state and explicit service boundaries over hidden lifecycle side effects. Do not introduce unnecessary JavaScript, fallback mechanisms that hide failures, or speculative abstractions.

If the project structure or attached step materials name a concrete output directory, treat that directory as the real application root. When it is outside the managed workspace, use the mapped alias `external-target/<drive>/...` with workspace file and dotnet tools; do not scaffold a parallel copy under `artifacts/...`, `output/...`, or another generated implementation folder. Before any scaffold call, check the mapped application root with `workspace_stat_path` or `workspace_list_files` unless the current step already proved the path is absent. If the named directory already is the app folder and is missing or empty, scaffold directly into it instead of adding an extra nested child directory unless the requirement explicitly asks for that extra layer. If it already contains a `.csproj`, `.sln`, or `.slnx`, inspect and repair it in place instead of calling `workspace_dotnet_new`. Treat durable artifact paths as proof destinations only, not as substitute Blazor project roots.

When the current run names an exact generated product root, the runnable host belongs directly in that root. Do not create a scenario-named sibling app, a sibling `*.Tests` project, or a copied/probe scaffold anywhere in the process-run output tree and then treat that as the deliverable. Tests belong under `<ProductRoot>/tests/<Host>.Tests`. Do not set `BaseOutputPath`, `BaseIntermediateOutputPath`, or `MSBuildProjectExtensionsPath` in the generated host as a path-shortening workaround; use the normal `bin`/`obj` layout and the current-run workspace aliases supplied by the tools.

Keep the current brief and project structure authoritative. Do not copy old prior-example domains, remembered external target paths, or prior-run acceptance criteria into a new Blazor task unless the current run explicitly asks for them. If policy denies an external-target path, treat that denied path as stale or ungrounded for this run; switch to the current grounded product root or current-run artifacts and do not retry that path. Never inspect, cite, copy, or infer implementation patterns from sibling external-target applications on the same host. The scaffold parent directory is only a `workspace_dotnet_new` argument; it is not a product root, source corpus, evidence source, or permission to read sibling folders. Framework patterns must come from loaded skills, official templates, BaseLib/component-library resources, tool descriptions, or current-run artifacts. Do not claim contextual examples, source files, templates, or implementation references were reviewed unless this execution actually inspected them through current-run tools or loaded them from attached skill/template resources.

For greenfield modern Blazor Web App work, scaffold with `workspace_dotnet_new` template `blazor`. Architecture notes that say "Blazor Server" for a new app are shorthand for the current Blazor Web App SSR/server-interactive model and never authorize the obsolete `blazorserver` template. If a template call returns an unsuccessful result, inspect that result, switch to the approved current template, and inspect the actual generated project before editing.

Workspace command timeout arguments are seconds, not milliseconds. Prefer the tool defaults for scaffold, build, test, and run operations unless diagnostics show a real timeout. For generated app tests, use `300` seconds or less unless a concrete diagnostic proves the tests need more time.

Avoid same-name collisions between Razor components and domain types. If the domain already defines a service, model, value object, or enum whose simple name matches the UI surface you plan to add, do not create components that rely on ambiguous type resolution. Rename the UI surface or qualify the domain type explicitly so the generated build stays deterministic.

Do not bind Blazor inputs or selects to `object`, `dynamic`, or other weakly typed holders that depend on runtime conversion. Keep form state concrete and explicit so route rendering fails at compile time instead of throwing `TypeConverter` or ambiguous binding errors at runtime.

Choose one test runner before the first test-project scaffold and keep that runner for the test project. Do not scaffold the same test-project root with a second template, and do not manually convert a generated test `.csproj` from one runner to another in the same attempt. When the scaffolded tests come from `dotnet new mstest` or the generated project references the `MSTest` meta-package, respect that API surface. Use the modern assertion methods exposed by the generated package, such as `Assert.Throws<T>`, `Assert.ThrowsExactly<T>`, or `Assert.HasCount(expected, collection)` when present, and avoid injecting legacy `[ExpectedException]`, `Assert.ThrowsException(...)`, or count assertions such as `Assert.AreEqual(expected, collection.Count)` when analyzer-approved alternatives exist. Preserve generated test package references and versions unless restore diagnostics explicitly prove they are unavailable. Do not replace generated MSTest references with xUnit/NUnit, or replace generated xUnit/NUnit references with MSTest, as a repair shortcut. Do not invent package versions such as a remembered latest; if restore reports `NU1102`, use the nearest version or generated template version shown by diagnostics, or return a blocker when no valid version is known.

Keep automated test projects inside the grounded product root but outside the Blazor host source folders, and make sure the host project does not include test sources, test output, or static web asset files. For a product root at `<ProductRoot>` with a host at `<ProductRoot>/<Host>.csproj` or `<ProductRoot>/src/<Host>/<Host>.csproj`, use a child such as `<ProductRoot>/tests/<Host>.Tests/<Host>.Tests.csproj`; do not create an ungrounded sibling beside `<ProductRoot>`. For `workspace_dotnet_new`, scaffold that test project with `parentDirectory: <ProductRoot>/tests` and `name: <Host>.Tests`; do not reuse the host scaffold parent directory with `name: <Host>.Tests`. If the Blazor host `.csproj` sits directly at `<ProductRoot>` and a child `tests/` or `Tests/` folder exists, inspect the host `.csproj` before final validation and add a durable exclusion unless one already exists. Prefer `<DefaultItemExcludes>$(DefaultItemExcludes);tests/**;Tests/**</DefaultItemExcludes>` in a host `PropertyGroup`; use equivalent explicit `Compile`, `Content`, `None`, and `EmbeddedResource` `Remove` entries only when that matches the project style. Do not write JSON/C#-escaped globs such as `Tests\\**\\*.cs` into `.csproj` XML. If a host build reports missing `Xunit`, `Fact`, `MSTest`, or test attribute namespaces, inspect for misplaced or included test files inside the host and move, delete, or exclude only those test sources; do not add test packages to the production host. If build or test output shows repeated nested paths such as `tests/<TestProject>/bin/.../tests/<TestProject>/bin/...`, `MSB3030` copy failures, missing `.staticwebassets.runtime.json`, missing `.staticwebassets.endpoints.json`, missing test `runtimeconfig.json`, or missing `project.assets.json` from nested test output, treat it as the Blazor host including child test output/static web assets. Repair the host layout or exclusions before rerunning. Do not recreate test files under the host after deleting them. After any test-layout or default-item repair, rerun the same host build that failed, the relevant test project, and `workspace_dotnet_run` for the runnable Blazor host.

Keep support code inside the same grounded product root. For small Blazor SSR apps, prefer `Domain/` or `Application/` folders in the host project. If the app needs a separate class library, create it below the product root, such as `<ProductRoot>/src/<Host>.Domain/<Host>.Domain.csproj`, by using a parent directory under `<ProductRoot>`. Do not scaffold `<ProductParent>/<Host>.Domain` as a sibling beside the product root unless that sibling path is explicitly grounded.

Do not rerun `workspace_dotnet_new --force` over a non-empty Blazor host or test project. Inspect and repair the existing scaffold in place; forced re-scaffolding can erase the implemented `Components/Pages` route and restore stock template content.

After `workspace_dotnet_new`, inspect the actual generated project path before assuming where the host lives. When the project structure provides an exact product root, scaffold by passing its parent as `parentDirectory` and its leaf folder as `name`, so the host is created inside that product root. Target the real scaffolded project path, and create tests under the grounded product root, typically in `tests/<Host>.Tests`.

Do not recover from scaffold conflicts by recursively deleting the host project, test project, target root, or any directory that contains a `.csproj`, `.fsproj`, `.vbproj`, `.sln`, or `.slnx`. Repair project directories in place. Do not delete scaffold core files one by one to make re-scaffolding succeed; preserve and edit `.csproj`, `Program.cs`, `Components/App.razor`, `Components/Routes.razor`, `_Imports.razor`, `Components/Pages/Home.razor`, layout files, `appsettings*.json`, and `wwwroot/app.css`. The only exception is a misplaced nested `*.Tests` folder directly inside a host project directory.

For Blazor WebAssembly, PWA, or static-hosted output, inspect `wwwroot/index.html` after final source mutations and ensure every local stylesheet, script, manifest, icon, service-worker, and generated-style reference resolves at the exact served path. If the app references `css/app.css`, `wwwroot/css/app.css` must exist; if only `wwwroot/app.css` exists, update the link to `app.css` or move the file. Do not reference Bootstrap or scaffold library files unless the generated product root contains them. Browser console 404s for local static assets are product defects that must be repaired before final evidence.

For Blazor WebAssembly and PWA apps, inspect `Program.cs` after adding application services and again after the final routed component edit. Register every component `@inject` service, every constructor-injected interface or concrete service, and every transitive interface dependency with the WebAssembly host, including interface-to-implementation mappings such as `AddScoped<ILocalStorageClient, BrowserLocalStorageClient>()` and concrete component dependencies such as `AddScoped<PantryPlannerStateStore>()`. If a local JavaScript file defines globals used by JS interop, load it before `_framework/blazor.webassembly.js` in `wwwroot/index.html`, or switch the service to explicit JS module import. A route that remains on the loading placeholder, renders blank, or exposes no enabled visible controls after startup is a Blazor runtime defect and must be repaired before final evidence.

If a previous attempt only scaffolded a stock app or wrote markdown artifacts, the next recovery attempt must mutate concrete source or project files before writing any artifacts or rerunning validation. For Blazor work, edit files such as `Components/Pages/Home.razor`, a domain or application service under the host project, the test project `.csproj`, and test source. Do not write implementation change-set or rollout artifacts until after the source changes build and tests pass in the same attempt.

Clean scaffolded test projects before retries. Delete or replace stale compiled sources such as `UnitTest1.cs`, `<Project>.Tests.cs`, old `.bak` files that are still included, and duplicate test classes before rerunning `workspace_dotnet_test` against the test `.csproj`. If the same test command fails repeatedly after rewriting one file, inspect the whole test project instead of repeating that same edit loop.

Put testable business logic in a public domain or application class and test that class from a test project under the grounded product root through a `ProjectReference`. Prefer a support library or application project when one exists; reference the web host only when that is the cleanest existing boundary. Use a distinct concrete type such as `<Feature>Service` under `<RootNamespace>.Domain` or `<RootNamespace>.Application`; do not write tests that instantiate the root namespace, project namespace, or a Razor component as the domain service.

If a test compile fails with `CS0118` or text such as `'<ProjectName>' is a namespace but is used like a type`, do not rerun the same tests or create a type whose simple name equals the root namespace. Create or inspect a distinct concrete type such as `<RootNamespace>.Domain.<Feature>Service`, add the test project's `ProjectReference`, update tests to instantiate that type, and rerun both host build and test validation.

Avoid root namespace/type collisions in generated Blazor apps. Do not create a domain type whose simple name equals the project or root namespace; use a distinct type such as `<Feature>Service` under `<RootNamespace>.Domain` and import that concrete namespace in `_Imports.razor`. A `.razor` component file also generates a C# type, so do not create `Components/<ProjectName>.razor` in a matching project/root namespace; put the main route in `Components/Pages/Home.razor` or name the component `<Feature>Page.razor`.

For Blazor Web App scaffolds from `dotnet new blazor`, inspect the generated `.csproj`, `Program.cs`, and route files before writing UI. Do not create legacy root `Pages/*.razor` routes such as `Pages/Home.razor` or `Pages/Index.razor`. Put routed UI under `Components/Pages`, usually by replacing the stock `Components/Pages/Home.razor` content for `/` when it exists. If a stale root `Pages/*.razor` file duplicates or replaces `@page "/"`, delete or move that legacy route before validation; duplicate routes can pass compile and fail when the app starts. If inherited architecture notes say `Blazor Server`, `Blazor Server-Side`, or `Razor Pages` while the scaffold or project structure says Blazor SSR/Web App, treat those notes as stale shorthand and keep the current Blazor Web App hosting shape.

Do not convert a `dotnet new blazor` Blazor Web App into older Blazor Server/Razor Pages hosting. Do not add `Pages/_Host.cshtml`, `Startup.cs`, `UseStartup<Startup>()`, `AddServerSideBlazor`, `MapBlazorHub`, `MapFallbackToPage("/_Host")`, `blazor.server.js`, or obsolete explicit `Microsoft.AspNetCore.Components*` package references to a current Blazor Web App. If a current Blazor Web App contains `Components/App.razor` and `Components/Routes.razor`, any added `_Host.cshtml`, `Startup.cs`, `AddServerSideBlazor`, `MapBlazorHub`, or `MapFallbackToPage` is a mixed-hosting defect that runtime proof must reject. If a bad repair already added those files or packages, delete `Pages/_Host.cshtml`, `Startup.cs`, legacy root `Pages/*.cshtml`, and stale root `Pages/*.razor` routes, remove obsolete package references, restore the generated minimal `Program.cs` with `AddRazorComponents().AddInteractiveServerComponents()` and `MapRazorComponents<App>().AddInteractiveServerRenderMode()`, keep `Components/App.razor` and `Components/Routes.razor`, and put the UI in `Components/Pages/Home.razor` before rebuilding.

For UI work, validate the rendered result with Playwright and screenshots, not only by reading Razor files. Capture screenshot, browser_snapshot or browser_evaluate state output, and browser_console_messages as current-run evidence when browser proof is in the same step; prefer durable `.png`, `.yml`, `.json`, `.txt`, `.md`, and `.log` artifact extensions. If browser tools return provider-native filenames before managed artifact import, cite those filenames; do not fail or escalate solely because workspace file tools cannot see the managed browser folder during the same attempt. For project-structure handoff, do not create a node per validation command or screenshot attempt; use the final accepted screenshot ImageAsset, one run-app proof node, one run-tests proof node, and one manager summary node, with detailed logs left under managed artifacts. For code work, run at least a targeted build and the relevant tests before handoff. Keep generated tests deterministic and bounded: do not write `while` loops that depend only on mutable app/game state without a maximum iteration guard and an assertion that each state transition succeeded. Build, test, and analyzer warnings in generated Blazor deliverables are validation defects, not ignorable risks; if a warning cites an editable product or test file, edit the cited file and rerun the same command until the warning is gone before finalizing. Use a `workspace_dotnet_test` timeout of 300 seconds or less for generated app tests unless a real diagnostic proves they need more time. If a validation command fails, do not repeat the identical command or rewrite the same file with identical content before changing the cause; inspect diagnostics, repair the file layout or project reference, then validate. When `workspace_dotnet_build`, `workspace_dotnet_test`, or `workspace_dotnet_run` fails, read the returned stdout/stderr previews and any referenced `stdout.txt`, `stderr.txt`, request, or receipt artifacts before changing project files or rerunning. Do not keep guessing by editing `.csproj`, framework, package, route, or test-runner settings from exit code alone. If `workspace_dotnet_test` is denied because the test project is missing, create or repair the test project under the grounded product root before rerunning. `workspace_dotnet_test` may target a `.csproj`, `.sln`, `.slnx`, or an unambiguous project directory; never target a `.cs` source file. If the delivery includes a launch script or runnable app URL, use `workspace_dotnet_run` after build and test to prove the app actually starts and renders instead of handing a startup failure to QA; leave `keepAlive` false unless this same step immediately needs browser tools, and call `workspace_dotnet_stop` with the recorded `startup.json` receipt before finalizing any kept-alive app. Remove scaffold leftovers such as default placeholder classes or tests instead of leaving conflicting templates in place. Treat untouched default navigation, default Bootstrap-looking home pages, and placeholder template sections as unfinished work for a serious user-facing app. Keep the generated `MainLayout` type/file unless you update every reference to it in `Routes.razor`, `NotFound.razor`, and `_Imports.razor` in the same change. Keep the change minimal and explain any residual risk clearly in the produced artifact.

Final product-validation order: finish the last Blazor product mutation first, then rerun every affected build/test/run/browser validation, then read a representative changed product file such as `Program.cs`, the host `.csproj`, `Components/Pages/Home.razor`, a domain/application service, or a test source, then write durable evidence artifacts, then submit the governed step outcome. Do not mutate product files after that final read. Managed evidence artifacts are proof records, not substitutes for the runnable host.

Before final evidence artifacts or a governed outcome, read at least one representative changed product file after the final product mutation. For Blazor work, prefer the changed routed component, `Program.cs`, `.csproj`, domain/application service, or test source that best proves the delivered behavior. If you change another product file after that read, repeat the read and rerun the required build, test, run, browser, or deliverable validation before concluding.

Never make validation pass by writing fake package, framework, runtime, browser, or test-tool shims. Do not define local `Microsoft.VisualStudio.TestTools.UnitTesting`, `Xunit`, `NUnit.Framework`, browser API, Blazor, or ASP.NET Core replacement namespaces/classes to satisfy compilation. Fix the real package restore, project references, test framework choice, target framework, or environment issue; if that is impossible with available tools, return a concrete blocker instead of hiding it.

If the step contract names explicit code-edit, build, test, and artifact paths, do not stop after scaffolding or reading files. The delivery slice is still incomplete until those edits, validations, and durable artifact writes have all happened for the real scaffolded project.
