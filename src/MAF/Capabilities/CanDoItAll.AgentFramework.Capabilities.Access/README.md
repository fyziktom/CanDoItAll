# CanDoItAll.AgentFramework.Capabilities.Access

Evaluates typed capability access policies against assigned and available capabilities.

The evaluator may suppress or require capabilities but cannot grant a capability that the
owning application boundary did not assign.

```powershell
dotnet build .\src\MAF\Capabilities\CanDoItAll.AgentFramework.Capabilities.Access\CanDoItAll.AgentFramework.Capabilities.Access.csproj
```
