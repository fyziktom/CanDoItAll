# CanDoItAll.AgentFramework.Capabilities.Abstractions

Defines capability identifiers, kinds, models, naming rules, text normalization, and
runtime-tool capability tags.

These contracts are provider-neutral and contain no catalog persistence, UI, or runtime
attachment behavior.

```powershell
dotnet build .\src\MAF\Capabilities\CanDoItAll.AgentFramework.Capabilities.Abstractions\CanDoItAll.AgentFramework.Capabilities.Abstractions.csproj
```
