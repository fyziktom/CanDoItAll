# CanDoItAll.AgentFramework.WorkflowExecutors.Abstractions

Defines workflow executor invocation, contribution, result, and observability contracts.

The abstractions remain independent of built-in executors, plugins, dependency injection,
and transport-specific behavior.

```powershell
dotnet build .\src\MAF\WorkflowExecutors\CanDoItAll.AgentFramework.WorkflowExecutors.Abstractions\CanDoItAll.AgentFramework.WorkflowExecutors.Abstractions.csproj
```
