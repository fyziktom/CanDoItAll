# CanDoItAll.AgentFramework.WorkflowExecutors.Standard.Transforms

Implements deterministic JSON transformation and Markdown rendering workflow executors.

Transforms validate input and output contracts and do not perform network or persistence
operations.

```powershell
dotnet build .\src\MAF\WorkflowExecutors\Standard\CanDoItAll.AgentFramework.WorkflowExecutors.Standard.Transforms\CanDoItAll.AgentFramework.WorkflowExecutors.Standard.Transforms.csproj
```
