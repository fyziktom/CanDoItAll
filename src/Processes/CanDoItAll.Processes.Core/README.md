# CanDoItAll.Processes.Core

Owns the provider-neutral process graph, state transitions, branches, artifacts, runtime
events, loop detection, validation, and tool-operation policies.

Core contains deterministic domain behavior and has no Blazor, EF Core, provider, or
external execution dependency.

```powershell
dotnet build .\src\Processes\CanDoItAll.Processes.Core\CanDoItAll.Processes.Core.csproj
```
