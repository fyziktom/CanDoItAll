# CanDoItAll.Processes.Drivers.SoftwareDeliveryEvidence

Pure software-delivery evidence and guidance policy package.

The package accepts caller-supplied snapshots and returns diagnostics, proof-policy results, and guidance fragments. It must not read files, call services, use dependency injection, execute processes, mutate process state, or act as a runtime host.
