# Implementation Prompt

Implement only the currently assigned phase from `project-structure-dependency-execution-bundle`.

Read the phase README first, then the referenced repo files, then update `reviews/01-execution-report.md` with commands, gates, and browser evidence as you go.

Keep dependency direction consistent with existing `ProjectObjectLinkKind.DependsOn`.
Do not skip fresh-SQLite validation when a proof step requires browser or database interaction.
Do not mark a UI phase complete without Playwright MCP interaction, screenshot capture, and written screenshot findings.
