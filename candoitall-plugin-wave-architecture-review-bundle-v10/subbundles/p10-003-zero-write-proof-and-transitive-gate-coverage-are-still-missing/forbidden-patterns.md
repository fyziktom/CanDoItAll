# Forbidden patterns

- a gate that only searches for the old `NormalizeAndHydrateAsync(...)` symbols
- tests that assert only the surface DTOs and not persisted state
- closure claims without exact required test names/results
- static proof without runtime build/test evidence
