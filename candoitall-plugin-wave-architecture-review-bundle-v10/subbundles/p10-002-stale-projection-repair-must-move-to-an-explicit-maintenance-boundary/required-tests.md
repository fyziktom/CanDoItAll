# Required tests

- `Explicit_projection_repair_removes_stale_system_managed_rows_and_orphan_layouts_idempotently`
- one normal-read test proving the stale rows still remain until the explicit repair seam is invoked
