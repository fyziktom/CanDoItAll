# Forbidden patterns

- proof that uses only built-in manifests
- adding a new page-level `switch(field.Key)` or equivalent branch for the test plugin
- adding plugin-specific editor-model properties just to make the unknown-plugin tests pass
