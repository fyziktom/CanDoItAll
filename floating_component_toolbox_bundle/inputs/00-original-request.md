# Original Request

## Raw Note R1

> we need to use floating window with kind of toolbox with components in different types of canvases. for example in project structure we have specific components, then in process canvas we have different components, in prompt factory we have differrent components and here in webgl we can have also different. we can display processes as we do now in 3D or project structure in 3D. Then we need to have kind of generic principle how to do toolbox with components floating window. analyze how we are doing it now and if it is possible to do it generic. We must preserve all things working as now. You must not break anything around project and process structures canvases.

## Raw Note R2

> if it is possible to do it generic way, than add it into proper library and implement it on all places where it must be. You must do real validations of that it is working with playwright mcp and screenshots. for example if you use project structure canvas components toolbox to add some block you must validate it is really added in canvas. Same with webgl. you must add new role with use of component toolbox and validate that it appear in 3D.
