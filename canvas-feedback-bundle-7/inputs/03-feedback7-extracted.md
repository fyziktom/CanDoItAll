# Extracted Feedback

Source: `C:\Users\lucys\OneDrive - TechnicInsider\Produkty\CanDoItAll\feedbacks\feedback7.docx`

## Notes

- `N001` Nodes that has some path inside should not display whole path like this. It should display one button with Text path and copy icon. If I mouseover on that button tooltip shows whole path. If I click it copy path to clipboard and show check icon on button for two seconds.
- `N002` Node that has some path inside that has also some file at end of that path (like on picture PVEInvoicing.csproj) must display name of that file on itself. 1) and this step will solve trouble that some paths are too long and we do not need to have node same large to cover that text. Lets always try to search ways for space optimization.
- `N003` Doubleclick on item that cannot have preview must open incanvas modal (centered on visible canvas area) that will offer in form of squared buttons with centered icon and text under icon as first button “Edit” and second most probable action. For example for script node it would be “Run PowerShell”. For prompts related it would be “Open Wizard in New Tab”, etc. You must identify it for every type of node.
- `N004` Use “settings” icon for cfg button
- `N005` Configuration modal has top hidden behind the top toolbar. It must be drawen little lower on y axis so it does not cross with top toolbar at all.

## Extracted Media

- `C:\repositories\CanDoItAll\output\feedback7-extract\media\image1.png`
- `C:\repositories\CanDoItAll\output\feedback7-extract\media\image2.png`
- `C:\repositories\CanDoItAll\output\feedback7-extract\media\image3.png`
