# Prospective re-check

## Expected result after remediation

Prospective pass if all type/subtype decisions flow through registry lookups and the UI becomes a consumer, not the semantic owner, of node kinds.

## QA focus

The registry must own semantics, not just labels and icons.

## Back-check question

Would the same skill lens still flag this concern after the remediation?

Expected answer: **No**, unless a cache/projection/UI layer still owns live truth by accident.
