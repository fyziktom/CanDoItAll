# Required tests

At minimum add or update tests that prove the invariant behind P8-005.

- one focused unit/architecture test for the ownership rule
- one integration test for the runtime behavior across persistence boundaries
- if UI/editor flow changes are involved, one component or Playwright proof

The exact test names may vary, but the invariant must be obvious from the test body and assertion set.
