# QA Prompt

Validate the contextual floating agent windows.

- Build affected projects and run focused tests.
- Use Playwright MCP in a large headed viewport.
- On project structure, open the Agents launcher, verify text search and tag filtering, double-click an agent, send a calculator-roadmap prompt, and capture launcher/chat screenshots.
- On process Steps canvas, open the Agents launcher, double-click an agent, send a review-role prompt, and capture launcher/chat screenshots.
- Open Agents page chat tab and confirm the contextual thread appears.
- Review screenshots for readability, clipping, lateral overflow, alignment, z-order, and whether floating windows remain usable next to existing canvas chrome.
