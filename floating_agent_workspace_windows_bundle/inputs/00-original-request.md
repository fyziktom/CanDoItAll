# Original Request

User asked:

> I need you to add new floating window that I can open on projects or processes pages. This window will contains agents that has allowed access some way to projects on project page or processes on processes page. They can have Read or Write or both.
> The floating window with those agents will be something like our toolbox. Or chat in linkedin. I attached example screenshot. But we must have search line and tageditor for search by tags.
> If I double click on some agent in that window it opens another floating window with chat with that agent. It is like new thread so anytime I can go to agents page chat tab and find that thread there. It must be same chat as we have there with all functions. Main purpose is that I am working on some project structure or processes structure and I need some help with analysis or improvements in the mindmap so I will open agent and ask for help.
> You must test that it is working with use of playwright mcp and screenshots. Test it in project structure to ask for adding some new nodes for example with simple roadmap for our calculator project and same in processes do some test to add another role for review, or something like that.

The prompt included a screenshot of a LinkedIn-style compact floating messaging panel and a separate chat window.
