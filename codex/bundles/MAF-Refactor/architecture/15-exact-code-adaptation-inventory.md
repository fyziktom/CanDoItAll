# Exact code adaptation inventory

## Reading rule

This is a baseline-oriented implementation map, not permission to edit every listed file at once. Claude Code must refresh symbol/reference evidence on the current branch and assign each change to the owning subbundle.

## Floating context and turn admission

| Current file / symbols | Current role | Required adaptation | Highest risk | Safe sequencing and proof |
|---|---|---|---|---|
| `Core/Context/AgentChatContextRegistry.cs`: `RegisterWorkspacePosition`, `ActivateScope`, `PublishModuleContext`, `CaptureAsync`, `CaptureCore` | One live active workspace/surface context, atomic fragments/attachments, navigation fence | Make its product role explicitly live UI observation; keep atomic publication and monotonic revision; add capture identity usable by turn service | treating published workspace scope/access as authority; race during navigation | preserve current tests first; add observation adapter/contracts; test old-or-new atomic capture, rapid navigation and disposal |
| `Components/AgentChatContextSurfaceProvider.razor`: publication reconciliation and navigation synchronization | Bridges module Razor state into the registry | Publish observation-only state and bounded attachments; avoid authority semantics in component contract | render loops, stale lease removing replacement, mismatched route | retain equality/revision behavior; component tests for replacement/disposal and no extra model invocation |
| `Workbench/Components/WorkspaceAgentChatContextProvider.razor`: active tab/route publication | Publishes current workbench position | Continue as attention/position source; include stable view identity without granting access | tab and route disagree during render/navigation | preserve navigation identity; test Canvas/Gantt query-tab transitions and rapid route changes |
| `AgentChatExecutionOrchestrator.cs`: `StartSendMessage`, `SendMessageCoreAsync`, `AdmitOperation` | Captures current context, builds invocation, starts workspace execution | Replace direct registry-to-invocation coupling with `capture observation -> classify transition -> resolve authority -> admit -> persist turn reference` | database profile changes between steps; affinity committed before admission; context/authority mismatch | introduce services beside current mapper; shadow only pure mapping; switch one whole send path; generation and failed-admission tests |
| `AgentChatContextInvocationFactory.cs`: `Create`, metadata V1, `CreateCompletionNotification` | Turns current snapshot into transient prompt/context and metadata | Split into observation composer, transition composer, turn-context factory and completion-notification mapper; V1 adapter remains read-compatible | universal context bag; source metadata becomes authority | add V2 contracts/readers first; exact digest fixtures; no payload/project-path authority tests |
| `AgentRunTransientContextRegistry.cs`: `Register`, `Resolve`, `Remove` | Holds opaque request-scoped context through approvals | Evolve into explicit execution-owned turn-context lease/cache with version/reference semantics; keep fail-closed unavailable behavior | restart or eviction strands approval; wrong live context substituted | preserve digest/reference equality; continuation must resolve original run reference only; restart/unavailable fixtures |
| `FloatingAgentChatModels.cs`, `AgentChatPositionModels.cs` | Mix active chat UI state, context publication, attachments and position models | Separate live observation, conversation affinity/epoch, immutable turn reference, and attachment contracts into focused files/projects | dependency cycles and overloaded records | extract smallest SDK-free records first; serialization and canonical-model review |

## Floating conversation/UI lifecycle

| Current file / symbols | Required adaptation | Risk and proof |
|---|---|---|
| `FloatingAgentChatCoordinator.cs`: active chat registry, run-state reconciliation, pruning | Add per-chat/session context-affinity state and next-turn transition status without making coordinator an execution-authority owner | multiple floating windows, hidden/reopened chats and stale event subscriptions; lifecycle and cross-chat isolation tests |
| `FloatingAgentChatHost.razor.cs`: registry/coordinator subscriptions and current context filtering | Display current surface/next-turn status per active chat; do not treat host snapshot as run context | component disposal and catalog filtering can mix active surface with older thread; multi-window component tests |
| `AgentChatPanel.razor.cs`: send/approval operations | Send uses new turn-capture command; approvals send stable proposal decisions and show original-run context versus next-turn context | bool approve-all behavior and UI selection changes during async operations; component plus operation-generation tests |
| `FloatingAgentChatContracts.cs`, `AgentFrameworkWorkspaceService.ExecutionFacade.cs`, `CurrentProfileAgentFrameworkWorkspaceService.cs` | Migrate public/application contracts to turn commands and per-proposal continuation while preserving profile-generation admission | wrapper chain can hide broad runtime or current-context recapture; source/caller scans and profile-switch tests |

## Execution coordination, persistence and policy

| Current file / symbols | Current role | Required adaptation | Highest risk | Safe sequencing and proof |
|---|---|---|---|---|
| `Core/Contracts/Contracts.cs`: `IAgentRuntime`, `AgentRuntimeResponse` | Broad execution/diagnostics/admin/continuation contract and result | Move SDK-free execution, continuation, diagnostics, administration and hosted-agent contracts into focused abstractions; keep compatibility facade only | losing result evidence or creating `IAgentRuntimeV2` monolith | ports first; migrate caller families; direct fake-port tests; no-new-caller guard |
| `Execution/AgentFrameworkWorkspaceExecutionService.ExecutionRuns.cs`: `ExecuteRunEntryAsync`, `CompletePreparedExecutionRunAsync` | Admission, preparation, provider/runtime invocation, usage, finalizer/schema, persistence, cleanup | Keep application orchestration but call narrow execution port with immutable request and scope/turn/authority leases | changed atomic persistence, usage, cancellation, tool/finalizer traces or cleanup | differential fixtures; migrate after ports; fault persistence/provider/cancellation; preserve primary failure |
| same file: `ContinueExecutionRunEntryAsync`, `BeginPendingApprovalContinuationAsync` call chain | Looks up pending run, applies bool decision, resumes runtime and persists | Use per-proposal decision command, expected run revision/pending-set fingerprint, original turn/authority, versioned state envelope | approval applied to changed set; duplicate continuation; current UI retargeting | expand API; persisted concurrency tests; keep internal bounded bool adapter until SB18 |
| same file: `CreateRuntimeExecutionOptionsCore`, `BuildRuntimeExecutionOptions`, `CreateRuntimeContextIntent` | Reconstructs runtime options from run metadata/transient lease | Build request from typed turn/authority/scope records; generic runtime intent must not parse product/process strings | conflicting trusted scopes and process leakage | typed records/readers first; source assertions and organization/project tests |
| same file: `ShouldOverrideProviderForGovernedProcessStep`, `IsGovernedMachineCriticalRun`, process log helpers | Generic Core contains process source/type/provider policy | Move to Processes-owned admission/provider/criticality policies | wrong provider or output contract after extraction | characterize process cases; typed policy injected from Processes; no string branch before SB14 unlock |
| `ExecutionInvocationMetadata.cs` | Large JSON metadata utility with generic, context, Project Structure and process keys | Split generic envelope/projection codecs from context and Processes-owned metadata contributors/readers | old run reinterpretation; process details remain in Core | V1 readers/fixtures; new typed owners; remove process constants only after compatibility mapping |
| `ConversationModels.cs`: run/session/runtime fields and approval records | Durable run/session state carries unversioned adapter payload | Introduce explicit runtime-state envelope and typed turn/authority references; retain legacy reader fields where needed | serialization break or private envelope exposed publicly | additive schema, fixtures, public projection tests, remove writers before readers |
| `WorkspaceExecutionAuditContext.cs` | Ambient audit/policy projection used during tools | Keep as telemetry/compatibility projection, not source of authority; populate from immutable execution request | AsyncLocal loss/inheritance changes authorization | explicit policy object at tool boundary; ambient mismatch negative tests |
| `AgentFrameworkWorkspaceExecutionService.ProcessLeases.cs` and recovery service | Owns kept-alive execution process cleanup/recovery | Preserve execution-owned lease identity and cleanup timing when service bundles/runtime ports change | leaked process or cleanup before terminal persistence | fault/restart tests; keep primary outcome independent of cleanup receipts |

## Scope-bound services and capability/tool composition

| Current file / symbols | Required adaptation | Highest risk | Safe proof |
|---|---|---|---|
| `Modules.AgentFramework/Workspace/AgentFrameworkWorkspaceFactory.cs`: direct `new MafAgentRuntime(...)` and manual graph | Replace mixed manual/root-DI graph with typed workspace/execution scope factory and owned lease | duplicate graphs, decorator/lifetime/disposal drift | create full new graph first; composition smoke; one whole-run selector; direct-construction scan |
| `Hosting/AgentFrameworkServiceCollectionExtensions.cs`, module service collection extensions | Register narrow ports, provider-backed LLM adapter, scope factory and outer adapters | registration order and fallback masking | fail-fast registrations; no `BuildServiceProvider`; production composition tests |
| `MafRuntimeDependencyResolver.cs` | Service locator/fallback for provider/workspace dependencies | Delete or reduce to composition-only typed factory; runtime behavior receives exact dependencies | services from wrong scope/root container | scope identity through tool invocation; missing registration fails at startup |
| `RuntimeCapabilityComposer.cs`: constructor, `CreateDefault`, `CreateCapabilityStateCoreAsync`, `CreateCapabilityComposition` | Builds tools/context/skills/MCP/memory/plugins and resolves services | Receive scope-bound bundle and typed contributors; eliminate service location and mixed stored/effective scope | tool availability/approval/order changes; cross-project services | golden descriptor/approval manifest; contributor direct tests; global collision failure |
| `RuntimeToolProviderComposer` / access filter | Converts module providers to MAF tools, metadata and approval wrappers | Keep policy/collision validation; eventually map SDK-neutral descriptors at adapter boundary | metadata/invoker mismatch; first-wins collision | duplicate/collision negative tests; exact tool manifest |
| `IAgentRuntimeToolProvider` and module implementations | Returns `AITool`, leaking SDK into business modules | Planned SDK-neutral descriptor/invoker migration or bounded transitional adapter | large secondary refactor and tool regressions | do only in owned scope; provider tests; composition smoke; no runtime-class edits for new provider |
| workspace file/command/artifact/image/spreadsheet/storage plugins | Access real scope-bound resources | Construct from one `WorkspaceExecutionScope` service bundle | mixed scope and external-target authority | identity assertion on every plugin/tool; Project X/Y concurrent tests |
| `McpCapabilityBuilder.cs` | Builds hosted/local MCP, secrets, browser path bounding | Depend on security abstraction and scope-bound services; keep provider/tool behavior | secret consumer authorization, MCP disposal and browser artifact paths | secret/MCP/browser tests; no `Modules.Security` reference; hosted/local fault tests |

## MAF adapter and provider runtime

| Current file / symbols | Required adaptation | Highest risk | Safe proof |
|---|---|---|---|
| `MafAgentRuntime.cs` | Broad facade and entry methods | Delegation-only compatibility facade, then delete in SB18 | hidden behavior/callers survive | caller scan, source assertions, facade direct delegation tests |
| `MafRuntimeAgentFactory.cs` / runtime build result | Agent creation, policy, capabilities, finalizer/trace/resource ownership | Implement execution adapter build through constructor-injected collaborators | disposal order, tool policy, provider compatibility | existing architecture/lifetime tests plus injected faults |
| `MafProviderUpdatePump`, streaming invocation handlers | Provider stream enumeration, repair/retry/update mapping | Remain MAF/provider adapter behavior behind execution port | lost update order, blank retry replay, usage double count | ordered update and actionable-content retry tests |
| `MafRuntimeSessionBuilder.cs`, `MafRuntimeSessionPersistenceDriver.cs` | Restore/create/serialize MAF session | Map only versioned MAF envelope payload; explicit legacy compatibility | silent transcript replay/reset; timeout loses state | persisted fixture matrix, provider/model/history mismatch tests |
| `MafApprovalContinuationDriver.cs` | Maps pending MAF requests and replays one bool | Accept exact adapter-level decisions produced by application command; bounded legacy adapter | all calls approved/rejected together or wrong call ID | mixed decisions, pending hash, duplicate resume tests |
| `MafFinalizerDriver` and output capture/repair | MAF-specific finalizer mechanics | Keep generic adapter mechanics; process completion authority remains outside | moving generic validation too far or losing trace sequence | finalizer required/shadow/repair/sequence tests |
| `Runtime/Providers/MafProviderRuntimeGateway.cs` | Combines provider diagnostics, test chat, image chat and model administration above runtime pool/drivers | Split diagnostics/admin from reusable provider-backed lightweight invocation; avoid another broad gateway | duplicated provider lifecycle/credential/usage/retry | direct runtime pool/driver tests and focused port contracts |
| `AgentFramework.Providers` contracts/drivers (`IProviderChatCompletionDriver`) | SDK-neutral provider chat completion | Reuse as lightweight inference execution foundation; add only missing ordered-message/streaming/settings semantics in correct contract | breaking all drivers or parallel protocol | extend additively; OpenAI/Azure/Ollama fake/protocol tests |
| `MafRuntimeServiceCollectionExtensions.cs` and provider registration | Registers collaborators, drivers, pool, gateway | Register narrow MAF adapters and provider-backed LLM implementation through outer composition | old facade remains default; duplicate singleton/pools | DI graph assertion and handle/disposal tests |

## Process ownership and workflow/lightweight inference

| Current file / symbols | Required adaptation | Highest risk | Safe proof |
|---|---|---|---|
| `Maf/Runtime/Execution/ProcessArtifactRecoveryService.cs` | Parses exact managed artifact and synthesizes process outcome | Delete from MAF; Processes-owned policy consumes generic execution evidence/failure | stale artifact accepted or ordinary gates bypassed | exact current write trace, status, blocker and gate integration tests |
| `Modules.Processes/Services/RuntimeIntegration/*` and completion coordinator/gates | Own process adaptation, receipts, artifacts and completion | Add provider/criticality/recovery policies and keep one completion path | recovery/materialization/submission occurs twice | pure policy shadow only; exact-once receipt/gate assertions |
| `MafWorkflowLlmComponentInvoker.cs` | Builds temporary agent/session and invokes broad runtime | Build `LlmInvocationRequest`, call lightweight port, retain workflow-owned schema/usage mapping | payload grants scope; usage/failure parity lost | no-agent source guard and workflow parity fixtures |
| Provider-backed lightweight LLM implementation (new) | Stateless provider inference | Compose over provider runtime pool/drivers, not MAF agent runtime | credentials/retries/usage duplicated; hidden tools/context | fake driver/lifecycle/performance tests; no agent types |
| Future `ILlmConversationService` contract/application owner | Ordinary transcript and per-turn inference | Design only in bundle; later persist transcript above stateless port | ordinary chat becomes disabled agent | dependency/source contract test; no UI/product context now |

## Registration, mocks, diagnostics and tests that are easy to miss

- `ProcessMockAgentRuntime.cs` and `ScenarioHarnessAgentRuntime.cs` must become focused decorators/adapters, not implementations of every new port.
- `SchedulerPlannerServiceCollectionExtensions.cs` and any direct runtime tool providers may hold broad-runtime assumptions.
- `CanDoItAll.OpenAiContextProbe/Program.cs` is a diagnostic caller and must move to the proper diagnostics/lightweight port.
- API integration test hosts can manually construct runtime graphs that diverge from production.
- `MafAgentRuntimeProviderHealthTests`, `MafRuntimeArchitectureServicesTests`, `FloatingAgentChatArchitectureTests`, workflow usage/isolation tests, process metadata/recovery tests and provider runtime lifecycle tests are key characterization assets.
- Source-based tests that assert old type names must be updated only after equivalent stronger architecture assertions exist.
