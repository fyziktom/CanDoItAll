# SB07 Semantic Invariants

- Performance improvement must not be claimed without measurement.
- A passing boundary scan must target the old runtime partials, not merely prove that helper names still exist elsewhere.
- Full-suite failures must be reported with counts and categories instead of hidden behind focused green tests.
