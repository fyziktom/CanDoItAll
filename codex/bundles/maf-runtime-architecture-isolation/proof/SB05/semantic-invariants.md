# SB05 Semantic Invariants

- Workspace scope and process command policy must not be weakened by dependency fallback extraction.
- MCP local command execution must still use the same workspace command service semantics.
- Remaining feature-driver partials must not be claimed as fully isolated.
