# QA Prompt

Reject any green UI/read-model state when finalizer diagnostics would reject the artifact. Reject MAF feature claims without production code and runtime proof. Reject full live testing unless SB18 explicitly says GO.
