# Implementation Prompt

Execute only the currently dependency-ready subbundle. Preserve literal process-context text, keys, limits, hierarchy semantics, and every unrelated page capability. Add no partial, nested boundary, interface-only wrapper, project reference, or mutable page-state abstraction. Capture direct positive/negative proof and update durable bundle status before continuing.
