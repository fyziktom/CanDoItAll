# QA Prompt

Review the changed owners, direct tests, source assertions, project graph, and before/after metrics. Reject compile-only proof, wrappers that leave duplicate behavior behind, page-dependent unit tests, new partials, and any unproven change to process context/output-root or hierarchy eligibility behavior.
