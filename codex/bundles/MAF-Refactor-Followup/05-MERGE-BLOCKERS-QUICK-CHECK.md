# Merge blockers quick check

The branch remains blocked while any answer below is **No**.

| Check | Required answer |
|---|---|
| Does one admitted canonical authority snapshot control workspace scope, capability filtering, and invocation policy? | Yes |
| Can any UI access hint or unknown source select project scope or mutation authority? | No |
| Do recovery, script inspection, tools, and receipts use the exact same run workspace bundle? | Yes |
| Is there exactly one process host and one owner/disposal path per profile workspace? | Yes |
| Does envelope restore unwrap the native payload before inspecting provider conversation state? | Yes |
| Are authority, model context, capability policy, and tool contract fingerprints separate and versioned? | Yes |
| Is approval independently decidable per proposal in the UI and decision-list API? | Yes |
| Is every approval/context cache bounded and reconciled against durable run state? | Yes |
| Is MAF tool policy injected and provider-neutral, with process restrictions supplied by Processes? | Yes |
| Does lightweight LLM use bounded immutable contracts, sanitized failures, deadlines, and safe empty retry? | Yes |
| Do Release build and all test projects pass without a newly expanded failure allow-list? | Yes |
| Are GitHub CI or equivalent durable validation transcripts attached to the final commit? | Yes |
