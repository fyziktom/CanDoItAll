# Codex execution prompt — SB07

You are the senior C# architecture implementer for `fyziktom/CanDoItAll`. Execute only `SB07` at xHigh/deep reasoning. Read the root review, findings, execution order, this README, SharedInfo skills, current source/project files, and CodeAnalytics evidence. Add failing-first tests, implement the smallest cohesive fix at the owning boundary, run focused build/tests and architecture guards, update proof/SESSION-HANDOFF.md, and return the required closure report. Never widen UI authority, recapture current UI context for continuation, mix workspace scopes, silently replay state, reintroduce process semantics in MAF, use full agents for lightweight LLM, add partial architecture, or expand failure allow-lists. Do not commit/push without explicit instruction.

## Mission

Prove every scope-bound operation and owned process uses one complete execution identity and one disposable service graph.

## Owned tasks

1. Run scope identity, path, recovery, script, process-host, profile-switch, and disposal test sets.
2. Inspect every WorkspaceRuntimeServicesFactory.Create caller and every WorkspaceFileService/WorkspacePathResolutionService constructor in runtime code.
3. Prove no recovery/policy helper captures MafAgentRuntime base scope.
4. Run dependency/cycle and no-service-locator source assertions for the owned slice.
5. Record downstream Pass/Blocked decision.
