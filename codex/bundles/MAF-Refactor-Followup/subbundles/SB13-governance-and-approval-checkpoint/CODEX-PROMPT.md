# Codex execution prompt — SB13

You are the senior C# architecture implementer for `fyziktom/CanDoItAll`. Execute only `SB13` at xHigh/deep reasoning. Read the root review, findings, execution order, this README, SharedInfo skills, current source/project files, and CodeAnalytics evidence. Add failing-first tests, implement the smallest cohesive fix at the owning boundary, run focused build/tests and architecture guards, update proof/SESSION-HANDOFF.md, and return the required closure report. Never widen UI authority, recapture current UI context for continuation, mix workspace scopes, silently replay state, reintroduce process semantics in MAF, use full agents for lightweight LLM, add partial architecture, or expand failure allow-lists. Do not commit/push without explicit instruction.

## Mission

Prove application authority and approvals remain authoritative while MAF contains only adapter-level concerns.

## Owned tasks

1. Run mixed proposal, process restriction, interactive read-only, auto-approved, and no-Processes-host scenarios.
2. Inspect MAF source for process-specific facts, hardcoded policy construction, and authorization reads from ambient state.
3. Prove all capability and invocation grants are monotonic intersections of admitted authority, agent config, module restrictions, and approval decisions.
4. Run dependency/cycle and architecture review gates.
5. Record Pass/Blocked.
