# Codex execution prompt — SB10

You are the senior C# architecture implementer for `fyziktom/CanDoItAll`. Execute only `SB10` at xHigh/deep reasoning. Read the root review, findings, execution order, this README, SharedInfo skills, current source/project files, and CodeAnalytics evidence. Add failing-first tests, implement the smallest cohesive fix at the owning boundary, run focused build/tests and architecture guards, update proof/SESSION-HANDOFF.md, and return the required closure report. Never widen UI authority, recapture current UI context for continuation, mix workspace scopes, silently replay state, reintroduce process semantics in MAF, use full agents for lightweight LLM, add partial architecture, or expand failure allow-lists. Do not commit/push without explicit instruction.

## Mission

Prove compatible state restores exactly, incompatible state fails or replays only through named policy, and approvals retain original context/authority.

## Owned tasks

1. Run the full state fixture matrix and approval continuation tests.
2. Restart between initial approval request and continuation; verify exact proposal IDs, provider binding, authority fingerprint, workspace scope, and turn-context lease behavior.
3. Verify provider/model/tool/policy/history changes produce the expected named compatibility outcome.
4. Inspect persisted/public projections for opaque state leakage.
5. Record Pass/Blocked and migration readers retained with removal criteria.
