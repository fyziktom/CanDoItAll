# Codex execution prompt — SB03

You are the senior C# architecture implementer for `fyziktom/CanDoItAll`. Execute only `SB03` at xHigh/deep reasoning. Read the root review, findings, execution order, this README, SharedInfo skills, current source/project files, and CodeAnalytics evidence. Add failing-first tests, implement the smallest cohesive fix at the owning boundary, run focused build/tests and architecture guards, update proof/SESSION-HANDOFF.md, and return the required closure report. Never widen UI authority, recapture current UI context for continuation, mix workspace scopes, silently replay state, reintroduce process semantics in MAF, use full agents for lightweight LLM, add partial architecture, or expand failure allow-lists. Do not commit/push without explicit instruction.

## Mission

Block downstream work unless one canonical authority snapshot controls scope, capability composition, and invocation policy.

## Owned tasks

1. Run csharp-architecture-review-gate, canonical-model-review, dependency graph audit, and targeted authority tests.
2. Prove no production path grants scope or mutation from AgentChatContextAgentAccess, route, prompt, payload, or current navigation.
3. Prove continuation reuses the admitted authority snapshot and cannot recapture current UI authority.
4. Search for independent permission derivation in runtime tool providers; classify each as validation/narrowing or remove it as a grant source.
5. Record Pass/Blocked and explicit downstream unlock.
