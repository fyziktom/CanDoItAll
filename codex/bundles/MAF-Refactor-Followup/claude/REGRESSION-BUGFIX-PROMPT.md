# Regression bugfix prompt

Reproduce the reported failure with fixed operation/run/session/authority/scope/provider identifiers. Classify its owner stage before editing. Add a failing regression test in the owning layer, apply the smallest cohesive fix, and run focused tests plus the current architecture checkpoint. Do not widen authority, recapture current UI context, bypass process completion gates, restore process semantics to MAF, or add a failure exclusion. Update the bugfix record and session handoff.
