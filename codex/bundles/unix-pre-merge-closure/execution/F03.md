# F03 execution report

Status: Completed

Proof tier: Governed

## Entry gate

Pass. F02 supplied governed total-cleanup proof. F03 owns MGR-001 and MGR-002 and remains inside the Manager registry/coordinator boundary.

## Raw note owned

Read schema-1 Manager records without crashing or inventing termination authority, durably rewrite them as current records, and reject malformed current ownership boundaries before recovery.

## Shipped behavior

- Schema 1 is deserialized through dedicated top-level legacy DTOs; schema 2 uses the current record contract.
- Current termination authority is an explicit polymorphic value: either a verified process boundary with a complete identity or unavailable authority.
- A legacy identity without `Boundary` maps to unavailable authority, `OwnershipUnverified`, and stable diagnostic `legacy-process-boundary-missing`.
- Schema-1 loads are atomically rewritten through `DurableFileWriter` as schema 2 and remain stable after reopening.
- Schema 2 validates Windows job and Unix process-group native/instance identity combinations before a record can reach discovery or termination.
- Coordinator and verifier branches require verified authority before reading a PID or invoking `TerminateOwnedProcessAsync`.

## Semantic proof

- Positive: a valid current Unix boundary round-trips exactly under schema 2; all 28 Manager ownership tests pass.
- Negative: a real schema-1 JSON fixture with the boundary removed migrates without exception, produces unavailable authority, returns no reclaim result, and leaves fake-host termination count at zero.
- Red-team negative: five malformed Windows/Unix boundary combinations are rejected on schema-2 load.
- Failing-first: the original current-model deserializer dereferenced the absent boundary and the test observed an exception instead of a safe migration.
- Anti-stub audit: no `TODO`, `NotImplementedException`, or newly thrown `NotSupportedException` exists in the changed production path.

## Validation

- Package-mode Release unit-project build: passed, zero warnings/errors.
- Full `ManagerProcessOwnershipTests`: passed `28/28`.
- Exact schema-1 migration test: passed `1/1`.
- Exact malformed-boundary test: passed `1/1`, covering five invalid combinations.
- Refreshed CodeAnalytics architecture review: passed with unchanged project direction and no new cycle.
- `git diff --check`: passed.

## Artifacts

- `proof/F03/manifest.md`
- `proof/F03/semantic-invariants.md`
- `artifacts/pre-merge/F03/unit-build.binlog`
- `artifacts/pre-merge/F03/manager-registry.trx`
- `artifacts/pre-merge/F03/legacy-migration.trx`
- `artifacts/pre-merge/F03/boundary-validation.trx`

## Progression decision

Pass. C1 is unlocked. Reopen F03 if later evidence reaches process discovery or termination without verified boundary authority.
