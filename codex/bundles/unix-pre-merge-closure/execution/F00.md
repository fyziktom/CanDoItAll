# F00 execution report

Status: Completed

Proof tier: Standard

## Entry gate

Pass. Root constraints, findings, requirements, invariants, validation strategy, and the complete F00 subbundle were read. The exact anchor and prepared-source-only delta are recorded in `status.md`. F00 owns characterization only; production behavior may not change until all three failures are captured.

## Baseline proof

- `git diff --check`: passed.
- Runtime portability runner self-test: passed `7/7`.
- Broad stable suite: not run, as required.

## Failing-first proof

- Unit test project package-mode Release build: passed with zero warnings and zero errors.
- Focused command selected exactly the three new fixtures with `--no-build --no-restore`.
- Result: failed `3/3`, passed `0`, skipped `0`, as required.
- F-001 failed because the current mapper threw `InvalidOperationException` for a post-boundary V1 payload instead of returning verified `LegacyV1`/`NeedsRecompile`.
- F-002 failed because process `26244` was still alive five seconds after the injected ownership attachment failure.
- F-003 failed because reclaim returned `Terminated`, proving the schema-1 record without `Boundary` still reached termination authorization.
- TRX: `artifacts/pre-merge/F00/failing-first.trx`
- TRX SHA-256: `66e2a52ee7b9d072a154803b907a085a3dd10bc727d74ccc291f26ab64e09f77`
- `git diff --check`: passed after fixture creation.

## Progression decision

Pass. F01 is unlocked. F02 and F03 remain dependency-ordered behind F01.
