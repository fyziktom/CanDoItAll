# F01 execution report

Status: Completed

Proof tier: Governed

## Entry gate

Pass. F00 supplied exact-anchor and failing-first proof. F01 owns F-001 and PMR-001 through PMR-004. No project reference, UI, or unrelated process behavior is in scope.

## Raw note owned

Replace wall-clock-based legacy process-plan classification with deterministic structured payload classification while preserving exact V1/V2 hashes and failing closed for partial, malformed, or conflicting shapes.

## Shipped behavior

- A dedicated structured classifier distinguishes marker-free V1 payloads, complete canonical V2 host-capability shapes, and ambiguous shapes.
- The mapper establishes the hash algorithm from explicit matching metadata or the deterministic shape; creation time is no longer an authority signal.
- Inferred V1 remains non-executable and is rewritten as `NeedsRecompile` with `HostCapabilitiesWereNotSealed`.
- Inferred complete V2 is executable only when its execution metadata agrees and its V2 hash verifies; missing algorithm metadata is repaired without changing payload or hash.
- Explicit version/shape conflicts and partial V2 payloads fail closed.
- Corrective migration `20260813012618_CorrectProcessPlanHashClassification` uses PostgreSQL JSONB structure checks and repairs databases that already applied the historical feature-branch migration.

The historical applied migration remains unchanged under the repository's migration policy. Its temporary timestamp/`LIKE` classification is superseded in the same migrate-to-latest chain and in already-upgraded databases by the corrective migration.

## Semantic proof

- Positive: a post-cut-off V1 fixture becomes verified `LegacyV1`/`NeedsRecompile`; a complete V2 fixture retains exact payload and hash while its missing version metadata is repaired.
- Negative: partial V2 shape and explicit LegacyV1/V2-shape conflict throw before execution.
- Migration: a real PostgreSQL database is upgraded to the historical hash-version migration, proves its V1 misclassification, applies the correction twice, restarts, preserves hashes/payloads, retains the partial row as `Unknown`, and rolls back to the pre-hash migration.
- Shallow-pass trap: merely removing the date check while treating any one host-capability property as V2 would authorize a partial payload. The missing `requiredRuntimeToolNames` case rejects that implementation.
- Anti-stub audit: no `TODO`, `NotImplementedException`, fixture-specific production branch, or template-only behavior exists in the changed production files.

## Validation

- Package-mode Release unit-project build: passed, zero warnings/errors.
- Focused runtime/hash tests: passed `7/7`.
- Focused PostgreSQL migration test: passed `1/1`.
- EF pending-model check: no changes since the latest migration.
- `git diff --check`: passed.

## Artifacts

- `proof/F01/manifest.md`
- `proof/F01/semantic-invariants.md`
- `artifacts/pre-merge/F01/unit-build.binlog`
- `artifacts/pre-merge/F01/runtime-classification.trx`
- `artifacts/pre-merge/F01/process-plan-migration.trx`

## Progression decision

Pass. F02 is unlocked. Reopen F01 if later proof changes V1/V2 hashes, payload bytes, or ambiguous-shape behavior.
