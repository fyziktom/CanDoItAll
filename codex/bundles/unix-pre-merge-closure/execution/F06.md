# F06 execution report

Status: Completed

Proof tier: Governed

## Exact candidate

- Branch: `unix-adoption`.
- Repository commit: `79730d6a4e41db6ed4c1da260a37fd0297c7d98c`.
- Target: `development` at `acc1ee4a5484dd98bd1df77f8e060a2a5a3b4c59`.
- Working-source fingerprint: `e1b83fb9d6a07e26fc94d4398f4b957fed7af56d41245fc637b41278988ffbff`.
- SDK: `10.0.303`.
- Dependency mode: package (`UseLocalCanDoItAllLibraries=false`).
- Runtime catalog: `2026-08-12.1`.

The implementation remains an intentional uncommitted review delta because no
commit or merge was authorized. The durable runtime stamp binds every retained
runtime result to the exact working source.

## Final validation

- Package-mode Release solution build passed with zero warnings and zero errors.
- Combined C1/F05 unit slice passed `211/211`.
- PostgreSQL process-plan correction migration passed `1/1`, including upgrade,
  restart, and idempotent correction; EF reports no pending model changes.
- MAF execution-run tracking integration slice passed `33/33`.
- Runtime portability Unit catalog passed `422/422`.
- Runtime portability Integration catalog passed `45/45`.
- Docker policy and Dockerfile static checks passed.
- A fresh developer image contains `setsid` from `util-linux 2.39.3`, runs as
  UID `1654`, and is retained as `candoitall-app:dev` for operator testing.
- The application and PostgreSQL services are healthy; `/health` returned HTTP
  `200` with `Healthy` at `http://127.0.0.1:8080/health`.
- Portability scan classified all `30,279` findings with zero unclassified
  executable-source findings. The reviewed baseline now contains `13,572`
  allowances.
- Canonical Unix-portability bundle validation passed for `341` files with zero
  errors and zero warnings after its provided checksum tool refreshed integrity
  metadata.
- Secret scan covered all `27` eligible text artifacts, excluded five identified
  binary binlogs, and found zero secrets.
- Static-tool self-tests passed `6/6` and `4/4`.
- `git diff --check` passed. The model snapshot status is line-ending metadata
  only; its content diff is empty.

## Scope decision

No broad stable-suite invalidation trigger fired: the public changes remain
inside the named persistence, process-host, Manager-registry, migration, and
container boundaries; central packages, shared runtime catalog selection, and
unrelated DI composition did not change.

## Progression decision

`MERGE READY FOR DEVELOPMENT — MACOS ACTUAL-HOST VALIDATION DEFERRED`
