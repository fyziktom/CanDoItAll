# F02 execution report

Status: Completed

Proof tier: Governed

## Entry gate

Pass. F01 supplied governed classification proof. F02 owns PLO-001 and PLO-002 and changes only local process-start ownership and its focused tests.

## Raw note owned

Once `Process.Start` succeeds, every later startup failure must synchronously abort partial ownership, terminate the started root and descendants, close ownership handles, and return no session or durable identity.

## Shipped behavior

- Startup keeps the process, prepared ownership boundary, and attached ownership in one failure domain until the session is complete.
- A failed attach can abort an observed Windows job or Unix process group before root-tree cleanup runs.
- Failed-start cleanup is non-cancellable and bounded, confirms established boundaries are empty, confirms the root exited, and preserves cleanup failures alongside the initiating exception.
- Ownership transfers to the session only after boundary attachment and executable-identity verification both succeed.

## Semantic proof

- Positive: all 13 local process-host tests pass after the change.
- Negative: the exact attach-failure test starts a fixture root with a child, injects failure after the child is observable, and proves both PIDs stop with no returned identity.
- Failing-first: the original path left the started root alive after five seconds.
- Shallow-pass trap: killing only the root cannot pass the strengthened root-and-child assertion.
- Anti-stub audit: no `TODO`, `NotImplementedException`, or newly thrown `NotSupportedException` exists in the changed production path.

## Validation

- Package-mode Release unit-project build: passed, zero warnings/errors.
- Exact attach-failure cleanup test: passed `1/1`.
- Full `LocalWorkspaceProcessHostTests`: passed `13/13`.
- `git diff --check`: passed.

## Artifacts

- `proof/F02/manifest.md`
- `proof/F02/semantic-invariants.md`
- `artifacts/pre-merge/F02/unit-build.binlog`
- `artifacts/pre-merge/F02/attachment-cleanup.trx`
- `artifacts/pre-merge/F02/process-ownership.trx`

## Progression decision

Pass. F03 is unlocked. Reopen F02 if later proof observes a residual root, child, ownership boundary, handle, or returned identity after failed start.
