# F04 execution report

Status: Completed

Proof tier: Behavioral

## Entry gate

Pass. C1 supplied a clean package-mode Release build and stamped runtime catalogs.

## Shipped behavior

- The ASP.NET runtime image explicitly installs `util-linux` alongside its existing Kerberos runtime dependency.
- Repository Docker policy now rejects an application runtime image that does not explicitly declare `util-linux` for `setsid` process-group bootstrap.
- No LinuxHeadless profile or desktop-capability source changed.

## Failing-first proof

After adding the validator but before changing the image, `Test-Docker.ps1 -SkipDockerCommand` failed with exactly one finding: the runtime image did not explicitly install `util-linux`.

## Validation

- Static and Compose Docker policy: passed.
- BuildKit Dockerfile check: passed with no warnings.
- Package-mode app image build: passed.
- Runtime user: UID `1654` (non-root).
- Dependency probe: `/usr/bin/setsid`; `setsid from util-linux 2.39.3`.
- Disposable PostgreSQL service: healthy.
- Disposable application service: healthy.
- Loopback request: `GET /health` returned HTTP `200` and `Healthy`.
- Built image identity: `sha256:e79bd27eb7b84d8d665ecd98da21007a018888da03bb08b42426dd687a400456`.
- Teardown removed both containers, both project-scoped volumes, both networks, the disposable image tag, and the generated secret file.

## Changed files

| File | Before SHA-256 | After SHA-256 |
|---|---|---|
| `repo://src/App/CanDoItAll.Web/Dockerfile` | `289f69c249568227bfd7daf3dbc016ada19214b75afa980de4d351d023a1e208` | `f18ac0016cd8ef4e106a4a1d34823ddad85780bd1f4c6e4f68ffa5272ab1c922` |
| `repo://tools/Validation/Test-Docker.ps1` | `882cd35b013b1eabb7d4dd9d44748eaade7df69bb70a57887e4b1e37bca8aa5d` | `c862224cf059b11651c170dd650448524bf222f31bf31b77602c4296647da15e` |

## Evidence

- `proof/F04/runtime-smoke.json`

## Progression decision

Pass. F05 is unlocked. Reopen F04 if a later final image loses `setsid`, runs as root, or fails application/database health.
