# Freeze and characterize the exact candidate

## Goal

Re-anchor the bundle to the actual branch HEAD and create failing-first proof for the three blockers.

## Entry

Read the root execution prompt, findings, requirements, invariants and
validation strategy. Reconfirm the exact repository anchor before editing.

## Tasks

1. Record branch HEAD, development HEAD, merge base, SDK and dependency mode.
2. Inspect any delta after the reviewed anchor before editing.
3. Add a post-cut-off LegacyV1 process-plan fixture that fails under current classification.
4. Add an injectable ownership-attachment failure fixture that proves a survivor under current startup cleanup.
5. Add a schema-1 Manager registry JSON fixture without Boundary and prove current handling is unsafe or non-typed.
6. Do not modify production behavior until the failing-first evidence is recorded.

## Rules

- Preserve unrelated changes.
- Use focused failing-first tests.
- Keep source comments in English.
- Do not push or merge.
- Do not weaken a validator to make evidence pass.
