# QA Prompt

Verify that every fix exercises production code. Reject proof where the artifact read-model says satisfied but finalizer validation would still fail. Reject MAF upgrade proof that only compiles but loses tool receipts, finalizer capture, approvals, handoff, workflows, or diagnostics.
