# Normalized Requirements

| ID | Requirement | Acceptance Evidence |
|---|---|---|
| R-01 | Make bundle proof portable across Windows, WSL, Linux, CI, and relocated repo roots. | Validator fixture proves Windows, POSIX, repo-relative, and relocated proof paths are handled or mapped safely. |
| R-02 | Add semantic invariant contracts to critical subbundle closure. | Each critical SB has an invariant matrix linking raw note, source files, failing-first test, passing test, production assertions, anti-stub audit, and red-team negative case. |
| R-03 | Ensure `CrossProjectWeekly` is truly cross-project while respecting policy boundaries. | Tests show cross-project clusters can form only when allowed and restricted content is not leaked. |
| R-04 | Add approximate semantic candidate discovery beyond exact shared keys. | Paraphrased memories with no exact topic/entity key can be paired within bounded candidate budgets. |
| R-05 | Store and use cluster-key coverage metadata. | Primary/shared keys require sufficient member coverage and expose coverage counts/ratios. |
| R-06 | Prevent unrelated claims from being grouped by mode plus primary key only. | Dream claim grouping includes semantic claim signatures/slots and rejects unrelated merged claims. |
| R-07 | Replace string-concat dream synthesis with structured claim synthesis. | Aggregate claims include conclusion, support roles, conditions, caveats, and source support maps. |
| R-08 | Improve entailment and contradiction validation beyond lexical overlap. | Tests cover negation, numeric, temporal, conditional, actor/action, optional/required, and scope reversals. |
| R-09 | Make professor capture work for natural teaching conversations. | Short corrections, Q&A teaching, examples/counterexamples, and explicit professor captures produce structured anchors. |
| R-10 | Separate professor anchor lifecycle from ordinary curator captures. | Non-professor curator captures are not marked as active professor anchors. |
| R-11 | Replace keyword mastery with durable mastery/use/integration events. | Assimilation requires event-backed mastery, accepted recall/use outcomes, and aggregate-ready dream or cluster integration. |
| R-12 | Make active/comparing/assimilated/faded anchor transitions closed and auditable. | Comparing anchors can return to active or rejected/assimilated based on validation outcome; all transitions have audit rows. |
| R-13 | Improve recall synthesis into task-facing briefs with line-level provenance. | Each statement has a statement type, caveat policy, omitted-detail warning, aggregate claim IDs, and original source map lineage. |
| R-14 | Refactor oversized services into maintainable orchestration, repository, strategy, formatter, and lifecycle components. | File-size and responsibility gates pass; tests cover collaborators directly. |
| R-15 | Move algorithm options away from static global access. | Options are DI/config driven and can be overridden per test/environment without changing global static state. |
| R-16 | Preserve deterministic testability. | Unit tests can run without external LLM, embeddings, or internet services. |
| R-17 | Do not implement economic memory governance in this bundle. | Scope guard transcript proves no economic-governance changes were made. |
