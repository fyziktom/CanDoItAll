# Original Request

User request:

> great. now please improve our tools around the filesystem.
> We already have our filedrivers that works fine. It just need more prepared commands like read list of files in folder or with subfolders, or copy folder, create folder, and other basic things agent might need to do with the files/folders.
> our file drivers help to check if the operation is in allowed area. thats very important.
> anyway I cannot find where those functions are defined as tools. I think it will be somewhere hidden in some class. I expected to have those shared/common tools defined in like in CanDoItAll.AgentFramework.Tools or some project like that. I understand that for example in case of the modules the registraion could be automatic and implementation of tools will be in module. but in case of basic tools we might have it more organized. analyze it and prepare bundle how to improve it and then implement it. Assure you are using our new Csharp related skills for correct design of the architecture.

Skills used:

- `candoitall-bundle-workflow`
- `csharp-architecture-governor`
- `csharp-provider-tool-plugin-isolation`
- `csharp-modular-refactoring`
- `csharp-factory-builder-composition`
- `csharp-testability-contracts`
- `candoitall-codeanalytics-mcp`
- `run-tests`
