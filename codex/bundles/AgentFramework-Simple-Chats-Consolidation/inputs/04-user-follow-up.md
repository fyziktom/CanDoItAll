# User follow-up — Simple Chat settings and avatars

The Simple Chat setup dialog is not properly divided into internal settings tabs. It must follow the established Agent settings-dialog composition more closely.

The Simple Chat definition editor must also use the existing Agent avatar-selection experience instead of a raw avatar URL field. The reusable experience includes bundled avatar selection, current-avatar preview, reset/default behavior, validated image upload, and AI avatar generation through the configured image provider.

After repairing the bundle for these requirements, execute the full bundle.
