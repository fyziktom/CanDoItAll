# Architecture checkpoints

## CP0 — after SB01

Required:

- exact execution SHA and clean ownership record;
- fresh CodeAnalytics dependency/cycle snapshot;
- full old-project caller and DI inventory;
- Core/Application/Runtime/Persistence/Components file ownership matrix;
- current schema/migration/transfer inventory;
- characterization selectors green;
- accepted usage identity, completeness, pricing, legacy, and route semantics;
- C# architecture gate Pass.

Unlocks SB02.

## CP1 — after SB05

Required:

- before/after ProjectReference graph;
- Core/Application direct tests without EF/Agent module;
- Runtime direct tests without Persistence/AppDbContext;
- Persistence PostgreSQL/profile/lease/transfer tests;
- no unintended table rename/drop or pending model delta;
- atomic new invocation pricing evidence and deterministic legacy migration;
- old Persistence runtime construction removed;
- no new partial/cycle/forbidden edge;
- governed manifests and C# architecture gate Pass.

Unlocks SB06 and confirms SB07 may proceed.

## CP2 — after SB06

Required:

- both source adapters independently tested;
- Both equals exact deduplicated union;
- retry/failure/cancel/legacy/unpriced/partial-source cases;
- Agent behavior and existing Agent projection compatibility green;
- usage behavior moved from the Agent execution partial into top-level owner;
- no UI/persistence implementation dependency in Usage;
- C# architecture gate Pass.

Unlocks integrated page/dashboard work.

## CP3 — after SB09

Required:

- Simple Chats immediately after Agents;
- inner tabs and provider editor function;
- Identity, Runtime, and Output and revision definition settings tabs function with validation and stable actions;
- Agent and Simple Chat editors consume one shared avatar selector and Simple Chat supports bundled/reset/upload/AI generation/save behavior;
- /chats redirect and no duplicate navigation/shell;
- all three usage scopes drive totals/charts/rankings/dialogs;
- named Playwright tests and Playwright MCP evidence at 1600x1000;
- Agent main/floating and Simple Chat main/floating parity;
- Agent avatar-selector parity after extraction;
- zero unhandled console/page errors;
- C# architecture and UI composition gate Pass.

Unlocks legacy removal.

## CP4 — after SB10

Required:

- zero production ProjectReference/namespace/DI caller for CanDoItAll.Modules.LlmChats*;
- old three projects absent from solution/filesystem;
- only redirect-compatible /chats surface remains;
- Web/API, App.Composition, AppDbContext/migrations, hosted dispatch, tests, and assembly scanning use target owners;
- no duplicate registrations;
- fresh CodeAnalytics no-new-cycle proof;
- C# architecture gate Pass.

Unlocks final closure.

## FINAL — after SB11

Required:

- frozen candidate SHA/diff hash;
- all focused selectors green;
- one authorized Stable run recorded;
- named Playwright selectors green;
- Playwright MCP Agent/Simple Chat main/floating/cost-scope proof;
- Playwright MCP settings-tab and shared-avatar/AI-generation proof in both editors;
- manifests, transcripts, screenshots, semantic invariants, checksums;
- final architecture gate and user-verification handoff.

No completion claim is permitted if either Agent or Simple Chat floating conversation parity is unproven.
