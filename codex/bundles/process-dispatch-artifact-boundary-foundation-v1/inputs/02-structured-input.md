# Structured Input

Canonical content lives in `inputs/03-structured-input.md`.

This alias exists because the shared bundle validator expects the standard initiative profile path `inputs/02-structured-input.md`.
