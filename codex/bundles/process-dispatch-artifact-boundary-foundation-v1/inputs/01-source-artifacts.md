# Source Artifacts

Canonical content lives in `inputs/02-source-artifacts.md`.

This alias exists because the shared bundle validator expects the standard initiative profile path `inputs/01-source-artifacts.md`.
