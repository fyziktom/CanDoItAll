# Additional API and effect outcome proofs
Frozen before execution: FullyQualifiedName~ProviderApiOutcomeIntegrationTests = 5 cases. Two Save/Delete HTTP post-commit receipts; two publication observer/owner-cancellation outcomes; one local persisted-health test. Uses a real registered PostgreSQL API host; only external diagnostics and secondary observers are controlled.

The provider program remains open until these and the broader focused provider selections, direct builds, portability, secret scanning and browser acceptance pass.

The first 5-case execution passed API receipts and both publication secondary-failure/cancellation cases (4), but exposed a real prior local-health persistence defect: UpdateProviderAsync converted the runtime update to an editor model and dropped HealthStatus/LastCheckedAtUtc. The fix carries diagnostic metadata inside the same canonical save, separate from the editor submission contract. Persisted health uses the existing public-health mapper's canonical success/failure status; transient diagnostic details remain in the returned diagnostic result. The bounded status column limit is shared with its EF configuration.

Updated selection frozen before the next execution: ProviderApiOutcomeIntegrationTests = 6 (positive and negative local-health cases now verify the actual published health projection, plus the previous four receipt cases).

Additional pre-effect audit: the draft-only pricing discovery adapter accepted a persisted imported ID with a forged local connector before resolving its secret. Freeze one failing-first test before the guard: FullyQualifiedName~Seam_source_managed_draft_discovery_rejects_before_secret_or_connector_effect (1). A poison resolver proves no connector can be reached; use the registered administration adapter and PostgreSQL imported fixture.

The discovery guard's baseline execution failed before connector invocation because the poison secret resolver was reached. The persisted profile connector now determines ownership before secret resolution or discovery, including forged caller connector keys. Rerun the frozen one-case selection after rebuilding.

Final owning selections: Unit 417; Components 84; Integration 256. The initial Unit expectation (412) incorrectly deduplicated five repeated public theory display names already present in historical discovery. All identities matched. Corrected to a multiset before execution; repeat discovery confirms 417. No test count is an architecture invariant. Filters and exact case inventories are in focused-plan.json.

The owning results are 417 Unit, 84 Components and 256 Integration, all passing after the documented seven obsolete-assertion repairs. Broader stable gate trigger: serialized editor concurrency revisions, scoped command composition and canonical/shared persistence outcome contracts cross owning project boundaries. Frozen expected stable discovery: Unit 6903, Components 1295, Integration 1346 and the two unchanged Memory projects 202 combined, total 9746. This derives from Providers-01 closure (6864/1278/1320), historical Memory inventory and the 82 added provider cases; discovery must reconcile before execution.

Final overlay audit adds the same operation-generation check in the four callers after awaiting the shared operation helper, so even caller-owned notification/dialog presentation is fenced. No test identities change. Refresh the affected component assembly before the stable checkpoint.

Stable discovery reconciled before execution: historical Providers-01 discovery is 6830 Unit /1278 Components /1315 Integration, not its execution-expanded 6864/1278/1320. The difference is 34 Unit and five Integration dynamic theory expansions. With 82 new cases and unchanged 202 Memory cases, expected discovery is 9707 (6869/1295/1341/202); current discovery matches. Expected execution expansion is 9746. The previous 9746 discovery expectation was invalid and no broad tests ran under it.

Stable follow-up: AgentEditorChildBoundaryTests.Shared_provider_refresh_retains_editor_draft (2 cases) preserves its context/field assertions; the older failure callback count (zero) is obsolete because the shared refresh button now reports unknown scope on exceptions as well as known committed availability changes. Expected safe message is updated, not raw fixture exception text.

Final secret-boundary audit freezes one failing-first case: FullyQualifiedName=CanDoItAll.Tests.Unit.AgentFramework.ProviderProfilesReadsTests.Secret_failure_is_explicit_partial_result_without_hiding_core_failure (1), now requires a nonempty partial-error message without raw fixture exception detail. Run before the safe-message fix after the ongoing stable assembly execution completes.

Final outcome audit freezes two failing-first cases before production repair: ProviderApiOutcomeIntegrationTests.Local_health_failure_before_persistence_is_retryable_and_does_not_change_revision and Api_local_health_diagnostic_failure_is_sanitized_and_does_not_persist (2). A throwing diagnostic runs before registry Update; canonical revision must remain unchanged. The command result must contain a safe failed diagnostic and no persistence outcome; API must return a stable sanitized 502. This is distinct from a completed negative health result, which still persists canonical unhealthy metadata. Full owning Integration filter will increase from 256 to 258 after this bounded follow-up; the frozen broad checkpoint remains historical at its original 9746 expected execution cases.

The one frozen broad run completed 9762 cases, including 218 Memory executions from 202 discoveries (sixteen dynamic theory expansions in the unchanged Memory project). The frozen discovery count 9707 matched; the expected execution count 9746 omitted those Memory expansions. This is a prediction correction, not undiscovered/missing tests or a rerun. Three failures require bounded follow-up: the obsolete shared-refresh callback assertion (2-case method), the unchanged history timing limit (1), and the repository secret-pattern scanner (1). The last found this run's ignored stable discovery inventory, whose public test display names expanded the security test's synthetic secret samples. Replaced 3 sample strings with fingerprints in that task-owned inventory; scanner/source exemptions remain unchanged. Original inventory SHA-256: b339407bda53dcb37f87b3015cb91f6244be96111cbfdae472dd5e8c05d2e3ca. No actual credential was involved.

## Final disposition
Status: complete for provider-owned obligations. The historical documentation gate blocker is explicit in bundle://reviews/closure.md.
Owned invariant contract: bundle://proof/SBC/semantic-invariants.md.
Portable commands and before/after failure output: bundle://proof/SBC/transcripts/.
Case receipts and all 31 topic bindings: bundle://proof/SBC/test-receipts.json and bundle://proof/SBC/topic-map.json.
Changed-source before/after hashes: bundle://proof/SBC/file-manifest.json.
Production assertions and final adversarial review: bundle://reviews/csharp-architecture-gate.md.
Anti-stub command/result: bundle://proof/SBC/transcripts/anti-stub.txt.
Downstream real API, shared workspace and browser proof: bundle://proof/SBC/browser/acceptance.md.
Final closure, exclusions, fixture corrections and unresolved repository-wide baseline debt: bundle://reviews/closure.md.
