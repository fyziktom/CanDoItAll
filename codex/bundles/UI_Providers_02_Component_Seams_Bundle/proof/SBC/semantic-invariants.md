# Closure invariants

P02-CLOSURE: every topic in topic-map.json must have passed direct behavior evidence; actual commit claims use registered PostgreSQL registry/shared services and real runtime materialization. API wrappers await their operations and preserve normal successful payloads with explicit committed-warning metadata. Public expected failures are sanitized. Browser fixture data belongs only to this run.

P02-STATIC: complete proposed source, including untracked production/tests, passes final portability enforcement without baseline writing. Intentional baseline changes are limited to the two relocated explicit ordinal-ignore-case checks. Secret scanning covers code and proof; historical matches remain identified rather than silently removed.

P02-NEXT: provider closure must precede preparation of the independent catalog extraction/watch bundle. This run implements no new UI assembly, sandbox, route, history refactor or watch optimization. Live sibling mode and revisions remain unchanged.

The final verifier re-reads all phase manifests, topic mappings, source ownership and actual negative/positive transcripts. Documentation gate baseline defects and excluded environment-specific test lanes must stay explicit; neither is relabeled as a passing run. The broad stable gate is executed once for its named serialized revision/composition/shared persistence trigger, with targeted follow-ups for observed failures.
