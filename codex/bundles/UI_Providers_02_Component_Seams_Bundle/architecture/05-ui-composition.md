# UI composition
Preserve the large-desktop provider tree/detail split and six sections; existing workspace scroll owner. Persistent semantic warning/pending/retired status uses existing Alert with explicit reconciliation action, outside the provider Save form. Do not hide a known commit behind transient error notification.

Connections remains a wide lazy overlay, with compact source form and catalog selection overlays owned by that component. Close cancels only its ownership. Sharing's existing publication/import content remains; add pre-Publish permanent identity explanation and explicit target failure/retry. No page redesign, small viewport work or custom shared component copies.
