# Architecture exit gate

PASS for this bounded development-loop change. Fresh CodeAnalytics snapshot snap-20260906141738-a1331b34 scopes the real UI and sandbox: no cycle and no blocking error. Its four member-size observations are informational heuristics, not reasons to split cohesive types. Duplicate generated attributes and unresolved factory-style AddSingleton are collector diagnostics; the existing local fixture registration is directly exercised by both browser modes. See inventory/architecture-exit.json.

The evaluated restore graph includes 12 projects, including live sibling substitutions, and no new production reference. The sandbox still directly references only the UI RCL. No AgentFramework module, ProviderManagement, Core, Web or persistence implementation enters its closure. Existing abstraction references remain as before. The new Unit project reference tests the real sandbox asset-mode contract; it does not alter the production graph.

AgentCatalogPanel/card/pure mapping remain in the existing UI assembly, with AgentCatalogHost/effects in the module. No source movement or copied specimen implementation. No Components/FileTools edit. Fast utility roots are explicit and justified by rendered source; compiled BaseLib styles retain ownership of sibling utility generation. Both mode outputs are explicit and fail predictably on missing assets or contradictory runtime selection.

Direct Release UI, Parity/Fast sandbox and unit builds pass; 5 unit and 30 component cases pass, real browser acceptance passes, and 27 final warm cycles pass. The full-app calibration renderer crash remains a disclosed tool limitation rather than a hidden architectural success. Full proposed portability and secret enforcement are recorded in the final closure.
