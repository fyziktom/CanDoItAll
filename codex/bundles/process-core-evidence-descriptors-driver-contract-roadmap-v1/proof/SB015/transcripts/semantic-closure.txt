SB015 semantic closure passed.

The implementation adds `ProcessArtifactProjectionEvidenceDescriptors.cs` in Core and `ProcessArtifactProjectionEvidenceDescriptorAdapter.cs` in the Processes module.

Current behavior remains module-owned:
- `ProcessArtifactProjectionOrchestrator` still owns coordinator construction and execution order; the adapter validates the source order against Core descriptors.
- `ProcessArtifactProjectionLineageBuilder` still owns lineage object construction and recovery context; the adapter describes the resulting facts.
- `ProcessProviderNativeBrowserOutputFacts` still owns browser output discovery, safe path resolution, file existence, and file length checks; the adapter describes satisfaction eligibility after those checks.

Gate proof:
- Build passed with 0 warnings.
- Architecture tests passed: 97/97.
- Focused integration tests passed: 14/14.
- Core source scan found no forbidden dependency, driver, side-effect, or stub tokens.
- UI/media drift scan found no UI or media changes in this slice.
