ExitCode: 1

Pre-SB016/SB017 gap:
- The inherited Core consumer map did not yet include the projection evidence descriptor adapter.
- The current bundle did not yet carry its own `architecture/06-core-adapter-ownership-map.md`.
- The architecture guard could prove the inherited map, but not that the current bundle owned the exact adapter map required by SB016.

Expected repair:
- Add the current bundle adapter ownership map.
- Update the architecture guard to assert both maps against actual dispatch Core consumers.
- Run source scans proving side-effect dispatch files and global usings do not hide Core references.
