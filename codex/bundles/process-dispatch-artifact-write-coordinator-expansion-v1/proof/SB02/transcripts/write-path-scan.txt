SB02 write path scan from live source on 2026-06-04

Command: rg -n "storagePlacementService\.PlaceAsync|RecordArtifactAsync" ArtifactProjection.cs
40:            RecordArtifactAsync);
266:            var placement = await storagePlacementService.PlaceAsync(
277:            var recordResult = await RecordArtifactAsync(
434:            var placement = await storagePlacementService.PlaceAsync(
445:            var recordResult = await RecordArtifactAsync(
573:            var placement = await storagePlacementService.PlaceAsync(
595:            var recordResult = await RecordArtifactAsync(
755:            var recordResult = await RecordArtifactAsync(
929:                var placement = await storagePlacementService.PlaceAsync(
940:                var recordResult = await RecordArtifactAsync(
1036:        var placement = await storagePlacementService.PlaceAsync(
1046:        var recordResult = await RecordArtifactAsync(
1199:                var placement = await storagePlacementService.PlaceAsync(
1210:                var recordResult = await RecordArtifactAsync(
1372:                    var placement = await storagePlacementService.PlaceAsync(
1383:                    var recordResult = await RecordArtifactAsync(
1596:    private async Task<Result<Guid>> RecordArtifactAsync(
1602:        return await processesService.RecordArtifactAsync(request, cancellationToken);

Classification:
- Lines 266/277: Process mock artifact. Storage-backed. Hard failure for missing/unreadable file and record failure; updates candidate external reference and expectation state after success.
- Lines 434/445: Workspace-written artifact. Storage-backed. Missing/unreadable file and record failure are soft skip/log; updates candidate state only after record success.
- Lines 573/595: Existing managed artifact. Storage-backed. Missing/unreadable file and record failure are soft skip/log; duplicate detection via expectation ids, detail artifacts, and external reference keys.
- Line 755: Completed-decision artifact. Record-only, no storage placement. Record failure is soft logged; updates candidate state only after success.
- Lines 929/940: Response-text artifact after writing target text file. Storage-backed. Existing matching managed file can route through existing-managed helper; failures are caught and logged; updates candidate state after record success.
- Lines 1036/1046: Response projection existing-managed helper. Storage-backed. Returns false on record failure so caller can continue response-text write path; updates candidate state after success.
- Lines 1199/1210: Provider-native browser expected output. Copies into managed path, storage-backed, soft logged failure; updates candidate state after success.
- Lines 1372/1383: Provider-native browser discovered output. Copies into scoped managed path, storage-backed, soft logged failure; updates candidate external key and matched expectation state after success.

Duplicate/candidate-state behavior:
- ExternalReferenceKeys prevent duplicate projection records across all classified paths.
- RecordedArtifactExpectationIds is updated only for expectation-backed successful records.
- Completed decision also uses HasProjectedArtifactExpectationExternalReference to avoid replacing already projected artifacts.
