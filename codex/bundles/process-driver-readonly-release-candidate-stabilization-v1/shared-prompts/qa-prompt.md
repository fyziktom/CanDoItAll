# QA / Red-Team Prompt

Reject the implementation if any critical gate lacks artifact-backed proof. Reject report-only, table-only, status-only, and non-empty-diagnostic-only closure. Verify that production behavior is exercised by tests and source scans, not only described in docs.
