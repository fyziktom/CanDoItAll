# Shared thinking final semantic verifier

Result: Pass for N011/R11 and N012/R12; not a clean-whole-repository result.
Reviewer: primary implementation agent; no independent reviewer claim.

Reread SB07/SB08 manifests, invariant contracts, original focused discovery/TRX records,
production ownership audit, actual MCP results, source dispatch/usage and host evidence.
Exact frozen discovery matches 206 Unit, 46 Components and 56 Integration passing records.
The broader checkpoint failures and repaired related cases are explicitly classified;
they have not been discarded or represented as successful runs.

## Semantic checks

- The screenshot's disabled effort was an explicit client guard plus missing catalog
  metadata, not stale deployment. Typed source producer and client consumer are present.
- Fail-closed unknown/unsupported handling and invalid override rejection are tested.
  Client UI exactly mirrors source Sol options; GPT-4.1 is disabled. No hash inference.
- Real per-agent Low/High values reach upstreams independently. Seven UI-configured
  agents produce nine HTTP-200 requests with Succeeded/Complete source usage. Same model
  with different efforts and different models with matching efforts are both evidenced.
- Source-default changes affect a subsequent unchanged client request without sync;
  explicit Low still overrides source High. Default restored to Medium afterward.
- Temperature ownership, actual SDK envelope handling and Responses terminal completion
  were reopened when real execution exposed defects. Red regressions now pass; no
  synthetic [DONE], fabricated usage or permissive background/tool parsing was used.
- Main suggestions intersect the real catalog; source/client labels and ordering match.
  Saved legacy selections, full pricing and actual catalog membership remain valid.
- Normal and open dialog screenshots were inspected at 1920x1080. Native OS option popup
  capture is explicitly limited; real DOM option equality and selection supply that proof.
- Production anti-stub search found no placeholder or test-agent special cases. No
  changed project references, runtime partial split or sibling source mutation is claimed.
- Deployment uses final image6 on all three hosts with retained named volumes. No 5214
  reset, 5032 change, credential disclosure or approval-policy relaxation was performed.

## Explicit limits and re-entry

Chat Completions cannot support every OpenAI reasoning/tools combination. Its earlier
Low-to-None compatibility adjustment is not positive effort proof. Use the UI-created
Thinking Proof OpenAI Responses profile for the verified Mini/Luna/Sol reasoning cases.
The original provider transport was preserved. Strict old clients require coordinated
upgrade; snapshots without new fields remain Unknown until synchronized.

Broad failures remain visible in proof/SB07/broad-regression-results.md. Reopen if a valid
explicit effort is lost, absent metadata is guessed, a default is cached client-side,
source/client choices diverge, terminal usage fails or any real name/price is fabricated.
The final completed-stage structural validator supplements, not replaces, this review.
