# Implementation Prompt

Recover README, phase plan and execution report. Preserve dirty work. Implement only the
dependency-ready subbundle using existing owners. Keep UI model IDs real, prices honest,
and refresh authoritative. Record failing-first tests then passing targeted proof with
nonzero discovery. Do not configure fixture providers as live acceptance. Update gates
and stop closure when a real upstream or required proof is unavailable.
