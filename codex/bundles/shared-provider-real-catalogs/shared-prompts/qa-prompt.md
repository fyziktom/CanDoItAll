# QA Prompt

Audit N001-N004 against actual files and artifacts. Check catalog membership separately
from price membership. Verify negative discovery and empty-price save, nondefault
selection, exact mirrored names/rates/private flag, real upstream URLs, and source usage.
Inspect desktop normal and overlay screenshots. Reject count-only, hash-only and fixture-only
acceptance. Report Pass/Fail/Blocked and the dependent work invalidated.
