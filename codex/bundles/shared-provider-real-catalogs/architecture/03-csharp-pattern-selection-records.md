# C# Pattern Selection

Use existing connector strategy and pure reconciliation functions. Extend typed refresh result
with discovered names, apply atomically on success. Use explicit Blazor change event for Kind.
Reject new factories/repositories/mappers: no independent lifecycle or boundary requires them.
