# C# Testability Plan

Pure price/catalog unit tests prove exact replacement and unknown-rate absence.
Existing component harness proves real kind binding, refresh editor state and no fabricated Add
row. Existing shared integration tests cover save/publication/sync and routed nondefault IDs.
Error/empty discovery must retain previous state with visible failure.
