# Simple Chats Definition Catalog closure

Status: implementation complete; broad-gate closure conditional on the additional PostgreSQL setup timeout.

## Ownership and dependency decision

The old catalog combined rendering, authorization, debounce, paging and editor targeting. Catalog markup, owned CSS, immutable cards/filters/phases/failures, typed intents and pure participant mapping now live in the new feature-local SimpleChats.UI Razor class library. The Surface has no injected services. SimpleChats.Components retains application-item mapping, authorization/gateway effects, the catalog session and the unchanged definition editor. A projection delegate supplies the real application-to-card boundary without a trivial interface or generic session framework.

The evaluated direct project graph is SimpleChats.UI -> BaseLib and Conversations.Components; Conversations.Components -> BaseLib and OverlayLib; OverlayLib -> BaseLib; BaseLib -> Common; Common -> none. That is five distinct projects. UI also references Microsoft.AspNetCore.Components.Web; Conversations.Components has that package and Markdig; BaseLib has the Blazor package, as does OverlayLib. There is no path to SimpleChats Application/Core/runtime, broad AgentFramework.Components or Modules. Sandbox closure contains 15 projects with no forbidden dependency or cycles. Reusing the existing participant card preserves avatars and card geometry without copying it.

LlmChatDefinitionCatalogSession owns authorization acceptance, applied filters/query, 250 ms debounce, cursor, accepted immutable cards, loading/error state and active-operation identity. Replacing filters invalidates old list/append requests immediately, before debounce; append is single flight. Only the current owner publishes, and the completing read disposes its token source. Public errors are fixed typed messages; logs include generation, failure count or exception type, not raw gateway content. Accepted card/tag data is copied.

LlmChatDefinitionCatalogPanel owns authorization lifetime, dispatcher rendering and editor/route composition. Route observation lasts for the component lifetime. A -> B replaces the keyed editor, A -> null -> A works, same-ID echoes do not reload, Guid.Empty means no target, and read-only access never constructs an editor. Local Create survives an unchanged null route. Route changes during pending authorization/list work use the latest target. Editor-generation callbacks prevent an obsolete child from closing or reloading its successor. Current Save closes and reloads once; a list reload failure cannot reopen the editor.

## Preserved functions and file map

Automatic initial load; server search with 250 ms debounce; search/tag/status validation; case-insensitive tag suggestions; reset; cursor Load more; read-only/manageable states; avatar/name/summary/status/revision/updated date/tags; card selection; Edit/New; route-driven targets; editor Save/Cancel and provider/model/field behavior all remain. Prompt/schema content never enters catalog cards. Existing LlmChatDefinitionEditorDialog is unchanged. Conversation execution, streaming, send/composer, provider effects and mutation hardening are outside scope.

New production files are the six-file SimpleChats.UI project (project, imports, presentation, Surface, CSS and README), the session in SimpleChats.Components, and sandbox fixture/specimen. The catalog host and application mapper were reduced; their former rendering/pure mapping moved into UI. Project/solution references and Fast Tailwind scanning include the new RCL. The sandbox's eight typed states are Denied, Loading, Empty, ReadOnly, Manageable, Filtered, Paged and Adversarial; actions update controlled presentation and an intent log only.

Three compact new test files cover session/host lifetime (14 cases), Surface/scenarios/intents (11) and sandbox behavior (one). The 15 existing definition editor compatibility cases remain, with shared fixture access expanded. SimpleChatsWorkspace route tests and AgentsHomePage regressions are included in the final selection. The existing shared Playwright fixture/runner gained a definition gateway and scenarios; the real Web host renders the production catalog and existing editor with isolated synthetic data. No conversation is started.

## Browser and composition review

Web verifies automatic 24-card loading and 27-card paging; one debounced search; tag/status/reset; long/adversarial encoded text; no prompt exposure or horizontal overflow; open A; live route A -> B; close; local Create; safe fixture Save with exactly one reload; and a read-only route with zero editor reads. Parity/Fast each verify all eight controlled states and actions. Screenshots confirm filter/card containment at 1600 x 1000 and the existing wide editor dialog's footer/layering. Existing BaseLib/Conversations components remain the primary composition primitives; CSS adds only catalog-owned containment.

## Next bounded work

Definition Editor state/read and mutation lifecycle is the smaller next Simple Chats slice: one editor boundary already exists, while a conversation shell spans list/selection and transcript rendering. Address pending editor reads, cancellation/stale child outcomes and mutation outcome ownership there. Then proceed through conversation list/selection and transcript rendering, composer/send, streaming/follower and attachments/provider effects. The later first Workflows slice should be its shell/dashboard/catalog/history read surfaces; WorkflowCanvasEditor remains a separate program. No future bundles were created.

## Final validation and delivery boundary

The Program A progression gate passed before any Program B production edit: 104 unit, 109 component and five integration cases (218 total, zero skips). Final focused discovery and execution matched: 134 unit, 150 component and five integration cases (289 total, zero skips). This includes Governance predecessor regression, Diagnostics, History, Definitions, the existing editor/avatar/provider tests, SimpleChatsWorkspace routes, AgentsHomePage lazy tabs and the three Simple Chats registration/architecture guards.

All twelve direct build targets passed, including Web, both sandbox asset modes and affected tests. Product and stable solutions also restored and built successfully. Tailwind Parity and Fast assets built. Final Web browser checks passed for Governance, Diagnostics, Request History and Definitions; all three specimens passed in Parity and Fast at 1600 x 1000. Catalog and editor screenshots were inspected locally; no screenshot is retained in the bundles.

The one final broad gate discovered 10,432 cases; seven data-driven tests expanded at execution by 55 cases, giving 10,487 executed: 10,482 passed, 5 failed and 0 skipped. The existing Components architecture allowlist omitted the newly authorized SimpleChats.UI reference. It was corrected, and the same guard now verifies the new UI project/package references and absence of injected application services. All three registration/architecture guards pass within the corrected 134-case focused unit run; the original broad failure is retained. Production source was unchanged by this test-only correction. 4 unchanged cases timed out during PostgreSQL schema setup, before product assertions; each exact isolated retry passed. The original broad failures remain recorded. The exact stable filter is recorded in validation-summary.json. Its executed project totals, original failures and any permitted isolated retry are recorded there; no original failure is replaced by retry evidence. The watcher smoke covers one Razor heading, one C# presentation string and one owned CSS edit on Web, Parity and Fast. Native update evidence is followed by explicit same-page navigation and visual/DOM verification, not a claim of automatic UI refresh. All three source files are restored byte for byte after each host.

The dependency audit and portability enforcement pass. The complete portability scan includes new untracked source and is not truncated. Every added/stale allowance was reviewed: case-insensitive tag suggestions moved with rendering, the new README code-fence label matches shell patterns, and an obsolete Diagnostics Razor MCP allowance was removed. The final enforcement ran without the write flag. Encoding, source-secret, retained-secret and manifest results are in validation-summary.json.

Canonical documentation validation initially rejected 118 unchanged historical `.log` files in UI_Agents_Component_Seams_Bundle. All match their manifest hashes, and the shared documentation standard explicitly permits durable evidence inside its owning bundle. The validator now accepts only tracked bundle logs with exactly one matching entry in a tracked manifest and the correct current hash. Nine contract tests cover valid evidence, outside-bundle logs, PID files, untracked manifests, modified/unlisted logs, duplicate entries, malformed digests and case mismatches. The historical evidence is unchanged; transient/unsealed logs and PID/PYC files remain forbidden. Canonical documentation validation passes. This bounded tooling correction does not change the frozen production source or require a second broad test execution.

Entry and final local/remote HEAD remain 504c47d8f3fa085085b3fdbf955b69b9fdfb8856 on components-decoupling; parent e9fb82be7a43f843303cbd81233450acb6d24dd0 and committed tree c155332c56fd6b0c062c454b9eb9329536dfbbab. Delivery is the uncommitted proposed working tree. No commit, staging, push or history mutation occurred. Components remains c3e6aa03a878994c0ba8aed6af017d0be75f3796 with its known five-file DialogService patch; FileTools remains clean at 7c7453c6583365ae5bd63f8fc6efc4a776e15818. Clean published-source reproducibility remains a delivery dependency; no clean-archive campaign was repeated.

Components MCP and CodeAnalytics succeeded. Scoped entry snapshot snap-20260908184408-98a29c82 and final snapshot snap-20260908191033-b48541aa had no cycles. Final duplicate-type warnings concern generated EmbeddedAttribute/ValidatableTypeAttribute metadata only. MSBuild supplied the authoritative evaluated closure. The initially missing Chromium executable was installed through the existing Playwright CLI. No essential local tool was unavailable.

The owner-authorized four-file shape replaces a multi-level bundle/proof hierarchy for these slices. Semantic review checked scope, ownership, public tests, production composition, immutable data, cancellation ownership, stale-result fencing, privacy and browser parity. Exactly nine new bundle files are delivered across three four-file bundles. Raw logs, discovery, DOM state and screenshots remain ignored. The manifests seal README, report and the consolidated validation summary, with no source-hash inventory or nested proof tree.
