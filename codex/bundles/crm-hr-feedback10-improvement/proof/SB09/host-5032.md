# SB09 Final Release Host On Port 5032

Review date: `2026-07-24`

## Launch

PowerShell `Start-Process` launched this command hidden, with separate stdout and stderr files:

```powershell
dotnet run --project src/App/CanDoItAll.Web/CanDoItAll.Web.csproj --configuration Release --no-build --urls http://127.0.0.1:5032
```

- Wrapper process id: `42104`
- Listener process id: `51760`
- Standard output: `repo://output/runtime/crmhr-feedback10-final/app5032-final3.out.log`
- Standard error: `repo://output/runtime/crmhr-feedback10-final/app5032-final3.err.log`
- Observed stdout length: `4111` bytes
- Observed stderr length: `0` bytes

The open listener retained the log handles, so no digest is claimed for those two live files.

## Probes

`Invoke-WebRequest` checked:

```text
GET http://127.0.0.1:5032/
GET http://127.0.0.1:5032/api/access/status
```

Both returned HTTP `200`.

`Invoke-RestMethod` checked:

```text
GET http://127.0.0.1:5032/api/crm-hr/parties?page=1&pageSize=1
GET http://127.0.0.1:5032/api/crm-hr/workforce?page=1&pageSize=1
GET http://127.0.0.1:5032/api/crm-hr/recruiting/applications?page=1&pageSize=20
```

Observed totals were `78` parties, `32` workforce records, and `8` applications.

## Log Inspection

- The final stderr file was empty.
- Searching stdout and stderr for `fail:`, `Unhandled exception`, `System.ArgumentException`, and `System.InvalidOperationException` returned no match.
- Browser console proof for the same host reported `0` errors and `0` warnings.

## Conclusion

The rebuilt Release host is healthy on port `5032`, its access and CRM-HR endpoints respond, its populated readback agrees with the seed evidence, and no server error pattern was observed. `Pass`.
