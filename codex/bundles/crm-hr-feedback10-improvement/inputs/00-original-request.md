# Original Request

The authoritative raw request is the preserved source document:

- `bundle://inputs/feedback10.docx`

Its text was extracted without normalizing grammar or scope into:

- `bundle://inputs/03-feedback10-extracted.md`

The source asks for the CRM/HR improvements represented by raw identifiers `N001` through `N010`. `N001` is only the literal section heading `CRM:`. `N002` through `N010` are the actionable feedback bullets. The rendered pages and embedded images remain preserved so implementation and QA can compare the original visual context instead of relying on rewritten prose.

Preparation must produce an implementation-ready bundle without editing the DOCX, extracted images, or rendered pages.
