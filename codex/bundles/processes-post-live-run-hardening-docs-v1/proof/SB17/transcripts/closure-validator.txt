# SB17 Closure Validator

Command:

```powershell
$output = & python codex\skills\bundles\candoitall-bundle-preparation\scripts\validate_bundle.py codex\bundles\processes-post-live-run-hardening-docs-v1 --stage completed --repo-root C:\repositories\CanDoItAll 2>&1; $matches = $output | Select-String -Pattern 'SB17|RN12|semantic-invariants|manifest|Production Behavior Artifact Matrix|Failing-first|Adversarial|Traceback|ValueError'; if ($matches) { $matches; exit 1 } else { 'No SB17 validator findings.'; exit 0 }
```

Exit code: 0

Output:

```text
No SB17 validator findings.
```
