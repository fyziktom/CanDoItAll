# Bundle Self Review

- The bundle starts with workflow/validator hardening because the latest Codex pass exposed a proof-gate weakness.
- The bundle uses failing-first tests before production changes to avoid another implementation that simply satisfies reports.
- The bundle keeps economics/governance out of scope.
- The bundle cites exact current source files and previous proof artifacts using portable references.
- The bundle has explicit red-team cases for professor learning, dream synthesis, clustering, recall lineage, and validator fixtures.
