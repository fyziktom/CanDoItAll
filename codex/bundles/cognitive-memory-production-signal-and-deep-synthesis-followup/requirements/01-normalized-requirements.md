# Normalized Requirements

| ID | Requirement | Acceptance signal |
|---|---|---|
| R01 | Strengthen workflow/validator gates so production behavior cannot be replaced by consumer-only code, seeded tests, or prose proof. | Fake completed bundle with consumer-only accepted-use signal fails validation. |
| R02 | Add failing-first tests for every current gap before production fixes. | Tests fail on current implementation and pass only after real fixes. |
| R03 | Add production accepted-use signal emission and wire professor assimilation scanning into lifecycle flows. | Accepted-use signal is emitted by accepted recall/workflow outcome, not test seeding. |
| R04 | Resolve professor anchors stuck in `Comparing` after dream validation review states. | Review service transitions anchors out of `Comparing` with audit proof. |
| R05 | Support natural multilingual professor capture, especially Czech with diacritics and Q&A teaching. | Czech natural corrections create structured professor anchors without explicit memory keywords. |
| R06 | Replace dream meta-text with internalized, claim-aware synthesis and claim-specific provenance. | Aggregate memory text is useful domain knowledge and source maps are claim-specific. |
| R07 | Add embedding/ranker-backed approximate candidate discovery for semantic paraphrases. | Paraphrased memories without shared exact keys are compared and clustered when semantically close. |
| R08 | Make recall synthesis task-facing with real user query/intent and line-level provenance. | Brief uses actual query and each statement resolves to exact claim/source lineage. |
| R09 | Refactor large cognitive memory services into maintainable components. | Large methods/services are decomposed with clear interfaces and DI tests. |
| R10 | Prove the complete professor-learning lifecycle end to end. | Red-team scenario passes without direct test seeding of lifecycle signals. |
