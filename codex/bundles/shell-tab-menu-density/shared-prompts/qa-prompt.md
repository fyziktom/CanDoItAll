# QA Prompt

Validate against the raw notes literally. On large desktop, confirm tabs, search, tab overflow controls, and status badges share one row. Confirm the sidebar has no internal nav scroll and exposes overflow items through `more_up`. Open the continuation panel and inspect readability, clipping, layering, small square icon cards, one-word labels, max-three-row grid, and matching dark menu background. Then repeat a narrower-width check to confirm desktop-only compaction did not leak.
