# Original Request

Raw notes from the user request on 2026-05-17:

1. "move tab search bar on same row as tabs at its end. Same those badges with stats about tabs move to same row as tabs. we need it for large desktop only now and this will save height on page."
2. "repair main menu. on some pages we are missing limitation of the page heights. On those where we already have it the menu has inside scrolling. Scrolling is not good option. In case that it must overflow it must display as last standard menu item with icon more_up that will open panel (when mouseover) with floating panel with other pages icons that did not fit into standard menu. They must look as very small square cards just icon and oneword name both centered in that square card. they will use grid with maximum 3 rows. In case they need more they will icrease columns. The other pages icon must be on panel simialr as we have switching of the db in menu. It must use same background as menu."

Source also included a screenshot of the current large desktop shell showing the search row below the tabs and status badges on the far right.
