# QA prompt

Verify that runtime `src`, `tests`, and `CanDoItAll.slnx` contain no retired-provider residue, PostgreSQL baseline proof remains single-migration and drift-free, Data Sources renders without retired-provider or snapshot controls, and all final validation commands recorded in `proof/SB08/manifest.md` are still reproducible.
