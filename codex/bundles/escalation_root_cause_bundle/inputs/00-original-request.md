# Original Request

The user requested a senior C# architecture bundle using `candoitall-bundle-workflow` for the GPTPro Extended analysis in `codex/bundles/escalation_root_cause_bundle`.

The important constraints from the request are:

- Prepare a detailed implementation bundle, not only a summary.
- Reflect the GPTPro root-cause analysis completely.
- Use the blocked 5032 process instance as the concrete example, but analyze similar trouble in all process templates, artifact templates, and related contracts.
- The bundle may be larger if that is required to solve the escalation class completely.
- Use C# architecture skills to validate the proposed design quality.

## Working Assumption

This turn prepares the implementation bundle. It does not implement production C# or template changes.
