# QA Prompt

Validate from source, tests, receipts, and live process evidence. Fail the subbundle if active seeded instructions still contain calculator/converter/unit-topic requirements, if `workspace_dotnet_run` is documented but not callable, or if the Blazor specialist cannot access component-library guidance and .NET build/run/test tools.

For the live validation subbundle, observe the generated app outputs but do not repair them manually. If either generated app fails, determine whether the failure is app-specific agent execution or a reusable platform gap. Only generic platform repairs may be made before rerunning the process.
