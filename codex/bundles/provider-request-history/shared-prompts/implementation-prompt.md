# Implementation Handoff

Use only after the user explicitly authorizes implementation. The current request was
bundle preparation only; reading this prompt does not change that authorization.

Read the bundle README, current phase, source anchor/drift, normative architecture contracts,
phase prerequisites, validation strategy and execution report. Run entry validation and
resolve any missing contract before editing. Implement only the selected phase and the
smallest necessary dependent edits. Existing user changes must be preserved.

Maintain strongly typed C# and explicit UI/Application/Domain/Infrastructure boundaries.
Use existing provider gateways, canonical owners, components, styles, token validation and
profile fences. No magic-string metadata, object/reflection dispatcher, hidden service
locator, silent fallback, invented history/price/identity, runtime partial split or unrelated
refactor. New classes follow the 250/400 responsibility review thresholds. Do not generate
XML documentation comments. Rare necessary comments are English. Use fully cuddled Egyptian
braces and one statement per line.

Read applicable architecture, dependency, testability, performance and component skills.
Verify real construction sites and actual producer paths. Small pure helpers need no
interface unless a real boundary/test seam warrants it. Do not add Tailwind/Radzen unless
already present and applicable; use the project's established components and styles.

Prove the phase's named positive and adversarial cases using existing test homes wherever
possible. Inspect actual discovery before execution, record command/CWD/result and reuse
valid binaries/evidence. Follow the proof tier: Governed phases require real transcripts,
hashes and producer/consumer lifecycle artifacts under proof/SBxx. Do not run broad suites
except the once-only justified frozen SB08 checkpoint.

No paid model call, active user-database mutation, deployment, destructive migration or
credential action is authorized implicitly. Deterministic fake upstreams and disposable
stores prove the core feature. Do not evade a tool approval/sandbox failure.

Update the execution report, requirement mapping and phase gate with actual evidence.
Review downstream invalidation. Stop at the phase's progression gate if proof fails;
never claim completion from a submitted command, old bundle, seeded mock screen or a
passing unrelated test. Do not begin another phase merely because it appears next numerically.
