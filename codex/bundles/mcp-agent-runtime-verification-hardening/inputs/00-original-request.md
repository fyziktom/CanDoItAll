# Original Request

Paste the raw user request or source note here without rewriting it.
# Original Request

The user reported that testing an MCP server from the Agents page, Capabilities tab failed for `Playwright Local MCP`.

Key asks:

- Analyze whether old database records are the reason.
- If old records are involved, update them to actual models.
- Truly validate the fix with Playwright MCP.
- Test agents using tools in project structure, workflows, or processes.
- Create another bundle for verification, bugfixes, and hardening.
- Use the bundle workflow skill.
- Keep UI testing to large screens only.
- Start the rebuilt app on port 5032 with the development database.

Attached screenshot:

- `C:\Users\lucys\AppData\Local\Temp\codex-clipboard-c2eaf70b-9a75-417a-927e-047959642319.png`
