# Implementation Prompt

Write an outcome-first reusable implementation prompt. Include owned inputs, hard constraints, prerequisite checks, smallest-change guidance, required proof, status-update rules, and stop conditions.
# Implementation Prompt

Repair the Playwright Local MCP setup-test path without introducing mock fallbacks. Register a real local stdio MCP client factory, carry message-framing through the typed descriptor/template/editor/runtime model, refresh managed development workspace records through an explicit seed version bump, and keep project/process agent tooling on internal runtime providers rather than legacy MCP catalog records.

Validation must include focused .NET tests and large-screen Playwright MCP proof against `http://localhost:5032/`.
