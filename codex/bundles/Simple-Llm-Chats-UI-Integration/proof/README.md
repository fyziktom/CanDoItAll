# Execution Proof

Execution creates `proof/SBxx/` artifacts according to each subbundle's tier.

SB01 must add an explicit repository `.gitignore` exception for this bundle's bounded proof directory before any proof is treated as durable. Never list ignored/nonexistent proof in checksums or closure claims.
