# SB11 — Unified floating conversation shell and windows

## Status

- `Not started`
- Stage: `floating`
- Proof tier: `Governed`

## Objective

Deliver the named outcome without activating or implementing later phases.

## Owned Requirements

- `SCUI-008`
- `SCUI-009`
- `SCUI-024`
- `SCUI-025`
- `SCUI-049`
- `SCUI-050`
- `SCUI-051`
- `SCUI-052`
- `SCUI-053`
- `SCUI-054`
- `SCUI-055`
- `SCUI-056`
- `SCUI-057`
- `SCUI-059`
- `SCUI-062`

## Prerequisites

`SB10`

## Current Source Anchors

- `repo://src/UI/CanDoItAll.Conversations.Components/ConversationFloatingCatalog.razor`
- `repo://src/UI/CanDoItAll.Conversations.Components/ConversationFloatingWindow.razor`
- `repo://src/Modules/CanDoItAll.Modules.AgentFramework/Pages/Components/FloatingAgentChatHost.*`
- `repo://src/App/CanDoItAll.Web/Components/Layout/MainLayout.razor`

## Explicit Non-Goals

- Do not implement work owned by a later subbundle.
- Do not add Project Structure/context, tools, skills, Memory, voice, attachments, or chatbot deployment behavior.
- Do not run an unfiltered Stable or full Playwright suite unless this README explicitly authorizes it.

## Implementation Steps

1. Create CanDoItAll.Conversations.Shell and its neutral contributor/window-descriptor contracts.
2. Refactor Agent floating catalog orchestration into an Agent contributor while preserving the existing coordinator/window behavior and compatibility facade.
3. Implement a Simple Chat contributor over active definitions, durable conversations, and the SB09 follower.
4. Render one catalog with Available/Active tabs and All/Agents/Chats filter; merge declared actions without semantic collapse.
5. Replace the MainLayout Agent-only host/launcher with the unified shell while retaining Agent-only context badges and fail-closed filtering.
6. Run targeted Agent and Simple Chat floating browser scenarios and issue CP3.

## Acceptance Criteria

- [ ] Unified catalog filters and lifecycle axes are correct.
- [ ] Agent context/affinity/history/close behavior remains unchanged.
- [ ] Simple Chat floating stream/reopen/cancel works.
- [ ] No ambient context reaches Simple Chats.
- [ ] Shell project has no product backend dependency.

## Validation Depth

- Proof tier: `Governed`.
- Use failing-first or characterization-first proof where production behavior changes.
- Record exact start/candidate commits, source paths/ranges, discovery, commands, results, and progression decision.

## Focused Test Selection

Relevant workspaces:

- `tests/Solutions/CanDoItAll.Tests.Components.slnx`
- `tests/Solutions/CanDoItAll.Tests.Playwright.slnx`

- Call `code_analytics_impacted_tests_get` from the actual final diff with `behaviorIntent=Unknown`.
- Verify every workspace is healthy and required selectors discover non-zero expected tests.
- Run every required selector; promote conditionals only on reported triggers or uncertainty.
- New named behavior tests must be included in the required selection even when static impact cannot discover newly added tests.

## Invalidation And Broad-Gate Decision

- Deferred; unfiltered Stable is forbidden in this subbundle.
- Reopen this work when any owned contract/source/behavior changes later.

## UI Composition Contract

One unified catalog overlay and one focused window; inspect filters, active list, dropdowns/dialogs, context badges, layering, clipping, and internal scroll.

## C# Architecture Impact

Creates one justified application-wide UI shell project and contributor adapters.

## Boundary Ownership

Conversations.Shell owns neutral catalog/window composition; contributors own product lifecycle.

## Dependency Direction

Follow `architecture/02-csharp-dependency-direction.md`; stop on any new cycle or forbidden inward reference.

## Pattern Decision

Contributor registry + DynamicComponent window descriptor + compatibility facade.

## Testability Contract

- Changed behavior must be testable through its new owner without constructing an unrelated full runtime.
- Include a negative or boundary case capable of detecting shallow delegation or state leakage.
- Browser proof supplements but does not replace component/application proof.

## Partial Class Policy

Do not add a partial class as the final separation boundary. A Razor component code-behind partial generated by normal Razor compilation is not permission to split unrelated responsibilities across new partial files.

## Architecture Proof Required

- Record before/after project dependencies and cycles when references change.
- Record direct source proof that the target owner, not the old facade, owns new behavior.
- Run `csharp-architecture-review-gate` for checkpoint or architecture-changing work.

## Progression Gate

CP3 Pass unlocks SB12.

## Reopen Triggers

- Any later change to an owned public contract, mapper, route, authorization rule, lifecycle state, or source-specific adapter.
- Any failed required selector, unexpected zero discovery, new cycle, missing proof artifact, or browser parity regression.

## Suggested Agent Prompt

```text
Execute SB11 only: Unified floating conversation shell and windows. Preserve all prerequisites and explicit non-goals, use current SharedInfo impacted-test selection from the actual diff, produce Governed proof, and stop at the progression gate.
```
