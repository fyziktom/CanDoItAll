# Manager Review

This bundle deliberately plans many subbundles so Codex cannot finish with a shallow wrapper move. The bundle should take longer than prior small changes because it touches every route group and forces multiple critical gates.
