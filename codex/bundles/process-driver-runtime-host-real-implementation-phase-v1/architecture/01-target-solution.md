# Target Solution

This file is the validator-compatible alias for `architecture/01-target-architecture.md`.

The target solution is a generic runtime-host contract boundary plus process-module-owned verification and dry-run runtime host pipelines, while keeping Process Core dependency-clean and blocking all execution-capable driver behavior until a later approval gate.

