# QA Prompt

Validate behavior through real source and tests, not report prose. Confirm code-first ratio, exact source references, focused tests, boundary scans, anti-stub scan, and red-team negative proof. Reject completion if bundle/proof churn dominates `src + tests`, if Process Core gains driver/module/runtime dependencies, or if skipped live tests are claimed as live proof.
