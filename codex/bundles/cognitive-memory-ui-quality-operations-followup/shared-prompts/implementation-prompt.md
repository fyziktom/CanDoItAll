# Implementation Prompt

Implement the Cognitive Memory UI quality operations follow-up. Keep the work large-screen only. Do not tune medium or small viewports. Add per-collection paging that applies before list materialization, expose the new quality functions through a dedicated tab, improve every existing tab with consistent desktop panes and pager controls, and validate with tests plus browser proof.
