Command: SB09 proof transcript: failing-first.txt
ExitCode: 1

# SB09 Red-Team Transcript

Pre-fix risk:
- Workflow artifact projection could rely on summary-token parsing and same-kind heuristics.
- Subprocess artifact projection could rely on summary-token parsing and same-kind heuristics.
- With multiple same-kind outputs, a title/kind match could satisfy the wrong process artifact expectation.

Red-team tests:
- `WorkflowArtifactProjectionMapping_SB09_INV_001_blocks_same_kind_heuristic_without_explicit_output_id`
- `SubprocessArtifactProjectionMapping_SB09_INV_001_blocks_same_kind_heuristic_without_child_mapping`

Behavior under test:
- Create multiple workflow artifacts and expectations with the same process artifact kind.
- Create multiple child subprocess artifacts with the same process artifact kind.
- Omit explicit mapping.
- Assert no artifact is selected and the diagnostic requires explicit workflow or subprocess mapping.

This proves the runtime blocks ambiguous projection instead of guessing.

