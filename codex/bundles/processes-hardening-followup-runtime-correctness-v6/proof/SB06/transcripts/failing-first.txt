Command: SB06 proof transcript: failing-first.txt
ExitCode: 1

# SB06 Failing-First Transcript

## Initial Focused Run

Command:

```powershell
dotnet test tests/CanDoItAll.Tests.Unit/CanDoItAll.Tests.Unit.csproj --no-restore --filter "FullyQualifiedName~AgentToolInvocationPolicyTests|FullyQualifiedName~WorkspaceCommandExecutionServiceTests"
```

Result: failed.

Observed failure:

```text
WorkspaceCommandProcessRunner.cs(341,71): error CS9174: IReadOnlyDictionary<string, ProductTargetFileFingerprint> cannot be initialized with a collection expression because the type is not constructible.
```

Fix:

- Replaced the empty collection expression with an explicit `Dictionary<string, ProductTargetFileFingerprint>`.

## Runtime Audit Assertion

After the compile fix, the focused run exposed an SB06 runtime-audit test failure:

```text
WorkspaceCommandExecutionServiceTests.PowerShellRunScript_SB06_INV_001_fails_post_execution_audit_when_nonmutating_step_changes_product_root [FAIL]
Assert.False() Failure
Expected: False
Actual: True
```

Root cause:

- The test execution run was not trusted as a governed process run because `RequestedByKind` was `test`, so process mutation metadata resolved to permissive defaults.

Fix:

- Set the synthetic execution run to `RequestedByKind = "system"` so the audit exercises the same trusted governed process path used by runtime dispatch.

