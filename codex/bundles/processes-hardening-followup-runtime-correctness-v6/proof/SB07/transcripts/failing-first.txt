Command: SB07 proof transcript: failing-first.txt
ExitCode: 1

# SB07 failing-first / red-team transcript

SB07 is a refactoring checkpoint, so the red-team proof targets the extracted boundaries directly rather than adding new user-visible semantics.

Red-team assertions added:
- `ProcessToolOperationAuthorizer_SB07_INV_001_denies_missing_operation_without_full_policy_runtime`
  - A governed `workspace_dotnet_test` call requiring `RunValidation` is denied when the step only allows `CaptureRuntimeProof`.
- `ProcessScriptSideEffectAnalyzer_SB07_INV_001_detects_writes_and_child_scripts_without_runtime`
  - A PowerShell script with redirection and child script invocation produces typed write and child-script findings.
- `ProcessCompletionArtifactValidator_SB07_INV_001_accepts_generic_nonsoftware_deliverable_without_dispatch_runtime`
  - A generic board-meeting decision memo deliverable is validated directly through the extracted validator, keeping process semantics non-software-specific.

These tests are included in the passing focused transcripts.

