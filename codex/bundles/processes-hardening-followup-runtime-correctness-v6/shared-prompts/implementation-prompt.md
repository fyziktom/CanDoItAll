# Implementation Prompt

Implement this bundle as production code. Prefer typed persisted state and extracted services. Avoid prompt-only fixes. Keep Processes above Workflows. Do not introduce SQLite paths or migrations. Add refactoring checkpoints and proof after each subbundle.
