# Original Request

```text
Use [$candoitall-bundle-workflow](C:\\Users\\lucys\\.codex\\skills\\candoitall-bundle-workflow\\SKILL.md) to preapare detailed bundle that solves this:
Main goal:
improve visual look of the application. 
Increase working space and make UI more clear.

Notes from architect:
We are tuning it only for large screen. Do not waste time on tuning it for small or medium screens now. This must be hard rule across the bundle even if [$frontend-skill](C:\\Users\\lucys\\.codex\\skills\\frontend-skill\\SKILL.md) is used that says it must be tested on all screens. Now is main focus large PC version.

I added "C:\repositories\CanDoItAll\codex\bundles\input-screenshots" screenshots of different app we are doing for simulation of economy. It has nice design. smooth, menu is in default colapsed, but it can be opened. When colapsed menu items it shows tooltip service on right side. 
You must use this type of concept. Reduce unnecessary text we have in menu item. Keep them minimalistic and add info in tooltips.

Add bottom side of left menu add setting and switch db. Remove it from top of the page and it will be always available in menu. If mouseover that item it will popup floating card with info about actual db and copy button to copy db info. 
This will save lots of space on page.

Assure that app/page uses maximum available width of page. 

You must analyze each page and get design proposals with [$imagegen](C:\\Users\\lucys\\.codex\\skills\\imagegen\\SKILL.md) . Then analyze during repairs sceenshots and improve it until it looks similar. Do not use own CSS. If something must be styled, use tailwind. Usually it is better to improve our baselib components to allow another styling options that can be selected with enums. Or add custom styles in Class property of our component. This must be hard rule. 

When page has too much informations use dialogs to make it more clear and keep info reachable in some form.

Use treeview to show projects. Use treeview also for processes list, workflows list, etc. Check C:\repositories\CanDoItAll.Economy. It has nice treeview for BUs structure. Something similar we need for clearer grouping/managing of processes workflows, and other larger lists we have across the app. 


Few customers asked us to record simple video presentation. Thats why we need to speed up those visual look changes/improvements. Pay attention to detail. It is more b2b app. It must look proffesional and understandable. 
```
