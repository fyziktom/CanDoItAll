# Implementation Prompt

Implement the current subbundle only. Preserve existing Blazor/component-library patterns, keep process state authoritative in `ProcessesService`/EF/runtime read queries, and use the smallest code changes that satisfy the subbundle acceptance checklist. After each mutation, reload or invalidate the runtime state projection explicitly. Do not add XML documentation comments.
