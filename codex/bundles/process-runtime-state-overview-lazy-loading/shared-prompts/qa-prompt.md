# QA Prompt

Validate count semantics, lazy loading, blocked-run stop behavior, and the real processes page. For UI proof, use a large desktop browser first, capture screenshots if the app is available, and answer: are the active/blocked/failed badges readable, are controls aligned, does the stop action appear only for blocked runs, and does the page avoid visible error UI?
