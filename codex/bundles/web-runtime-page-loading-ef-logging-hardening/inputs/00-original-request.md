# Original Request

User reported that the web app runs again, but still needs hardening:

1. Loading of processes takes very long time, maybe because templates or process info could be lazy loaded only when people open that section.
2. Project structure is much slower now. When adding a node it takes longer before it shows on the canvas.
3. Loading of workflows page takes really long time. The user suspects it always loads all templates even if they are already added.
4. Turn off EF output to console because it messes up output and consumes resources. It should be configurable and default off.

The user explicitly requested using `C:\Users\lucys\.codex\skills\candoitall-bundle-workflow\SKILL.md`.
