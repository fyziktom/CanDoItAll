# Original Request

Date: 2026-05-02

User request:

```text
I asked one agent in project structure to add architecture as new mermaid assets nodes. I found few troubles during that:
1) agent was not able to reach external folder to do analysis of code. It must be possible to select some external folder as workspace for agent too. For example when I have cloned some repo and I need from it to do analysis or changes there.
2) Agent was not adding those mermaids as asset mermaid node. it must know in project structure tool and mcp that if it is adding mermaids it must add them as that specific mermaid asset node. same for other files. 
3) I am not sure if agents has tool to access our storage driver. it should have same default system as tool for project structure or processes. It will default tool for any agent and in agent settings I will just specify if it can read or write. 
4) assure that agents has also standard tools for reading files on drive. it should be some native tool that agent can search, browse and read files.
Use [$candoitall-bundle-workflow](C:\Users\dell\.codex\skills\candoitall-bundle-workflow\SKILL.md) to solve this.
```
