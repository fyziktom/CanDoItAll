# Implementation Prompt

Implement only the selected subbundle from `canvas-workbench-popover-hardening-2026-04-10`.

- Treat the bundle as the contract.
- Keep the change set minimal and inside the shared `CanvasWorkbench` runtime unless the bundle proves otherwise.
- Preserve all current functionality, especially annotation actions and overlay behavior.
- Prefer explicit state cleanup and safe split-file access over broader refactors.
- Update the bundle status, gate rows, and raw-note closure immediately after proof.
