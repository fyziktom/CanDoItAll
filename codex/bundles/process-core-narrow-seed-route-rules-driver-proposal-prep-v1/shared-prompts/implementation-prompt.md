# Implementation Agent Prompt

You are implementing a narrow Process Core seed. Do not broaden the scope.

Move only the route pure-rule/read-model family into `CanDoItAll.Processes.Core`.

Do not move orchestration, EF, storage, workspace, AgentFramework, finalizer application, claim lifecycle, transition execution, or driver API.

Preserve all behavior. Start with failing architecture/source-scan tests before production moves. Keep all subbundle rows explicit in the execution report.
