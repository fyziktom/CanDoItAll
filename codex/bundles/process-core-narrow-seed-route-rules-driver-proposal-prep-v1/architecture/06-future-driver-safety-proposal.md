# Future Driver Safety Proposal

The future helper-driver idea remains valid, but not ready as production code.

A later driver-contract bundle must define:

- permission modes,
- read-only verification boundaries,
- explicit side-effect denials,
- manager vs executor mode,
- audit logging and proof contracts,
- no implicit shell/network/workspace mutation.

This bundle only updates documentation and negative architecture tests.
