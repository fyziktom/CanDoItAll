# QA Prompt

Reject a package-only MAF 1.6 claim unless the bundle explicitly marks it as compatibility-only. Verify real adoption for each selected 1.6 feature. Reject any process artifact validation fix where read-model satisfaction and finalizer validation can still disagree.
