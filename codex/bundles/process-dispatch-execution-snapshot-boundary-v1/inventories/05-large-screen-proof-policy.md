# Large Screen Proof Policy Inventory

Expected browser validation for every subbundle: N/A unless a UI route is touched unexpectedly.

If UI is unexpectedly touched:

- Only use large-screen desktop validation.
- Do not add mobile, small-screen, medium-screen, tablet, Android, or iPhone screenshots.
- Record why UI was touched and why large-screen proof is sufficient.

Final scan must fail if proof artifact paths include forbidden viewport tokens.
