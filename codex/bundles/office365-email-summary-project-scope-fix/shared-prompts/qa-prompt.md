# QA Prompt

Validate that the Office365 category summary workflow can run from a project-structure node in `candoitall_development`, fetch the categorized email, summarize the client request, create a markdown asset below the workflow node, and move the message to the processed category. Confirm no `project-scope-not-provided` or empty-context-pack failure remains.
