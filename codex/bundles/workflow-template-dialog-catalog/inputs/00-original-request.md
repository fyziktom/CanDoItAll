# Original Request

## User Request

> great. i tested to run workflow with email and it worked without trouble. it was also faster than before, so refactoring trully helped. I need you to analyze our workflows tab with templates. we must move that into some dialog. that tab is not good especially in flow of UX. it must be as button on Workflows tab. catalogue of workflows templates then must be as dialog loaded only when someone will open that dialog. otherwise it does not load the templates. In the dialog it must be possible to see basic description and button to show preview. it will open dialog with workflow canvas so they can see how workflows looks like. if they like it they can "add to my drafts". if same workflow is already there, it must name it with some prefix like 01, 02, etc. Use [$imagegen](C:\Users\lucys\.codex\skills\imagegen\SKILL.md) to create UI design proposals for all dialogs separatelly. then use those designs to build the dialogs layouts with use of our components to be close enough (you must validate that with screenshots against proposals).
>
> also small thing. rename workflows that contains SEAMARK. it was specific company from where we had some template offer, but that workflow should be able to do generic analysis of the offer to get summary info. All those templates of workflows should be as kind of generic examples for new users. we should avoid exact names and sensitive informations.
>
> this is larger tasks so use [$candoitall-bundle-workflow](C:\Users\lucys\.codex\skills\candoitall-bundle-workflow\SKILL.md) to solve this as new bundle. you must implement and test it all.

## Additional Active Constraint

- The app is large-screen-only for this request, so UI testing must skip small and medium screen passes.
