# Original Request

> analyze our candoitall project structure mcp. We have kind of difficult system of setup because of those rights. Also there is trouble when I do setup of token and then I switch db in runtime and it has empty settings of project structure mcp token again. we need system of how to copy token between databases. If I create token in one of them, then I can in db management open some db and click to show modal that will offer list of db from where I can take actual project structure mcp token setting. If we are creating new db in ui it must ask if we want to transfer basic settings. It must show checkboxes with options like "project structure mcp token", "ai providers", "ai agents", "processes", etc. It means that this process of transfer of some parts of db must be like generic system because we can use it for different types of settings/records.
