# Actual browser review

Final acceptance: PASS, Chromium 149.0.7827.55, 1600 x 1000. The isolated database/file store and real Web composition used test-only lost-response decorators; no arbitrary endpoint, diagnostic process or chat prompt was invoked. The retained fixture and browser harness are under tools; final receipts/screenshots are under proof/browser/browser-3.

Normal view: primary split agent tree and detail area retained. Header counts and creation actions fit; filters and the real capability card are usable in the first viewport. The endpoint summary and a live agent tooltip are visible without clipping. The selected agent remains authoritative; raw access reason spacing survives recovery.

Details overlay: actual capability metadata dialog is centered, with visible title, typed tabs, editor and footer actions. It closes through its normal UI. No extra scroll owner or wrapper was introduced; existing tree/card panes retain ownership.

Receipt-less diagnostic: the real diagnostic first publishes proof, then loses its response. The warning explains that it may have executed; Verify is disabled until explicit acknowledgement. Acknowledgement performs zero calls; only a subsequent deliberate Verify increases the count. Proof remains Verified.

Curator: a real managed chat is created before its response is lost. Its actual conversation/ready state was inspected. The test then uses the normal Minimize window button to reveal the acknowledgement. The full-width warning and action are readable below the header, whose statistics remain at least 200px wide. Acknowledgement leaves canonical chat identity/session count and launcher count unchanged. The minimized chat remains available. Zero page errors.

Final canonical deltas: one assignment, two explicitly requested diagnostics, one Curator launch and one persistent Curator session. Acknowledgements add none. The task-owned host was gracefully stopped and its PID disappearance recorded; unrelated processes/overlays were not closed.

Negative evidence remains: first-run screenshot review found the warning compressed header statistics, corrected by its own detail row; second-run automation attempted to click through an open floating chat and timed out. This was a harness interaction error, corrected by actual inspection/minimization, without forced clicks or overlay redesign. Final screenshots normal, details and warning-after-inspection were visually inspected.
