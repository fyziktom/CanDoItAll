# Correctness evidence map

Exact expanded FQNs and commands are frozen in [final owning selection](proof/final-owning-selections.json). Case counts below describe this execution, not architectural invariants. Original RED transcripts and intermediate failures are retained separately from final GREEN.

| Requirement | Direct evidence and acceptance |
|---|---|
| A0 | Independent remote/tree and committed predecessor manifest receipt; historical closure unchanged. |
| A1/A8 | Host maps one operation entry and one Curator snapshot to pure immutable presentation; the existing no-service Surface and public host/recovery tests exercise action availability and busy state. Source audit retains every application outcome/retention type in Module; no project graph change. |
| A2 | `AgentCapabilityCorrectnessTests.Accepted_agent_updates_are_monotonic_and_assignment_recovery_finds_the_desired_set` covers equal/backward/future clocks and subsequent desired-set recovery (3 cases). `CapabilityCorrectnessIntegrationTests.Proof_observation_time_does_not_regress_agent_revision_or_assignment_recovery` covers equal/older proof observation using the real file store (2). `Future_agent_revision_survives_real_assignment_commit_and_read_only_recovery` covers committed assignment. Additional deletion and canonical-cleanup tests prove affected-only advancement and idempotent normalization. Existing superseded-publication/concurrency tests prove no rejected revision advancement. |
| A3 | `Escaping_command_failure_finishes_its_own_entry_without_automatic_replay` covers assignment and diagnostic (2); existing `Stale_completion_cannot_remove_a_newer_attempt` retains old-completion fencing. |
| A4 | Receipt-less acknowledgement component test proves retained warning, explicit action and zero replay; receipt-backed test denies abandonment. `Old_diagnostic_acknowledgement_cannot_release_a_newer_attempt_or_an_assignment` protects exact attempt/type ownership. Real browser loses a diagnostic response after canonical proof publication and acknowledges without another diagnostic. |
| A5 | Public Curator tests cover reconstruction, acknowledgement, previous known chat preservation, and stale acknowledgement against newer pending/unknown attempts. Real browser inspects the actually created managed chat, then proves unchanged session identity/count and launcher count after acknowledgement. Acknowledgement returns admission to Ready; it does not label the unknown attempt Opened. |
| A6 | `Final_provider_capture_receives_current_catalog_content_and_its_current_revision` directly observes the public snapshot argument inside the final write callback. Its valid RED is current content revision 4 paired with supplied revision 3. |
| A6a | `Unchanged_provider_lease_preserves_fingerprint_and_allows_proof_publication` proves an unchanged real provider can publish proof. Earlier A6 witnesses stopped before the callback because JSON copying lost the readonly fingerprint. Those are retained as masked/setup evidence, not direct A6 proof. |
| A7 | Real store/provider-source outage and missing-source tests prove no diagnostic dispatch. `CapabilityWorkspaceResolutionTests` proves outer profile resolution returns typed unavailable. API tests cover 400 input versus 409 unavailable, target identity, automaticReplaySafe=false, sanitized body, success ApiAck and OpenAPI 200/400/409. |
| A9 | Direct builds; current owning discovery/execution; named stable checkpoint for shared normalization; actual browser/DOM/screenshot inspection; complete-source portability/secrets and governed manifests. |

## Negative evidence integrity

Initial RED: Unit 5 failures; Components 3 failures plus the already-safe receipt-backed case passing; Integration 7 failures. The intermediate integration run deliberately retained A6 pairing and outer resolution defects after the lease fix, producing the direct 4-versus-3 witness and thrown profile-resolution failure. Separate missing-source and two additional revision-producer tests failed before correction.

Discovery guards prevented execution when a rendering selection was expected as 11 but actually contained 22 cases, and when the stable parser initially excluded Memory namespaces. These are harness setup corrections, not semantic RED. The initial browser's Curator warning was placed in the three-column header; screenshot review found compressed statistics. The warning now occupies a separate detail row, with geometry and screenshot verification.
