# Bounded implementation and validation

1. Record entry and committed predecessor manifests. Freeze exact test names and discovery before RED execution.
2. Add direct factory/recovery and real file-store proof tests; escaping command liveness; public receipt-less and Curator acknowledgement UI; current-revision provider callback; unavailable infrastructure/API mapping. Keep setup failures separate from semantic RED.
3. Implement common monotonic revision, coordinator integrity and explicit acknowledgement, exact provider snapshot revision and typed unavailable outcome. Introduce pure presentation records in place before moving any source. Correct Capabilities-03 ownership/requirements/move inventory.
4. Directly build Core, broad Components, Module, Web and changed production dependencies before test builds/listing. Run exact new Unit, Components and Integration/API selections plus affected Capabilities-01/02 selections and relevant existing agent configuration/provisioning tests. No fixed test-count architecture invariant.
5. Browser at 1600 x 1000 verifies acknowledgement, no automatic replay, normal assignment/proof/details composition. Run portability, docs, complete-source/retained-evidence secret scans and manifest/link validation. Record historical documentation debt separately.
6. Close with semantic test map and compact receipts (LF JSON/Markdown, gzip original transcripts); preserve historical proof. Then execute corrected Capabilities-03 SB00 before any movement.

No schema or project graph changes occur in 02G. The completed source audit selected the broad checkpoint below because canonical normalization is shared.

Named broad-gate invalidation: the proven A2 cleanup defect requires changing shared catalog normalization, which can run for all workspace consumers. After affected gates pass, run one frozen full stable checkpoint and the owning seed/catalog cases. This breadth is justified by the shared canonical revision contract, not by bundle size.
