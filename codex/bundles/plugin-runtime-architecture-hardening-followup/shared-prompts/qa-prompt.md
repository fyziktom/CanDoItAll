# Shared QA Prompt

```text
Review the completed subbundle against C:\repositories\CanDoItAll\codex\bundles\plugin-runtime-architecture-hardening-followup.

Focus on correctness, maintainability, generic plugin boundaries, secret redaction, EF/performance behavior, and whether the captured proof actually validates the requirement. Start with findings. Use exact file/line references. Confirm the execution report was updated and that downstream progression gates are honest.

For UI subbundles, inspect the browser screenshots and repeat the browser flow if the screenshot does not prove the required state. For package subbundles, prove real package assembly activation, not manifest-only behavior.
```
