# Frozen validation plan

Initial red selection is frozen before source edits: ProviderVerificationHardeningTests (4 executable cases: two sync facts plus two terminal theory rows) and SharedProviderDeliveryHardeningTests (2 facts: different imported postcondition and callback-failure retry). First build the fresh Unit/Components assemblies, list exact classes, verify 4 + 2, then execute. Additional exact names follow A6 in inputs/request.txt and will be mapped to executable methods before each new filter is run.

Owning release selections start with the exact historical 02D filters (inventory/owning-plan.json), freshly discovered and built: expected baseline 426 Unit, 96 Components, 275 Integration plus successor cases. No historical pass is reused as current execution. The module-owned delivery contract is consumed by provider workspace, thinking/refresh UI and agent editor: include their rendering/lifetime tests and real registry/source/API fixtures at one named cross-assembly provider checkpoint. No schema/API/global result or root DI change is planned; no unfiltered solution test gate.

Direct production builds: ProviderManagement, module, Web. Architecture project/reference comparison before/after; complete proposed-source portability and secret comparison; documentation debt adjudication against entry HEAD. Browser acceptance uses isolated fixture at 1600x1000 and real UI controls: unknown verification; known commit callback failure; reconstruction delivery retry without writes; normal save, sharing and import. No operator DB.

E00/E01 are Governed behavior changes. Proof stores one compressed original TRX per run and compact command/discovery/result records; no tracked .log. E02 must satisfy every A1-A7 requirement before catalog H00.
