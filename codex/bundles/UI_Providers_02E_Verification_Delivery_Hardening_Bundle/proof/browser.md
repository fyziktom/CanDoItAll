# Fresh provider browser acceptance

Executed 2026-09-06 on the Release Web production composition at 1600x1000. Task fixture used a separate PostgreSQL database, isolated profile/vault roots, a loopback synthetic remote catalog, production registry/reconciliation/adapters, and task-only response-loss / one-shot parent-catalog-read fault decorators. No operator database or upstream provider was used. Fixture source hashes are retained in validation.json; private vault/configuration files are not proof artifacts.

- Existing local draft: name `P02E Unsaved local name`, notes `P02E retained notes`, raw suggested models `first\n\n second \n first`, Runtime selected. Source Test committed, parent catalog read deliberately failed. The overlay displayed resolved-write / delivery-pending feedback. Close/reopen, Retry source delivery: source write count stayed 1 -> 1. Raw values and the same textarea DOM node survived. Then Discover/Apply imported one unrelated provider; the local draft/raw text remained.
- Local publication: Publish committed, parent catalog reconciliation failed. Switch Runtime -> Sharing to recreate the child, then Retry: publication write count stayed 1 -> 1; authoritative Published and Unpublish rendered. Normal Unpublish confirmation retained permanent public identity and rendered unpublished state.
- Local first save: task response loss produced an unconfirmed New attempt with one actual row. A later name edit survived Verify provider write; row identity stayed 47bad381-7950-481b-992d-0ca8e545cdc0 and its write count stayed 1 -> 1. A deliberate subsequent Save updated that same row. A response-lost Publish then verified current published state without another write (counter 3 -> 3).
- Imported provider Connection fields were disabled; Sharing rendered local alias/enabled controls and authoritative available/selected status. Another source Test with parent failure showed the same pending delivery UI and recovered through explicit Retry.
- Inspected four JPEGs: target delivery pending, source overlay delivery pending, local verified later edit, imported authoritative state. Existing desktop layout, explicit warning/retry controls and scroll owners remained readable. Source dialog displays both a pending-state alert and the existing error/retry region; this is redundant feedback but neither hides the pending state nor replays a write.

Browser automation calibration retained in this record: waiting for Sync before any imports timed out (the action is conditional); initial Unpublish wait omitted its confirmation and timed out. Subsequent steps used the real rendered Discover/Apply and confirmation controls. These were harness selector/sequence errors, not passing application trials.

This acceptance used Playwright MCP for behavior only. It is not a timing measurement and contributes no samples to the separately gated direct catalog benchmark. Lower-level tests prove wrong-state, failure, stale, disposal and simultaneous delivery cases deterministically; browser evidence does not pretend to cover those races exhaustively.
