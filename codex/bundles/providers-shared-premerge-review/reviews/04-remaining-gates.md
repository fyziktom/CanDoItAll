# Remaining closure actions

These are the remaining execution gates, not completed proof. The product repairs, affected tests, documentation and source skill edits are reviewable in the current worktrees. No commit, push or merge is requested.

## Canonical OpenAPI capture and skill synchronization

The canonical 5032 listener observed during execution is the user's normal Release Web process, PID35584, started before the repairs; its parent is dotnet PID6112. Revalidate both ownership and command before any stop. Do not kill a different process based on these historical PIDs.

The concrete remaining action is a brief interruption of that app, an identified final Release host at localhost:5032 using an isolated disposable profile, capture of both canonical document endpoints, and restoration of the user's original app invocation. Do not run migrations or inference against the user's profile. Preserve the current uncommitted source identity with explicit pre-commit provenance rather than creating a commit.

scripts/Export-ApiContract.ps1 is prepared and syntax/negative-guard checked. It rejects an unrelated process, a changed assembly, nonexclusive port ownership, unapproved dirty capture, different endpoint bytes, noncanonical server URL and a sharing operation count other than five. Supply the actual new process ID and reviewed Web assembly hash, plus -AllowPrecommit only after that capture authority is resolved.

After a successful capture, inspect all actual route/operation/schema counts and drift, update the SharedInfo support README, and run Test-CanDoItAllWebOpenApi.ps1, Test-SharedInfo.ps1 and current skill validation. Install only these previewed packages through the SharedInfo installer: _candoitall-api-shared, candoitall-api-shared-providers, candoitall-api-agents, candoitall-api-llm-chats, candoitall-api-workflows. Use the required installed-path approval, then reopen/hash active files against source. Existing active copies and the August 18 snapshot must not be called current.

## Original three-application gate

Original bundle STATUS.md and plan/03-historical-handoff.md preserve seven failed governed lifecycles and seven application image builds against an exhausted budget. The specific outstanding authority is one replacement SB07 lifecycle and one application image build, with cumulative nine/nine ceilings reserving one lifecycle/build for SB12.

The owned runner is repo://tools/SharedProviders/Run-SharedProviderE2E.ps1. Its topology is one central application and two independent clients with deterministic upstream fixtures. Read its original SB07 selection/authority and current source identity before execution. No retry, paid inference, unlimited Docker run or substitution of the separate two-instance lane is included. Preserve the historical ledger and attach the actual new result.

## Independent implementation review

Preparation reviewers did not inspect these repairs. The current verifier-review.md is explicitly the implementing agent's adversarial audit plus CodeAnalytics. A separate human or authorized review agent must assess the final diff, actual production paths, failing-first/passing proof, source/artifact hashes and all exclusions. Do not relabel self-review as independent review.

## Final reconciliation

The single frozen Stable outcome is recorded: 9,424/9,424 passed, no failures/skips, with every deferred theory expansion reconciled against its source. After the remaining authorized actions, synchronize source/export/active hashes, update all status and traceability rows, and rerun the completed-stage structural validator plus semantic closure review. Structural validity alone does not establish merge readiness.

The reason to request the remaining authority is the preserved bundle-execution boundary and original exhausted host-test budget, together with interrupting a user-owned app and an explicitly required pre-commit capture exception. These requirements are not waived by otherwise passing local tests.
