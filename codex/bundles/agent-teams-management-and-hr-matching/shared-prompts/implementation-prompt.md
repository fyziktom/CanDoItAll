# Implementation Prompt

Implement the current subbundle only.

- Read the subbundle README and this bundle's `analysis`, `architecture`, and `traceability` files before editing.
- Keep AgentFramework teams in the AgentFramework catalog/service boundary.
- Reuse BaseLib `TreeView`, `Grid`, `Stack`, `Split`, `Cluster`, and existing `AgentSelectionCard` before adding custom layout markup.
- Preserve current agent creation, chat switching, CRM-HR projection, and launch planning behavior.
- Capture the proof required by the subbundle and update `reviews/01-execution-report.md`.
- Stop and repair the bundle if implementation reality forces a scope or dependency change.
