SB18 final security red-team, 2026-07-13

Direct test coverage retained and rerun in the final affected/security lanes:
- current semantic binding and current occurrence re-resolution.
- outside-root, forged, cross-context, wrong-operation, revoked, expired, removed-source, and authorization-revision denial.
- handle capacity, actor/context isolation, content/read intent, and save/overwrite intent.
- zero-browser known content and zero-browser known occurrence.
- save conflict, failure, cancellation, sequential revision, post-persistence revision, and overwrite denial.
- masked log state for secret, endpoint, handle, and implementation detail.
- cache Disabled zero-interaction; runtime/scope/query/revision isolation; TTL/eviction; payload/continuation bounds; failed/cancelled non-retention; stale-listing non-authority.
- remote cancellation, capability honesty, bounded streaming, pooled HttpClient reuse, and secret/endpoint masking.
- stable resource identity only; no opaque handle/display-path/metadata authority; promotion failure/cancel rollback; forged/stale/cross-actor/wrong-storage denial.
- profile isolation, traversal denial, unsigned preview/download denial, current handle content, authorization requirement, save/conflict, and real promotion/reopen.
- inert hostile Markdown/SVG/unknown/oversize behavior and strict Mermaid HTML-label hardening.

Final live negative:
- GET /storage/objects/preview?token=forged => 401.
- final Project Structure PDF resource entries contain no /storage/objects/preview request.
- final PDF uses an in-memory blob URL issued from already-authorized bytes; one FileInteraction root and zero FileBrowser roots remain after handoff.

Result: Pass.

