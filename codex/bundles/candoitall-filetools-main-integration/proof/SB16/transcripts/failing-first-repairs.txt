SB16 failing-first and browser-driven repair record, 2026-07-13
Working directory: C:\repositories\CanDoItAll
Run label: SB16-managed-browser-failing-first-and-repair.

Playwright operations were executed against the managed PublishedDll runtime. The exact selectors used for the mutation defect were `[data-testid="interaction-save"]`, `[data-testid="interaction-save-status"]`, `[data-testid="interaction-conflict-rebase"]`, and `[data-testid="interaction-conflict-overwrite"]`. The failing runtime returned exact statuses `Save conflict` and then `Save failed`; the repaired runtime returned `Save conflict` and then `Saved`. Playwright tool calls completed successfully; the deliberately awaited `Saved` checks on the failing runtime timed out, which is the recorded failing-first signal.

1. Legacy canvas thumbnail authority bypass
- Failure: real canvas requested the old unsigned /storage/objects/preview route and received 401.
- Repair: removed media-presentation and route derivation from ProjectStructureGraphAdapter; canvas now keeps metadata and emits an empty MediaPreviewUrl.
- Passing evidence: graph test rejects managed route projection; final browser/network has no thumbnail request or 401.

2. Shared overlay width collapse
- Failure: the overlay's outer shared Stack inherited grid/justify behavior and collapsed the FileInteraction body to roughly half width.
- Repair: outer Stack now explicitly owns width 100%, a minmax(0,1fr) grid column, and stretch alignment/justification.
- Passing evidence: 1900 dialog/body/interaction widths 1152/1115/1073; 1440 final widths 1150/1115/1071 with no overflow.

3. Managed Mermaid diverted to legacy viewer
- Failure: managed Mermaid attachment still matched the notes-only HasMermaidViewer path.
- Repair: HasMermaidViewer excludes nodes that can render the governed attachment interaction; managed Mermaid reaches direct FileInteraction.
- Passing evidence: browser direct interaction uses workbench-mermaid and shared Mermaid wrapper.

4. Mermaid HTML labels not fully disabled
- Failure: Components wrapper set only flowchart.htmlLabels=false; the current Mermaid runtime also honored the root htmlLabels setting and produced foreignObject.
- Repair: Components 0.1.3 sets htmlLabels at root and flowchart; tests link and inspect the shipped JavaScript asset.
- Passing evidence: Components hardening 3/3; browser foreignObject 0, text elements 5, scripts 0.

5. Conflict rebase and second save failed
- Failure: stale save correctly reached Save conflict, but both a normal later revision and Retry against current revision failed because AuthorizedFileSaveTarget compared the requested revision to the immutable handle issuance revision before calling storage.
- Repair: removed the redundant issuance-revision comparison. Exact handle/context/operation checks remain, overwrite still requires explicit permission, and the revisioned storage driver compares current storage truth.
- Passing evidence: new SB16_SequentialSaves_AcceptPersistedRevisionBeyondGrantSnapshot test; two-tab stale first attempt conflicts; retry against current revision saves 80 bytes and reaches revision 4; API bytes/hash match.

6. Overwrite policy negative
- Failure proof: Retry without revision was denied because the session lacked Overwrite permission.
- Result: expected safety behavior; local text remained and status became Save failed. No fallback or silent overwrite was added.

Passing regression command:
dotnet test tests/Unit/CanDoItAll.Tests.Unit/CanDoItAll.Tests.Unit.csproj -c Release --no-restore --filter "FullyQualifiedName~FileAccessAuthorizationTests.SB16_SequentialSaves_AcceptPersistedRevisionBeyondGrantSnapshot" -warnaserror
Result: passed 1, failed 0. Exit code: 0.

Artifact exits:
- Screenshot captures and final browser DOM/network/console queries: tool success.
- Public content API GET, lease release POST, and temporary project DELETE: HTTP 200.
- Final source no-route and anti-stub scans: expected no-match; no product fallback added.
