Run label: SB40 independent architecture/security fake-proof review
Captured: 2026-07-12
Working directory: bundle://

Command: verifier review of SB35-SB40 manifests, semantic contracts, production source, test transcripts, browser screenshots, live process proof, and CodeAnalytics dependencies
Exit code: 0

Invariant IDs reviewed:
  SB35-ARCH-READY
  SB36-SELECTION-OWNERSHIP
  SB37-AGENT-MEMORY-ROUTING
  SB38-TRANSPORT-WORKER-INTEGRITY
  SB39-EXTERNAL-ISOLATION
  SB40-INV-01
  SB40-INV-02
  SB40-INV-03
  SB40-INV-04

Fake-proof checks:
  Rejected UI-only settings proof. The final real-seam test drives MemoryAgentContextContributor through the real handler, production mock driver, and persisted ledger.
  Rejected registry-order proof. Automatic and explicit routing use authorized stable bindings; unknown alias proves zero dispatch.
  Rejected directive-description proof. The explicit seam test proves the provider query is sanitized.
  Rejected fabricated context packs. Provider execution and completed ledger rows are observed in one test lifecycle.
  Rejected in-process external-only proof. The live test uses a separately launched authenticated service and the real main NativeRemote driver.
  Rejected post-materialization response limits. HTTP declared/chunked and MCP result caps execute before response mapping.
  Rejected provider-controlled feedback correlation. The host validates and replaces the provider handle.
  Rejected single-process worker claims. PostgreSQL phase leases now provide cross-replica owner/token fencing; InMemory remains explicitly process-local.
  Rejected optimistic mutation capability claims. Ingestion delivery, feedback delivery, events, and cancellation remain hidden or typed unsupported where no unique driver/lifecycle exists.
  Discovered and rejected a parallel legacy Mem0 catalog/runtime bypass that could attach Microsoft.Agents.AI.Mem0 outside the generic provider bindings. The package, template, configuration DTO, attachment path, and implicit workspace-memory injection were retired; the final production src scan for Microsoft.Agents.AI.Mem0, Mem0Provider, AddMemoryProviderAsync, mem0-shared-memory, and MemoryCapabilityConfiguration returns zero matches.

Negative proof:
  SB35/SB36 preserved non-zero characterization for implicit fallback and foreign-owner operation access.
  Current suites cover disabled/no-directive, duplicate/quoted/unknown aliases, foreign owner, wrong project/credential, response oversize, malformed/unsupported response, and unavailable/unsupported provider with no unauthorized dispatch.
  Retired catalog-memory capabilities are rejected or filtered by the runtime, catalog, and seed-normalization paths; exact changed integration tests pass 2/2 and focused runtime/template/cleanup unit tests pass 51/51.
  A broader seed-instruction test class has 4 pre-existing stale assertion failures. This review does not describe that unrelated class as clean and does not use it as retirement evidence.

Residual observations accepted as non-blocking:
  The AgentFramework.Memory root/Context/Tools namespace SCC discovered during final audit was repaired by moving the contributor, tool provider, and DI extension to their owning namespaces. Refreshed CodeAnalytics confirms it is gone.
  The remaining MemorySourceSnapshotCursor/cursor-exception same-project type cycle does not create a project-reference cycle or boundary reversal.
  External Persistence has one same-assembly module cycle.
  The retained legacy main CognitiveMemory module remains test/migration-only and is excluded from base composition.
  At-least-once replay after catastrophic lease expiry requires provider idempotency.
  Source-ingestion delivery and mutation drivers are future capabilities and are not advertised as working agent features.
  The Components MCP catalog transport remained unavailable; direct BaseLib component use, component tests, and desktop/narrow Playwright proof cover the functional UI gate.
  The 4 stale broad seed-instruction assertions remain owned by the Agent Framework seed-test maintainers; they must be rebaselined before that broad class can be called green. They do not reopen the exact 2/2 retirement integration proof or the zero-match production scan.
  Obsolete AgentMemoryRecord compatibility APIs remain but are not injected. Agent Framework runtime/catalog maintainers own deletion after migration consumers are confirmed absent; until then a guard must prevent reconnection as a runtime memory path.
  The editor file-backed save/read path lacks one joined persistence-to-runtime dispatch test. Agent Framework editor/persistence test maintainers own a test that saves bindings/mode, reloads the agent, and proves ordered dispatch.
  Custom transports beyond HTTP, MCP, NativeRemote, and the deterministic mock test driver need an explicit IMemoryProviderDriver adapter. Memory integration maintainers/provider authors own adapter, response-cap, failure, and lifecycle proof; unsupported kinds continue to fail explicitly.
  Picker health is stored rather than live-probed. Memory UI/provider-health maintainers own a bounded refresh with freshness metadata and healthy/degraded/stale/unavailable UI proof; runtime authorization/selection remains fail-closed meanwhile.

Decision: PASS WITH EXPLICIT NON-BLOCKING FOLLOW-UPS. No active legacy Mem0 bypass remains, and no remaining observation permits implicit provider fallback, cross-owner access, raw credential storage, cross-project external data access, or base-host native coupling.
