Run label: SB40 final real-host browser validation
Captured: 2026-07-12
Working directory: repo://

Command: dotnet test tests/Playwright/CanDoItAll.Tests.Playwright/CanDoItAll.Tests.Playwright.csproj --no-restore --nologo --filter 'FullyQualifiedName~MemoryProviderManagementPlaywrightTests|FullyQualifiedName~AgentMemoryProviderSettingsPlaywrightTests' -v:minimal
Exit code: 0
Result: 5 passed, 0 failed, 0 skipped.
Elapsed: 1 minute 5 seconds.

Routes:
  /memory
  /agents

Viewports:
  desktop 1440x1000
  narrow 390x900

Memory route assertions:
  zero-provider state rendered without implicit provider dispatch;
  two demo providers could be added;
  healthy/degraded and unique-driver gating controlled executable selection;
  synchronous query returned the deterministic provider context pack and operation correlation;
  ingestion, feedback, and cancellation actions were hidden when the provider did not support them;
  RCL and iframe surfaces rendered;
  unsafe iframe URL was rejected and the safe persisted URL was retained.

Agent route assertions:
  the picker showed only enabled, healthy, uniquely-driven synchronous providers;
  two healthy providers were bound as business-memory and programming-memory;
  Optional and Required behavior plus order persisted in the editor;
  ExplicitDirective disabled memory tools;
  the binding list had no horizontal overflow at 390 pixels;
  every Up, Down, and Remove action remained inside the narrow viewport;
  the final Remove action received a Playwright trial click.

Screenshots:
  bundle://proof/regression/screenshots/agent-memory-multiple-providers-explicit-desktop.png
  bundle://proof/regression/screenshots/agent-memory-multiple-providers-explicit-mobile.png
  bundle://proof/regression/screenshots/memory-ui-zero-provider-desktop.png
  bundle://proof/regression/screenshots/memory-ui-zero-provider-mobile.png
  bundle://proof/regression/screenshots/memory-ui-query-context-pack-desktop.png
  bundle://proof/regression/screenshots/memory-ui-unsupported-mutations-desktop.png
  bundle://proof/regression/screenshots/memory-ui-provider-rcl-iframe-desktop.png

Visual review:
  desktop hierarchy, labels, provider provenance, and validation state were readable;
  narrow cards stacked without lateral clipping;
  binding actions remained reachable;
  no raw settings JSON or credential value was exposed;
  unsupported mutation surfaces were absent rather than presented as partially working actions.

Proof boundary:
  browser validation exercised real hosted UI and production composition with deterministic providers.
  Authenticated external-service interoperability was proven separately at the process seam in terminal-validation.txt; this transcript does not claim that the external credential failure/success pair was repeated through browser network traffic.

