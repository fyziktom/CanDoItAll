# SB06 proof artifacts

State: `PASS`

This directory contains governed proof for shared-provider effective profile projection, hardened
runtime dispatch through the existing OpenAI-compatible path, personal/shared coexistence, explicit
selection, strict publication-model binding, per-request access-context propagation, safe failure
disclosure, cancellation, source-managed audio denial, and no fallback.

- `proof-manifest.json` records the exact frozen 18/16/10 selections and supporting focused lanes.
- `manifest.md`, `semantic-invariants.md`, and `semantic-changed-files.md` describe the durable
  behavior and focused SB06 delta.
- `architecture/` records before/after references, before/after CodeAnalytics, public-surface review,
  and honest independent cross-review chronology. Final architecture and security re-reviews pass
  with no P1/P2 blockers. `architecture/sb04-downstream-invalidation-release-revalidation.md`
  records the later genuine Release 18/16/10 trust restoration after SB07 changed named SB04 relay
  semantics.
- `behavior/` records runtime projection, model binding, and hybrid-selection semantics.
- `security/` records typed credential containment, context isolation, failure disclosure, and
  cancellation behavior.
- `transcripts/` preserves builds, exact discovery/runs, supporting invalidation, and superseded
  attempts for honest chronology. The authoritative downstream-revalidation transcripts are the
  `sb06-revalidate-*-release.txt` files, backed by the current SB04 Unit/Integration Release build
  transcripts; earlier SB06 build and supporting-lane Debug transcripts remain chronology only.

No broad, browser, Playwright, multi-instance, live-provider, paid-provider, or UI lane ran. The
single broad aggregate remains SB12-owned. `changed-files.md`, `hashes.sha256`, and the closure
validator transcript complete the governed SB06 proof package.
