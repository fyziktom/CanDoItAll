# SB04 proof artifacts

State: `COMPLETE`

This directory contains the governed proof for bounded OpenAI-compatible relay, exact
surface-specific field policy, incremental SSE, client-owned tools, Base64-only image execution,
operation-aware metadata-only audit/usage, hosted stale-row recovery, and security containment.

- `proof-manifest.json` is the machine-readable result and exact 24/22/12 selection record.
- `manifest.md`, `changed-files.md`, and `hashes.sha256` inventory the durable evidence.
- `architecture/` records ProjectReference/CodeAnalytics before and after, the semantic-reopen
  chronology, public-surface/ABI review, independent review, and the SB02 invalidation link.
- `behavior/` records the frozen supported/denied subset and passing relay/stream/image behavior.
- `security/` records no-open-proxy, header, error, secret, content, and access-context containment.
- `transcripts/` preserves entry, failing-first, build, discovery, run, scan, hygiene, and closure
  commands, including superseded attempts required for honest chronology.

The original failing-first compile transcript predates later semantic repairs and is not claimed
as their red source. Final authority comes from the post-repair exact 24/22/12 runs, supporting
usage 7/7, fresh SB02 18/14/6 plus EF no-drift, final independent PASS, and the force-refreshed
CodeAnalytics snapshot.

The August 25 named wire-contract reopen keeps those frozen counts and adds current Release
24/22/12 evidence for explicit Responses non-retention (`store:false`), operation/model mismatch,
structured-output narrowing, and deterministic cancellation. Its architecture and security
reviews are `architecture/august-25-contract-revalidation.md` and
`security/august-25-contract-revalidation.md`.

No credential, prompt/response content, tool argument, image output, private endpoint, or raw
upstream error is stored as proof.
