# Risk-to-proof map

| Risk | Preventive design | Required negative proof | Final artifact |
| --- | --- | --- | --- |
| R-001 secret leak | sanitized catalog mapper | serialization/log/database scan | SB03/SB04/SB05 scans plus actual named-client logging prove no credential, private endpoint, raw upstream error, access context, subject, prompt/response/image content, request/response body, or secret value crosses catalog/relay/source persistence or logs; SB07 retains container confirmation |
| R-002 open proxy | routing ID + adapter registry | caller URI/header/model tampering | SB04 exact policy/compatibility proof passes: current persisted publication/model/secret state selects one of five production adapter rows, while caller URI, header, upstream-model, unknown route, purpose, and operation overrides fail before upstream dispatch |
| R-003 tool semantics | function-tool relay | built-in tool rejected; function call round-trip | SB04 proves bounded function definitions/tool choice/calls round-trip without central execution and rejects built-in, hosted, MCP, web/file search, code-interpreter, computer-use, and undeclared tool forms; SB07 retains real client execution E2E |
| R-004 runtime branch spread | OpenAI effective projection | dependency/switch guardrail | SB06 maps the shared connector origin once in the outer adapter to the existing `ProviderKind.OpenAi` raw/MAF runtime. No `ProviderKind.Shared`, shared `IProviderAdapter`, second execution engine, or inner connector branch exists; CP04/SB06 remains `PASS` after the August 25 genuine Release 18/16/10 downstream revalidation |
| R-005 cycle | lower abstractions + outer composition | CodeAnalytics no cycle | Final force-refreshed snapshot `snap-20260825100508-300644c7` reports 14 scoped projects, 766 source documents, 35 modules, 5,281 dependency facts, 34 direct product references, zero project cycles, and zero error findings; the governed two module/one nested-type cycles are unchanged and the final architecture review passes |
| R-006 token duplication | source-owned secret reference | persistence scan and source edit invariant | Real SB05 multi-import sync stores/resolves one source secret-record ID, source edit propagates only the reference, and scans reject credential values from persistence/logs |
| R-007 destructive sync | availability state machine | outage/unpublish preserve local ID | SB05 real HTTP/PostgreSQL proof distinguishes transient/auth/contract/trust failures from authoritative absence, preserves stable rows/IDs, retires without deleting, and reuses identity on recovery/reappearance |
| R-008 route collision | publication-namespaced ID | duplicate upstream model scenario | SB01 routing 10/10, SB03 canonical projection/models API, and SB04 policy/dispatch proof preserve distinct publication-namespaced IDs and exact persisted upstream models for duplicate names; SB07 retains real multi-app E2E |
| R-009 silent fallback | explicit availability gate | central down while personal exists | SB06's freshly revalidated Release 10/10 hybrid proof shows an explicitly selected unavailable shared profile fails even when a usable personal profile exists. Shared profiles are excluded from voice options, an explicitly persisted ineligible shared voice ID stays empty instead of selecting a personal provider, and typed audio denial occurs before credential/HTTP dispatch; SB07 retains real multi-host proof |
| R-010 advanced feature egress | allowlisted fields/tool types | persistence/background/web/file/MCP denied | SB04 exact Chat/Responses shapes reject cross-surface tools, choices, schemas, text/images, unknown siblings, unsupported parallel fields, persistence-enabling or malformed `store` values, background/conversation/include/user/modalities, hosted tools, caller endpoints, invalid Base64/detail, and explicit malformed image counts; omitted Responses `store` is canonicalized to `false` and only explicit JSON `false` is accepted |
| R-011 stream failure | streaming session/cancellation | first-chunk timing and disconnect | SB04 exact 12/12 real-host/dispatcher proof covers Chat/Responses first chunk, ordered terminal `[DONE]`, split UTF-8, usage, bounded malformed/oversized frames, downstream cancellation/upstream disposal, idle timeout, and midstream failure with no synthetic success; SB07 retains multi-app E2E |
| R-012 context trusted | auth/access separation | forged context cannot authorize | SB01 access 10/10, SB03 authorization 10/10, and SB04 invoke-scope/malformed-token tests prove the opaque header cannot replace authentication; SB04 records it only as metadata and omits it from upstream shape, while real multi-hop proof remains SB07 |
| R-013 context upstream leak | header allowlist | upstream capture lacks header | SB04 deterministic upstream capture proves access context, subject, authorization metadata, correlation metadata, caller headers, cookies, and private headers are absent; SB07 retains the real multi-hop/container assertion |
| R-014 usage false/double | one invocation record/completeness | missing usage remains unavailable | SB04 enforces operation-disjoint tokens versus positive ImageCount across contracts, EF/migration/snapshot, projection, and aggregation; missing stays unavailable, mixed/non-positive values fail, ABI is preserved, and fresh SB02 18/14/6 plus usage 7/7 and EF no-drift restore trust |
| R-015 SSRF | URI/network policy | userinfo/redirect/private policy/DNS cases | SB05 exact 18/18 plus real named-handler proof rejects unsafe syntax/schemes, public plain HTTP, unapproved private/special/mixed destinations and rebinding; redirects/proxy/cookies are disabled and TLS validation remains intact |
| R-016 migration drift | module configs + migration gate | clean DB migrate/model validation | After SB04's ImageCount schema change, fresh SB02 PostgreSQL 14/14, deletion/reference 6/6, state 18/18, and EF no-pending-model prove aligned configuration/migration/designer/snapshot. Existing migration amendment assumes it has not reached a durable database; SB12 final gate remains |
| R-017 in-process false confidence | 3-app real HTTP | full Docker scenario | SB07 |
| R-018 remote field mutation | service-side ownership | forged UI/service update denied | SB08/SB09 |
| R-019 OpenAPI overclaim | exact operation subset | no audio/management routes | SB03 discovery metadata plus SB04 marker-scoped OpenAPI proof expose exactly Chat, Responses, and Images POST routes and no audio/management/ETag claim on inference; SB11 retains final security-scheme, full export, SharedInfo, and operation-absence proof |
| R-020 test overspend | machine budget | command audit/broad count <= 1 | SB01 12/10/10, SB02 18/14/6, SB03 18/14/10, SB04 24/22/12, SB05 18/22/16, and the freshly revalidated Release SB06 18/16/10 exact filters pass; none ran a broad gate and SB12 retains the single aggregate. SB07 has exhausted and exceeded the Docker lifecycle/image-build ceiling with seven failed lifecycle attempts and seven image builds, so further Docker work is blocked pending explicit one-lane/one-build authority and a durable budget amendment |
| R-021 paid dependency | deterministic upstream | external network denied | SB04/SB05 use deterministic HTTP/DNS/socket fixtures, local hosts, and real PostgreSQL. SB06's freshly revalidated Release 18/16/10 proof uses deterministic local HTTP and real PostgreSQL with no paid/live provider or external network; SB07/SB12 retain container/E2E determinism |
| R-022 cleanup too early | final no-down contract | healthy running status after closure | SB12 handoff |
