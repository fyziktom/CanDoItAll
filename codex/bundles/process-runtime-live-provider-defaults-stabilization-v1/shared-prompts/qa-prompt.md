# QA Prompt

Verify that deterministic representative processes still launch and complete, live OpenAI uses managed provider profiles, provider failures are precisely classified, and no Process Core/runtime-driver boundaries drift. Treat skipped live tests as not proof.
