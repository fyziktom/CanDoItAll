# Original User Request

codex completed the previous bundle. The user asked to verify whether processes work like before. They explicitly asked to check that agents/processes still use the CanDoItAll default providers through MAF, because the app has default providers and the previous live test required many env variables. The user instructed Codex to use `5.4mini` and larger max tokens, but the review found that OpenAI rejected `5.4-mini` with `model_not_found`.

The next bundle must stabilize process runtime functionality before any further Process Runtime Core extraction.
