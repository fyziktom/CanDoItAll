# QA Prompt

Validate the repair with targeted integration tests around `TransitionStepAsync`, source assertions for finalizer-to-transition context forwarding, anti-stub search, Blazor process template governance tests, and a liveness check for the running local web app. Treat any manual stale-lineage success as a release blocker.

