# Validation

- git diff --check
- repository hygiene/static source-contract tests only
- no full build or full suite unless anchor drift changes source ownership

Run affected scopes only unless this checkpoint explicitly authorizes a full suite. Record exact commands and counts.
