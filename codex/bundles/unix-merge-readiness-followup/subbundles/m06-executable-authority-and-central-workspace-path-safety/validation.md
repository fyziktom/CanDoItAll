# Validation

- Unix file executable by other user only/current user denied
- PATHEXT malformed/path-like/excessive entries
- workspace symlink/reparse link to outside root
- legitimate contained missing leaf and existing paths
- focused path/storage/executable/runtime tests only

Run affected scopes only unless this checkpoint explicitly authorizes a full suite. Record exact commands and counts.
