# Validation

- one full stable per Windows/Linux
- runtime catalog both
- headless two-cycle
- Docker smoke
- migration fixtures
- redaction/static

Run affected scopes only unless this checkpoint explicitly authorizes a full suite. Record exact commands and counts.
