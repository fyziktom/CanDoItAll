# Validation

- test host: parent spawns child; parent exits on graceful signal; child attempts to survive; both must be gone
- nested child/grandchild, timeout, dispose, root-already-exited, PID mismatch, repeated stop
- Workbench/Manager/MCP lifecycle integration slices
- actual Windows and Linux focused execution; no full suite

Run affected scopes only unless this checkpoint explicitly authorizes a full suite. Record exact commands and counts.
