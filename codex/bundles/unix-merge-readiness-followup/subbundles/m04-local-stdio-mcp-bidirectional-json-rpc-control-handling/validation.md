# Validation

- fake stdio server sends ping before tools/list or tools/call response
- unsupported peer request, notification, malformed JSON, overlong line, excessive unmatched messages
- process exits before/after response and timeout cleanup
- focused MCP projects and runtime MCP slice only

Run affected scopes only unless this checkpoint explicitly authorizes a full suite. Record exact commands and counts.
