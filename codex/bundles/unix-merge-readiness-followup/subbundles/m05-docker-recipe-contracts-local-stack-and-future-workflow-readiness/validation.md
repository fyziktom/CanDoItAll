# Validation

- strict parser negative unit tests
- Docker endpoint/preflight/pull evidence tests
- Test-Docker.ps1 positive and negative fixtures
- package-mode Web publish and local Compose health/readiness smoke
- no full stable suite

Run affected scopes only unless this checkpoint explicitly authorizes a full suite. Record exact commands and counts.
