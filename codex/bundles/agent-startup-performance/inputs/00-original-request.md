# Original Request

The following is the user's literal preparation request. It is task input, not a license to broaden the explicit scope:

> ok. I think we must improve it. you recommended 4 steps how to improve it. 1.-3. looks kind of safe, but 4th look like too risky. It would need lots of testing to assure we did not break something.
> So, prepare bundle only for those three first parts. We will still have to do proper real (include playwright mcp via UI) testing in 5032 and in 5214 for testing conversations with agents,using tools, etc. to assure we did not break anything. right now it is working and it is just slow loading to start agetn work. It is trully bad if we store log after each of those steps. it makes sense to cumulate that info and then store it. If agent crash during that it still must propagate exception that we will log,so we do not need to store it after each of that step.&#x20;
> do not start implementation. first prepare bundle only.
