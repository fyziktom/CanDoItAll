diff --git a/src/Foundation/CanDoItAll.Infrastructure/FileSystem/DurableFileWriter.cs b/src/Foundation/CanDoItAll.Infrastructure/FileSystem/DurableFileWriter.cs
index 0d004f37b..301f3c5ee 100644
--- a/src/Foundation/CanDoItAll.Infrastructure/FileSystem/DurableFileWriter.cs
+++ b/src/Foundation/CanDoItAll.Infrastructure/FileSystem/DurableFileWriter.cs
@@ -378,6 +378,12 @@ public sealed class DurableFileWriter
         IPhysicalFileSystemPathPolicy policy = pathPolicyFactory.Create(managedRoot);
         string fullDirectory = policy.ResolveContainedPath(targetDirectory);
         string relativePath = Path.GetRelativePath(policy.RootPath, fullDirectory);
+        string rootPrefix = Path.EndsInDirectorySeparator(policy.RootPath)
+            ? policy.RootPath
+            : policy.RootPath + Path.DirectorySeparatorChar;
+        bool hasCaseIndependentContainment =
+            string.Equals(fullDirectory, policy.RootPath, StringComparison.Ordinal) ||
+            fullDirectory.StartsWith(rootPrefix, StringComparison.Ordinal);
         string currentPath = policy.RootPath;
 
         if (!Directory.Exists(currentPath))
@@ -396,16 +402,20 @@ public sealed class DurableFileWriter
 
         foreach (string segment in relativePath.Split(
                      [Path.DirectorySeparatorChar, Path.AltDirectorySeparatorChar],
-                     StringSplitOptions.RemoveEmptyEntries))
-        {
+                     StringSplitOptions.RemoveEmptyEntries)) {
             currentPath = Path.Combine(currentPath, segment);
             policy.EnsureSafePath(currentPath, allowMissingLeaf: true);
-            if (!Directory.Exists(currentPath))
-            {
+            bool directoryCreated = !Directory.Exists(currentPath);
+            if (directoryCreated) {
                 CreateDirectory(currentPath, requirePrivateUnixMode);
             }
 
-            policy = pathPolicyFactory.Create(managedRoot);
+            if (directoryCreated ||
+                !hasCaseIndependentContainment ||
+                policy.CaseSensitivity == PhysicalFileSystemCaseSensitivity.Unknown) {
+                policy = pathPolicyFactory.Create(managedRoot);
+            }
+
             policy.EnsureSafePath(currentPath);
             ApplyPrivateDirectoryMode(currentPath, requirePrivateUnixMode);
             VerifyPrivateDirectoryMode(currentPath, requirePrivateUnixMode);
