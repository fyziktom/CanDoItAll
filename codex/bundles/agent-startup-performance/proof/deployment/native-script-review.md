# Native deployment script review

Decision: **Pass for the reviewed bounded procedure and scripts; deployment is not yet performed.** No native signal, launch, restart, browser interaction or provider request was executed by this reviewer. The only running-app interaction was the authorized read-only environment recapture; it retained the original process/start identity and frozen environment.

## Reviewed safety boundaries

- Capture verifies the exact app PID58036, parent22496, executable path and unchanged original assembly hash. The127-entry environment is fingerprinted against its frozen value and protected with DPAPI CurrentUser. Only allowlisted metadata/hashes are retained in portable proof; the protected blob itself remains private and is not copied.
- Existing private-directory protection is verified and retained. It must have the current owner, a protected DACL and exactly one explicit current-user FullControl allow rule with child inheritance. An unexpected ACL/reparse point fails explicitly; it is not silently widened or replaced.
- New private directories change only the Access section through FileSystemAclExtensions, then verify the existing current-user owner. An initial fresh DirectorySecurity/SetOwner attempt hit a privilege error; the final implementation avoids owner reassignment and SACL access rather than requesting broader privilege. Actual new-directory creation and repeated existing-directory validation passed with identical ACLs. Recapture then passed with the real private directory's ACL unchanged.
- The original launcher gap was fixed: decrypted bytes must match the frozen environment SHA256/count; Assert-NativeLaunchProfile verifies Project/http, exact application URL, every current profile environment override and a strict allowlist rejecting extra launch controls. The launch-settings file hash must match capture. The five current overrides were verified without exposing values.
- The launcher clears its inherited environment, restores only the protected snapshot and passes no environment values on the command line. It restricts candidate artifacts to the approved short root .artifacts/asn-20260831; omitting that argument remains the explicitly reviewed original-binary rollback path.
- The child inherits the dedicated hidden wrapper console. Both output streams drain asynchronously; the wrapper handles its own CTRL_C while preserving child shutdown. The signal helper validates the exact app PID/start and refuses signaling unless the console membership exactly equals the app, known parent, helper and optional wrapper. It has no force-kill fallback.
- Assert-NativeQuiescence rejects pending journals and any newly active/unclassified run. The one exact historical paused approval is preserved through31hashes including its run JSON. Existing before proof records121runs,120terminal and that one unchanged pause. This is a filesystem snapshot, not an admission lock; the coordinated no-new-requests window and a fresh immediate pre-signal/post-restart check remain required.

## Verification and evidence

All six retained scripts parse without errors. Reviewed hashes are in native-scripts/reviewed-script-hashes.json. Actual ACL/new-directory/idempotence proof and read-only recapture identity/hash checks are in native-scripts/capture-acl-verification.json and capture-recapture-verification.json. The environment plaintext and protected blob are excluded. A secret-pattern scan of the copied scripts/build artifacts reported no matches.

The native candidate build is complete. The initial long artifact-root publish failed with MSB3021/Windows MAX_PATH while copying template files; both its exact command and failure log remain under native-build/. The successful retry changed only the isolated artifact path to .artifacts/asn-20260831, retained local-library resolution and exited0. Build-output and published Web DLL hashes agree; the original live-bin DLL retains its frozen baseline hash. No production/test source change was made to work around the path limit.

The procedure document now reflects the short artifact root, inherited hidden console and wrapper handler; its earlier CreateNoWindow=true instruction would have prevented that console lifecycle and was corrected. Root still owns readiness, fresh quiescence/identity checks, controlled graceful replacement, rollback, genuine UI/tool checks and paired performance acceptance. Passing this review alone does not claim any of those actions occurred.
