# Frozen startup changes: final scoped review

Decision: Pass for the frozen source and the existing functional proof design. No new blocking defect or mandatory additional functional case was identified in this review of all13 frozen source/test/project files. Current file hashes exactly match the source freeze. This reviewer implemented SB02 earlier, so this is not an independent SB02 author review; root's separately retained independent SB02 review remains the approval for that part. This reviewer is separate from the SB01/SB03 implementer and previously recorded the independent SB03 review.

SB01 keeps filesystem facts local to one synchronous existing-directory traversal. Only known case behavior with exact ordinal containment can reuse the policy. Creation, unknown case behavior and case-dependent containment reacquire it. Each segment still gets fresh physical path validation before/after mutation; write/flush/commit callbacks and post-await fresh policy creation remain. Existing real Windows/Linux path tests and new actual platform-affirmative link/recreation/case tests cover these boundaries. Early-return platform cases are not affirmative evidence on the other platform. There is no global filesystem cache or skipped commit durability stage.

SB02 retains canonical publication validation, exact relationship checks, profile-cache integrity checks, existing validation precedence and disabled-versus-missing projection behavior. The two-query selected loader gates joined source materialization on unique import cardinality, which has a real relational corrupt-source/duplicate-import regression case. Full-set loading deliberately keeps all three typed reads and unrelated conversion failures. Local profiles still use the original mapper; revision composition, generation guards and dispatch secret resolution are unchanged. The internal validated shape is ephemeral and contains references to input entities; it is not an immutable cache or an authentication signature. The single new friend-assembly declaration follows an existing project dependency and adds no public API, project reference, schema or DI registration. Existing tokens still define supported invalidation; valid out-of-band mutations without token changes gain no new detection promise.

SB03 retains both journal validations, fresh typed payload reads and conflict checks, workspace locking, complete chat projection rebuilding and ordered commit callbacks. Only a Prepared origin supplied by the private immediate caller and a freshly observed changed typed payload can skip the redundant raw text comparison. Recovery and matching typed payloads retain the raw comparison/canonicalization path. Individual provider usage records/history routing, JsonStore and serialization/writer behavior remain outside this optimization. A noncooperating external writer installing an identical target after validation can cause an additional identical physical write; this accepted limit does not introduce a new stale-read skip. Root's measured host result, not physical-read counts alone, establishes the net performance outcome.

The added tests exercise retained rules and meaningful negative paths: unchanged-token malformed publications/profile metadata, duplicate relationship precedence, unrelated malformed typed source rows, local mapping exceptions, shape/materialization parity, depth-independent probe counts with unchanged commit stages, path replacement, changed prepared payloads, target canonicalization, all five conflict paths, recovered journal roll-forward, and startup/provider failures with durable failed activity plus sanitized diagnostics. No test assertion requires a live provider. The duplicate-index fixture restores its captured uniqueness definition in its isolated PostgreSQL lease; it is not an application migration.

This source review does not turn the initial broad-suite failures into a green first run. Root owns their separately retained environmental/old-output attributions and exact follow-ups. Root also owns final real-UI/tool coverage and host performance acceptance. No additional tests, builds, provider requests, UI actions or source changes were performed for this final review.
