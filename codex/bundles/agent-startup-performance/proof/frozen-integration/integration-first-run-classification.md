The first frozen Integration execution completed at2026-08-31T15:57:52Z after2609.472seconds. It produced1335 case results:1323passed,11failed and1skipped live-Ollama opt-in case.1334cases executed. The TRX `notExecuted` counter incorrectly contains0, while its individual result contains the one `NotExecuted` case and total minus executed equals1; the per-case outcome is the authoritative skip evidence here.

Discovery1330 expands to1335 because `External.PluginCatalogIntegrationTests.packaged_plugin_preview_simulation_avoids_live_external_effects` is one deferred theory at discovery and six runtime cases. No discovered method is missing. This count expansion is not an implementation failure.

The original `FileSandboxWorkspaceUsageProjectionIntegrationTests.File_source_checkpoints_large_manifest_within_host_deadline_and_resumes_after_restart` passed with its unchanged internal2-second deadline. The complete fixture, including setup and recovery, took23.334seconds; this is not the internal deadline measurement.

Ten failures coincide with the disposable test PostgreSQL1GiB tmpfs exhaustion. Nine carry SQLSTATE53100 directly. The million-entry `Scale_search_obeys_plan_row_and_latency_budgets` case failed with `NpgsqlException`/`EndOfStreamException` during lease cleanup. A PostgreSQL PANIC writing its write-ahead log at15:23:31.125Z occurs inside that case's15:23:22.398–15:23:33.153Z interval. The other nine errors follow at15:23:36.499–15:23:38.781Z. The observed PANIC explains loss of the database connection; the cleanup exception may obscure the original main-body exception. This is test-server capacity evidence, not evidence of a production startup regression. No failed case reports SQLSTATE42P01; broad database missing-relation logs must not be assigned to these failures without a matching case.

The remaining earlier failure is `Persistence.PostgreSqlClaimQueryPlanIntegrationTests.PostgreSql_hot_claim_queries_use_dedicated_indexes` at15:16:44–15:16:46Z. It fails the exact index-name assertion atlines118 and35. Its retained step-dispatch query plan uses a Bitmap Index Scan on `PK_process_runtime_steps`, with no sequential scan, and executes in0.108ms. It does not contain the test's required `IX_process_runtime_steps_RunId_Status` name. This result is separate from the later disk exhaustion. The exact case is authorized for one unchanged-binary rerun on the replacement server; no SQL, planner setting, index or test timeout is changed.

The full first execution is retained and is not called green. Root authorized a narrowly scoped environmental retry after all original test processes exited: same cached PostgreSQL18.3 image, loopback52049, credentials,2CPU and ownership, with4GiB data tmpfs and6GiB RAM cap. The original container is retained stopped. The replacement script verifies live5432/5210/5214 identities, images and start times remain unchanged. The follow-up selects the entire affected HistoryQuery+HistoryCapture classes(23cases), the exact strict-index case(1), and the two unchanged Components preview cases(2), serially with no build or source edit. Any subsequent pass supplements, rather than erases, the original failure.

Evidence: `frozen-integration-integration-sanitized-review.json`, `integration-nospace-severity.json`, `integration-owned-db-resource-observation.json`, `integration-first-execution-command.json`, `integration-first-execution-summary.json`, and `test-postgres-capacity-replacement.json`. Original TRX and full transcripts remain under the owned `.artifacts/agent-startup-performance/frozen-validation` directory.