# QA prompt

Validate that process execution works from user-visible launch surfaces and representative templates. Reject proof that uses only manual transitions, API-only launch without completion, or report-only status. Check source scans for Core leakage, driver hooks, reflection discovery, fallback selectors, secret leakage, and concrete bundle path coupling. Require large desktop UI proof for user-facing launch/readback.
