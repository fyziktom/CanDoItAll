# Implementation Prompt

Implement only the current subbundle for `BaseLib Tabs Repair`.

Rules:

- Read the bundle root `README.md`, `plan/01-phase-plan.md`, this subbundle README, and the traceability rows it owns before editing.
- Keep the BaseLib tabs contract Tailwind-owned. Do not hand-edit `C:\repositories\CanDoItAll\src\CanDoItAll.Components.BaseLib\wwwroot\css\output.css`.
- Do not introduce new shared `zy-*` selectors.
- Use the Radzen repo only as reference for behavior and layout ideas, not as a style source.
- If the dedicated sandbox examples reveal a shared-component defect, reopen the earlier phase instead of papering over it locally.
- Record commands, screenshots, browser analytics, and subbundle gate status in `reviews/01-execution-report.md` before leaving the phase.
