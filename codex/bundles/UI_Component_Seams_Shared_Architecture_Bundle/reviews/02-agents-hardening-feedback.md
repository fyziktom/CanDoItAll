# Agents hardening feedback, 2026-09-05

The owner-authorized follow-up to commit `96ee03a97c510d5363636fb06b903b9bc12f47dc` supplies concrete lessons for later UI bundles. This is an architecture-reference revision, not an executable program-wide migration.

Accepted lessons are now in the owning architecture documents and assessment template:

- One authoritative owner for route-significant state; explicitly reconcile any host interaction mirror. Test A -> null/invalid -> A as well as an unchanged parent echo.
- Pass cancellation into the effect owner, not only into an awaiting task. Disposal must release this host's presentations while preserving unrelated ones, and allow the same semantic target in a replacement host.
- Distinguish known rejection, known commit, commit with secondary warning and unknown write outcome at the actual producer boundary. A known committed identity is not erased by later projection/cache failure or cancellation. Keep unknown replay blocked.
- Core load failure renders no editable draft; reference-catalog partial failure remains an explicit, separate contract. Retry retains the target.
- An unsafe characterization oracle is temporary evidence. Once the owner authorizes correction, replace it with the safe contract instead of maintaining contradictory acceptance tests.
- Keep policies and value normalization independent of Razor; do not add a cross-domain reference merely to deduplicate trivial trimming.
- Manual mutable snapshots require recursive public-contract completeness and independence checks. Public model reflection is different from accessing private component fields.
- Stable section definitions drive both rendering order and semantic mapping. Capability reads belong on the existing read port.
- Put pure policies/services in Unit, rendering/lifetime in Components and real persistence/registered adapters in Integration. Freeze exact affected discovery and preserve behavior coverage.
- Give unique dialog surfaces separate shell/content IDs; explicitly scoped repeated collection selectors are a different contract.

The Agents child owns exact source changes, focused filters, runtime evidence, invalidation and unresolved repository gates in [SB08 hardening closure](../../UI_Agents_Component_Seams_Bundle/reviews/03-hardening-closure.md). Do not reuse its historical stable proof as a new run or claim the scoped graph is a lightweight project.

No new interface quota, universal host/store, mandatory service layer, physical project extraction or canonical URL design is introduced. Keep the first catalog sandbox and actual development-loop measurement a separately scoped consumer. Editor extraction remains deferred until its full nested subtree is suitable.

Both reference bundles remain temporary branch artifacts. The child reports a pre-existing documentation gate failure from tracked historical proof logs. Merging a later deletion does not erase those blobs from history; resolve artifact disposition at the actual authorized merge checkpoint.
