# G03 - Extraction, sandbox and lightweight watch closure

Status: IMPLEMENTED; FOCUSED AND BROWSER VALIDATION PASSED; FORMAL CLOSURE BLOCKED. The latest inline owner scope replaces the older large timing protocol. See the [report](../report.md) and [single smoke summary](../proof/final/development-loop-summary.json).

1. The in-place Surface passed original G00 semantic/security checks. The real pre-extraction Web baseline preceded movement: three process-cold starts and one Razor/C#/CSS edit twice; exact source hashes were restored.
2. Six pure files moved to existing UI/Governance. Module retains session, reads, cancellation and context effects. Real timeline/metrics moved once and both consumers migrated. No wrapper, copy or forbidden reference was introduced.
3. The existing sandbox gained fourteen typed scenarios and controlled intents. Web, Parity and Fast acceptance pass. No production workspace/database/provider/execution services are registered in the sandbox.
4. Final post-Web and sandbox-Parity smoke each passed three cold starts and six edits. A visual header defect invalidated the first successful post/sandbox series; both were repeated, retaining all observations. The pre-move baseline predates bounded route/option/header corrections. This is not an isolated performance comparison or Fast speed claim.
5. Final source restoration, direct builds, focused tests, the failed single stable gate, its explicit failure adjudication, real dependency graph and static/security evidence are retained together. Published Components delivery and inherited documentation debt remain explicit limits.

There is no per-phase raw proof tree. Raw logs remain ignored. Diagnostics remains gated because the broad execution failed; no handoff or implementation follows here.
