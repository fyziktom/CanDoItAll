# G03 — Fresh baseline, real extraction and measured sandbox

Status: PLANNED, NOT EXECUTED. Prerequisite: G02 Module semantic/browser closure and authorization for this child. Follow the order below. No source movement before valid baseline.

## 1. Baseline in the final Module owner

Rediscover current SDK, siblings, source/package mode, test inventory and generated assets. Freeze canonical safe Governance fixtures with real run records and every displayed section, same actual 1600 x 1000 frame, display timezone/locale and stable timestamps. Full-app measurement uses controlled persisted/read-only fixtures without provider execution; the lightweight sandbox later uses a derived safe display snapshot. A fake delay/error browser fixture is separately labeled and is not a canonical timing fixture.

Freeze nine **actual supported** production-rendered probes: three Razor text/markup edits, three C# presentation edits and three CSS edits in the consumed pipeline. Record original/replacement bytes, source SHA, DOM/computed-style predicate, restoration SHA and causal asset path before running. Current panel has no CSS file; G02's actual result determines legitimate scoped/theme CSS probes. If three supported CSS probes cannot be observed without inventing UI, stop baseline and replan. No benchmark-only style/component or hand-edited generated asset.

Use direct dotnet watch and the existing native asset pipeline, owned ports/processes/browser, no managed watcher/manual refresh and no parallel builds/tests. Record at least three process-cold starts and each of nine edits repeated three forward/reverse times. Process-cold means new process with recorded caches, not a cold machine. Confirm first-visible and separately stable frame/scroll/overlay predicates. There are no Governance charts to invent a chart-settlement predicate for; real displayed content/layout must settle.

## 2. Move only the verified pure closure

Move the real surface/code/any justified CSS, immutable display records/intents and pure mapper into existing src/UI/CanDoItAll.AgentFramework.UI. Move real ExecutionTimelinePanel and MetricSummaryPanel from broad Components into that same pure UI owner; update both Governance and AgentRuntimeDetailsDialog consumers directly. Preserve source namespace if that avoids needless public churn; inspect current package/binary policy and document supported compatibility. No duplicate implementation, old forwarding wrapper, reverse dependency, new project or movement of AgentRuntimeDetailsDialog/session/read/query/backend effects.

Existing Models/UI references should suffice. Re-evaluate actual transitive graph; UI/sandbox must not reach Core, Module, Persistence, runtime providers, Voice, AppComponents or broad Components. If the real closure cannot satisfy this, stop and repair the boundary before continuing. A zero-edge scoped analyzer query is not proof. Record old absence/new presence and final assembly/static-web-assets ownership.

## 3. Existing sandbox specimen

Extend the current typed sandbox codec/specimen with Governance; absent/unknown still means Catalog. Preserve Catalog/Capabilities/Overview scenarios, routes, history replacement and Parity/Fast behavior. Use actual moved children and assets with deterministic loading, empty, ready, missing-agent, missing-run, stale-list, detail-error, pending-new-target, wrong-target-rejected, long-content and all outcome/approval examples. Include All and specific agent, selection/retry intent log, no production calls. No workspace/store/provider/diagnostic/execution/approval command registration or invocation.

Verify actual font/icon/tooltip/CSS isolation/layout/overflow assets in full app and both sandbox modes. Shared responsive compatibility stays its single existing source. Any necessary renderer/asset correction retains initial failures and invalidates affected baseline and comparison series. Reconstruct pre-owner only with explicit byte-restoration proof if a late correction requires it; never relabel the old baseline.

## 4. Sequential comparison and closure

Compare pre-move full app, post-move full app, sandbox Parity and Fast on the same environment/fixtures/asset pipeline. Per host: three process-cold runs; three distinct Razor, C# and CSS probes, three forward repetitions each, and separate reverse/restoration observations. Record durable write points, watch generation, process start/ID, document timeOrigin, browser sentinel, navigation/reload/hot reload/restart/failure and first-visible/settled values. Preserve all failures/exclusions/outliers. Report per-edit/type min, max, range, median and failure count; do not claim universal Fast improvement or general performance from graph size.

Build each real changed owner/consumer including broad Components and both sandbox modes; freeze and run exact compiled selectors. Record named broad-gate invalidation from public assembly movement and execute its required consumer/stable scope. Browser review includes original runtime-details consumer, real complete Governance sections and dropdown/tooltips. Finish portability/documentation/source-secret/retained scans, source and generated-asset restoration, owned process exit receipts, evaluated graph, links and manifests. Preserve historical gate debt honestly. No production routing, other editor extraction or new hotspot begins here.
