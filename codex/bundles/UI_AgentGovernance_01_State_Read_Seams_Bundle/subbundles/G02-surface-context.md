# G02 — Controlled real surface and host effects

Status: IMPLEMENTED; focused proof green. Final aggregate closure is recorded in [the report](../report.md). Prerequisite: G01 closed with canonical-read and public lifetime proof. Keep rendering in Module until this child closes.

Extract the real ListDetailShell and its full current subtree into AgentGovernanceSurface. Inputs are immutable presentation and typed intents for SelectAgent, SelectRun, Refresh and lane-specific Retry. Names are implementation judgment; semantic selection remains controlled. A local visual expansion may be surface-owned only if not route/workspace meaning. No injected workspace/query/navigation/dialog/notification/runtime service, no second selected identity and no forwarding controller.

Preserve actual run rows, badge-tone precedence and the G00 approved safe summary policy, compact statistics, approvals, artifacts, checkpoints, tool receipts, timeline and metrics. Do not add approve/reject/run/continuation commands. Use existing BaseLib components, shared responsive CSS and real children. The two existing children stay in their current assembly in this phase but adopt G00 lightweight values and UTC policy; update both actual consumers before G03. Any CSS added must solve a proven real layout need, not manufacture benchmark edits.

AgentGovernancePanel remains the thin effect host: session construction/disposal, public parameter application, typed intent orchestration and accepted agent/access callbacks. Page routing/selection/context stays AgentsHomePage. Effects verify current request immediately before publication and after awaits. Parent echo must not create duplicate reads or clear a missing target. Successful detail changes do not publish a new agent unless its accepted identity actually changed. Do not use global dialog CloseAll.

Proof: no-service surface rendering; one typed intent per public action; immutable state/descendant independence; all real sections and loading/empty/stale/error/ready states; no infrastructure exception. Real page tests prove current selection/context and Overview independence. Browser review at 1600 x 1000 covers normal, all-agent, preferred/missing, list/detail failure, rapid replacement, long content, actual dropdown/tooltips and real scroll ownership. Include runtime-details child compatibility baseline.

Close G02 only after Module UI/browser proof passes, old panel responsibilities are removed, source/dependency/asset inventory is frozen and exact G03 movable closure is recorded. Still no physical extraction or sandbox change. G03 starts with its own fresh baseline, never inherited timing.
