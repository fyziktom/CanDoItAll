# Future validation and preparation proof

Use current docs/testing.md, CI and shared standards at execution entry. Read before changing source/tests/tooling. Keep live Components and FileTools mode, English comments/documents, and all accepted Overview/Capabilities/provider behavior. No command below has been executed as Governance proof during preparation.

## Before each child execution

Rediscover local and independent remote HEAD/tree, ahead/behind, index/worktree/untracked paths, sibling HEAD/branch/status and required patch hashes. Re-evaluate graph/source mode, SDK/global.json, Node/Tailwind/browser versions and generated Web/Fast assets. Inventory actual tests/callers. Preserve unrelated work; do not commit/push/merge/clean or overwrite a sibling. Read the predecessor manifest, not just a closure sentence.

Build each changed production owner directly and owning test projects. List compiled tests with the repository runner, freeze exact FQNs/arguments and counts, then run those exact selectors. Correct zero/unexpected discovery before accepting proof. Source-inventory method names do not replace compiled discovery. Distinguish semantic RED, setup failure, failed assertion and skipped/aborted cases. Never add private reflection or uninitialized production fakes.

G00/G01 owners: Module; Unit/Components; Integration production read adapter and database-profile workspace fixture. Existing page/context tests and canonical execution-history consumers are required when touched. G02 adds current real child/host browser validation. G03 adds UI, broad AgentFramework.Components, Web, sandbox Parity/Fast, real runtime-details consumer and their test owners. Build Models/Core only if changed by a separately proven narrow correction.

Use CodeAnalytics impacted tests on actual diff only, with healthy production/test workspaces and explicit contract/behavior change intent. Existing tracking suite may expand if Core/store/composition changes; no backend change is intended. Full stable is required only for a recorded shared-public-API/assembly/project-graph/asset invalidation. G03 moves existing public children and therefore requires explicit consumer impact review and the resulting broad gate. It must not be omitted merely to save time.

## Runtime and browser proof

Use repository PostgreSQL fixtures and actual registered current-profile adapter for canonical reads. Seed safe existing run/approval/artifact/checkpoint/receipt/log/metric records; invoke no external provider, live tools or approval mutations. Test token propagation and missing identity independently of desired fake outcomes. UI race fixtures may use delayed public reads; they prove orchestration only.

At 1600 x 1000, verify the [composition questions](ui-composition.md), normal/all-agent/preferred-agent/missing states, selection echo, list/detail separation, retry, no raw thrown error and no late callbacks. Browser screenshots/DOM must include real sections, open dropdown/tooltips, overflow and both original consumers. G03 additionally verifies all existing sandbox defaults plus Governance scenarios in Parity/Fast. Inspect actual screenshots and computed layout, not capture-only evidence.

## Final gate and artifact policy

Run portability scanner self-tests and regenerate/enforce without --write-baseline. Review any intentional baseline delta with exact source reasons; never waive it because tests pass. Run documentation gate, complete proposed-source secret scan and retained compressed/UTF-16 artifact scan with no size shortcut. Keep raw sensitive fixtures/process environment outside proof. Retain compact sanitized commands/results/TRX/DOM/screenshots, SHA manifests, source restoration and owned process exits. No credentials, secret IDs or serialized runtime sessions in new UI/artifacts.

The Overview prerequisite has an explicit inherited documentation failure for 118 tracked historical log paths. Preserve that finding and historical proof; do not call the repository gate green or erase artifacts to hide it. Future execution must re-evaluate whether the failure set is unchanged. New defects/blockers require correction or an explicit blocked child, not invented success. Repository merge readiness is never claimed.

Preparation-only checks: preserved owner/prerequisite inputs; source-backed adjudication; architecture-role coverage; relative links; all non-bundle source/test/project/assets and siblings unchanged from preparation entry; current scans and manifests. No .NET rebuild/test is required merely to write this plan; existing executed Overview results remain labeled historical prerequisites.
