# Preparation architecture and QA review

Decision: **READY FOR FUTURE G00, SUBJECT TO OWNER EXECUTION AUTHORIZATION**. This is a compatible-shape preparation review. No Governance runtime, test or extraction closure is claimed.

Architecture roles are present: actual hashed source/consumer inventory; responsibility map; evaluated current graph plus scoped CodeAnalytics limitations; alternatives for each selected pattern; exact public RED and registered-read tests; foundation checkpoints; UI composition and artifact rules. The compact G00-G03 shape matches the predecessor and does not need canonical filename/scaffold replacement.

The scope fixes actual read lifetime and extraction boundaries. It preserves existing agent-generation guarding rather than replacing it wholesale. The proposed session has a real per-instance responsibility; the read adapter has a direct public seam and a simpler-delegate alternative to adjudicate in G00. No service bag, interface-per-method, partial-file separation, magic test/member count or broad Core/provider redesign passes this review.

QA checks: all twelve candidate risks are adjudicated with source qualifications; missing-ID page echo remains a required future witness; exact proposed Facts are explicitly not compiled/executed; source-only method inventory is not discovery. Browser questions name actual dropdown/tooltips/scroll/sections and desktop frame. G03 records genuine CSS uncertainty and blocks movement on invalid baseline. Shared children and their second consumer are included, so no hidden broad-components edge is excused.

Manager/sequence check: Overview closure receipt predates preparation. G00 owns current behavior, G01 the critical read/session foundation, G02 the real surface/context, G03 fresh baseline then moves/sandbox/comparison. Each requires a separate written closure/manifest. No Governance code/test/project/asset edit is authorized now. [Preparation report](../preparation-report.md) records final read-only source drift, links and scans.

Residual uncertainty: future compiled RED outcomes, canonical fixture setup, complete missing-ID parent echo, exact G02 CSS owner and actual watch predicates must be determined in their assigned child. These are executable prerequisites, not fabricated completed proof. Existing repository documentation debt and uncommitted Components patch remain explicit; merge readiness is not asserted.
