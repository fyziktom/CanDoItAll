# G00 architecture entry gate

PASS for G01 implementation only. Current source, exact compiled witnesses and real public failures support the approved boundary. One cohesive read adapter and one per-instance session are justified; no generic controller or per-method interfaces. The page remains semantic selection owner. No new project/physical movement in G01/G02. Actual graph is acyclic; both shared rendering consumers and full public model fields are inventoried.

The G00 time/data record supersedes preparation's raw-summary fallback assumption under the newer explicit security requirement. Display values are immutable, purpose-specific and bounded. Shared child policy changes must be verified through Runtime details as well as Governance. No disclosure claim relies on truncating raw secrets.

G01 closure requires all 30 lifetime cases green, direct session tests, four registered canonical-read cases, context compatibility and portability. The four current presentation failures remain explicitly G02-owned until shared child mapping is applied. Critical stale/wrong-target failures cannot be deferred past G01. Root manifest/link gate remains mandatory before starting production.
