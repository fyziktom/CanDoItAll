# G00 closed: current behavior and semantic RED

The new implementation authorization is active after the verified Phase A proposed-byte seal. G00 changed the bundle and one component-test file, with no production, project, asset, sibling or history edit. Existing preparation proof is historical.

The direct Module build passed. The first component build had four bUnit type-name setup errors; the corrected build passed. Compiled discovery froze 34 cases before execution. Initial RED: 28 failed, 6 passed. A stronger renderer-error assertion then exposed the stale-detail failure missed by the first witness. The rebuilt, rediscovered final RED has 29 semantic failures, 5 passing characterizations, no skips or setup failures. Exact cases and owners: [adjudication](red-r2/adjudication.json); [discovery](compiled-discovery.json).

Passing current behavior: agent-generation stale-success guard; same requested-ID echo preserves manual selection; explicit All then A transition; synchronous reads/echo issue one load; real-page missing request stays targeted through the tested retry sequence. The direct panel's missing-ID null callback is nevertheless unsafe and fails its public callback contract; suppress it without changing page routing. No claim that the tested real page currently loses its target.

Confirmed failures cover stale list/detail errors and callbacks; uncanceled reads; post-disposal publication; old finally; refresh versus manual selection; delayed list acceptance; missing/empty-ID null publication; wrong detail identities; lack of independent retry/stale/unavailable states; UTC/culture; raw payload/timeline leakage. The selected-run disappearance timeout is an absent required state after completed reads, not a host hang.

Four existing page-context and four registered-workspace baselines passed after exact 4+4 discovery. These registered workspace tests are baseline composition proof; G01 must still add canonical run filtering/detail/token integration. No broad stable run is warranted for G00 test-only work.

Actual MSBuild graph: no cycles or forbidden dependency, live sibling mode retained. Full reachable public model/property inventory is [recorded](domain-field-inventory.json). [Approved ownership, time and data decisions](../../reviews/G00-ownership-and-presentation.md) resolve the new requirements. G01 owns 30 current-panel lifecycle cases; G02 finishes four timestamp/payload/shared-child cases. Counts identify this checkpoint, not architecture invariants.

Complete proposed-source portability enforcement passed with the existing baseline. Source and retained secret scans found no added secret findings. Documentation remains failed on the identical inherited 118 tracked historical log paths; no waiver, deletion or merge-ready claim. The root proof policy retains every referenced file. No raw denied sentinel value is retained.

G01 is unlocked after root manifest/link validation. G02 and G03 remain gated. No physical movement, sandbox or measurement work occurred.
