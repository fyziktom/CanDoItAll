# Actual post-Overview source

[Exact paths, absence and hashes](../inventory/source.json), [existing test source inventory](../inventory/existing-tests.json), [CodeAnalytics](../proof/preparation/codeanalytics.json).

AgentGovernancePanel has a Razor file and a 256-line code-behind partial; **no component CSS file exists**. It injects one broad IAgentFrameworkWorkspaceService property, has no constructor dependencies, no disposal interface, no CTS, and no durable mutation state. It directly creates ExecutionRunQuery(AgentId: selectedAgentId, Take: 30). Partial code organization does not separate selection, queries, errors and rendering.

The host owns agent catalog, selected agent, runs, selected run/detail, one isBusy, one agentSelectionGeneration and callback bookkeeping. RefreshRunsAsync fences list and automatic-detail successes by agent generation. Manual SelectRunAsync does not. LoadAsync owns unguarded catalog load and global finally. Details are fetched before a successful list is accepted. Stale exceptions still escape catch blocks. SelectedAgentChanged reads mutable current selection after async work. The panel renders records; it exposes no approve/reject command.

AgentsHomePage renders the panel only on Governance and supplies PreferredAgentId, SelectedAgentChanged and ContextAccessStateChanged. The page owns workspace/route state and chat-context bridge. Overview now leaves selection access authoritative, so Governance must not publish an unrelated old failure into it.

Production composition registers CurrentProfileAgentFrameworkWorkspaceService as scoped IAgentFrameworkWorkspaceService. It resolves the current profile workspace and forwards cancellation for all reads. The existing IAgentExecutionHistoryReader owns list/detail plus artifact/checkpoint/receipt reads; it is inherited by the workspace interface, not independently registered for this panel. Agent catalog is ListAgentsAsync(false). Core ListExecutionRunsAsync filters actual AgentId and orders/limits the store result. GetExecutionRunDetailAsync uses the canonical run store, throws InvalidOperationException for missing run, and enriches provider-native MCP receipts. Enrichment can inspect existing runtime artifact metadata; it remains Core infrastructure and never moves to UI or sandbox. Do not match exception text to invent a typed not-found contract.

ExecutionRunRecord and ExecutionRunDetail are existing Models value contracts containing more than displayed data (including serialized state and operational metadata). IReadOnlyList does not make supplied mutable arrays independent. Future immutable display snapshots must own collections and whitelist existing displayed fields. Approval ArgumentsJson, raw session state and structured-output payload are not new UI fields.

ExecutionTimelinePanel and MetricSummaryPanel are real service-free Razor children in broad AgentFramework.Components. Both also render in AgentRuntimeDetailsDialog. That broad assembly already references UI and Core/Voice: adding UI -> broad Components would create the wrong direction. Move real children and update both consumers in G03, preserving source namespace if useful; do not copy or move the data-loading dialog.

Current tests: the Overview lifecycle class explicitly invokes Governance's public access callback to prove page isolation; it does not exercise Governance's own races. AgentsHomePageTests and ModuleChatContextBuilderTests cover page/header/context contracts. ExecutionRunTracking integration tests exercise persistence, approvals and stored details, not this panel's lifetime. No dedicated Governance component/session test source was found. Exact future witnesses are separate from these existing tests.

Scoped snapshot snap-20260907164254-5c18b873 loaded 547 types, 5728 members, 55 registrations and 205 documents. Its Governance finding is informational member aggregation. Twenty-three informational diagnostics include partially interpreted factory DI and truncated diagram exports; no blocking load error. Wider existing Module/type cycles remain outside scope. Empty scoped project-reference/filter arrays are not dependency proof; use the evaluated live graph.
