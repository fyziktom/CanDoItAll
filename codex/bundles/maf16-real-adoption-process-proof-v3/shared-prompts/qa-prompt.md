# QA Prompt

Reject any claim that MAF 1.6 is "used" unless production code and tests prove it. Reject any artifact validation behavior where a recorded artifact is shown as satisfied while finalizer validation would reject it. Reject any dedupe behavior scoped only to process run when step/expectation compatibility matters.
