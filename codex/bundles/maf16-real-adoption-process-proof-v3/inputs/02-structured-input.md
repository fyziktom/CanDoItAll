# Structured Input

| Input | Structured interpretation |
| --- | --- |
| MAF 1.6 adoption proof | Runtime reflection and compile evidence must prove actual package symbols and safe deferrals. |
| Artifact dedupe scope | Existing projection identity and external reference reuse must not cross step or expectation boundaries. |
| Required narrative artifacts | Required content-backed narrative artifacts must fail predictably when stored content is missing. |
| Read-model parity | Operator read models, run health, and UI status mapping must expose the same invalid-content condition the finalizer records. |
| Live-test readiness | The next real live run needs a deterministic preflight and runbook rather than another full live attempt from an ambiguous state. |

