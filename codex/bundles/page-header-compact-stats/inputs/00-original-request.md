# Original Request

User request received 2026-05-18:

> Use the CanDoItAll bundle workflow to fix large stat cards across pages. The processes page screenshot shows the correct compact pattern: minimal header, small badge stats instead of large stat cards, and icon-only actions on the right.
>
> Analyze how it is done on the processes page, identify all other pages that are wrong, and improve them. Badge stats need tooltips with details. Header add/refresh/similar actions should be icon-only like processes, also with tooltips. All of these tooltips should wait 2 seconds before opening.
>
> Prefer a general prepared header component so page headers can be maintained from one source. Also improve tabs/subpages where large stats are used, especially CRM module tabs. Validate with screenshots. Main goal is saving height on large screens. When rows are stacked, recompose so title, badges, actions, searchline, etc. fit in one row where possible without overlay or overflow. Focus only on large screens for now.
