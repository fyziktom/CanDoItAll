# Governed semantic invariants

Authority: owner requested careful component-edge refactoring without losing functionality; sandbox iteration and bookmarkability preparation are related but separate. Normalized ownership is R-001–R-059 and B01–B30. Source identities/hashes are in changed-files.json; exact cases and execution are in SB06 transcripts. Each invariant below names a plausible shallow pass that the proof must reject.

| ID / owned rows | Expected production behavior and source | Positive / adversarial evidence | Rejected shallow implementation |
|---|---|---|---|
| INV-STATE / B01–B08 | AgentsWorkspaceState/AgentWorkspaceSection own semantic state; AgentsWorkspaceQuery owns aggregate reads; AgentCatalogPanel consumes Snapshot/Selection and emits typed intents; AgentCatalogHost owns effects and result reconciliation | Existing route/context/page/history cases; no-service controlled render; real repair/team operations; requested-ID and managed lookalike tests; SB06 component and unit transcripts | Renamed page fields with eager history reads; injected workspace hidden behind another component; ID bookkeeping never consumed by results |
| INV-SESSION / B09–B12, B16, B19–B20 | AgentEditorSession owns Draft/EditContext/Target and invalidation token. AgentDetailsDialog changes section without replacing draft, replaces on Clear/new target, rejects old completion. Host checks target before and after catalog I/O | Session target/reset/dispose/two-instance cases; save reset/dispose success/failure; first-save host echo; avatar delayed result; SB06 Saved_catalog_refresh_publishes_selection_only_for_the_current_editor_target (both cases) | Fake preseeded identity; replacing every parameter-set draft; accepting old callbacks because cancellation was requested; validating identity only before an await |
| INV-WRITE / B13–B18, B21–B23 | AgentEditorDraftPolicy creates independent request. AgentEditorCommands preserves optimistic version and returns Committed/Rejected/Unconfirmed. Pending reconciliation blocks another write; retry repeats reads. Real adapter retains root/permission/model normalization | Registered commands real catalog round-trip/conflict; later edits; acknowledged refresh retry and callback failure; unknown result; real existing/new capability toggle/wizard; deletion channels | Save of mutable live draft; refreshing token before write; treating timeout as proven failure; replaying create on refresh error; losing edits made during save |
| INV-COMPOSITION / B22–B29 | AddAgentFrameworkUi registers real shell/catalog/editor/read/access/resource/command adapters. Real editor descendants and actual host consume them normally. UI-only reference metadata excludes secret values | Real constructor/DI and actual database tests; root/storage/avatar/provider/Memory child cases; focused and stable gate; actual browser actions and screenshots | Mocked god-controller-only proof, suppressed descendants, RuntimeHelpers.GetUninitializedObject, private component reflection, screenshots without mutations |
| INV-HANDOFF / B30 | Catalog is a semantic sandbox candidate; existing project/model/asset graph is still heavy. Navigation remains existing codec/DialogService behavior | SB01 evaluated graph/watch-file inventory, actual controlled rendering, SB05 scoped dependency facts with limitations, final source/asset review and six readiness verdicts | Claiming small build graph or faster watch because interfaces exist; calling editor isolated while omitting its storage/Memory/wizard graph; treating meeting proposals as approved routing |
| INV-PORTABILITY / all touched protected source | Complete current-source scan, reviewed added/stale deltas, no-write final baseline enforcement | Initial scan fail, portability-review.md, refreshed complete scan and final enforcement transcript | Tracked-only scan omitting new files, unexplained baseline rewrite, ignoring stale findings, scanner weakening |

Source paths are rooted at repo://src/Modules/CanDoItAll.Modules.AgentFramework, tests at repo://tests/Components/CanDoItAll.Tests.Components and repo://tests/Unit/CanDoItAll.Tests.Unit/AgentFramework. Invariant IDs occur in the actual final run labels; individual exact case names map through bundle://proof/SB06/coverage-map.md and the discovery transcripts.

Preserved B12 is a characterized existing defect: core-load failure still exposes a blank editable form. No owner approval for a behavior change arrived. This is explicitly excluded from a claim of fail-closed editor loading. External AvatarPicker notifications can outlive its instance; parent draft publication is guarded. These are owned follow-ups, not hidden passing claims.

## Producer / consumer / lifetime evidence

| Production state/result | Producer | Consumer / disposal | Negative proof |
|---|---|---|---|
| Overview/usage and accessible selection | Real workspace/query and EF bound-resource adapter | AgentsHomePage typed state and existing AgentChatContextSurfaceProvider | Two lazy history hosts, partial HR/fatal overview split, selected context cases |
| Catalog snapshot/intents | Real AgentCatalogOperations / real controlled panel | AgentCatalogHost and callbacks; panel has no feature services | Supplied data/repair tests, no-service render, managed identity lookalikes |
| Loaded identity/version/draft | AgentEditorReads -> real workspace/metadata adapters -> AgentEditorSession.Load | Real EditForm, Save capture, typed CurrentTarget | Real reader two-target test; delayed old load success/failure; separate contexts |
| Committed identity/version | AgentEditorCommands -> actual SaveAgentAsync acknowledgement -> ReconcileAsync | Session target, host TargetChanged, Saved handler; Clear/disposal invalidate | Real first-save/update, conflict, pending-refresh retry with write count; host echo and post-await target check |
| Command request and later draft edits | AgentEditorDraftPolicy.Capture | Real command persistence; HasLaterEdits preserves live draft and updates acknowledged token | Pending write captures old snapshot; later name remains in same EditContext |
| Editor child changes | Real AvatarPicker / root/storage fields / Memory panel | Session-captured callbacks or keyed EditForm subtree | Pending avatar/reset; root/storage invalid/remove/retry; memory invalid IDs/driver selection |

## Failing-first association

- SB05/transcripts/lifetime-behavior-first-results.log: meaningful old-session publication and mutable-request failures; fixture-setup failure logs are not semantic negative evidence.
- SB05/transcripts/target-echo-settled-results.log: actual EditContext replacement on acknowledged identity echo.
- SB05/transcripts/host-result-lifetime-results.log: stale old-target closed result clears selection after Clear.
- SB06/transcripts/page-save-echo-first.log and SB07/browser-initial-actions.json: first real save opens two editor presentations through the owning page's selected-ID echo. The host now marks the acknowledged identity as already open before publishing selection; the same real page test proves first save and subsequent update keep the same single editor.
- SB06/transcripts/catalog-refresh-first.log: normal real-save result passes; Clear during awaited catalog refresh incorrectly changes selection. The corrected case is included in final focused discovery/results.
- SB01 baseline versus current B18 characterization demonstrates the acknowledged-write/reconciliation correction. Pure preservation cases use baseline/after evidence, not manufactured failing-first results.

All paths must exist and hashes must match before the verifier accepts them. Stable/browser/artifact evidence is recorded in manifest.md; an unrun or failed gate cannot be converted to pass by this contract.
