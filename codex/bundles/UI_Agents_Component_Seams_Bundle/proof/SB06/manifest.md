# SB06 coverage and harness audit

Baseline: 68db2ee0e63a2ce6baa681e9722acc0a67877b21, branch components-decoupling, live sibling source. No commits, new projects, or sibling changes.

The selector initially extended SB05's 114 cases with five ExternalWorkspaceRootSelectionFieldTests and eight StorageCatalogSelectionComponentsTests. Final source review added two delayed catalog-refresh cases; real browser proof added one actual page first-save/update echo case. Final expected/discovered/executed count is 130, all passed in transcripts/component-browser-corrected-results.log. Unit expected/discovered/executed count is 28, all passed in transcripts/unit-results.log. These are the existing root/storage scenarios required by B22, not unrelated class expansion. Historical transcripts/component-discovery.log contains the earlier 127 cases; final exact discovery is component-browser-corrected-discovery.log with 130 named/data cases. Unit closure expects the existing 26 route/state/context cases plus two AgentEditorDraftPolicyTests, total 28. Actual result transcripts determine acceptance; counts alone do not.

WorkflowsPageTests.Agents_shell_exposes_workflows_page_navigation now awaits the actual agents-shell-open-workflows button click. Other Workflows cases are untouched. AgentsHomePageTests.Simple_chats_follows_agents_and_renders_both_nested_workspaces awaits tab click dispatch and completion of the normally constructed real conversation gateway query before checking the rendered conversation workspace and disposing its services. The earlier SB05 114-case run passed 113 and failed during database-service disposal after that test switched away while reads were pending. The subsequent six-case page rerun passed but source verification showed it did not yet contain the intended waits; it is a rerun, not proof of the repair. A later 129-case attempt still caught pre-load teardown; the explicit real-query completion observer resolved it. The final component-browser-corrected build/discovery/results (130 cases) are authoritative for the current source. See SB07/architecture-review.md for the later browser-driven host correction.

Affected editor tests render the real AgentDetailsDialog. Fake readers and workspace probes are normally constructed at public operation boundaries. Reflection still used by DispatchProxy.Invoke implements the public interface dispatch; it does not read private component members or allocate uninitialized services. Catalog tests render the actual host or render the controlled panel with no service registrations. Numeric tab/private editor subclasses were removed with their owning phases. Session and command tests exercise real EditForm contexts and real registered adapters. Durable ownership proof consists of no-service controlled rendering, real constructor composition, draft-copy policy and adversarial lifetime tests, not file-count or injection-count assertions.

See coverage-map.md for B01–B30, transcripts for commands and discovered names, and SB07 for governed source/artifact integrity, broad gates and browser closure. Missing or failed evidence reopens the owning phase.
