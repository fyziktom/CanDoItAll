# SB04 editor section and session entry

SB03 passed 23 focused cases. B12 was characterized by both core-failure tests; existing blank/editable behavior is preserved because the optional separate-fix question has no owner answer. This is an existing defect, not a safety guarantee. Session safeguards in B16/B19/B20 are authorized refactor requirements.

Chosen boundaries: AgentEditorSection maps ten semantic identities to the existing Tabs API. AgentEditorTarget is distinct from catalog selection. A component-owned AgentEditorSession holds the mutable draft/EditContext/target and cancellation lifetime; replacing/resetting/disposing invalidates captured work. No DI session or test-only InitialSession parameter. IAgentEditorReads/AgentEditorReads handles core load and independent provider/project refresh; IAgentEditorAccessQuery/AgentEditorAccessQuery translates Projects/Security picker data to minimal Id/Name/KindLabel UI projections through normal concrete adapters. Secret kind is display text only; no secret value enters the projection. Commands and normalization remain SB05 responsibility.

Expected existing editor coverage: 30 AgentDetailsDialog family cases + four characterization cases, migrated from private reflection/uninitialized services to actual component loading through a deterministic IAgentEditorReads fixture and public section parameter/buttons. Sixteen dependent page/catalog host cases remain available from SB03 and are invalidated where target callbacks change. New AgentEditorSessionTests: Section_changes_retain_edit_context_and_unsaved_draft; Target_change_ignores_old_load_completion (success/failure); Reset_uses_create_target_without_changing_catalog_selection; Dispose_ignores_delayed_load; Separate_editors_keep_independent_drafts; Reference_failures_are_independent (providers/secrets); Projects_remain_lazy_and_retry_does_not_replace_draft. Expected nine new cases. Current production readers also require normal-DI load/mapping proof; add exact cases before implementation of that proof.

Real descendant coverage carries forward: AvatarPicker/gateway, workspace root field/registry, storage field/source/nested dialog, runtime provider controls. New all-section/lazy-reference cases must register and render the real Memory child/store/drivers; do not stub the section. The existing SharedProviderRefreshButton service boundary and capability wizard are owned by SB05 interaction proof. External module/project extraction remains out of scope.

Additional reader proof frozen: Registered_reader_loads_existing_identity_version_and_reference_adapters (one case) and Registered_reader_returns_independent_drafts_for_new_and_existing_targets (one case). Component total for initial SB04 discovery is 45: 30 existing + 4 characterization + 9 session + 2 reader. A retry failure in a newly immediate deterministic read must be captured before repairing single-flight state.

## First behavioral run and retry safeguard

Discovery: expected 45, actual 45. First execution: 44 passed, one failed, zero skipped (transcripts/component-first-results.log). All 30 existing editor cases, four characterization cases, production readers and stale-target/dispose/instance/section cases passed. Projects_remain_lazy_and_retry_does_not_replace_draft failed because the single-flight field retained a synchronously completed failed load after its finally block cleared it. The second click reused the completed task and did not issue a read. Repair retains only a still-pending task; no automatic load/retry introduced. This transcript is actual failing-first behavioral evidence for B11/B19, not a missing-type compile failure.

Additional lifecycle coverage frozen: Reset_invalidates_pending_project_result_and_allows_new_request (one case). Host keeps per-presentation typed targets, updates them from the real editor callback, and releases them on close/dispose; the reset test now checks that consumer as well as catalog selection. No URL behavior is introduced. Source changes reopen the 23 SB03 dependent cases. Expected combined discovery 69 (46 editor + 23 catalog/page), before any further additions.

The first pending-reset test attempt had an ambiguous Clear selector and is not behavioral proof. Corrected to the real StickyActionFooter control. The reset-behavior-first-results.log and closure-results.log assertions expected one read after enabling project access, but that toggle already requests the list in existing behavior. These are invalid test-oracle failures, not evidence of a production defect. Corrected the oracle to assert one read before the toggle, rejection of the stale response, and a second read after the toggle. Reset now clears that unfinished demand/error while retaining already-loaded reference data. B11/B19 require an explicit new request after cancelling a pending old one. Host target callbacks are scoped by presentation identity; close/dispose releases the target and rejects late Saved callbacks. This does not retain dialogs across navigation.
