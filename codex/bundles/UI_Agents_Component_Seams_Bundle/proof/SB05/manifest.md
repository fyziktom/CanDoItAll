# SB05 entry and behavioral contract

SB04 corrected lifecycle run passed 69/69, zero skipped. Contract placement subsequently moved shared service inputs/results out of Pages; build passes and dependent gates are reopened until rerun. Current local CodeAnalytics snapshot snap-20260905015214-4cc89364 identified that dependency; it is stale after the placement correction. Existing unrelated Hosting/root and nested image-provider cycles are not new extraction boundaries.

Frozen additional failing-first cases: Reset_ignores_late_save_completion (success/failure), Dispose_ignores_late_save_completion (success/failure), Save_snapshots_draft_and_preserves_later_edits. Expected five in AgentEditorCommandLifetimeTests. Public workspace probe delays acknowledgement; actual component/read adapters and registered host services remain in use. These are publication/request ownership tests, not database commit proof. Existing SB01 acknowledged-write/refresh-failure transcript remains the baseline for the corrected explicit committed outcome.

Current proof: 20/20 expanded command/child cases and 2/2 pure policy cases passed. Pure policy cases are Capture_preserves_version_and_owns_nested_mutable_state and Capture_normalizes_tags_and_defaults_without_changing_visible_draft. Memory child preserves its existing empty allowlist semantics (adding a context binding only extends a nonempty existing allowlist); the initial expectation of an inserted selector was a test-oracle error, not a production regression. Shared-provider test initially failed compilation because its typed routing ID needed .Value; the discovery mismatch correctly blocked stale assembly execution.

Target echo: target-echo-settled-results.log proves the actual host echo replaced EditContext; host-target-first-results.log shows the echo passes after retaining already-acknowledged target identity. The latter run's stale catalog-result case was initially synchronized incorrectly because the host queues dialogs and does not render target cleanup. host-result-lifetime-results.log uses a bounded dispatcher wait for that public lifecycle state and demonstrates the actual defect: a stale old-target deleted result clears catalog selection after Clear. Host now consumes target identity when accepting Saved/closed results. This is actual producer/consumer/lifecycle evidence, not bookkeeping-only preparation.

Host result consumer audit reopens SB03/SB04 result handling: extend Reset_uses_create_target_without_changing_catalog_selection to deliver an old-target deleted result after Clear and assert the original catalog selection remains. The target dictionary must be consumed for result identity validation, not only exposed as bookkeeping. No production deletion is issued in this delayed-result test.

Host-echo safeguard frozen: Target_echo_after_first_save_retains_session_and_later_edits (one case). After first acknowledgement the target has already changed from Create to the returned ID; an owner echoing that same ID must not cause an unrelated session reload. The current catalog host stores the target and future hosts can also echo the public AgentId parameter. This is B16/B19 contract proof, not route retention.

Further actual-child proof frozen: Real_capability_wizard_creates_and_assigns (new/existing agent, two cases), using the actual dialog host, inline-skill wizard and workspace catalog persistence. No capability execution. Managed_delete_is_rejected_by_registered_command_and_preserves_catalog (one case) exercises the existing application guard through the real command adapter. Registered_commands_reject_root_preparation_before_write (one case) isolates a normally constructed external-root adapter failure. Existing 13 Memory child cases prove validation, ordering, requirement and selector pruning; count will be confirmed by discovery.

Additional proof frozen before child/session callback edits: Registered_commands_round_trip_settings_and_preserve_optimistic_version; Real_save_refresh_retry_reuses_acknowledgement_without_another_write; Real_save_callback_failure_keeps_current_version_and_does_not_replay; Unknown_write_outcome_retains_draft_and_requires_catalog_check; Existing_capability_toggle_saves_whole_unsaved_draft_new_agent_stages (two cases); Editor_memory_binding_round_trips_real_child_settings; Shared_provider_refresh_retains_editor_draft (success/failure); Avatar_generation_failure_retains_unrelated_draft; Reset_rejects_pending_avatar_result. Actual registered command adapter, public workspace probe, real descendants and disposable catalog are required. Existing 13 Memory and two SharedProviderRefreshButton descendant cases are included in the phase selector after exact discovery. AvatarPicker itself is in an external owned project: parent/session callbacks can reject stale draft publication, but its existing global notifications remain an explicit child-lifetime limitation until separately scoped; do not claim all descendant notifications are session-scoped.

Initial five-case fixture failed before behavior because its registered workspace factory was not resolved; that transcript is not failing-first behavior proof. Corrected run showed actual reset-success, disposal-success/failure and mutable-request failures. Reset-failure also contained an unsynchronized form-render assertion, fixed by waiting for the blank draft before completing the old save. First implementation run: 82/83 passed; only that test synchronization failed.

Command design: narrow IAgentEditorCommands with actual workspace adapter, explicit save acknowledgement followed by read reconciliation; component-owned pending acknowledgement prevents another write while identity/version is unresolved. Retry refresh repeats reads only. Callback failures after acknowledgement are reported separately and not automatically replayed. A captured, deep copied draft isolates pending writes; late local edits survive reconciliation with the returned identity/version. Existing whole-draft capability writes, new-draft staging, Save staying open, Clear creating a new target and exact delete result channel remain invariants. A stale session may not publish success, failure, confirmation, callbacks, busy state or refreshed data.

Pure draft copying/tag/runtime/image/project normalization has its own responsibility; external-root registry resolution belongs to the actual command adapter. No new project or public cross-module contract extraction. Real adapter/version/conflict/normalization and descendant scenarios will be named here before their implementation.
