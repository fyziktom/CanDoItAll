# SB03 controlled catalog entry

SB02 gates passed (26 Unit/15 Components). Scope B04–B08/B29/B30 preservation. One production caller, AgentsHomePage. No project/sibling changes. AgentCatalogPanel retains its existing real markup/CSS/card/tree children and local search/expansion. AgentCatalogSnapshot, AgentCatalogSelection and typed AgentCatalogIntent variants carry controlled inputs/output. AgentCatalogHost owns request/open-once, selection/context publication and existing dialog/chat result handling; a single Razor file avoids another partial file. IAgentCatalogOperations/AgentCatalogOperations owns actual load/repair/team writes. Host effects remain at the UI host; operations have no component/callback/navigation dependencies.

Frozen tests: existing ten AgentCatalogPanelTests (including two card cases), six AgentsHomePageTests and three selected Unit context cases. Replace private-field requested echo assertion with select -> parameter echo -> no dialog; replace private saved handler with real dialog result -> refreshed catalog/no selection. Add AgentCatalogBoundaryTests: Controlled_panel_search_and_selection_emit_intents_without_services; Requested_agent_opens_once_and_changed_request_opens_again; Real_operations_respect_initial_data_and_repair_then_reload; Team_delete_preserves_no_confirmation_and_clears_selection. Expected component total 20 before further discoveries; selected Unit context remains three. Add cases if review finds uncovered team/member behavior; record before edits to that scenario.

Preserve exact managed IDs and current delete confirmation differences. No claimed editor sandbox or measured speed improvement. The catalog-only candidate omits editor/team host interactions explicitly, while its real AgentSelectionCard/TreeView/assets remain in the graph inventory.

Coverage refinement before additional tests: add Real_operations_update_and_delete_only_the_selected_team and Team_dialog_results_refresh_once_and_cancel_has_no_write (two data cases: details/members). These cover actual team writes and host cancel/result paths omitted by the historical ten cases. New expected component total 23. Controlled rendering test will use static data and only BaseLib services; production host/operation tests retain the real harness. Missing member-write failures remain explicit review candidates until covered.

## Executed gates

Direct production build and refreshed component assembly: exit 0, zero warnings/errors. Initial compile found an ambiguous provider model import, fixed with the existing runtime-service alias. Discovery expected 23, actual 23, execution passed 23 with zero skipped (transcripts/component-discovery.log, component-results.log, production-build-final.log). All original ten catalog behaviors retained. Public result tests replace both private-reflection assertions. Real team operation tests use the disposable component test database; deleting the target leaves the second team intact. The controlled-render case uses only BaseLib services and static agent data. Source scans find no feature services/dialog/chat injection in AgentCatalogPanel and no private reflection in AgentCatalogPanelTests. Existing selected context-builder tests remain reusable from SB02: their production builder/model source has not changed; current page interactions ran here.

SB03 owning gate passes. Catalog view contains presentation/filter/tree/typed intent logic; host contains load/request/selection/dialog/chat coordination, and operations own repair/catalog/team I/O. A tree-member selection retains its distinct original host behavior via SelectTeamMember; card selection suppresses the requested-ID echo via SelectAgent. No URL/project/sibling changes. Snapshot contracts still use AgentDefinition/AgentTeamDefinition and existing card/tree assets, so lightweight project/browser sandbox readiness is not claimed. Final full graph/asset handoff remains SB07.
