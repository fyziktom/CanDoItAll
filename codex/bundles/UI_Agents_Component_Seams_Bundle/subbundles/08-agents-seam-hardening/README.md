# SB08: Agents seam hardening

Status: source hardening and selected functional gates passed; the pre-existing tracked-proof-log documentation/merge blocker remains open. See [current closure](../../reviews/03-hardening-closure.md) and [compact evidence](../../reviews/04-hardening-evidence.json). Owner authorization: the 2026-09-05 follow-up review and request to analyze and repair HEAD 96ee03a97c510d5363636fb06b903b9bc12f47dc. SB01-SB07 evidence remains historical; SB03-SB07 closure is reopened for the contracts below.

## Accepted scope

Correct save outcome classification at its real producer boundary; fail closed on core editor load with same-target Retry; rearm requested selection after null/invalid transitions; cancel host-owned operations and fence late publications. Remove draft policy dependence on Razor, guard public snapshot completeness, make section mapping explicit, move capability reads to reads, distinguish dialog shell/content IDs, and relocate service-only tests into their owning test lanes. Reflect reusable lessons into the shared architecture bundle.

No new projects, sandbox, production routes, dependency references, sibling edits, commits, merge or history rewriting. Existing successful save, optimistic concurrency, partial reference failure, Clear from a ready editor, lazy project loading, and editor-instance ownership remain required.

## Entry gate

Pass for this bounded repair: current source confirms the review's primary defects, and the owner explicitly authorizes the formerly deferred unsafe-load repair. The pure definition-validation boundary must distinguish rejection before persistence from unknown persistence failure. The production projection exception carries a committed identity. Unknown writes remain blocked from replay.

## Acceptance and validation

Behavioral proof tier. Required positive and adversarial cases cover known rejection/commit-warning/cancellation/unknown, failed load and Retry without changing identity, request A -> null/invalid -> A and unchanged echoes, disposal during reads/mutations/dialog/chat work, copy completeness and nested ownership, stable section mapping, and real registered adapter persistence/projection paths.

Build each changed production project and the owning Unit, Components and Integration assemblies. Select exact affected topics after source/test inventory; record expected and actual discovery before execution. Invalidation keys: editor/catalog contracts, Core draft validation, model normalization, moved test owners, workspace projection acknowledgment. No unfiltered stable gate is presently selected: these changes have bounded consumers and do not change schema, shared runtime composition or build infrastructure. Expand only for a named newly discovered invalidation trigger.

Run the complete proposed-source portability scan, review every delta and require final no-write enforcement. Validate maintained documentation. Record compact current evidence and large-desktop browser behavior; do not relabel historical proof as a new run.

UI composition: catalog remains the primary page surface. The existing full-size ten-section editor keeps its body scroll owner and stable actions. Failed core loading replaces the form with a shared Alert and Retry/Close actions visible in the first viewport; no metrics or sizing redesign.
