# Owner hardening review, 2026-09-05

The owner supplied a review of commit 96ee03a97c510d5363636fb06b903b9bc12f47dc and requested: "analyze it and improve/repair it."

Accepted requirements: accurate pre-write rejection/commit/commit-warning/unknown outcomes; failed core loading cannot expose an editable create form; requested A -> null/invalid -> A can reopen; host lifetime cancellation and stale completion fences; pure draft policy independent of Razor; public mutable-copy completeness guard; stable section mapping; capability reads on the read port; unique dialog shell/content IDs; correct Unit/Components/Integration placement; shared architecture lessons.

The review's suggested fully controlled host API is an alternative, not a mandate. This bounded repair preserves existing host parameters and makes mirror/acknowledgment transitions explicit. Generic Conversations UI must not gain an AgentFramework model dependency solely to share string trimming.

The follow-up catalog UI assembly/sandbox and watch measurements remain separate. The merge/history advice does not authorize staging, commits, merge or history rewriting. Historical proof blobs are left unchanged; new hardening evidence is compact.
