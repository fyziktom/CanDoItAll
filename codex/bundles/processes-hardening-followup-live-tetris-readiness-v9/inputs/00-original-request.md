# Original Request

The user asked Codex to follow the prepared bundle in `codex/bundles/processes-hardening-followup-live-tetris-readiness-v9`, use the `candoitall-bundle-workflow` skill, improve the bundle first, then execute it.

The user clarified that a later demonstration video may build a simple sample app, but the reusable Processes runtime, process templates, skills, and instructions must stay generic for common Blazor WASM PWA application delivery. They must be usable for any Blazor WASM PWA topic and must not contain hardcoded app-topic names or instructions.

## Literal constraints

- Use `candoitall-bundle-workflow`.
- Improve the bundle before implementation.
- Remove or repair topic-specific process/template instructions.
- Keep the Blazor-related process generic for common Blazor WASM PWA application delivery.
- Execute the repaired bundle and validate changes with tests.
