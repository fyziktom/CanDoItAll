# Original Request

User feedback in the current Codex thread, May 18, 2026:

> great. now just few things to tune.
> some of them might be little more complex than it looks like on first sight so create own bundle with [$candoitall-bundle-workflow](C:\Users\dell\.codex\skills\candoitall-bundle-workflow\SKILL.md) for this.
> 1) first small thing is to add delay also for all menu items before tooltip will popup. at least same as we have for Opened project on floating menu. few seconds.
> 2) then I need you to improve menu so modules can also can add another items. for example we have module agents that adds in menu Agents item. but this module contains also workflows, that we need as menu item too. it does not have to be somehow under agents visually. the related menu items should be just as next items after the main of that module.
> for example now we have it like this
> ....
> CRM/HR
> Agents
> Resources
> ...
> and we need it like this:
> CRM/HR
> Agents
> Workflows
> Resources
> ...
> in case that module specify that some its subpage must be also in main menu. This will need to add this in kind of generic form for all modules. Module must specify that it is subitem, but now in menu it will look the same to simplify desing. later we will do some better subitems system in menu. but it is tricky so skip it for now and just make a note why we have this prepared.
