# QA Prompt

Validate the completed subbundle as a reviewer. Check whether requirements are actually implemented, whether new persistence has migrations, whether tests prove the important regression, and whether the execution report contains concrete proof. Reject shallow status updates.
