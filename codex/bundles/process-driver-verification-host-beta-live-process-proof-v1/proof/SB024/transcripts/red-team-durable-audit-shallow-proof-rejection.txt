SB024 Gate H red-team durable audit shallow-proof rejection
Working directory: C:\repositories\CanDoItAll
Run label: Gate H adversarial negative proof

Invariant IDs:
- SB024_INV_001: verification audit records must have a durable EF-backed persistence model and PostgreSQL migration.
- SB024_INV_002: audit append and query must preserve observation hashes while redacting requester secrets before persistence and returned host results.
- SB024_INV_003: existing current PostgreSQL schemas adopted from EnsureCreated must record the full current migration chain, including the audit migration, before MigrateAsync runs.

Rejected shallow proof:
- A passing in-memory host test is insufficient because it does not prove audit records survive a new dependency injection scope or process lifetime.
- A migration file alone is insufficient because the runtime DI could still resolve the in-memory store in the full module path.
- A query API that returns raw RequestedBy values is insufficient because audit records can contain secrets from operators or automation callers.
- Recording only the initial baseline migration during schema adoption is insufficient because EF will try to apply already-represented post-baseline migrations and fail on existing objects.

Adversarial negative proof used:
- Process_verification_audit_store_SB023_INV_001_persists_redacted_hashes_and_supports_queries creates a new EF-backed store, appends through the host, disposes the scope, queries from a new scope, and rejects raw sk-test, hunter2, and bearer-token requester values.
- Bootstrap_adopts_existing_postgresql_schema_without_migration_history creates a PostgreSQL schema through EnsureCreated, verifies empty migration history, then proves bootstrap records the full expected migration chain including 20260610113813_AddProcessVerificationAuditRecords.
- durable-audit-entity-migration-source-assertions.txt shows the EF entity, table name, migration, module DI replacement, query service, and current PostgreSQL migration IDs are production source-backed.
- gate-h-source-diff-and-anti-stub-audit.txt rejects placeholder/stub/fake matches and bundle-path drift in Gate H source files.

Positive semantic proof used:
- durable-audit-focused-tests.txt shows the host-focused integration suite passed 25 tests, including durable audit persistence, redaction, hash, query, and no-mutation assertions.
- postgresql-audit-migration-bootstrap-tests.txt shows the migration bootstrap suite passed 3 tests with new-schema, idempotent, and existing-schema adoption coverage.
- durable-audit-redaction-query-source-assertions.txt ties test assertions to ISecretRedactor, ObservationHash, GetAsync, ListAsync, and ProcessVerificationAuditQuery production source.

Result:
PASS. Gate H rejects in-memory-only, migration-only, raw-secret, and baseline-only shallow proofs. Durable audit records are persisted through EF, queried through a bounded typed query API, redacted before persistence, and covered by PostgreSQL migration/bootstrap proof.
Exit code: 0
