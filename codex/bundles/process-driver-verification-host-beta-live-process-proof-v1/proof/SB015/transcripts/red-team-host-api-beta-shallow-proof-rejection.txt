Gate E red-team: host API beta shallow proof rejection

Rejected shallow-pass claims:
- A sync-only `Verify` method is insufficient for SB013 because the host beta API must expose `VerifyAsync` with a `CancellationToken` and a cancellation proof.
- Throwing exceptions for expected lane/payload denials is insufficient for SB014 because callers need a structured `ProcessVerificationHostResult` and `ProcessVerificationHostDenial`.
- A fallback selector is rejected. The host must select the exact requested lane and return an unsupported/unregistered-lane denial when that cannot be done.
- Generic object or dynamic payload dispatch is rejected. The host must keep typed lane payload selection over the existing lane-specific payload collections.
- Live OpenAI proof is irrelevant to Gate E; host beta proof must remain source/test-backed and must not require live-provider flags or secrets.

Positive proof required and present:
- Async/cancellation source assertions: bundle://proof/SB013/transcripts/host-api-async-source-assertions.txt.
- Structured denial source assertions: bundle://proof/SB014/transcripts/host-denial-source-assertions.txt.
- Focused host integration tests: bundle://proof/SB013/transcripts/host-api-async-and-denial-focused-tests.txt.
- Gate E anti-stub audit: bundle://proof/SB015/transcripts/gate-e-source-diff-and-anti-stub-audit.txt.

Result: PASSED. Gate E rejects sync-only, exception-only, fallback selector, and generic payload closure.
