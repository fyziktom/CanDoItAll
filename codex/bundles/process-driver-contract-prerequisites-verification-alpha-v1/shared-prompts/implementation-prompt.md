# Implementation Prompt

You are implementing `process-driver-contract-prerequisites-verification-alpha-v1`.

Work phase by phase. Do not skip gates. Do not introduce production driver APIs, registries, DI registrations, runtime selectors, manager commands, shell execution, Office/Graph runtime calls, workspace writes, storage writes, or process mutation.

Keep Core deterministic and dependency-clean. Any Core public API addition must update owner classification and architecture tests.

At every critical gate, write proof transcripts and update `reviews/01-execution-report.md`.
