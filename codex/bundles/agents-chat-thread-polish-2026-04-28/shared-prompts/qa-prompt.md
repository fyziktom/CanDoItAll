# QA Prompt

Validate the Agents chat page at `/agents?tab=chat`. Confirm the left thread card fits, the selected title is editable, the switch-agent modal filters by text and tags, favourite stars persist and sort first, and no dialog content is clipped.
