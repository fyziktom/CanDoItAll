# Implementation Prompt

Implement the Agents chat thread and switch-agent modal polish with the smallest service and component changes. Use shared BaseLib components for editable text, tag filtering, tooltips, dialogs, buttons, and layout. Persist thread title edits and favourite agent tags explicitly.
