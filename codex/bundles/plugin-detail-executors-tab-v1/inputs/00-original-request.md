# Original Request

Use `candoitall-bundle-workflow` to solve this:

Most of the plugins will have some executors for workflows. Add to the plugin details on plugin page (as another tab in plugin detail) the list of the plugin available executors with their short description/instructions. Each plugin must have this kind of info inside so it is loaded dynamically from the plugin.
