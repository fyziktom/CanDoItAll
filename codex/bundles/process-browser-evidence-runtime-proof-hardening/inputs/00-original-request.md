# Original Request

Source: development DB review requested on 2026-05-22.

```text
it is getting closer. Look at the db now. I tested the workflow (that passed ok) and then process of multi team sw delivery. it did not so bad job, but final app was not properly tested. there are not screenshots evidences.
if it would do the test with playwright mcp it would discover that the items in tetris are not comming (or at least they are not visible and there is some js trouble in console output of the running app). this should not happen when I run complicated process like this.
Use [$candoitall-bundle-workflow](C:\Users\lucys\.codex\skills\candoitall-bundle-workflow\SKILL.md) and analyze it and prepare detailed bundle about how to repair/improve it. Our processes core still must remain generic. Same as process definition can be like multiteam delivery for software, but detail should be in project strucure info (usually general info about architecture) and then in skills and instructions of the agents. Or in the process steps definitions (like it must always capture screenshots and validate if it does what it should...this is generic no matter what language is app written in, etc).
```

## Raw Note IDs

| Id | Literal note | Normalized destination |
| --- | --- | --- |
| `N001` | "final app was not properly tested" | Runtime proof gates and final demo-readiness proof |
| `N002` | "there are not screenshots evidences" | Browser evidence contract, storage projection, artifact-record validation |
| `N003` | "Playwright MCP ... would discover that the items in tetris are not comming ... not visible" | Representative interaction and semantic UI acceptance checks |
| `N004` | "there is some js trouble in console output" | Console diagnostics capture and pre-stop/post-stop classification |
| `N005` | "this should not happen when I run complicated process like this" | Release-readiness gating and conformance observations |
| `N006` | "processes core still must remain generic" | Architecture boundaries and process-definition/agent-instruction split |
| `N007` | "detail should be in project strucure info ... skills and instructions ... process steps definitions" | Project-structure acceptance hints and generic process step evidence contracts |
