# A04 isolated delivery closure

A clean `git archive` of each recorded repository was materialized into an isolated sibling layout; only explicitly inventoried owned proposal files were overlaid. FileTools received zero overlays. No worktree/index/history operation was used. The complete input manifest, archive hashes and overlay hashes are in [source-layout.json](source-layout.json).

Direct isolated Module and Web builds passed using explicit sibling-root properties. The initial long Windows path hit the existing template-copy path limit; the retained retry used a verified task-owned short drive mapping. Both actual commands and timestamps are in [commands-short-root.json](commands-short-root.json). [Final built-input comparison](final-built-input-comparison.json) confirms all ten owned production/test/sibling inputs still match the successful isolated build. Later proof-tooling/documentation changes do not enter those .NET compile inputs.

Common and BaseLib were packed locally as `0.3.0-overview01d.20260907193451`, never published. A neutral external consumer restored, built and executed against those exact packages: [commands](external-commands.json), [package graph](external-package-graph.json), [package hashes](package-receipt.json). It has zero ProjectReference inputs and exactly the intended two packages. The first repeated CLI source attempt was a setup failure; explicit NuGet.Config resolved it.

Complete primary package mode is not claimed. A follow-up packed all seven required Components packages locally and attempted the actual primary Module build with source substitution disabled. Restore returned NU1101/NU1102 for unavailable FileTools 0.3.0 packages (the existing public packages exposed 0.1.18, and Spreadsheet was absent). [Exact follow-up adjudication](primary-package-followup/adjudication.json) and commands retain this concrete setup limitation. No FileTools source was changed or package published. The successful external API consumer and isolated source build remain independent passing proofs.

The temporary short drive mapping has been removed after verifying ownership: [initial removal receipt](short-root-removal.json) and [final follow-up shutdown](primary-package-followup/shutdown.json). The archived source and local packages remain available in ignored scratch. The retained script contains the resulting reproducible protocol; its successful staged commands, rather than an unexecuted claim about the revised convenience script, are the proof.

Real delivery order remains Components commit/push, primary commit/push referencing the available contract, then clean post-push verification. This task stages, commits and pushes nothing.
