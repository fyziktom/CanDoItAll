# A01 retention closure

Allowed strategy 1 is implemented: exact historical Overview and Governance proof is retainable through two narrowly scoped ignore exceptions and two byte-preserving attributes. No old proof file was edited, recompressed or relabeled as fresh. The old root manifests are also preserved compressed before current document corrections. Entry inventory records every file, size, compression estimate, MIME role, owning claim and Git membership; the final complete retained secret scan resolves its pending scan field.

The deterministic proposed-inventory validator passes all 1186 Overview entries and 100 Markdown links, and all 33 Governance entries and 24 links at the retention checkpoint. Those reports precede the subsequent punctuation/document follow-up; final roots must be resealed and checked in A06. The same validator's actual-tree mode reads Git blobs, so local-only evidence cannot satisfy a post-push claim. Fourteen direct tooling tests cover ignored proof, missing tree data, hash mismatches, unsealed members, path escape, Markdown links and text encodings.

New execution evidence lives under retained/Axx. The owner command and repository commit order are recorded in ../../plan/owner-verification.md. No file was staged or committed. Historical pass/failure and timing statements remain historical. Governance preparation proof is now retainable before any Governance execution can begin.

## A06 follow-up

The original 14-case checkpoint remains historical. Two subsequent validator regressions now bring the current tooling selection to 16 passing cases, including proposed Git clean-filter byte preservation and actual-tree verification from a temporary Git fixture. Five fresh fixture provenance notes and three narrow byte-preserving attributes retain historical outside-proof files without changing their recorded bytes. Final A06 manifests supersede entry counts only; old proof claims remain unchanged.
