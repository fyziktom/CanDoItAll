# Initial public regression adjudication

Production remained unchanged for the RED runs. Unit: 8 cases, 6 passed and the two exact display-name cases failed on the extra U+00C2. Components: 10 cases, nine delayed-registration cases passed and the nested team icon picker remained beside the unrelated overlay after its editor closed. Sibling: 15 initial cases, 13 passed; the pre-canceled owner left an orphan reference. A separate cross-origin case failed in NavigationManager URI validation before the service ran, so it is setup failure, not product RED. The invalid cross-origin fixture was removed; the corrected owning filter contains 14 cases. External full document navigation disposes the application owner; internal different-path navigation is tested directly.

All six session and nine usage-dialog delayed-registration witnesses passed. Each canceled token observed prompt cancellation, accepted a later registration once, and was disposed by completion; old success/failure/cancellation/finally did not replace current state or clear newer loading. Repeated owner disposal did not repeat cancellation. Source review confirms each CTS has one guarded disposal path. No Overview session, query or usage-dialog production correction is justified.

The bounded fixes bind the team nested overlay to its component token and suppress late completion after removal; DialogService adds a reference before installing cancellation so an immediate callback can remove it. A completion racing registration also disposes the completed registration. No global CloseAll call or generic navigation framework was added.
