# Reproducing the Phase A browser acceptance

The real Web fixture is the exact historical source retained at `UI_AgentsOverview_01_State_Read_Seams_Bundle/proof/O03/tools/overview-browser-fixture`. Its Program.cs and project hashes were compared with the newly built fixture and are identical. Copy those two files into `.mcp-state/overview-browser-fixture` in a clean checkout; their relative project references intentionally resolve from there. Build that project in Release after the recorded sibling contract is available. This fixture uses real Web composition and isolated test persistence with public read fault seams; it does not call a live provider.

Use Node 24.19.0, Playwright 1.61.1 and its Chromium 149.0.7827.55, as recorded at entry. Copy `browser-web.cjs` and `run_browser.py` from the parent A06 directory to a fresh ignored replay directory. Invoke the copied Python runner with `--playwright-module` pointing to the installed Playwright package and a unique `--attempt` value. Do not invoke it in the sealed retained directory: command receipts and script snapshots belong to the new execution, not to this proof. The browser fixture uses ports 5275 and 17305; verify they are free first.

For sandbox compatibility, build the existing sandbox in both `CatalogAssetMode=Parity` and `CatalogAssetMode=Fast`, then run a copy of `browser-sandbox.cjs` with `OV_PLAYWRIGHT` set to that same package path. It uses ports 5393 and 5394, verifies runtime mode and real asset requests, captures 25 images per mode, and stops only its spawned process tree. Preserve the new output as a separate execution rather than rewriting any historical capture.

The harness drives real DOM events and browser Back/Forward. The product's tabs replace history; the Defaults navigation scenario seeds browser history with the public browser History API before Back/Forward. It does not inject application state or invoke private component methods. The fixed initial exact-label selector failure and missing runner module argument are retained separately.
