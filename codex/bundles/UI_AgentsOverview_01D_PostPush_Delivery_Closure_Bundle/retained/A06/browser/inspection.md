# Fresh browser inspection

Web completed 32 frozen scenarios at 1600 x 1000, with additional 1280/900 responsive checks. The six retained acceptance images were visually inspected: normal Overview, Governance context label, Defaults after query/fragment navigation, nested team icon picker, editor/picker cancellation, and actual page departure. Exact DOM assertions verify `Agents · Overview` and `Agents · Governance`, no extra mojibake prefix, identical Defaults reference across navigation, and removal of owned dialogs.

Parity and Fast each completed 18 scenario assertions and 25 captures. Both baseline and long-content 900-pixel captures were inspected. Real Apex chart scripts, avatars, fonts, actual component controls and responsive layout remain present; no horizontal dashboard overflow or external runtime requests occurred. This is fresh browser compatibility proof, not new watch timing.

Selected exact JPEGs and compressed DOM/host receipts are retained. Capture inventories preserve hashes and explicit omission reasons for redundant local captures. The initial Web exact-label selector timeout and the runner's missing module-argument failure remain separate setup receipts; neither is relabeled as a passing attempt. All successful browser-owned hosts stopped.
