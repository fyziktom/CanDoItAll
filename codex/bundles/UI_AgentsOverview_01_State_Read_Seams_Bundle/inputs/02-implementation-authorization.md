You are Codex GPT-6 Astra running at xhigh reasoning effort.

Act as a senior .NET 10 / Blazor architect, asynchronous state-machine
reviewer, UI boundary engineer, browser validation engineer, and direct-watch
measurement engineer.

Repository:
    fyziktom/CanDoItAll

Branch:
    components-decoupling

The remotely observed branch head at prompt preparation time was:

    ad2ded645e65b8b205959f6fed816d082b99a7be
    capabilities phase2

Its observed direct parent was:

    b540b465cf408dd9c9adeb939538b78ced9bc7bb

Its observed committed tree was:

    3c9d3eb3eded55bf3e1aea83225dbccf8635dc66

This is an observation, not a pin. Rediscover the actual local and remote
state before editing.

All source-code comments and bundle documentation must be in English.

Do not ask for clarification. Inspect the actual current source and make the
smallest correct decision. A prepared plan is input, not proof. Correct it
when current committed source or meaningful RED evidence disproves an
assumption.

======================================================================
MISSION
======================================================================

Perform three sequential activities:

A. Seal the committed Capabilities-02G/03 predecessor and adjudicate two
   bounded extraction-handoff concerns.

B. Correct where necessary and execute the already prepared bundle:

    codex/bundles/UI_AgentsOverview_01_State_Read_Seams_Bundle

Reference:

    CDA-UI-SEAMS-AGENTS-OVERVIEW-01

Execute sequentially:

    O00 -> O01 -> O02 -> O03

C. Only after Agents Overview-01 closes, prepare but do not implement:

    codex/bundles/UI_AgentGovernance_01_State_Read_Seams_Bundle

Proposed reference:

    CDA-UI-SEAMS-AGENT-GOVERNANCE-01

Do not implement Governance production changes during this run.

======================================================================
AUTHORIZATION RECORD
======================================================================

Preserve the existing Overview preparation directive as historical input.

Add the current owner instruction as a new execution authorization under the
Overview bundle, for example:

    inputs/02-implementation-authorization.md

Update the Overview README/execution ledger so that:

- preparation remains historical;
- O00-O03 are now owner-authorized;
- execution remains sequential;
- each child records its own closure before the next child begins;
- no document pretends that planned proof was already executed.

Do not rewrite predecessor closure observations.

======================================================================
REPOSITORY SAFETY
======================================================================

Before any source edit, rediscover and record:

- current branch;
- local HEAD and committed tree;
- independently read remote branch HEAD;
- local/remote ahead-behind state;
- index and worktree status;
- untracked files;
- current Components sibling revision, branch and cleanliness;
- current FileTools sibling revision and cleanliness;
- actual live sibling source/package mode;
- SDK from global.json and dotnet --info;
- Node, Tailwind, Playwright and browser versions;
- current generated production and Fast assets;
- current solution and project graph;
- current test discovery.

Do not reset, clean, checkout over, overwrite or discard unrelated work.

Do not merge, rebase, squash, amend, rewrite history or perform branch
cleanup.

Do not commit or push.

Do not modify sibling repositories unless a separately proven blocker makes
a sibling change unavoidable. No sibling change is expected or authorized.

Do not claim repository merge readiness.

======================================================================
ACCEPTED PREDECESSOR
======================================================================

Treat these bounded programs as closed unless the committed-tree seal or a
new direct regression disproves their result:

- CDA-UI-SEAMS-AGENT-CAPABILITIES-01
- CDA-UI-SEAMS-AGENT-CAPABILITIES-02
- CDA-UI-SEAMS-AGENT-CAPABILITIES-02G
- CDA-UI-SEAMS-AGENT-CAPABILITIES-03

Preserve the accepted architecture:

- AgentCapabilitiesPanel is the application effect host.
- Capability session, commands, recovery and persistence remain in Module.
- AgentCapabilitiesSurface is service-free.
- Pure capability rendering/state/intents live in AgentFramework.UI.
- AgentCapabilityList has one real implementation used by both real
  consumers.
- Application outcome and recovery types do not move into UI.
- Explicit acknowledgement is circuit-scoped and does not prove rollback.
- Provider/catalog recovery rules remain unchanged.
- Current catalog revision and immutable provider fingerprint behavior remain
  unchanged.
- Existing Capabilities sandbox scenarios and query defaults remain.
- Production bookmarkability remains unchanged.
- General capability CRUD recovery remains outside this run.

Do not reopen Capabilities behavior merely because Overview touches the same
page or UI project.

======================================================================
READ BEFORE EDITING
======================================================================

Read at minimum:

Predecessor and shared architecture:

- codex/bundles/UI_Component_Seams_Shared_Architecture_Bundle/
- codex/bundles/UI_Agents_Component_Seams_Bundle/
- codex/bundles/UI_AgentCapabilities_02G_PreExtraction_Correctness_Bundle/
- codex/bundles/UI_AgentCapabilities_03_Extraction_Sandbox_Bundle/
- codex/bundles/UI_AgentsOverview_01_State_Read_Seams_Bundle/
- docs/testing.md
- current portability, documentation, secret and evidence rules

Current Overview/page ownership:

- AgentsHomePage.razor
- AgentsHomePage.razor.cs
- AgentsHomePage.razor.css
- AgentWorkspaceSection.cs
- AgentWorkspaceRouteState and AgentsWorkspaceState
- AgentFrameworkAgentsChatContextBuilder.cs
- AgentChatContextSurfaceProvider.razor
- AgentsWorkspaceQuery.cs
- BoundAgentResourceQuery.cs
- AgentOverviewModels.cs
- ProviderUsageContracts.cs
- ProviderUsageQueryService.cs
- all IProviderUsageProjectionSource implementations
- ProviderUsageConsumerList.razor
- AgentOverviewUsageList.razor
- AgentUsageDisplay.cs
- AgentUsageDialog.razor
- ProviderUsageDialog.razor
- ModelUsageDialog.razor
- all Overview/page/query/usage/dialog tests

Physical extraction and assets:

- CanDoItAll.AgentFramework.UI.csproj
- its current Capabilities and Catalog files
- CanDoItAll.AgentFramework.UiSandbox
- sandbox query codec and specimen composition
- Tailwind/input.css
- Tailwind/catalog-fast.css
- Tailwind/main/component-layout-utilities.css
- actual generated Web and Fast CSS
- current Charts package/static assets/JS registrations

Shared avatar handoff:

- AgentAvatarActionButton.razor
- CanDoItAll.AppComponents.csproj
- CanDoItAll.Conversations.Components.csproj
- their README files
- every repository consumer of AgentAvatarActionButton
- any repository public API, package or binary compatibility policy

Governance preparation input after Overview closure:

- AgentGovernancePanel.razor
- AgentGovernancePanel.razor.cs
- its existing tests and workspace operations
- execution run/detail/approval contracts

======================================================================
PHASE A — COMMITTED PREDECESSOR SEAL AND HANDOFF ADJUDICATION
======================================================================

Do this before Overview RED implementation.

----------------------------------------------------------------------
A0. Seal the pushed Capabilities result
----------------------------------------------------------------------

The historical Capabilities-02G/03 closure and original Overview preparation
correctly describe a run whose remote HEAD was b540... and whose new bytes
were unstaged/untracked.

The implementation is now observed in commit ad2ded....

Create a new predecessor handoff receipt inside the Overview execution proof.
Do not rewrite the historical closures.

Verify at least:

1. current local and remote commit identity;
2. parent and committed tree;
3. ahead/behind and worktree/index state;
4. Capabilities-02G manifest against committed bytes;
5. Capabilities-03 manifest against committed bytes;
6. Overview preparation manifest against committed bytes;
7. all relative links;
8. every production/source/asset path recorded by the final Capabilities-03
   proposed-source inventory against the actual committed tree;
9. moved-file absence at old paths and presence at new paths;
10. both live sibling identities and cleanliness;
11. generated asset identity where predecessor proof depends on it.

If committed production bytes differ from the bytes represented by the final
Capabilities-03 validation inventory, stop normal execution and rerun only
the semantically invalidated predecessor gates before accepting the handoff.

A commit existing is not proof that it contains the tested bytes.

----------------------------------------------------------------------
A1. Adjudicate AgentAvatarActionButton assembly compatibility
----------------------------------------------------------------------

The component currently has:

- implementation assembly: CanDoItAll.Conversations.Components;
- retained namespace: CanDoItAll.AppComponents;
- AppComponents -> Conversations.Components project direction;
- no reverse edge;
- repository source consumers rebuilt;
- no predecessor claim of external binary compatibility.

This is not authorization to move or rename the component again.

Before Overview measurement, determine from actual repository policy and
published artifacts whether the old AppComponents assembly identity is a
supported binary contract.

Prove one of these outcomes:

1. No supported binary contract exists.
   - Record why source compatibility is the required contract.
   - Preserve the current implementation and namespace.
   - Update only documentation if an actual omission exists.

2. A supported binary contract exists.
   - Add the smallest valid binary compatibility bridge, normally a .NET type
     forwarder from AppComponents to the current public type if technically
     valid.
   - Do not duplicate the Razor component.
   - Do not add a reverse dependency.
   - Do not create a new project.
   - Add a compiled external-consumer or ApiCompat-style regression proving
     the old assembly-qualified reference resolves.

Do not infer binary compatibility from successful repository recompilation.

Any required production correction must be completed and validated before
the Overview O03 pre-move timing baseline so that the Overview comparison is
not contaminated by an unrelated assembly change.

----------------------------------------------------------------------
A2. Freeze the responsive asset contract
----------------------------------------------------------------------

The current accepted source of runtime-composed responsive compatibility is:

    Tailwind/main/component-layout-utilities.css

It is imported by both:

    Tailwind/input.css
    Tailwind/catalog-fast.css

Do not copy these rules into Overview CSS or create a second compatibility
file.

Inventory the semantic selectors actually required by the Overview rendered
closure. Do not turn the current count of rules into an architecture
invariant.

If the existing shared file is sufficient, make no source change.

If browser evidence proves a missing runtime-composed selector:

- add it only to the single shared source;
- regenerate every affected theme normally;
- preserve the original failure;
- repeat every timing/browser series affected by the asset change;
- verify production and Fast computed behavior.

----------------------------------------------------------------------
A3. Refresh prepared Overview receipts
----------------------------------------------------------------------

The prepared Overview files contain historical b540/working-tree
observations. Preserve them as preparation evidence.

Add current execution-entry receipts rather than silently replacing the old
ones.

Re-evaluate:

- current source inventory;
- actual caller inventory;
- actual project graph;
- test names and discovery;
- CSS/JS/static assets;
- current public API surface;
- O00-O03 file ownership.

Correct prepared architecture only where current committed evidence requires
it.

======================================================================
PHASE B — EXECUTE AGENTS OVERVIEW-01
======================================================================

Execute O00 -> O01 -> O02 -> O03 sequentially.

Do not begin a later child until the previous child has a written closure and
its required proof passes.

======================================================================
O00 — CURRENT-PAGE CHARACTERIZATION AND RED EVIDENCE
======================================================================

Use the prepared O00 and semantic test map as input.

Add meaningful public failing-first witnesses against the current real page
and public query seams. New-class compilation failures are not semantic RED.

Preserve the planned cases for:

- rapid usage scope A -> B where A completes last;
- route-driven scope replacement during A;
- disposal during Overview read;
- disposal during Usage read;
- stale failure after newer success;
- history-host aggregate suppression;
- explicit Overview retry;
- typed partial usage;
- usage scope isolation;
- one accepted count owner.

Add the following missing semantic cases.

----------------------------------------------------------------------
O00.1 Aggregate failure must not poison unrelated context
----------------------------------------------------------------------

Add public tests proving:

- a failed Overview aggregate does not override a Ready selection access
  state on Agents, Chat, Capabilities or Governance;
- an independently resolved HR Agent remains available to its header action
  when Overview or Usage fails;
- a bound-resource failure does not erase an independently resolved HR Agent
  or accepted Overview;
- a Usage failure is not presented as an Overview failure;
- a header/HR/bound failure is not presented as an Overview failure;
- summary facts are omitted when no accepted Overview exists rather than
  populated with fake zeroes;
- an initial Overview failure and a refresh failure after accepted data are
  different presentation states.

Define and record the exact context access matrix:

1. Selection-oriented tabs use their current selection access lane.
2. HR action availability uses the HR/header lane and its own command state.
3. Non-selection tabs are not globally failed merely because summary
   aggregates are unavailable.
4. For the Overview view itself, decide from the context contract whether a
   fatal Overview read produces Failed context or Ready context without
   summary facts. Record and test the decision.
5. Loading and unavailable summary facts must never become invented facts.

Do not add usageScope to the chat navigation identity unless source evidence
proves that usage scope changes the semantic agent context rather than only
the displayed usage projection.

----------------------------------------------------------------------
O00.2 Preserve prior accepted data honestly
----------------------------------------------------------------------

Add tests for:

- initial Overview failure with no accepted snapshot -> unavailable, not zero;
- same-scope Overview refresh failure after accepted data -> retain the prior
  accepted snapshot but expose stale/failure state;
- same-scope Usage refresh failure after accepted data -> retain prior
  accepted same-scope data but expose stale/failure state;
- requested scope B failure while accepted scope is A -> never label or
  enable A as B;
- successful partial B -> accepted B with visible warning;
- an old finally cannot clear a newer busy/error owner.

Do not require discarding useful accepted data merely to simplify state.

----------------------------------------------------------------------
O00.3 Freeze exact evidence
----------------------------------------------------------------------

Before each RED execution:

- build the owning test project;
- list actual compiled tests;
- freeze exact FQNs and theory arguments;
- record setup versus semantic failures;
- preserve relevant DOM/state evidence;
- keep current correct cases as characterization, not fabricated RED.

Close O00 only after the ownership map and exact O01 implementation plan are
updated from actual evidence.

======================================================================
O01 — SPLIT READS AND PAGE-LIFETIME SESSION
======================================================================

Implement the smallest coherent state/read boundary inside the existing
AgentFramework Module first. Do not physically extract rendering yet.

----------------------------------------------------------------------
O01.1 Replace the combined shell transaction
----------------------------------------------------------------------

Replace the current all-or-nothing ReadShellAsync use with cohesive operations
on the existing query boundary, expected in shape to resemble:

- ReadHeaderAsync
- ReadOverviewAsync
- ReadUsageAsync

Exact names may differ.

The header operation must preserve independent outcomes for:

- HR Agent identity/readiness;
- avatar mapping;
- bound-resource count/readiness.

A failure in one header source must not erase successful independent header
data.

Remove the obsolete AgentsShellSnapshot/ReadShellAsync shape only after every
caller and test fake is migrated. Do not leave a permanent compatibility
facade without an actual caller and explicit removal point.

Preserve the existing demand decision:

- Providers and RequestHistory start no new aggregate reads;
- cached accepted summary may remain visible there;
- entering a history host fences/cancels in-flight aggregate work;
- other non-history sections retain the prepared current-demand behavior;
- this child is not a broader lazy-loading redesign.

----------------------------------------------------------------------
O01.2 One page-lifetime session
----------------------------------------------------------------------

Create one per-page Overview session/state owner.

It must not be:

- a circuit-wide mutable scoped service;
- owned only by the conditional Overview renderer;
- a generic UI controller;
- an event bus.

The page constructs it once and disposes it once.

The session owns:

- accepted Overview snapshot;
- Overview loading/stale/failure state;
- desired usage scope;
- accepted usage snapshot and its accepted scope stamp;
- Usage loading/stale/partial/failure state;
- header snapshot and independent header partial/failure state;
- independent Overview, Usage and Header generations;
- independent owned cancellation sources;
- exact same-request reuse;
- explicit retry identities;
- immutable copies of accepted collections.

A completion may publish only when both its generation and semantic request
identity still match.

Cancellation is not rollback. Noncooperative tasks must still be rejected by
generation fencing.

----------------------------------------------------------------------
O01.3 Desired scope versus accepted scope
----------------------------------------------------------------------

The workspace/route remains the sole desired-scope owner.

The session may retain an accepted-scope observation stamp, but it must not
become a competing route selection owner.

Required behavior:

- A -> B immediately changes desired route state once.
- Existing accepted A data may remain internally available.
- A is never displayed, labeled or passed to a dialog as B.
- Detail actions remain disabled until matching accepted B data exists.
- A late A success/failure cannot alter B.
- A same-scope route echo does not start a duplicate read.
- Explicit retry starts one new request.
- A returned ProviderUsageSnapshot with the wrong Selection is rejected.
- Partial matching B is valid accepted B with visible typed warning.
- conflicting source metadata remains a fatal invalid result rather than a
  plausible partial total.

Reuse existing ProviderUsageQueryService contracts. Do not create a second
usage aggregation implementation.

----------------------------------------------------------------------
O01.4 Safe error ownership
----------------------------------------------------------------------

Separate:

- fatal Overview error;
- stale Overview refresh error;
- fatal Usage query error;
- typed partial Usage source warnings;
- header HR unavailable;
- bound-resource unavailable;
- cancellation/supersession.

Do not display raw infrastructure exception messages.

Log server-side diagnostics through the existing logging conventions using
safe identifiers and failure type. UI presentation receives bounded public
messages and actionable retry hints.

Failed data is not zero data.

----------------------------------------------------------------------
O01.5 Accepted totals and chat context
----------------------------------------------------------------------

One accepted Overview snapshot must supply:

- persistent header counts;
- Overview metric values;
- summary facts for AgentChatContextSurface.

Do not keep separate mutable count fields that can diverge.

While accepted totals are unavailable, the header uses unavailable/loading
presentation and chat context omits summary facts.

Selection access, HR availability and Overview aggregate availability remain
independent as defined in O00.

----------------------------------------------------------------------
O01.6 Defaults and refresh semantics
----------------------------------------------------------------------

Preserve the current confirmed Defaults command and warmup semantics.

After successful warmup, refresh only the lanes demanded by the current
section, using the new session.

An Overview retry:

- does not repeat warmup;
- does not reopen confirmation;
- does not reload HR/bound/Usage unless explicitly requested by its lane;
- cannot clear newer work.

A late callback after page disposal is suppressed without claiming the
Defaults command rolled back.

Close O01 only after all state/read RED cases are GREEN and direct
registered-query integration evidence passes.

======================================================================
O02 — SERVICE-FREE SURFACE AND EFFECT LIFETIMES
======================================================================

Keep implementation in Module during this child.

----------------------------------------------------------------------
O02.1 Controlled Overview surface
----------------------------------------------------------------------

Extract the existing real Overview dashboard markup into a service-free
controlled surface.

It receives only immutable presentation state and typed callbacks/intents.

It owns:

- local rendering;
- metric, chart, list and team presentation;
- loading/empty/partial/stale/failed/ready states;
- disabled/busy display.

It must not inject:

- workspace/query services;
- ProviderUsageQueryService;
- NavigationManager;
- DialogService;
- NotificationService;
- persistence/runtime/provider services.

Use the actual existing stats, charts, consumer lists and team buttons. Do
not replace the real subtree with a mock renderer.

Likely typed intents include:

- change usage scope;
- retry Overview;
- retry Usage;
- retry affected header lane;
- open consumer/provider/model details;
- open team.

Names are not prescribed.

----------------------------------------------------------------------
O02.2 Page remains the effect host
----------------------------------------------------------------------

AgentsHomePage retains:

- route/query codec;
- tab/workspace state;
- HR action;
- Defaults command;
- selected-agent/team/context bridge;
- NavigationManager;
- notifications;
- usage dialog references;
- team navigation.

Do not create a forwarding controller merely to dispatch a handful of typed
intents.

One typed intent must produce one route/effect action.

Existing replace-history behavior for usage scope remains unchanged.

----------------------------------------------------------------------
O02.3 Harden all three usage dialogs
----------------------------------------------------------------------

Apply the same bounded lifetime rules to:

- AgentUsageDialog
- ProviderUsageDialog
- ModelUsageDialog

Each dialog must:

- capture a valid requested accepted scope;
- own a cancellation source;
- pass the token into ProviderUsageQueryService.QueryAsync;
- use request generation/identity fencing;
- reject a returned snapshot whose Selection differs from the captured
  request;
- distinguish cancellation from failure;
- suppress late result/error/finally after removal or replacement;
- show bounded public errors rather than exception.Message;
- preserve typed partial-source warnings where relevant;
- own independent chart/options/collections;
- dispose its cancellation source.

Add public tests for:

1. closing during a pending read cancels the token;
2. a noncooperative late success after close changes no UI;
3. a noncooperative late failure after close publishes no notification;
4. parameter/request replacement rejects the old completion;
5. wrong-scope result is not displayed;
6. raw infrastructure detail is not rendered;
7. chart options and series are independent between dialog instances;
8. matching partial data remains usable with warning.

Do not add a generic dialog query framework.

----------------------------------------------------------------------
O02.4 Owned overlays
----------------------------------------------------------------------

The page tracks only the three Overview usage dialogs it opens.

On usage scope replacement or leaving Overview:

- close/cancel only owned Overview usage dialogs;
- leave unrelated dialogs untouched;
- do not call CloseAll;
- closing a dialog cancels its query;
- a late dialog result cannot reopen or notify.

Page disposal follows the same rule.

----------------------------------------------------------------------
O02.5 Real context and persistent header behavior
----------------------------------------------------------------------

Prove through the real page that:

- header counts and Overview metrics use the same accepted snapshot;
- tab changes do not create a competing count store;
- HR action remains in the page header across tabs;
- selection-oriented access state remains authoritative;
- Overview failure does not globally poison unrelated tabs;
- history hosts do not start new aggregate reads;
- one team click emits and performs one navigation;
- usage dialog actions use only a matching accepted scope.

Close O02 only after component/browser behavior passes in the Module owner.

======================================================================
O03 — PHYSICAL EXTRACTION, SANDBOX AND MEASUREMENT
======================================================================

Do not move source before a fresh O01/O02 final-state baseline is captured.

Capabilities timing numbers are historical input and cannot replace
Overview-specific measurement.

----------------------------------------------------------------------
O03.1 Fresh pre-move baseline
----------------------------------------------------------------------

Freeze after O01/O02 closure:

- exact source hashes;
- final Overview fixtures;
- real 1600 x 1000 frame;
- chart settlement predicates;
- SDK/sibling/asset identity;
- direct-watch commands and ports;
- nine supported Overview edits:
  - three Razor;
  - three C# presentation;
  - three CSS;
- DOM/computed-style/chart predicates;
- restoration bytes.

Capture full-app process-cold and warm edit-to-visible baseline before any
physical Overview move.

If direct watch is not reproducibly observable, stop before movement and
report the exact blocker. Do not substitute Capabilities data, a managed
watcher, or manual refresh.

----------------------------------------------------------------------
O03.2 Move only the pure rendered closure
----------------------------------------------------------------------

Move the actual service-free Overview rendering closure into existing:

    src/UI/CanDoItAll.AgentFramework.UI

Expected movable responsibilities include, subject to current dependency
proof:

- AgentsOverviewSurface Razor/code/CSS;
- immutable presentation records;
- pure presentation mapper;
- independent chart option/series construction;
- the real current ProviderUsageConsumerList;
- necessary pure formatting helpers.

Leave in Module:

- page;
- page-lifetime session;
- workspace query;
- header reads;
- route state;
- dialog opening;
- the three data-loading dialogs;
- HR/defaults/team effects;
- notifications and navigation.

Do not move AgentOverviewSnapshot or ProviderUsageSnapshot merely to create
new duplicate DTOs. Map application values into immutable presentation at
the boundary.

Adding these lightweight dependencies to AgentFramework.UI is permitted only
after actual graph evaluation proves the direction:

- CanDoItAll.AgentFramework.Usage value contracts;
- CanDoItAll.Components.Charts.

Forbidden UI dependencies remain:

- Modules.AgentFramework;
- AgentFramework.Core;
- Persistence;
- ProviderManagement runtime;
- Voice;
- AppComponents;
- broad AgentFramework.Components;
- query/application implementations.

Do not create another UI project.

Do not copy rendering or leave a wrapper back at the old owner.

If AgentUsageDisplay has both moved-surface and Module-dialog consumers,
choose the smallest honest pure owner and update consumers directly. Do not
keep two implementations.

----------------------------------------------------------------------
O03.3 Existing sandbox only
----------------------------------------------------------------------

Extend the existing Agent Framework UI sandbox with typed Overview specimen.

Preserve:

- absent/unknown specimen -> existing catalog default;
- catalog scenarios and query codec;
- capabilities scenarios and query codec;
- Parity/Fast mode behavior;
- reload and replace-history semantics;
- current route.

Use deterministic safe Overview fixtures covering:

- loading;
- initial fatal Overview failure;
- stale refresh failure after accepted Overview;
- empty;
- ready;
- header partial HR unavailable;
- bound-resource unavailable;
- usage loading;
- same-scope stale usage;
- requested B while accepted A;
- wrong-scope rejection presentation;
- matching partial data;
- unknown and unpriced usage;
- long consumer/provider/model/team labels;
- all three usage selections;
- ranked and failing consumers;
- team shortcuts;
- disabled and enabled detail actions;
- safe public errors.

Sandbox interactions change only controlled sample state and an intent log.

Do not register or invoke:

- workspace/query implementations;
- ProviderUsageQueryService;
- database profiles;
- persistence;
- providers;
- diagnostics;
- chat/process launch;
- external endpoints.

Register and serve actual chart UI/static assets. Browser proof must observe
the actual settled chart, not only a placeholder or title.

----------------------------------------------------------------------
O03.4 Asset and CSS contract
----------------------------------------------------------------------

Use the existing shared runtime-composed compatibility CSS source.

Verify in both Web and Fast:

- actual Split/layout computed styles;
- chart container geometry;
- responsive layout at the frozen viewport;
- list/card overflow;
- long labels;
- scoped CSS selectors;
- icons/fonts/avatar assets;
- chart JS loading and settlement.

Do not duplicate compatibility rules into Overview CSS.

Any asset correction invalidates and requires repetition of all affected
browser and timing series. Preserve failed attempts separately.

----------------------------------------------------------------------
O03.5 Direct-watch comparison
----------------------------------------------------------------------

Use the proven Capabilities-03 protocol mechanics but new Overview-specific
edits and predicates.

Compare sequentially on the same environment:

1. pre-move full app;
2. post-move full app;
3. sandbox Parity;
4. sandbox Fast.

For each host:

- at least three process-cold runs;
- three distinct supported Razor edits;
- three distinct supported C# rendering edits;
- three distinct supported CSS edits;
- at least three forward repetitions per edit;
- reverse/restoration measurements retained separately.

Record:

- durable source hash/write point;
- browser predicate and settled chart/frame predicate;
- process ID/start identity;
- watch iteration/update generation;
- document timeOrigin/sentinel;
- navigation/reload;
- hot reload/rude edit/restart/failure classification;
- every failure and excluded attempt.

Report min, max, range, median and failure count.

Do not remove outliers from the headline.
Do not claim universal Fast superiority.
Do not claim general performance improvement from a smaller graph alone.

Restore all source and generated assets and stop only owned processes.

----------------------------------------------------------------------
O03.6 Final validation
----------------------------------------------------------------------

Before each execution:

- build affected production projects;
- build owning test projects;
- list current compiled tests;
- freeze exact selections;
- then run.

Required direct builds include all changed real owners/consumers, expected to
include:

- AgentFramework.Models where touched;
- AgentFramework.Usage where touched;
- AgentFramework.UI;
- Conversations.Components;
- AppComponents if the compatibility adjudication changes it;
- AgentFramework Module;
- Web;
- sandbox Parity and Fast;
- Charts/static asset consumers;
- Unit, Components and Integration tests.

Run the complete stable suite only when a named shared public API, assembly,
project graph or asset change semantically invalidates it. The avatar
compatibility correction, if implemented, is such a named trigger. Record
the reason either way.

Real browser acceptance must cover:

- Overview normal;
- loading;
- initial failure;
- stale refresh failure;
- partial usage;
- scope transition;
- header partial;
- HR action independence;
- selection-tab context independence;
- all three dialogs;
- dialog cancellation;
- team navigation;
- history host;
- sandbox Parity and Fast;
- long-content and responsive states.

Run final:

- project/reference cycle review;
- portability self-tests and enforcement;
- documentation gate;
- complete proposed-source secret scan;
- retained decompressed/UTF-16 artifact scan;
- relative-link and byte-manifest sealing;
- committed/proposed source inventory;
- worktree/index/sibling review.

Do not weaken a gate or relabel historical evidence.

======================================================================
PHASE C — PREPARE GOVERNANCE-01 ONLY
======================================================================

Only after Overview-01 has a manifest-checked closure, prepare:

    codex/bundles/UI_AgentGovernance_01_State_Read_Seams_Bundle

Proposed reference:

    CDA-UI-SEAMS-AGENT-GOVERNANCE-01

Do not implement Governance production changes in this run.

The preparation must inspect actual post-Overview source and cover at least:

- AgentGovernancePanel Razor/code/CSS;
- agent selection and PreferredAgentId route echo;
- ListExecutionRunsAsync;
- GetExecutionRunDetailAsync;
- execution run/detail/approval contracts;
- ContextAccessStateChanged ownership;
- current tests and production composition;
- current UI/sandbox project graph.

Known candidate risks to adjudicate:

1. Agent A -> B while A list/detail completes late.
2. Run R1 -> R2 where R1 detail completes last.
3. Current SelectRunAsync has no request identity or cancellation.
4. Panel disposal while list or detail is pending.
5. Old finally clearing a newer busy state.
6. Refresh completion overriding a newer manual run selection.
7. List success followed by detail failure:
   preserve the usable list and expose a detail-lane error rather than
   erasing everything, if product semantics support it.
8. Missing requested agent must remain fail-closed.
9. SelectedAgentChanged and ContextAccessStateChanged must publish only for
   the accepted target.
10. Real approvals, outcomes and run summaries must render through a
    service-free controlled surface.
11. No raw infrastructure errors.
12. Later physical extraction/sandbox/direct-watch work must be separately
    staged and measured.

Prepare a compact sequence such as:

- G00 current characterization and ownership adjudication;
- G01 accepted agent/run/list/detail session;
- G02 effects/context and service-free surface;
- G03 physical extraction, existing sandbox and measured closure.

Names may change when current source proves a better decomposition.

Record exact future RED witnesses, ownership, dependency direction,
validation and non-goals.

No Governance production/test/project/asset change is authorized in Phase C.

======================================================================
OUT OF SCOPE
======================================================================

Do not implement:

- new Capabilities behavior;
- general capability CRUD recovery;
- AgentDetailsDialog refactoring;
- Governance production changes;
- Diagnostics refactoring;
- Agent Chat refactoring;
- Simple Chats refactoring;
- Voice or Floating Chat refactoring;
- provider architecture changes;
- production URL redesign;
- routed dialogs;
- generic controller/state framework;
- event bus or outbox;
- durable distributed recovery;
- sibling repository work;
- history cleanup;
- merge preparation.

Do not move Simple Chats into generic AgentFramework.UI.

======================================================================
FINAL REPORT
======================================================================

Report:

1. Actual entry and final local/remote HEAD and trees.
2. Worktree/index and sibling handling.
3. Committed predecessor byte/manifest handoff result.
4. Whether ad2ded committed bytes match the final tested Capabilities bytes.
5. AgentAvatarActionButton source/binary compatibility decision.
6. Any compatibility source change and its direct external-consumer proof.
7. O00 confirmed/disproved findings and exact RED evidence.
8. Final header/overview/usage ownership.
9. Final context-access matrix by Agents section.
10. HR action behavior after aggregate failure.
11. Desired versus accepted usage scope behavior.
12. Initial failure versus stale refresh behavior.
13. Header/Overview/Usage cancellation and generation behavior.
14. Query boundary changes and removed obsolete facade.
15. Safe error and partial-source behavior.
16. Usage dialog lifetime and wrong-scope behavior.
17. Owned overlay behavior.
18. Exact pure files physically moved in O03.
19. Final UI and sandbox dependency graphs.
20. Shared responsive asset decision.
21. Sandbox Overview scenario coverage.
22. Real chart/static asset browser proof.
23. Exact builds, discovery and executed tests.
24. Full-app and sandbox browser scenarios.
25. Overview pre/post direct-watch measurements and limitations.
26. Restoration and owned-process shutdown receipts.
27. Portability, documentation, secret and manifest results.
28. Remaining Overview limitations.
29. Path and reference of the prepared, unimplemented Governance bundle.
30. Separate verdicts for:
    - committed predecessor integrity;
    - Overview semantic readiness;
    - physical extraction readiness;
    - sandbox readiness;
    - development-loop readiness;
    - production bookmarkability;
    - Governance preparation readiness;
    - repository merge readiness.

Do not claim repository merge readiness.