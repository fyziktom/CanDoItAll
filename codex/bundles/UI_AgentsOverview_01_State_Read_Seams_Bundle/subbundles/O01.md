# O01 â€” Accepted read session and immutable presentation

Status: CLOSED / PASS. [Execution closure](../proof/O01/closure.md). Proof: Behavioral critical boundary. Prerequisite: authorized execution and O00 closure. Outcome: the real page uses one per-page session for safe accepted Overview/usage state including independent header outcomes. Surface extraction belongs to O02 under the newer authorization.

Scope OV01-OV08, T01-T11/T13-T15 and direct session tests. No physical movement, generic controller, query algorithm/provider/history/CRUD changes. Implement [ownership](../architecture/01-csharp-boundary-map.md) and explicit [product decisions](../requirements.md).

1. Split reads on existing query into independent header/overview/usage demand while retaining source algorithms and partial semantics. Preserve history-host suppression. Update real registration and all callers; remove obsolete shell envelope only when no real caller remains.
2. Create one page-lifetime session. Separate CTS/generations/load/errors for header, overview and usage; cancel/fence on replacement/history/disposal. A wrong-scope result fails explicitly. Every publication/finally owns its generation. Capture independent accepted collections. Route echo must not duplicate a read.
3. Remove copied overview counts from header state. Header derives from accepted overview; HR/avatar/bound-resource lane remains distinct and explicitly unavailable on failure. Do not make aggregate failure erase independently loaded header context.
4. Keep rendering in the page during O01. Render safe lane states and retry controls; retain accepted same-scope data with stale/error status. Never present A as B. Independent chart/rendering extraction follows in O02.
5. Retry only failed reads; no Defaults warmup or HR launch replay. Keep existing route keys/history behavior and partial/source integrity errors.

## C# Architecture Impact

Replace actual page read/mapping ownership; thin delegates into an unchanged page implementation are not accepted.

## Boundary Ownership

Page semantic route and header rendering; session owns all accepted data and three independent read lifetimes; query owns I/O. Use one session even when Overview is not rendered.

## Dependency Direction

No physical project edges yet. Session never depends on chart/Razor types. Surface never calls query/navigation or application services.

## Pattern Decision

Per-page session + controlled Surface + existing cohesive query; direct construction needs no singleton/new service bag. Central decisions govern alternatives.

## Testability Contract

O00 RED must turn GREEN. Add direct session names from [semantic map](../plan/semantic-test-map.md), independence and wrong-scope witnesses. Run current owning [filters](../inventory/test-inventory.json) plus newly frozen FQNs. Real registered query Integration covers split reads/header failures; component tests prove public composition, not private members.

## Partial Class Policy

Normal Surface Razor/code-behind allowed; no page partial file used to hide old ownership.

## Architecture Proof Required

Build Module/Web and any changed owning dependency/test project; freeze discovery before running; direct session plus page/integration proof. Inspect actual 1600x1000 normal/error/partial/long rendering. Show source responsibilities removed, graph directions and no competing count owner; fresh CodeAnalytics with scoped limitations. Static/secret gates and RED/GREEN artifacts required. No broad stable absent actual shared-contract invalidation. Reopen on route/query/session/presentation drift; O02 waits until this critical boundary passes.
