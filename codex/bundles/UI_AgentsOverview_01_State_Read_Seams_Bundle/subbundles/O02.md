# O02 — Owned effects and production composition

Status: PREPARED / NOT STARTED. Proof: Behavioral. Prerequisite: authorized execution and O01 closure. Outcome: typed Overview intents drive current page navigation/dialogs, with accepted-scope and owner lifetime guarantees.

Scope OV09-OV11 and T12/T15, all E witnesses. Keep page as existing effect host. Do not introduce a forwarding controller/second workspace store. Defaults/HR/backend command semantics remain unchanged; no general mutation recovery or dialog-internal redesign.

Dispatch scope intent once into workspace/route; preserve replace-history and same-route echo behavior. Open each usage dialog only with the accepted matching selection. Own a scoped overlay lifetime; scope change, leaving Overview or disposal closes only these references. Each dialog separately cancels/fences its lazy QueryAsync on removal. Dispose CTS once; old finally/result cannot clear newer state or publish notification. Preserve unrelated overlays and focus/accessibility.

Team shortcut maps exact stable team ID into current route once with existing push behavior; no new URL semantics. Header HR action, Defaults confirmation/warmup, global navigation, context bridge and history lazy demand must remain operational. A read retry never repeats command effects. Suppress late command callbacks without claiming rollback.

## C# Architecture Impact

Page becomes thin typed effect dispatch over an already extracted state/read/rendering seam. Minimal dialog load lifecycle corrections are the only dialog internals in scope.

## Boundary Ownership

Page owns route/effects; session accepts read state; dialog owns its query lifetime. A route acknowledgement is not another selected scope.

## Dependency Direction

All effects stay Module. No renderer-to-page or UI-to-runtime dependency.

## Pattern Decision

Reuse existing page/real DialogService; bounded owned references/tokens, no global CloseAll or generic effect framework.

## Testability Contract

Exact E/T12 tests plus affected O01/current filters, including real DialogHost, unrelated dialog, all three captured-scope cases, downstream catalog echo and workflow navigation. Build/list before execution. Actual registered query Integration and real Web browser complement fakes.

## Partial Class Policy

No added partial page used as the final effects boundary; no copied old handler implementation hidden elsewhere.

## Architecture Proof Required

Direct changed Module/Web/test builds, frozen selections, real page browser normal/partial/retry/three overlays/HR/defaults/history/team/context, inspected screenshots. Observe cancellation tokens and noncooperative late completion. Review callback/notification/CTS ownership and removed page responsibilities; static/secret gates. Broad stable only for named actual public impact. O03 may be prepared for execution only after O02 closes; explicit authorization and fresh pre-move measurement checkpoint remain mandatory.
