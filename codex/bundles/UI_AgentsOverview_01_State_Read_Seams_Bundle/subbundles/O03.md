# O03 - Real extraction, existing sandbox and measured closure

Status: CLOSED; see proof/O03/closure.md, with the explicit inherited documentation follow-up. Proof: Governed. Owner authorization is recorded in [input 02](../inputs/02-implementation-authorization.md); O00, O01 and O02 have separate manifest-checked closures. Outcome: real Overview rendering in existing lightweight UI, actual existing-sandbox specimen, and reproducible direct-watch comparison. No production route, other-module or sibling work.

## Ordered checkpoints

1. Before physical source movement, rediscover source/dependencies/assets/SDK/siblings and freeze the actual final O01/O02 Overview fixtures, 1600x1000 content frame, launch commands/ports, browser predicates, test inventory and supported edits. Capture real full-app pre-move baseline. O00 layout proof and Capabilities timing cannot replace it. If direct watch is not reproducibly observable, stop before movement and report exact blocker.
2. Move the actual service-free Surface, pure state/presentation/mapper/options, real current ProviderUsageConsumerList and necessary pure formatting/CSS closure into existing AgentFramework.UI. Leave session/read/effects and three data-loading dialogs in Module. Preserve real old helper/list consumers with valid direct dependencies; no duplicate implementation or wrapper returning to Module. Keep old unused AgentOverviewUsageList unchanged unless a demonstrated dependency requires a minimal consumer update.
3. Add current lightweight Usage value-contract and real Charts references only after evaluated closure proves the planned direction. Keep query execution/registration outside UI/sandbox. Build all affected projects/consumers, inspect scoped CSS, direct graph/cycles and actual chart JS. Verify responsibility/source disappears from old owner, not just successful compilation.
4. Extend existing sandbox typed specimen with Overview; catalog/capabilities remain unchanged defaults/query/history/modes. Use deterministic safe values for Both/Agents/Chats, loading/empty/failed/retry/partial, long labels, rankings, unknown/unpriced costs, teams and stale-scope presentation. Changes update controlled sample state/intent log only. Do not register workspace/usage sources/provider/database/diagnostic/chat/runtime services. Charts UI registrations/assets are allowed and required.
5. Run post full app and sandbox Parity/Fast on same protocol. Preserve every attempt/failure/restoration receipt. Close only after direct/browser/static/manifest gates and independent readiness verdicts.

## Measurement contract

Reuse the proven direct-watch protocol mechanics from Capabilities-03, but freeze Overview-specific edits/fixtures/predicates afresh. Do not copy prior numbers. Direct `dotnet watch` plus actual Tailwind; no managed-watch refresh or manual reload masking unsupported edits.

Separate process-cold startup from warm iteration. At least three distinct supported Razor edits, three C# rendering edits and three CSS edits; each repeated at least three times per host, forward AND reverse. Hosts: pre-move full app, post-move full app, sandbox Parity and Fast. Measure first edit-to-visible and settled chart/frame separately. Match same machine, SDK, sibling mode, fixture, dimensions, input assets and workload; record caches, port/process ownership and sequential/randomized ordering. No cold-restore claim unless measured.

Freeze file bytes/hashes, durable write timestamp, DOM/computed chart/layout predicate, managed update generation, SDK watch messages, process ID/start/iteration, document sentinel/timeOrigin and quiet settlement. Classify managed delta, enhanced/browser navigation, full document reload, process restart, rude/unsupported edit and failure from actual evidence. Chart placeholder/label alone cannot prove the plot updated. Retain animation and asset settlement behavior; use production-representative settings.

Report every edit/host/sample, min/max/range/median and failures with raw compact evidence. Do not drop outliers, silently repair failed protocols or claim general improvement from source movement. Corrected assets/protocol changes require comparable affected-series repeats and preserved excluded attempts. Restore exact source AND generated assets and stop only owned processes. Keep fixture secrets outside retained evidence.

## C# Architecture Impact

True project extraction; all command/read/lifetime ownership remains outside UI. Session or operation types never move merely because rendered.

## Boundary Ownership

Renderer owns Overview CSS anchor and current card/list scroll. Page owns header CSS and effects. Existing sandbox hosts real components, charts, avatars, tooltips, fonts/icons/assets.

## Dependency Direction

Re-evaluate full transitive UI/sandbox graph including live Charts. No Core/Module/Persistence/provider/Voice/AppComponents/broad Components edge. Retain actual local/sibling revisions and scoped CodeAnalytics limits/cycles.

## Pattern Decision

Move real controlled subtree; no alternate sandbox project, copied rendering or fake chart. Reuse existing small value contracts rather than arbitrary interface/type proliferation.

## Testability Contract

Carry exact O01/O02 owning FQNs; freeze current list/helper/chart and catalog/capabilities/sandbox compatibility additions before movement. Tests prove real declarations/behavior and independent state through public seams. Do not write filename-shape/partial-count architecture tests. Actual browser charts and asset parity are mandatory, not substituted by bUnit JS mocks.

## Partial Class Policy

Normal component Razor/code-behind is allowed; no backward facade/partial monolith. Source review must show old behavior removed.

## Architecture Proof Required

Direct builds of every changed project and real consumers, current frozen Unit/Components/Integration selections, actual Web and both sandbox modes; evaluated graph/cycle/references, CSS/JS/Tailwind runtime parity, compact byte/relative-link manifests. Run broader stable only for named shared public/graph/asset invalidation at a frozen checkpoint. Static portability/docs/source+retained secrets; inspect normal/error/partial/long/overlay screenshots and scroll/keyboard states. Record unresolved documentation debt separately.

Invalidate dependent proof for source/query/fixture/SDK/sibling/asset/protocol drift. Close with semantic/physical/sandbox/watch verdicts and explicit no route/merge claim. Further roadmap work remains separately authorized.
