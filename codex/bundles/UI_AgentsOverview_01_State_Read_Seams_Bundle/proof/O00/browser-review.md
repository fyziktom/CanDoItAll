# Current-page browser characterization

Inspected all eight retained 1600 x 1000 frames and DOM text: Normal, Partial, Empty, HoldOverview, OverviewFailure, UsageFailure, HeaderFailure, BoundFailure. Browser 149.0.7827.55; real WebApplicationFactory/Kestrel host with isolated database and controlled aggregate read sources. Real Apex charts render in normal, partial and header-failure states. No browser page errors; owned process exited 0.

Normal is a compact single-page dashboard with persistent header/actions, eight metric badges, ranked lists, actual bar/donut charts, and team/detail actions. Dashboard width and scrollWidth both 1419 within the 1600 viewport. Empty correctly reports real zero totals. Partial preserves usable chart/list data with warning; the extra warning moves secondary actions below the first viewport, with the existing page scroll owner. Loading and fatal frames show misleading loading placeholders; Overview/Usage/bound failure disables HR and exposes controlled private error text. Header failure alone preserves Overview but exposes raw HR error in a notification. These are characterization defects, not acceptance.

The donut's second legend row is clipped in current normal/partial frames. O02 must inspect real chart geometry and make a bounded rendering correction before the O03 final-seam baseline if confirmed. The loading frame was captured before outer tab-session settlement; it is not a timing frame. O03 requires a stricter settled full-app/chart predicate. Existing shared responsive CSS has no demonstrated missing selector; do not duplicate it.

Fixture is safe and deterministic; source below tools/browser-fixture stages under .mcp-state/overview-browser-fixture so its project references resolve. Set OV_PLAYWRIGHT to an installed Playwright package, build Release, and run tools/browser-o00.cjs from repository root. Ports 5275/17305 are owned exclusively for the run. This observes current defects; it is not the O01/O02 GREEN runner or O03 timing proof.
