# Evidence-selected O01 implementation

1. Replace AgentsShellSnapshot/ReadShellAsync on existing IAgentsWorkspaceQuery with Header, Overview and Usage methods. Registered production query retains current core/EF/usage sources. Header returns immutable HR identity/avatar metadata, independent typed HR/avatar/bound failures and optional bound count; one failure does not erase the other values. Log safe lane/type/identity only.
2. Add directly constructed per-page AgentsOverviewSession. Own accepted immutable snapshots and three independent requests with CTS, generation, semantic scope, pending task reuse, explicit retry and stale errors. Workspace remains the sole desired route state. History cancels pending aggregate requests; completed accepted data remains. Own cancellation never publishes a failure.
3. Migrate the page to the session and remove copied mutable aggregate counts. Header derives from accepted Overview plus its independent header lane. Render safe unavailable versus stale and matching-scope partial states; provide explicit lane retry/refresh actions without commands. Reject wrong-scope results, preserve same-scope data and fence all old completions/finally paths.
4. Make optional bound context facts truly optional. Selection tabs use their access lane; other tabs including Overview remain Ready without unknown facts. HR button readiness depends on its accepted HR/header lane and command state. Keep usageScope outside chat context identity.
5. Preserve confirmed Defaults and warmup; refresh only demanded lanes afterward, suppress disposed-owner callbacks. Add page disposal and owner tokens for existing page commands, without claiming rollback.
6. Migrate current Unit/Integration query callers, add direct session identity/copy/wrong-scope/retry tests and real registered query partial-failure integration. O00 public cases must turn GREEN. No Surface/chart extraction in O01; that belongs to O02.

No new scoped session DI service, generic state framework, query aggregation rewrite, provider mutation, sibling edit or project edge. Source owners: Services/AgentsWorkspaceQuery.cs; new Pages/AgentsOverviewSession.cs; existing page Razor/code; AgentFrameworkAgentsChatContextBuilder; owning Unit/Components/Integration tests. CSS and physical files remain in their owners.
