# O00 closure

Status: PASS — characterization and meaningful RED foundation, 2026-09-07. This is permission to start O01, not a claim that the current page is repaired.

Entry owner builds: Module, Web, Unit, Components and Integration all pass. Fresh baseline discovery/execution: Unit 37/37, Components 12/12, Integration 1/1; zero failed/skipped. New regression discovery: 24 exact expanded cases. Corrected RED execution: 20 semantic failures, four characterization passes, no setup/aborted/skipped cases. [Exact adjudication](adjudicated-cases.json), [plan](implementation-plan.md), [browser review](browser-review.md). The first run's five bUnit setup failures are retained separately and not counted as semantic proof.

Confirmed: stale usage success/failure/finally can publish into a newer scope; route replacement does not reliably supersede pending usage; disposal/history do not cancel aggregate reads; Overview/Usage/bound failure couples independent values and denies HR/selection context; explicit read retry/stale affordances are missing; unrelated prior scope remains visible/enabled on failure. Context failures occur on all four selection tabs. Current raw private error text is observable in real browser notifications/cards.

Characterization retained: an HR catalog failure alone does not fail Overview, header/metric counts currently agree, same pending route echo reuses usage, and matching partial usage remains usable. Existing history hosts suppress initial aggregates and usage refresh does not reload summary. Copied mutable count fields still exist and O01 removes that second storage; the agreement test is behavioral evidence, not a source-shape invariant.

Eight real Web browser states captured and inspected at 1600 x 1000. Normal/partial/header-failure use actual settled bar/donut charts; no page errors; dashboard width equals scroll width. Donut legend clipping is a bounded O02 rendering investigation. No timing baseline is claimed. Owned fixture exited 0. Components MCP was unavailable (Transport closed); current sibling source supplied the exact existing Cluster/CdaChart composition contracts without changing the sibling.

Context decision: accepted selection access owns Agents/Chat/Capabilities/Governance. Other views, including Overview, remain Ready with unknown summary facts omitted. HR action uses its own header/command lane. Bound failure is independent. Usage scope remains outside chat navigation identity. Same-scope stale data stays visible; mismatched data never becomes the requested scope.

Portability self-tests 6 and artifact-scanner self-tests 4 pass. Final complete proposed-source portability enforcement passes with 14,383 reviewed executable-source findings unchanged; no baseline edit. Source secret scan has no new/removed findings. No broad stable trigger: only tests and bundle/documentation changed. Retained decompressed/UTF-16 scan and byte manifest accompany this closure. Predecessor manifests remain historical; current handoff normalization is separately sealed in A0.

Manual subbundle/architecture gate: PASS. Actual query ownership, all callers, direct public tests, state/error/lifetime rules and sequential boundaries are reviewed. [Implementation plan](implementation-plan.md) is authoritative over old Surface-in-O01 preparation. O01 stays Module and must make these cases GREEN. O02 and O03 have not begun. No physical movement, provider/Capabilities behavior, sibling edit, commit/push or Governance implementation occurred.
