# Committed predecessor handoff

Current entry: components-decoupling at ad2ded645e65b8b205959f6fed816d082b99a7be, matching independently queried remote. Parent b540b465cf408dd9c9adeb939538b78ced9bc7bb; tree 3c9d3eb3eded55bf3e1aea83225dbccf8635dc66. Clean index/worktree at entry; both live siblings unchanged and clean. [Receipt](committed-handoff.json).

A0 PASS with explicit normalization qualification. All 72 final source paths match the tested working bytes or recorded absence. Committed bytes differ on 39 text paths only by CRLF normalization. Capabilities-02G has 256 exact manifest entries; Capabilities-03 has 797 exact and three newline-only differences; Overview preparation has 25 exact and two newline-only differences (one mixed-line-ending JSON). All normalized bytes match, including generated assets. All eight moves have old absence/new presence; 131 relative links resolve against the committed tree. Supplemental committed-byte manifests describe the actual commit. No historical manifest/closure has been rewritten. Literal committed bytes are not falsely called byte-identical. The compiler/asset inputs in this worktree are exactly the previously tested bytes; no semantic predecessor gate is invalidated. The current owning builds/discovery are separately recorded before O00 RED.

A1: source compatibility is the supported contract. Root README explicitly disables NuGet publication until a release contract is defined; evaluated AppComponents IsPackable=false; remote tags and GitHub release list are empty, and public NuGet AppComponents index returns 404 ([observation](publication-observation.json)). This does not assert knowledge of private external binaries. No repository binary compatibility promise or supported published old assembly was found. Keep implementation in Conversations.Components and namespace CanDoItAll.AppComponents. No forwarder, reverse dependency, duplicate component or external-binary proof is appropriate. Add the missing ownership note to AppComponents README only.

A2: retain Tailwind/main/component-layout-utilities.css as the single source imported by Web and Fast. Overview uses existing Split direction/wrap/alignment/justification at desktop breakpoints; no new selector is currently justified. O02 browser and O03 computed-style proof will decide sufficiency. Any demonstrated correction reopens affected browser/timing series.

A3: historical preparation observations remain intact. New entry source/caller/graph/asset/discovery receipts live under this proof. Current authorization supersedes the old phase allocation: O01 session/reads, O02 actual Surface/effects, O03 physical move only after baseline.
