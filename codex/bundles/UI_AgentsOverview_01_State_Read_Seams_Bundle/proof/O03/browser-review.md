# Final O03 browser review

Final source browser executions: Web r2 passes 28 scenarios; sandbox r3 passes both Parity and Fast, retaining 25 screenshots per mode. All report zero page errors and stopped owned hosts. Current direct builds/tests after the corrected-baseline replay verify restoration to these exact final source bytes. Later measurements use fresh direct-watch hosts and the separate canonical data fixture.

The Web harness serves the real Web application and page with isolated PostgreSQL and controlled public read/projection failure seams. It exercises real UI event handlers, DialogService, three data-loading usage dialogs, HR chat opening, Defaults confirmation/warmup, team navigation and history demand. It does not claim that an injected failure is a naturally occurring database outage. Registered-query integration cases independently cover the production read composition. Timing uses the normal Web project with its actual canonical stores and query registrations, not this fault-injection host.

Representative final screenshots were actually inspected; their exact paths and hashes are in [visual inspection](visual-inspection.json). Normal rendering has real settled bar and donut plots, bundled avatars/icons, usable ranking cards and team/detail actions. Initial Overview failure has unavailable summary facts while independent usage remains useful. A same-scope failure retains accepted data with a clear stale warning. Pending or failed replacement scope shows no previous-scope usage and disables details. Header and bound failures preserve successful independent lanes. Loading, empty, partial, unknown/unpriced and long-content sample states remain distinct. Long labels stay within cards; long model labels wrap in the real Module dialog. The real consumer/provider/model dialogs render their charts and tables; pending-dialog cancellation is asserted through public controls and query counters.

Both sandbox modes serve actual Apex JS, BaseLib fonts/avatar assets and the UI CSS-isolation bundle. The desktop dashboard frame is x=125, y=223.59375, width=1419 at 1600 x 1000. Main cards remain 440px high. Web/Parity and Fast use their own declared asset entries; small typography/chart-title vertical differences are retained, not claimed to be pixel-identical. Both have nonempty actual series and no horizontal overflow. At 1280px the corrected existing 2xl breakpoint produces a usable two-column chart layout; at 900px the layout is one column.

Sandbox actions preserve existing catalog/capabilities defaults and query behavior. Scenario/scope selection uses explicit tokens and replace-history. Retry updates only its sample lane; team/details update only the intent log. No production query, database, provider, diagnostic, chat/process or external endpoint is registered or called. Source-managed behavior is outside this Overview child and unchanged.

Retained earlier executions remain separate: the first sandbox script failed an asynchronous assertion before awaiting its public log postcondition; the next run proved the 1280px plot-collapse defect. [Responsive re-entry](responsive-reentry.md) records the correction and repeat baseline. Original timing calibration failed because it counted decorative legend SVGs; actual plot selectors corrected the harness without inventing a source regression. No failed attempt is represented as passing.

## Post-suite source hygiene confirmation

After stable success, one extra page CSS EOF newline was removed. All generated nonblank CSS lines and measured Tailwind/probe bytes remain identical. Module/Web/private browser owner rebuilds pass and the real Web script repeats all 28 scenarios with zero page errors and owned exit zero. Three new normal/long-model/responsive screenshots were inspected; see hygiene-visual-review.json. There are now 28 actually inspected O03 images across the original 78 final-owner captures and 28 additional hygiene confirmations. Earlier raw results remain unchanged; no new behavioral or timing claim is inferred from whitespace.
