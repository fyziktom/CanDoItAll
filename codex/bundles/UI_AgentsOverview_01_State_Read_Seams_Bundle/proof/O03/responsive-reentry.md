# Responsive correction and baseline re-entry

Original pre-move measurement ran before any O03 movement and passed three cold plus 54 warm observations. It remains retained as superseded evidence; none of its observations are relabeled as corrected source.

Sandbox browser r1 failed an immediate asynchronous intent-log assertion; retained DOM already contained the correct eventual log. The script was corrected to await the public UI postcondition, with no production edit. Browser r2 then exposed a real defect: at 1280px, long provider content left the inherited four-column bar plot with axes but no usable series. Its screenshot and actual plot-path predicate establish semantic visual RED.

The production correction changes ColumnTemplateXl to the existing ColumnTemplate2Xl on the real dashboard Grid. This gives 1280px a usable two-column chart layout and preserves four columns at the frozen 1600px viewport. No compatibility CSS is copied or added. Final Web browser r2 and sandbox r3 pass at 1600, 1280 and 900px with actual Apex series and usable geometry.

Because the corrected Razor bytes differ from the original baseline, the guarded replay temporarily moves the seven exact files back to Module and restores the original UI project/import bytes. It checks those bytes against the earlier source inventory, except for the single proven breakpoint correction. The sandbox source remains preserved and excluded from this temporary Web build. The evaluated pre-owner graph returns to 12 nodes. Three corrected cold and 54 warm observations pass with no failures. The finally block restores all eleven post-owner files and both generated assets exactly; the receipt records postOwnerRestored=true. Final direct builds and all 363 focused/compatibility cases then pass on the restored extraction.

Post-extraction Web, Parity and Fast use the same protocol v2, canonical fixture, SDK, sibling mode, viewport and nine edit predicates. Their completion and comparative conclusions belong to the final measurement report, not this re-entry receipt.
