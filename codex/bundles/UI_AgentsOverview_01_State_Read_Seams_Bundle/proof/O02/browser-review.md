# O02 real Web browser review

PASS: 25 scenarios at 1600 x 1000, Chromium 149.0.7827.55. All 25 screenshots were visually inspected with their DOM/state evidence. Real Web composition uses an isolated PostgreSQL profile and controlled public aggregate sources; real team identities are seeded through the registered workspace. No provider request was sent. The real Defaults command and HR chat opening were exercised.

Normal/long charts, list truncation and geometry are legible; dashboard client/scroll width is 1419. Initial unavailable and stale accepted states are distinct, with no invented summary facts or wrong-scope rankings. Matching partial usage remains visible. The selected Chats control was explicitly awaited and matches both the URL and content after scope failure/recovery. Header failure disables HR only; aggregate and bound failures do not. All three usage dialogs render actual charts, and closing each pending dialog cancels its read token. Defaults cancel and execute paths, real team navigation, and both history hosts pass; history navigation starts no new aggregate reads.

The inherited compact bottom legend clipped its second row. The separate layout probe preserves that failure and exact clipping bounds. The final Surface uses the existing top-legend option; both labels and donut are now fully visible in Normal and Long. No chart sibling or responsive asset source was changed. Provider dialog's wider bottom legend remains legible and unchanged. Loading dialog chrome is closable, and the body scrolls for the long consumer table.

HR chat screenshot also exposes an existing misencoded context-label separator in the accepted context contributor; no agent-chat redesign is attempted here. Owned query-navigation/unrelated-modal preservation and noncooperative late completions are proven by real DialogService/Host component tests; the browser run does not claim to create a background click through an inert modal.

Both browser processes exited zero through their owned control endpoints. No other process was stopped. Current assets and sibling mode are frozen again before O03 timing.
