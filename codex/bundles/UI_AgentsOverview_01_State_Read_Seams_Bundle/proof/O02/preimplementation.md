# O02 preimplementation checkpoint

O01 closure and its 115-entry manifest verified before this phase. First failing-first witnesses use real DialogService/Host, current dialogs and registered ProviderUsageQueryService via controlled public projection sources. Freeze compiled discovery before execution; no new Surface-class compilation failure counts as RED.

Source observation to adjudicate with a direct test: DialogService itself calls CloseAll on every LocationChanged, including a same-page query update. This may prevent the page from preserving unrelated overlays even after it owns its own tokens. No sibling edit is made at this checkpoint. An unavoidable blocker must be proved before invoking the owner's conditional sibling exception.

Surface/mapper/options remain in Module until O03. Dialogue read effects and session stay Module. No new controller or generic dialog framework. The existing cohesive query can be reused for direct wrong-snapshot-scope injection without altering usage aggregation. Current source-metadata rejection remains separately covered through the production query.
