# O02 architecture review

PASS for the in-Module boundary. The real dashboard markup, pure immutable state, typed intents, chart options and formatting/ranking now have a service-free Surface. A no-application-service component test renders the actual Surface/card/chart closure. Page remains the existing effect owner; no controller, mediator or second workspace store was added. Session remains page-lived and owns all accepted read data, including persistent header data. The page maps values to presentation; no Razor policy call or effect reaches the Surface.

The three lazy dialogs use the existing cohesive workspace read port, whose ReadUsageAsync delegates to the real ProviderUsageQueryService.QueryAsync with the same captured token. This permits a direct wrong-snapshot identity witness without inventing another query interface. Source-metadata rejection remains separately tested through the registered production usage service. Each dialog owns a cancellable request, generation and matching selection, immutable copied collections and independent options. No generic dialog framework was added. Bounded errors omit exception messages and partial matching evidence stays usable.

The page tracks only its three usage dialog kinds and their group token/generation. Repeated intents do not open a duplicate; closing a dialog re-enables only the current action owner. Query/scope replacement and leaving Overview cancel that group. Page removal cancels the group while preserving an unrelated dialog. The real DialogService navigation blocker and conditional sibling exception are documented in sibling-blocker.md; defaults/path navigation behavior is unchanged without the new explicit scope. Seven direct shared tests and seven unchanged publishing approvals pass in enforcement mode. The additive API and reviewed source hash are the only approval deltas.

Module-only CodeAnalytics snapshot snap-20260907123645-5c18b873 contains 561 types, 5820 members, 55 service registrations, 220 findings, 20 questions and 23 informational diagnostics. It sees the new types; the identifier suffix is not a source-byte hash. Existing Module/Hosting and internal nested-type cycles remain; no new architectural owner cycle is introduced. This is not a complete solution graph. O03 must separately evaluate UI/Usage/Charts edges before moving source.

The new BaseLib public opt-in API is a named broad stable invalidation trigger. The program's broad stable run is deferred to its final source/assembly state (O03 final validation, or the pre-move stop if measurement cannot proceed), so it is not falsely claimed here. O02 closure is the explicitly required in-Module component/browser checkpoint. No measurement or source movement has started. FileTools remains untouched.
