# O01 architecture gate

PASS for the state/read boundary. One directly constructed per-page session owns accepted immutable collections, independent Header/Overview/Usage generations and cancellation sources, explicit retry, same-request reuse and scope observation. Its private ReadLane/ReadRequest types are lifecycle bookkeeping, not advertised architecture boundaries. No new interface or DI registration was added; the existing query now separates cohesive reads and the obsolete shell envelope has no source/test callers.

Workspace route state remains authoritative for desired scope. The session observation only fences read completion. Counts and facts derive from the single accepted Overview. Selection tabs use selection access; other tabs including Overview remain Ready while unknown facts are omitted. HR uses header readiness, independent of aggregate errors; unknown bound counts are omitted independently. Usage scope is not chat-navigation identity.

The current scoped CodeAnalytics receipt reports Module-only analysis (549 types / 55 services), no error diagnostics and existing Module/Hosting and image-provider nested-builder cycles. It is not a whole-solution graph proof. Actual project references are unchanged. The final interactive-startup correction only changes the page lifecycle entry point and was directly rebuilt and retested. No renderer/service extraction, runtime contract redesign, sibling edit or provider/Capabilities change occurred.

Portability baseline delta: avatar key dictionaries use OrdinalIgnoreCase for textual GUID lookup, never filesystem identity. Two reviewed old-source allowances were replaced by the corresponding immutable query/session lines; 14,383 total allowances are unchanged. Final regenerated no-write enforcement passes. No broad stable trigger in this bounded owning-module read change; all query/context callers and direct page consumers are covered by the frozen selections. The future shared rendering assembly/graph change has a separate invalidation decision.
