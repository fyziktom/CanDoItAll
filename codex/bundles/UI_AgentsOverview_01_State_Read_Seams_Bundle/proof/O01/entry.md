# O01 entry

Prerequisite: manifest-closed O00. Scope: independent query operations, directly constructed per-page session and safe page states/context. Surface remains in the page. Context omits unavailable bound facts even when a stale bound value remains visibly labeled in the header. Header/Overview/Usage generations and owned tokens are separate; read retries do not replay commands. No public or private reflection into component state.

Validation selections: carry current owning Unit/Components/Integration topics; add AgentsOverviewSessionTests, AgentsOverviewReadLifecycleTests and AgentFrameworkModuleChatContextBuilderTests for the changed optional bound-fact public contract. Direct registered PostgreSQL query fixtures cover independent real failures. Build Module/Web and all three owning test projects, then freeze actual expanded discovery before execution. No broad stable trigger at this checkpoint.
