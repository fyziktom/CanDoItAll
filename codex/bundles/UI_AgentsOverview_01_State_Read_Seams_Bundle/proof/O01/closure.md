# O01 closure

Status: PASS, 2026-09-07. O01 state/read work is closed; O02 may now begin. Physical movement and measurement remain gated on O02 closure.

Direct Module, Web, Unit, Components and Integration builds pass. After the final page startup correction, Module/Web/Components and the real Web browser fixture were rebuilt and the 36 component cases rediscovered and rerun. Final owning selection: 65 Unit + 36 Components + 4 Integration = 105 passing cases, zero failed/skipped. This includes all 24 O00 cases, 18 direct session cases and three new PostgreSQL-backed registered-query tests. Exact discovery names/arguments, commands, TRX counters and earlier failures are retained under this directory. Counts describe this execution, not an architecture invariant.

The first O01 component run passed 34/36; two assertions incorrectly assumed header completion at aggregate completion. They now await the independent public header state and pass. Browser RED discovered that awaiting initial aggregate reads during prerender delayed the whole page. The corrected page starts the session once on first interactive render; later route parameters still supersede through the session. Browser validation then passed the 13-state sequence and a separate actual-long-label check. Nominal Long in the first successful sequence was not distinct fixture coverage and is not counted as such. See [browser review](browser-review.md).

Ownership: page owns route/effects and constructs/disposes one session; session owns Header/Overview/Usage accepted state, immutable snapshots, cancellation, generation/request fencing and retries; existing query owns the three read operations and independently classified header parts. No obsolete AgentsShellSnapshot/ReadShellAsync caller remains. Counts have one accepted source. Unavailable facts are omitted, same-scope stale data retained, mismatched usage hidden/disabled, partial matching usage remains usable. HR and selection context no longer depend on aggregate success. Defaults refreshes only currently demanded lanes and read retry never replays warmup. [Architecture gate](architecture-review.md).

Complete proposed-source portability enforcement passes after explicit review of two relocated ordinal avatar-key comparer allowances; no count increase and no weakened rule. Source scan: 6,673 text files / 1,156 binary files, 245 historical fingerprints, zero new/removed matches. Retained compressed/UTF-16 evidence scan passes. Manifest verification accompanies this closure. No new assembly/asset edge exists yet, so no broad stable run was triggered. O00 self-tests remain applicable because scanner implementation is unchanged.

Remaining work belongs to O02: actual controlled Surface, honest unavailable list/chart wording, chart legend geometry, dialog lifetime and owned overlays. O03 will measure before moving those pure files. These are not claimed complete here. No commit/push, sibling edit, route redesign, Governance implementation or merge-readiness claim.
