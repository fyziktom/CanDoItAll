# Current execution decisions

Owner authorization: [02](../inputs/02-implementation-authorization.md). The original preparation remains historical. This document resolves differences before implementation.

- O00: compile current-page public regression witnesses; distinguish semantic RED from setup defects and existing correct characterization. Freeze exact expanded discovery before each execution. Close ownership and the O01 plan from real evidence.
- O01: one page-created, page-disposed session owns accepted header/Overview/Usage snapshots, independent loading/error/generation/CTS state and request reuse. Workspace alone owns desired route scope. Split existing query into independent Header, Overview and Usage reads. Header preserves independent HR/avatar and bound results. No renderer extraction in this phase.
- O02: extract the real service-free controlled Surface while still in Module. Page retains navigation/defaults/HR/context and owned dialog effects. Harden all three usage dialogs and prove public browser behavior before O03.
- O03: freeze final O01/O02 source and fixtures; collect current full-app cold/warm baseline before any move. Then move pure rendering to existing UI, add typed Overview specimen to existing sandbox, and run post/Parity/Fast series. No source movement without reproducible direct-watch baseline.

## Context contract decision

The context builder creates an unrestricted Agents workspace context; aggregate counts are optional summary facts, not authorization. Overview and other non-selection tabs remain Ready without summary facts if aggregates are unavailable. Agents, Chat, Capabilities and Governance use accepted selection access even when aggregates fail. HR action depends on independently accepted HR identity and its own command busy state. Header bound failure cannot deny HR or poison Overview. No accepted Overview means no summary facts; unknown bound count is omitted, not fabricated as zero. Usage scope changes only a usage projection, so it stays outside chat navigation identity. Public tests prove this matrix.

## Accepted versus desired data

Retain accepted data internally through refresh failure. Same-scope accepted data remains visible with a stale/error state. Desired B with accepted A never labels or enables A as B. Matching partial B is usable with a visible typed warning. Wrong-scope/invalid metadata is a bounded failure. Exact same pending route echoes reuse work; explicit retries create a new generation. Late success/error/finally cannot publish into a superseding request or disposed owner.

## Scope and proof

No Capabilities/provider/routing redesign, sibling edit, commit/push/history operation or Governance implementation. No broad stable gate during O00-O02 by habit. Reassess one named graph/public UI/asset invalidation checkpoint during O03. Historical proof is never relabeled as new execution. Each child writes closure before downstream implementation.
