# O03 current execution protocol

Current status: corrected baseline, extraction, sandbox, browser and all four timing series passed; broad stable passed 10236 cases with source-adjudicated deferred-theory expansion; final byte sealing is recorded in closure.md and proof/O03. The pre-move specification below remains the protocol history. See the responsive re-entry and final measurement receipts in proof/O03.

O02 closed with 149 manifest entries, 154 owning cases and 14 sibling public-contract/ownership cases passing. The named stable-suite invalidation is the proven BaseLib public API correction plus this upcoming project extraction. Execute that broad gate once on the final source state, outside all timing windows. Historical documentation debt remains independently reported.

## Frozen environment and fixture

Direct `dotnet watch`, SDK 10.0.303, Node 24.19.0, Tailwind 4.2.1 and Chromium 149.0.7827.55; 1600 x 1000 browser viewport. Live Components/FileTools mode remains unchanged. The exact sibling source correction is the O02-owned four-file change, not a package substitution.

Use a task-owned isolated PostgreSQL profile. The fixture saves two local rendering providers, two technical agents, six synthetic completed usage observations and one team through actual registered stores. It does not invoke a provider, diagnostic, connector, chat or process. The registered production usage projection verifies the six observations before exporting immutable presentation values for the future sandbox. Defaults add their actual catalog entries; the exported state, not guessed totals, is the comparison contract. Credentials remain in ignored private files and never enter proof.

The real pre-move full app uses its normal Web project and production composition. It does not run a WebApplicationFactory or substitute its read/query services. Three process-cold starts use existing restore/filesystem caches. Warm runs measure three distinct edits each in Razor, C# presentation and CSS, each with three forward and reverse observations. No build/test workload may run concurrently with a headline timing series.

## Observable completion

Record durable flush SHA/time, process and owner IDs, runtime watch iteration and managed update generation, SDK watch events, browser document identity/timeOrigin, reload/enhanced-navigation signals, first visible predicate and final settled frame. Require two actual Apex plot SVGs with nonempty series paths, usable geometry inside the viewport, the exact six-observation metric, stable path/geometry snapshots and two animation frames. Titles alone are insufficient.

The first calibration failed because nested legend-marker SVGs were counted as plots. Saved DOM and screenshot proved both charts rendered correctly. Protocol v2 selects `svg.apexcharts-svg`; v1 and its failure remain separate. No source probe was applied by calibration and no headline sample was discarded. Protocol and assets are frozen before the measured series. No managed-watch tool, manual reload or hidden refresh repairs are used.

## Exact next ownership

Move five Overview state/presentation/surface files and the real ProviderUsageConsumerList plus AgentUsageDisplay into existing AgentFramework.UI/Overview. The formatting helper becomes an honest public pure utility because the retained Module dialogs and usage list still consume it. No old wrapper or duplicate implementation. Existing usage/application snapshots stay in their current value-contract owners. Page/session/query/dialog reads remain in Module.

Evaluate the current live project graph before adding Usage value-contract and Charts references. Current sandbox transitive closure contains 12 projects with no forbidden edge/cycle. CodeAnalytics independently reports no cycle in the current UI scope; Module-level pre-existing cycles are not represented as newly fixed.

Extend existing sandbox with explicit Overview specimen/section/scenario tokens and controlled sample interactions. Preserve catalog default, existing Capabilities scenarios, route and Parity/Fast mode. Use actual chart registration/static assets. The frozen baseline export is shared as data; no query/runtime/database service is registered in the sandbox.

## New public verification names frozen before implementation

Unit owning class: `CanDoItAll.Tests.Unit.AgentFramework.OverviewSandboxContextTests`.

- `Overview_token_and_unknown_tokens_preserve_existing_defaults`
- `Every_overview_scenario_round_trips_without_enum_ordinals`
- `All_three_scopes_have_matching_accepted_sample_identity`
- `Mismatched_samples_do_not_enable_detail_actions`
- `Baseline_is_the_frozen_canonical_projection`

Component owning class: `CanDoItAll.Tests.Components.AgentFramework.OverviewSandboxTests`.

- `Overview_query_renders_real_charts_without_production_services`
- `Named_states_render_their_explicit_public_presentation`
- `Scope_intent_updates_sample_and_replace_history_query`
- `Retry_clears_only_sample_failure_without_io`
- `Detail_and_team_intents_log_without_production_navigation`
- `Existing_catalog_and_capabilities_specimens_remain_available`

Exact theory rows and compiled discovery will be frozen after building, before executing these tests. Preserve all O01/O02 owning tests and affected avatar/catalog/capabilities/sandbox compatibility selections. Counts are execution receipts, not architecture invariants.

## Stop and completion rules

Do not move source until the new full-app baseline succeeds. If watch cannot be made reproducibly observable, preserve failures and stop before movement. After extraction and sandbox browser acceptance, repeat the exact timing protocol on post-move Web, Parity and Fast. Retain every sample and restoration, all outliers and failed attempts. Report min/max/range/median separately for first-visible and settled, cold and warm, forward and reverse. Restore exact probe bytes and all generated assets; stop only owned processes.

Close only with final direct builds, exact discovery/tests, the named stable gate, actual browser screenshots/charts, evaluated graph/cycle review, static portability, source/artifact secret scans, link/byte manifests and final worktree/sibling review. Prepare Governance only after that manifest-checked Overview closure.

## Rendered-closure adjudication

The actual Overview state has consumer/provider rows and team shortcuts, but no model-row collection. Adding one only for a sandbox would invent another renderer and violate the prepared pure boundary. Keep model read/rendering in the real Module ModelUsageDialog and validate long model labels there. The sandbox models detail action remains a typed controlled intent. This is an explicit source-based qualification of the requested long-label matrix, not a claim that the sandbox renders model details.
