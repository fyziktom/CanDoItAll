# Desktop UI and browser acceptance

Use Components MCP/compact-ui-composition before changing markup or CSS. Target **1600 x 1000** real desktop app. No tablet/mobile redesign or new multi-viewport campaign; preserve existing responsive rules.

Keep existing Overview below workspace tabs: compact eight-metric strip, four-column main grid, top/failing-consumer lists, 310px real bar/donut charts, team shortcuts. Full detail remains on-demand Consumers/Providers/Models dialogs. Header, HR and Defaults remain page chrome. Two visual count consumers must share one accepted value owner. Preserve ranking, label trimming, units, accessible summaries and unknown/unpriced/partial data. Place a bounded Retry near the failed read lane; no new generic toolbar/hero/editor.

Current desktop cards are 27.5rem high with existing chart/list containers and scroll policy. Transfer Overview CSS with its real renderer scope anchor, keep header CSS on page. Preserve one vertical scroll owner per existing region, no lateral overflow or competing nested scroll. First viewport must expose equivalent stats/charts/rankings/actions. Long names cannot squeeze side metrics. Smaller-breakpoint rules move unchanged.

Three dialogs remain WideDense for chart/table content; no textarea/form added. Inspect normal and each open overlay for close focus, action visibility, charts/tooltips, layering, table/body scroll, clipping and overflow. Scope/owner change closes only Overview-owned overlays and their reads; unrelated dialogs survive.

Planned real Web scenarios: Both/Agents/Chats; rapid scope and route changes; initial/pending/failed/retry; partial usage retains good data; history direct entry then Overview; unchanged header/HR/Defaults; all three accepted-scope dialogs; one team navigation; empty/long content; chart accessibility/tooltips. Use isolated safe fixtures. Deterministic failure paths may use a bounded test host, separately from mandatory real registered Web composition.

O03 runs real Web and existing sandbox Parity/Fast using identical safe snapshot/geometry/theme/assets and actual charts. Verify computed runtime-composed classes and cascade layers, not just generated class strings. Inspect chart JS ready state, not merely a placeholder element.
