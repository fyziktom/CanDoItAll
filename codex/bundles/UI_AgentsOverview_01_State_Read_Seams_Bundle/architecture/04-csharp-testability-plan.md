# Testability

[Current discovery](../inventory/test-inventory.json) references the actual Capabilities-03 stable run; it is not a new Overview run. O00 must build/list/freeze again. [Semantic map](../plan/semantic-test-map.md) freezes proposed names before implementation.

Current-page regressions render real page, control public IAgentsWorkspaceQuery with TaskCompletionSource, drive scope buttons/route parameters, and observe rendered data/actions/notifications/calls. Deliver late results after cancellation to prove fencing independently. No arbitrary sleeps. Missing-class compile errors are setup failures, not RED.

Keep production history lazy reads, HR header, Defaults confirmation, typed routes/Simple Chats and catalog echo witnesses. Already-correct cases remain characterization PASS. Direct Unit tests instantiate session and mapper without page or runtime. Prove wrong-scope rejection, old success/failure/finally, disposal, retry lane, partial data and independent collections/options. A forwarding wrapper back to the page must fail these tests.

Component tests render real Surface/chart/list without feature services; bounded chart JS setup is allowed in bUnit, but actual chart browser JS remains mandatory. Assertions cover public figure/summary, rows and typed values. Integration uses real registered workspace query, AgentUiAdapterTestHost and bound-resource query with isolated data; fake success cannot prove registration.

Use real DialogHost for delayed dialog reads, scope change/disposal and preservation of unrelated overlay. Parent removal alone does not prove dialog query cancellation. No private reflection, uninitialized services, exact partial/file counts or global CloseAll. Public-contract reflection is allowed only for meaningful manual-copy completeness.
