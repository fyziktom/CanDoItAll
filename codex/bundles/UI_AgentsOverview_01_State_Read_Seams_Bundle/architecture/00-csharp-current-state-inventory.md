# Current ownership and adjudication

The [preparation inventory](../inventory/source-inventory.json) and its CodeAnalytics snapshot remain historical. Current execution evidence is in O00-O03 proof. The implemented state described here follows the accepted O01/O02 boundary and the actual seven-file O03 move.

| Current owner | Responsibility | Direct evidence |
|---|---|---|
| AgentsHomePage | Route/workspace state, persistent header, selection access/context, HR/Defaults, typed effect dispatch and only its owned usage dialogs | Public page lifecycle/effect cases and real Web browser |
| AgentsOverviewSession, Module | One explicitly constructed/disposed page lifetime; independent Header/Overview/Usage CTS, generations, accepted data, errors and exact pending-request reuse | Direct Unit transitions plus noncooperative public-page races |
| AgentsWorkspaceQuery, Module | Existing cohesive interface now exposes ReadHeaderAsync, ReadOverviewAsync and ReadUsageAsync; independent header sub-outcomes; real registered backend ports | Unit service tests and four registered/database-backed Integration cases |
| AgentFramework.UI/Overview | Real controlled Surface, immutable state, pure mapper/options, ProviderUsageConsumerList and AgentUsageDisplay | No-feature-service rendering, explicit intents, two-instance independence and browser |
| Three usage dialogs, Module | Existing real query/rendering, captured accepted scope, cancellation/generation/identity, bounded errors and partial warnings | Public actual-dialog tests, real DialogHost/browser and query-token counters |
| Existing UiSandbox | Typed Overview specimen, safe immutable sample states, replace-history codec and intent log; actual shared assets/charts | New Unit/Component selections and both browser modes |

O00 confirmed stale usage publication, route replacement suppression, cancellation/finally and aggregate failure-coupling defects through 20 semantic failures. Four already-correct cases remained characterization. O01 makes accepted totals the sole count owner and separates context access, HR readiness and aggregate availability. O02 proves actual owned overlay behavior; a narrow shared-service opt-in was necessary because the original global LocationChanged handler closed unrelated dialogs.

The obsolete AgentsShellSnapshot/ReadShellAsync envelope and its callers are removed. No circuit-scoped mutable controller, interface-per-read, duplicate chart renderer or query implementation moved into UI. Existing Providers/RequestHistory aggregate suppression, confirmed Defaults/warmup, persistent HR, catalog/Capabilities and current production URLs remain.

Current evaluated sandbox closure is 14 projects, compared with 12 before this extraction, with no forbidden edge or cycle. Final scoped UI CodeAnalytics reports zero service registrations/diagnostics/cycles and nine informational member-count findings. It does not prove the entire solution is acyclic; pre-existing Module/Hosting and runtime nested-type cycle reports remain outside this child. See [O03 review](../proof/O03/architecture-review.md) and the evaluated graph receipt.
