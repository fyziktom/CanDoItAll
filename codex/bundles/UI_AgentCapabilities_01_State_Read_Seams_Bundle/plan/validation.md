# Validation

Direct Release builds: module and Web (new read registration), existing UI and broad Components consumers, Unit/Components and any affected support fixture assembly. No product project/reference or route change is expected.

Freeze and run exact new Unit/Components selections, then exact affected existing AgentCapabilities, AgentPanelSelectionFailClosed capability, AgentCapabilityList, managed curator identity and AgentsHomePage consumers. Keep read/presentation tests outside database fixtures; retain at least one real production composition test through the registered read adapter. No whole stable estate without a named public-contract invalidation.

Real full-app browser at 1600 by 1000 against an isolated database: select agent, filter, assign/remove, verify, preview, details open/close, each setup kind open/close, exact curator availability, and A-to-B selection while the public read adapter completion is held. A disposable validation-only host follows the already used provider WebApplicationFactory pattern. No capabilities sandbox or production delay switch is added.

Full proposed-source portability scan, reviewed baseline deltas and final enforcement without writes; scanner self-tests; complete source/artifact secret scans. Same historical grouped documentation finding is reported if unchanged, never hidden or relaxed. English comments and docs. Seal exact Git blob manifests and relative links with the already proven evidence validator. Publish the final owned source/evidence to the branch explicitly required by the user, without merge/history operations.

Close only after one host / one service-free controlled surface, single accepted-selection owner, cancellable stale-safe reads, real production composition, unchanged project/routing/sibling boundaries and explicit child-02 defect handoff. Then prepare child 02 only.
