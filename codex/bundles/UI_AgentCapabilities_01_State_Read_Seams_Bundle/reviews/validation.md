# Executed validation

[Summary and source hashes](../proof/validation-summary.json), [exact test commands and discovered names](../proof/green-receipt.json), [direct build receipts](../proof/build-receipt.json), [browser results](../proof/browser-summary.json).

Final: 13 Unit and 46 Component cases, zero failures/skips, matching frozen discovery. This consists of 42 new public-contract cases plus 17 existing owner cases. Component coverage includes the real registered read adapter with a database-backed CapabilitySetupFlowService harness and AgentsHomePage consumers. New session tests stay in Unit; controlled rendering/host UI tests stay in Components. Browser writes and proof use real production services and isolated PostgreSQL, not a fake command returning success. No additional broad stable run was invalidated by this bounded change.

Six direct Release builds passed: module, lightweight UI, existing broad Components, Web, Unit and Components tests. The Components test project reports the pre-existing xUnit2031 warning in AgentCatalogBoundaryTests line 384. No new warnings remain. A preliminary module build exposed two incorrectly named preview result properties; corrected to the existing AllowedCapabilities/Diagnostics contract. One direct-build command used an incorrect UI project path; the [path correction receipt](../proof/build-path-correction-receipt.json) is retained, and the verified project path passed.

Failing-first: three read-lifecycle failures and one passing unsafe assignment characterization were recorded against the original panel. The first green attempt exposed fixture corrections, not new product changes: verification requires an attached capability; the fake save needed to persist attachments for its subsequent read; ImmutableArray assertions must compare sequences; bUnit rendered-wrapper Dispose does not remove a component, so the final disposal test uses DisposeComponentsAsync. Original RED receipts remain historical. The final disposal test also has direct session cancellation and real-browser supersession corroboration. First iteration command/test receipts remain retained.

Browser calibration encountered the application's delayed startup-profile confirmation and stopped before assignment. The final harness explicitly clicks Continue for the isolated profile before interacting, then passes every required action, authoritative assignment/removal/proof, all dialogs, exact curator availability and delayed A -> B read. This is a fixture lifecycle correction, not a suppressed product failure. Nine screenshots and DOM extracts were inspected. No external model/tool process was invoked: verification uses an inline skill with embedded instructions.

[Portability review](../proof/portability-review.json): all 22 added and 20 stale matches reviewed; UI text/kind predicates and explicit case-insensitive display/tag search only. Final enforcement without writes passes at 14300. Scanner tests: six portability and four secret tests pass. Complete proposed source: 6200 UTF-8 files at scan time, 245 existing matches, zero additions/removals. Artifact scan includes UTF-16 and decompressed gzip, zero findings. Final publication runs the complete-source scan again after documentation is finished.

Documentation reports the same single grouped finding for 118 historical tracked .log files. No new logs are tracked; that existing gate is not weakened or described as passing. This slice closure is not repository merge readiness.

Persistence clarification: the full-app fixture uses an isolated PostgreSQL profile for application/projection composition; agent/capability canonical catalog writes use the registered file store in that isolated profile workspace. The browser verifies those real canonical reads. This is not a claim that agent catalog persistence is a provider-registry SQL transaction.
