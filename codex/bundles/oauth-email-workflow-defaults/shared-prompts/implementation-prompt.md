# Implementation Prompt

Implement the selected subbundle only. Preserve the raw requirement that the behavior is generic across workflows. Use the existing plugin OAuth and workflow runtime boundaries, add targeted tests, update the execution report while evidence is fresh, and stop if a gate cannot honestly pass.
