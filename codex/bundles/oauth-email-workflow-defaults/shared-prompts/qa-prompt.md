# QA Prompt

Validate that blank email OAuth connection ids resolve from Plugin settings without masking invalid explicit ids. Validate that Project Structure workflow start exposes readable skip options for project-structure write nodes and that selected skips reach runtime as preview simulation steps.
