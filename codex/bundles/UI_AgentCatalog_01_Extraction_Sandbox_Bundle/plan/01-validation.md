# Validation selection and evidence

Current source inventory from the completed provider stable run: AgentCatalogPanelTests 10 cases, AgentCatalogBoundaryTests 20 cases, TailwindSourceMonitoringTests six cases. Exact public display names are in test-inventory.json. At execution re-entry discover these exact filters before running; stop on zero/unexpected discovery and reconcile deliberate changes. Counts describe this baseline, never architectural invariants.

Components filter:
FullyQualifiedName~CanDoItAll.Tests.Components.AgentFramework.AgentCatalogPanelTests.|FullyQualifiedName~CanDoItAll.Tests.Components.AgentFramework.AgentCatalogBoundaryTests.
Expected current discovery: 30.

Unit filter: FullyQualifiedName~TailwindSourceMonitoringTests.
Expected current discovery: 6.

These preserve actual card tooltips/actions, exact managed identities, controlled selection/intents, request echo, first-save host behavior, dialog/lifetime and target cancellation. Keep behavior assertions; do not add private-field reflection, uninitialized runtime objects, filename-shape or exact assembly/type-count tests.

Direct builds at SB01: new CanDoItAll.AgentFramework.UI.csproj, existing CanDoItAll.AgentFramework.Components.csproj, module AgentFramework.csproj and Web.csproj. SB02 also directly builds UiSandbox.csproj. Use existing tests/Solutions owning entry points, Release /m:1 and freshly built assemblies. Add tests only for newly uncovered public behavior; freeze their exact names/theory rows before implementation/execution.

Independent boundary proof: evaluated new UI/sandbox dependency closure must not contain the production module/Core/Voice/EF/composition graph; build and start the actual sandbox with only UI services. This is a graph/runtime proof, not a source-file naming assertion. Existing Components tests can remain in their current owning project without dragging its harness into the runtime sandbox.

Browser: large desktop full-app and sandbox parity with real card/TreeView/tooltip/avatars/static assets; all typed action intents; loading/empty/search/selection/expanded-tree states; screenshot inspection and network/computed-style verification. Production host still opens its real dialogs/chat effects; sandbox records effects as intents. Keep these distinct.

Build/restore topology, assembly public surfaces and static assets justify one named frozen solution/stable consumer gate after focused tests if impact analysis confirms cross-cutting exposure. Never start with broad suites. Special Docker/runtime-portability/live/long-running/quarantined/browser aggregate lanes remain separate and must not be claimed without execution.

Run current portability self-tests, complete proposed-source scan including untracked files, reviewed deltas and final enforcement without --write-baseline; scan complete source/proof for secrets. Keep all source comments English. Record source before/after hashes, exact commands/discovery/results, compact transcripts and the final architecture/measurement validity review. No measurements or new tests were executed during preparation.

SB01 execution expansion: actual direct consumers add AgentEditorSessionTests (10 cases) and AgentSeamFinalizationTests (8 cases). The 48-case Components and 6-case Unit selections were frozen, discovered, and passed after fresh builds. See proof/SB01/test-receipts.json and adjacent discovery. These are inventory counts, not architecture invariants.
