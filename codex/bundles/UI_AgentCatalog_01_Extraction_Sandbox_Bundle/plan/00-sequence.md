# Sequential execution and reopen rules

Execution is authorized by the latest owner request, gated on Providers-02D closure. SB00 through SB03 are closed with their own proof and the final handoff. The sequence below remains governing.

1. SB00 baseline: rediscover HEAD/diff/provider closure/siblings, freeze fixture and graph/assets; collect full-app pre-extraction cold/warm baseline using the protocol. This avoids measuring only two already-changed hosts.
2. SB01 extraction: move cohesive rendering/card/mapper/contracts; wire old consumers; directly build new UI, old Components, module and Web; run exact catalog/card and asset tests. Reopen baseline if behavior, fixture, asset pipeline or SDK changes.
3. SB02 browser host: add minimal UiSandbox and representative fixtures, real components/assets, typed controlled state/intent feedback. Prove startup without database/provider/module services and inspect parity. Reopen SB01 on runtime-reference leaks.
4. SB03 comparison/closure: after behavior and asset parity, collect sandbox plus post-extraction full-app cold/warm trials on the same machine; review all failures/mechanisms, compare to SB00 and state limitations. Run one justified broader stable checkpoint only if the project/composition/static-asset changes affect those consumers; never repeat merely for proof formatting.

All units sequential in the shared checkout. No parallel source moves against active provider/agent work. The original preparation run created no implementation or measurement. Current execution evidence is recorded separately by unit.
