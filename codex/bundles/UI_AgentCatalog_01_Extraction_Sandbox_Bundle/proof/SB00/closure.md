# SB00 baseline closure

SB00 is valid and closed. SB01 may begin; no catalog production source had moved at this checkpoint. Providers-02D remains the accepted prerequisite. Repository-wide documentation debt (118 unchanged historical log artifacts) remains separately failed; this is not a whole-branch merge verdict.

The original ledger contains 27 comparable successful warm trials (nine patches, three repetitions each), three successful process-cold starts, and one retained instrumentation-invalid CSS attempt. All visible undos passed. All nine frozen source, SDK/lockfile, and generated-asset hashes match their pre-trial values. See summary.json, pre-ledger.jsonl and source-restoration.json. Original TRX receipts prove 30 Components and 6 Unit cases, with no failures or skips; discovery and original test output are adjacent. These counts describe this execution, not architectural constraints.

The three screenshots were inspected at 1600 by 1000: normal catalog, twelve representative private-provider cards including the long name and missing-summary fallback, and filtered empty state. Actual images loaded; all 17 referenced stylesheets returned HTTP 200. Interactive filtering proves twelve matches and restoration to forty cards. Expanded real TreeView and card actions remain present. Geometry and font evidence is in browser-geometry-and-assets.json.

Timing uses one Python perf_counter_ns clock. Cold includes managed tool dispatch; warm includes tool transport, 500 ms backend polling, 750 ms quiet observation, the browser predicate, and two animation frames. These are observed end-to-end timings, not isolated compiler times. There are no clean-compilation cold figures. The same populated caches and one managed Tailwind companion are used; redundant calibration watch processes were stopped before sampling. Slow valid samples remain in the distribution.

Two manager parsing issues are explicit: initial WaitingForChanges can precede health confirmation without setting its WatchReady bit; .NET 10 reports Static asset changes applied / No managed code changes to apply, while the sibling parser expects older wording. Initial readiness therefore uses raw waiting state, ready actual runtime and confirmed identity plus interactive rendering. CSS v2 uses the actual static completion event and computed style, without inventing a managed-generation advance. The invalid first CSS attempt is retained outside the comparable v2 distribution. Razor/C# keep the original settled-generation protocol. Every post-extraction host must use these same category-specific protocols.

Runtime and fixture configuration was fixed before primary sampling: isolated PostgreSQL profile, forty agents, six teams, twelve tagged representatives, private-provider metadata, normal managed identities, and the same bundled avatar sources. No personal application data was used. The fixture export hash is in summary.json. SDK 10.0.303 (global.json 10.0.302/latestPatch), Node 24.19.0, Tailwind 4.2.1 (installed equals lock), Chromium 152.0.7977.66. Machine: Windows 11 Pro 10.0.26200, i9-13900H, 14 cores/20 logical processors, 68405329920 bytes RAM. Machine facts were recorded during pre-trial calibration; this document packages that observation afterward.

Branch/local and remote remain components-decoupling at e5a8d5c6b7ad19c99c805a76cde84b99d08d9eee plus the provider recovery diff. Live Components c3e6aa03a878994c0ba8aed6af017d0be75f3796; FileTools 7c7453c6583365ae5bd63f8fc6efc4a776e15818; SharedInfo 00e6fe6389eba9a6bc3b2bb78e27fb10b7597292. Source mode is retained. CodeAnalytics snapshot snap-20260905233626-af2806c3 has no blocking error and two existing cycles; it is the pre-extraction graph, not a new-project proof.

Next validation expansion is justified by direct type-consumer discovery: AgentEditorSessionTests and AgentSeamFinalizationTests also consume the moved catalog contract. Freeze their actual discovery before the SB01 test execution. One final broader stable checkpoint is justified by new project/solution and transitive static-asset composition, after focused checks. No routing or provider-history work is authorized here.
