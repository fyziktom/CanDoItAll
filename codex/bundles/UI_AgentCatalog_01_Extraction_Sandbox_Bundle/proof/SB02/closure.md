# SB02 closure

Closed as a runnable, independent catalog browser host. The sandbox has one direct project reference, to CanDoItAll.AgentFramework.UI. Its restored closure contains the real card, Conversations components and live BaseLib assets, with no old AgentFramework.Components, module, Core, provider runtime, Voice, EF or production composition graph. Standalone SourceWatch loaded 12 projects. The setup-only fixture generator lives under bundle tools, outside the sandbox and product graphs.

The final sandbox built directly in Release with zero warnings/errors. The separately compiled fixture recipe also passed. SB01's 48 Components and 6 Unit cases cover the unchanged extracted production implementation; SB02 adds real browser acceptance through its public UI. The broader final stable gate is a separate cross-assembly checkpoint and is not claimed here before completion.

The browser uses the frozen 1600x1000 CSS viewport and exact catalog bounds (125, 222.59375, 1419, 810.59375). The embedded fixture preserves the baseline rendering projection: 40 agents, six teams and 12 searchable custom cards. All five linked stylesheet responses have nonempty content, the Material Symbols font is loaded, and all 40 normal avatars load. The shared theme's 115598 bytes have the same SHA-256 as the full-app source. Selection, editor/new/team/member/delete/chat intents, loading, empty, favorite, private/status badges, seeded avatars and explicit initials use actual components. No production effect is invoked by sandbox intent output.

Long-name tooltip DOM is outside the card scroller and clamps within the viewport. Edge validation temporarily positions the real tooltip target and removes transformed containing blocks, then restores all inline styles; recorded target boxes prove the four actual corners. Normal and edge geometry, opacity/background and text were checked. The regular screenshot path dismissed the transient tooltip during capture, so tooltip-visible-cdp.jpg is a separately inspected native browser capture (device pixels, not a changed CSS viewport). Other screenshots capture the normal, loading, empty and supported fallback states.

Calibration uncovered and repaired two sandbox integration defects before timing: the SDK linked asset initially had the wrong physical ContentRoot (HTTP 200 with an empty body), and the development probe omitted the launch ownership fields required by the manager. The final ContentRoot uses portable SDK metadata; no copied CSS, fake directory or Web project reference remains. The probe matches the existing managed protocol. A broken avatar URL was an invalid fallback fixture assumption: the existing shared component supports missing-value seeded images and explicit initials, not automatic broken-URL recovery. Its implementation was preserved. Incomplete early CSS/readiness assertions and images remain calibration history, not passing primary measurement evidence.

Managed Razor and Tailwind calibration edits both became visible through hot reload and restored exact source bytes. These two calibration samples are excluded from the three-host primary comparison. CSS uses the previously frozen v2 static-completion protocol because this SDK's raw message is not recognized by the manager's generic settled parser.

Portability passes with 14300 reviewed findings. The 49 added occurrences are the SDK environment/launch metadata and inert fixture/developer text; policy categories, protected scope and pattern hash are unchanged. The full new/stale baseline diff was inspected. The complete proposed text scan found 244 historical secret matches, zero new matches and zero unreadable files. Final whole-proposal scanning will run again after measurement proof is complete.

CodeAnalytics snap-20260906025532-8cba401d has no blocking error or cycle. DI0002 is a collector ambiguity for AddSingleton(CatalogFixture.Load()); direct source and successful rendering prove the immutable snapshot registration. The informational nine-member readiness record is one cohesive external protocol payload. The restore closure, not this deliberately scoped collector's empty external-reference list, proves the transitive project boundary.

SB03 may now run matched post-extraction full-app and sandbox measurements after the independent stable test activity stops. No performance improvement is claimed by this closure.
