# Reproduce the catalog comparison

Use the same machine, global.json-selected SDK, Chromium version/1600x1000 viewport, sibling revisions, Debug SourceWatch lane and locked Tailwind pipeline for all hosts. Read plan/frozen-edits.json and proof/SB00 before execution. Primary pre-extraction data must be collected before source movement. The archived baseline is historical; rebuilding the current checkout cannot recreate the old boundary.

The Python coordinator takes --repo <absolute repository root> and optional --port 17299. It validates each frozen source patch, flushes bytes before starting its monotonic clock, records browser observation, refuses concurrent patches, restores exact source bytes and requires visible undo. Its output is .mcp-state/catalog-measurements/ledger.jsonl. Keep this directory private; no database password belongs in proof.

The three JavaScript runners execute through the repository dotnet-watch and Playwright MCP tools inside functions.exec. They deliberately use one coordinator clock for source write and visible browser assertion. Load browser-measurement-helpers.js into store("cat-browser-helper", source), and the selected runner source into the matching cat-warm-script, cat-css-script or cat-cold-script key. Load the nine edit definitions into cat-edits. cat-session holds the active managed app session ID. The executing scope supplies host ("pre", "post", "sandbox"), url, edits or repeats, and the actual app_start request. Run cold repeat 1, all six managed edits x3, all three CSS edits x3, then cold repeats 2 and 3. Only one host and one managed Tailwind companion may be active. Preserve raw start/status/wait results and all failed observations.

The full app uses the one isolated PostgreSQL fixture created before the baseline; retain it unchanged for the post-extraction run. The sandbox embedded fixture preserves the same rendered projection, with unused instructions, configuration and secret references removed. The fixture parity receipt documents the projection. Reproducing a new run requires a new isolated test profile, the same seeded catalog and custom fixture recipe, and regeneration of the sandbox snapshot from that profile before either timing phase. Never point the fixture or measurement launch profile at an operator's database.

Full-app launch: src/App/CanDoItAll.Web/CanDoItAll.Web.csproj, Debug, WatchRun, SourceWatch, URL http://127.0.0.1:5291, /agents?tab=agents. Use the environment produced by the isolated fixture's CreateEnvironmentVariables and a temporary project launch profile. Keep database credentials in its ignored password file; the MCP overlay does not accept arbitrary database settings. Back up the original launchSettings bytes, verify other profiles did not change, and restore them after stopping the host. Do not baseline temporary machine paths.

Sandbox launch: src/Sandboxes/CanDoItAll.AgentFramework.UiSandbox/CanDoItAll.AgentFramework.UiSandbox.csproj, Debug, profile "Catalog sandbox", WatchRun, SourceWatch, URL http://127.0.0.1:5391, /agents?tab=agents. The default scenario and frame match the full app's catalog geometry. Prove the circuit is interactive by filtering to 12 custom cards and restoring 40; SSR card counts alone are insufficient.

For Razor/C#, wait for the managed WatchSettled milestone, then the actual browser predicate and two animation frames. If necessary, record one explicit browser reload. For CSS, use the frozen v2 "Static asset changes applied" raw-log milestone plus computed-style verification: SDK 10.0.303's static-update message is not recognized by this MCP revision's generic settled parser. Do not treat its stuck generation bit as application failure or silently vary the protocol by host.

Timing includes dispatch/poll/browser-tool transport latency. Record hot reload, browser reload, process restart or failure and retain outliers. Process-cold samples use restored, previously compiled caches; they are not clean-build measurements. Aggregate only comparable primary samples, separately from calibration and the retained invalid pre-CSS parser trial. Report minimum, maximum, range and median per edit/category/host, with the sample matrix and actual limitations.

The setup-only Fixture/CatalogFixture.csproj is outside every product and sandbox graph. Run it once with the absolute repository root argument using the repository test PostgreSQL prerequisites. It creates only a new isolated profile and stores its generated environment and snapshot in .mcp-state/catalog-data. Keep that profile until all full-app samples finish; afterward remove only the database and root named by this fixture. Do not run setup between pre and post samples. Its output contains no password; the ignored password file must never enter proof. The current embedded sandbox snapshot is the actual baseline projection, not a newly generated replacement.

After creating a new isolated fixture, run export-catalog-fixture.py --input <isolated snapshot.json> --output <new reviewed fixture.json> before freezing either host. The exporter removes unused instructions, configuration, allowed-secret references and capability proof notes, and refuses to overwrite files. For this run, --verify src/Sandboxes/CanDoItAll.AgentFramework.UiSandbox/Fixtures/catalog.json reproduced all 202188 bytes from the retained baseline snapshot, with zero secret references. This read-only verification does not regenerate the frozen fixture.

Optional SDK appendix: extract-sdk-durations.py correlates each primary trial's before/after log cursors with the SDK's reported apply message. Pass --ledger, --logs <raw managed-log JSON or JSON.gz files>, and --output. Keep these durations separate from browser-observed time; a reported static-asset 0 ms is the SDK's granularity, not zero user latency. Every baseline primary trial maps to exactly one apply event.

The report generator accepts the preserved proof/SB03/ledger.jsonl.gz directly. All archived app logs are original tool envelopes compressed losslessly; SDK event mapping is a derived appendix. The completed matrix and its observed performance limits are in proof/SB03/closure.md.
