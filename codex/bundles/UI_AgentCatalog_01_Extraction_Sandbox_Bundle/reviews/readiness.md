# Preparation readiness

Status: Pass for future execution planning; execution remains separately authorized. Manual semantic validation is appropriate for this compatible compact child: inputs, requirements, source/boundary decisions, dependency order, work units, exact test baseline, browser assets, measurement protocol and closure obligations are explicit.

Prepared only after provider closure on 2026-09-05. The source audit found the panel's stable identity/icon/workload helpers already in Models; no new fake identity abstraction is justified. The real selection card's broad assembly must not become a sandbox dependency. Moving its pure mapper requires making the card options/API public for existing switch-dialog consumers; this is explicitly planned.

Current selection inventory is 30 Components and six Unit cases from actual completed test receipts; re-discovery is mandatory at implementation. Source hashes are in source-inventory.json. No new project, sandbox, move, route or watch trial was executed during preparation. Therefore route implementation, physical extraction and measured performance remain unproven, while this bounded experiment is implementation-ready.

Residual considerations: Models/Conversations/real sibling UI dependencies still cost compilation; Tailwind intentionally retains the common full source scan in the primary comparison. A lightweight graph may improve warm C#/Razor iteration more than global CSS generation; measure rather than assume. Existing module/type cycles and repository documentation artifact debt are outside this extraction unless an observed blocker requires reopening the plan. Preserve active user work and live siblings at re-entry.

Final closure must map C01-C10 to actual artifacts and state whether measurements support improvement, no improvement or an invalid comparison. It must not promote prepared trial recipes into measured results.
