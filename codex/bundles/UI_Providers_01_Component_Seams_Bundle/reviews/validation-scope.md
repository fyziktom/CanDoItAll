# Frozen validation checkpoint

At the corrected source checkpoint, 27 Unit cases pass; 86 bounded Component cases pass across the 80-case successful remainder and rerun of six corrected label assertions. No production source changed between those runs.

Named broad-gate trigger: CodeAnalytics conservative Unknown impact analysis cannot resolve production Razor/referenced source in its single-project test workspaces, sees public/registration shape changes and reflection dispatch, and explicitly returns AllSuppliedSuites. Workspace source discovery is nonzero/healthy but impact completeness is false and confidence low. See test-impact.json. No behavior-preserving assertion was used to narrow it.

Promote Unit, Components and Integration to project-wide executable stable coverage at this frozen checkpoint, once each. Keep the repository's mandatory lane exclusions (Playwright, LiveProcess, LongRunning, Quarantined, UnixRuntimePortability, RequiresHostDocker); those lanes are not passing claims. Memory is not a supplied/changed workspace. This supersedes the earlier conditional no-broad-gate plan. Actual discovery and execution counts must agree; analyzer source-method counts are not runtime test-case expectations.

## Runtime discovery reconciliation and local invalidation

Broad Unit list-tests displayed 6830 entries; 6864 runtime cases passed. The 34-case expansion is fully attributable to three existing non-pre-enumerated data theories: FloatingAgentChatSettingsValidatorTests.Validate_rejects_values_outside_inclusive_boundaries (1 -> 8), MafFinalizerToolFactorySchemaCharacterizationTests.CreateCapture_produces_the_policy_tool_name_description_and_json_schema (1 -> 8), and ProcessRuntimeToolPreflightServiceTests.Process_starting_tool_contracts_have_deterministic_host_routes (1 -> 21). Comparing discovered/executed base method names found no other mismatch or omitted method.

Broad Components: 1278 expected/discovered/executed; 1277 pass and one workflow-preview wait timeout. The exact unchanged WorkflowsPageTests.Workflow_canvas_preview_prompts_for_project_context_and_can_skip_project_writes passed on focused rerun (5/5 together with four final provider regressions). No workflow source or test was changed, and no timeout was increased. Retain the initial failure in evidence; do not claim a clean uninterrupted broad run.

After the stale-continuation correction, all 27 provider Unit cases passed on rebuilt source. All 28 provider Component cases are covered by the 24 passing remainder and four passing strengthened/repaired textarea-value assertions. bUnit records the raw textarea binding in its value attribute; asserting AngleSharp.Value read empty independently of the captured binding. The final tests assert before and after delayed completion, preserving the actual negative obligation.

Local invalidation is limited to ProviderProfilesSession's accepted-read result and its sole production consumer AgentProviderProfilesPanel. Repository search finds no Integration consumer of those new methods. The broad baseline tests are retained; final affected consumers are revalidated directly without repeating unrelated broad suites.

## Completed final evidence

Broad Integration list-tests displayed 1315 test entries (excluding two build-output lines); 1320 runtime cases passed. The only expansion is PluginCatalogIntegrationTests.packaged_plugin_preview_simulation_avoids_live_external_effects (1 -> 6). All other discovered/executed base methods agree. Unit 6864 + Components 1278 + Integration 1320 = 9462 executed stable cases, with the single initial Components timeout described above; no cases were skipped.

After the final target-heading and New metadata-refresh correction, all six directly affected Component cases and all 27 provider Unit cases passed again. New cases explicitly verify raw Suggested models text both after catalog refresh and after delayed superseded editor completion. The six-tab/context, pricing, catalog refresh and source-managed coverage from the unaffected passing remainder stays valid. No additional production dependency or Integration consumer changed.

The final direct module build passed with zero warnings/errors. Browser-host build and runtime assembly hash agreement were verified. Final portability scanned tracked and untracked source and passed enforcement without a baseline write: 14251 reviewed findings unchanged. Browser proof on the final runtime includes catalog refresh preserving New draft text. See closure.md and browser-evidence.json.
