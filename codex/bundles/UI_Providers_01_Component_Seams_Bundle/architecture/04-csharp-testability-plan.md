# Proof contract

Behavioral tier. Discover exact cases before execution; record discovered/executed counts. Existing bounded characterization selection includes four provider classes and adjacent Agents catalog/editor lifetime classes (70 cases discovered before provider edits). First SB09 run: eight expected behavior failures; repaired run eight pass.

Unit: direct session and section tests with fake reads, delayed success/failure, disposal, target reset, missing provider, same-target Retry, secret warning/reference retention, read-only projection, stable context/section. Production adapter composition is proved by unchanged component tests using actual registered services and persistence. Add direct adapter failure/cancellation tests if the composed scenarios cannot inject the secret failure separately.

Components: public UI events plus fake read boundary; fail-closed rendering/Retry, superseded selection and reset, secret warning, source-managed actions, all six lazy tabs preserve context. No private component reflection or runtime source shape assertions as a substitute for behavior.

Build changed production project directly. Rebuild affected test assemblies; run focused Unit and Components (and Integration if new adapter cases are placed there). Portability scan must include new source; review baseline changes then final enforcement without write-baseline. Run docs validator; pre-existing 118 historical tracked log files are a separate known blocker, not permission to weaken it.

Browser: large desktop, provider load/tree, all section identities, lazy history, New/reset, filter and connections. No live provider invocation or real configuration edits. Small task-owned temporary fixtures only if needed.

Invalidation: module-local adapter registration is bounded, no schema/shared configuration/CI/root-composition change. No broad stable rerun unless evidence reveals wider affected behavior. Historic suite numbers are not current proof.

## Execution reconciliation

The named conservative CodeAnalytics AllSuppliedSuites trigger did occur; [validation scope](../reviews/validation-scope.md) records the resulting single broader stable checkpoint and explicit local invalidation after final read/UI refinements. Direct adapter proof consists of four Unit cases, for 27 new provider Unit cases total. There are 16 new provider rendering cases and eight SB09 regressions: 51 new cases in all. Current outcomes and historical failed attempts are preserved in the compact [artifact summary](../reviews/test-artifact-summary.json). The completed [closure](../reviews/closure.md) controls current status.
