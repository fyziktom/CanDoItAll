# Capabilities-02 closure

Status: CLOSED for the bounded assignment, diagnostic-publication and effect-lifetime program. SB00 -> SB01 -> SB02 -> SB03 completed sequentially. This is not repository merge readiness. The documentation gate still reports the unchanged 118 historical tracked .log files; that repository debt is explicit, not waived or relabeled as passing.

Entry branch and independently read remote HEAD: `components-decoupling`, `d066d367d6abc147e046bee1308ea5f702e65cfd`. Entry was clean. All proposed changes belong to this task; no unrelated work was reset, overwritten or discarded. Components remains `c3e6aa03a878994c0ba8aed6af017d0be75f3796`; FileTools remains `7c7453c6583365ae5bd63f8fc6efc4a776e15818`. Live sibling source mode is preserved. No merge/rebase/squash/history rewrite or branch cleanup was performed. Final HEAD remains the entry SHA; the implementation, tests and bundles are staged locally. The independently re-read remote remains at the same SHA. The source under review is that HEAD plus the owned staged change.

## Input closure and adjudication

Every finding is recorded in [entry adjudication](reviews/adjudication.md): all 20 confirmed, with exact qualifications for catalog/index revision separation, diagnostic versus setup effects, the empty nested-dialog set and non-idempotent chat admission. Before implementation the prepared bundle was strengthened with the actual commit timeline, immutable desired-set semantics, explicit recovery classifications, per-agent attempt retention, diagnostic input receipts/write-callback fencing, API compatibility, semantic lifetimes and 49 frozen proof topics.

C02-01 through C02-09, C02-11 and C02-12 are Solved within the documented bounded scope. C02-10 is Solved as an extraction-boundary inventory and next-child preparation authorization; extraction, sandbox implementation and timings are deliberately not executed here. The exact [49-topic map](plan/required-topics.json) links passing test identities and records nested dialogs as an audited empty set. [Validation](reviews/validation.md), [architecture review](reviews/architecture-exit.md) and [browser review](reviews/browser.md) contain the evidence and limits. The [feature gate manifest check](proof/checks/evidence-index-feature-gate.json) passed for all then-current retained files; final sealing is repeated after closure/preparation documentation.

## Implemented result

Assignment captures a stable attempt, exact before/desired sets, expected agent revision and a private complete editor copy before awaiting Save. The controlled surface keeps authoritative attachments visible. Typed validation/conflict/commit/warning/unknown outcomes follow the real file-catalog -> workspace-index -> current-profile CRM/HR boundary. Cancellation before dispatch writes nothing; cancellation after dispatch is not rollback. Canonical recovery performs only reads: exact desired current state satisfies the postcondition; exact unchanged before state allows deliberate same-submission retry; intervening state needs explicit adoption; failed/wrong/older evidence remains unconfirmed. Resolved entries are removed only by a matching acknowledgement.

Verification captures independent agent, attachment, capability and provider inputs. It separates diagnostic observation from proof publication, revalidates after observation and inside the locked catalog callback, and exposes a private immutable receipt for read-only persistence verification. Detachment or changed inputs cannot publish stale proof or advance timestamps. The legacy Task workspace signature remains compatible; the API retains success acknowledgement and returns sanitized typed non-committed results (400 for rejected inputs, otherwise 409, automatic replay false), with target and available attempt/timestamp only.

The scoped coordinator permits one assignment/proof operation per agent and isolates different agents. Target replacement cannot erase unresolved work. Preview owns the latest target-linked token. Details/setup own individual panel-lived references and cancel child reads/diagnostics; dispatched global capability saves stay observed. Curator owns one fixed-agent launch independent of selection, retains known chat identity and blocks unknown replay within the circuit. The host still orchestrates those effects, callbacks and authoritative session refresh. The renderer remains service-free and preserves local filters/raw rule draft.

## Evidence and limits

125 distinct owning cases passed: Unit 30, Components 74, Integration including API 21, zero skips. Five direct production builds passed. Real browser acceptance passed at 1600 x 1000, including actual non-navigation disposal and one real managed Curator chat. Failing-first stale proof, authoritative assignment, A-B-A, preview/overlay/Curator and API evidence is retained alongside fixture/selector/compile failures. Portability final enforcement passed with 14302 reviewed findings. Complete proposed text has zero new secret findings; retained decompressed/UTF-16 evidence has zero findings. The inherited documentation failure remains visible.

Recovery is circuit-scoped, not durable across a new circuit/restart. Provider and file-catalog consistency uses optimistic revision checks, not a distributed transaction. Unknown launch cannot prove chat absence. Global capability catalog CRUD recovery and the historical details-load fallback were not redesigned or declared hardened. No arbitrary external endpoint or chat prompt was used. No whole stable suite was invalidated by this bounded source-compatible facade and unchanged project/schema/provider contracts.

Shared feedback adds only proven authoritative-presentation, canonical-recovery, diagnostic-publication and target/panel/circuit ownership rules. Typed semantic state and controlled rendering are Proven. Route binding remains Deferred; no new production bookmarkability exists. Lightweight capability extraction is Ready for a separately executed pre-move baseline. Capabilities sandbox and watch performance remain Deferred and unmeasured. The [next child](../UI_AgentCapabilities_03_Extraction_Sandbox_Bundle/README.md) is prepared only, reference `CDA-UI-SEAMS-AGENT-CAPABILITIES-03`. Preparation followed this feature closure; SB00 has not started.

Additional formatting check: whole-diff Git whitespace validation reports original CRLF proof bytes (the initial check found 9836 diagnostic lines, none outside proof). Those bytes are preserved. Automatic approval review rejected changing whitespace settings because it could weaken validation; no such setting was changed. This formatting result is reported separately from passing source/build/behavior/manifest gates.
