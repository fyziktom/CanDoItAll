# SB02 gate

Status: PASS for the bounded behavioral phase. Entry and predecessors remain unchanged.

The Core verification facade now delegates immutable diagnostic input capture and locked proof publication to one narrow owner. Agent, attachment, capability and provider inputs are checked after diagnostics and again at the write callback. Public typed outcomes preserve the legacy Task signature. A private proof receipt supports canonical read-only classification; it never replays diagnostics. The production runtime provider snapshot is refreshed asynchronously and checked at its last observed revision; this is optimistic validation across independent stores, not a distributed transaction.

The existing scoped per-agent coordinator excludes assignment/proof overlap, keeps pending and unresolved state across reconstruction, and removes only matching resolved attempts. Preview cancellation follows the selected session and attempt; dialogs follow panel lifetime; dispatched Curator creation is observed without owner cancellation, with known chat retained and unknown launch blocked across reconstruction. No new partial, project, service bag, outbox, routing or sibling change was introduced.

Owning evidence: sb02-gate-unit 30 PASS; sb02-gate-components-2 73 PASS plus sb02-details-ownership 1 PASS; sb02-gate-integration-2 20 PASS plus sb02-runtime-revision-2 1 PASS. All have exact discovery and zero skips. The extra runtime test uses the registered SQL registry/runtime snapshot and a real file catalog; its first fixture failed because the catalog shadow lacked the required provider reference, now corrected.

RED evidence includes original stale diagnostic publication, five preview/dialog/Curator failures, two API 500 failures, and the corrected A-B-A historical entry-host replay. Earlier compile, selector, fixture-registration and discovery errors remain negative evidence and are not described as product failures. The historical entry host was temporarily built for one test and restored byte for byte; no history or branch operation occurred.

Nested dialog audit: both actual rendered subtrees currently open no nested DialogService references. Their direct overlays and asynchronous reads/setup diagnostics have explicit cancellation ownership. The shared DialogService intentionally closes all overlays on navigation; a separate validation-only non-navigation browser probe will distinguish that existing behavior from panel disposal.

SB03 owns fresh direct production builds, browser proof, final code/dependency audit, complete portability/secret scans and compact artifact sealing. This phase pass is not final slice or repository merge readiness.
