# Sequential execution

SB00 is the critical foundation. Rediscover branch/remote, working diff, active siblings, governing bundles/rules and registered storage. Preserve other work. Freeze source fingerprints, exact test discovery and a transaction/effect timeline before editing production. Collect failing-first assignment rejection/unknown/reconciliation and owned-dialog/preview tests. The predecessor's 59-case selection is historical baseline, not new evidence here.

SB01 depends on SB00 classification. Implement independent assignment submission, target/attempt outcomes and read-only reconciliation. Keep diagnostic/dialog behavior for SB02. If a storage boundary cannot be proved, retain Unknown with an explicit verification protocol. Test real file-store/profile/CRM composition and public UI behavior.

SB02 reuses assignment state and the accepted read/session seam. Harden verification, preview, details/setup and curator lifetimes; fence after every await and own busy state per operation. No global close. Test public delayed services, real DialogHost and safe verification persistence.

SB03 runs exact owning selections/direct builds, full-source portability/secrets and 1600x1000 real-app scenarios. Retain commands/discovery/counters/hashes, failures and inspected screenshots. Seal the actual Git artifact set. Preserve historical proof/documentation failures honestly and add only generalized proven feedback. Reassess extraction readiness without movement/sandbox/routing work.

Reopen triggers: different registered store, changed atomic-write/revision semantics, authority changes, child-dialog backend changes, surface services, project graph changes or altered owner API response contracts. Re-run dependent proof only; state a named reason before broadening the estate. No merge/rebase/squash/history cleanup. Ordinary checkpoint publication only when authorized by the execution request.
