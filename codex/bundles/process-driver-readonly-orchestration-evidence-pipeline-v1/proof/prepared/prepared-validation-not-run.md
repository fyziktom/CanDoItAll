# Prepared Validation

This bundle was prepared outside the repository checkout. Codex must run `scripts/validate_bundle.py --stage prepared` and the active `candoitall-bundle-validator` before implementation.
