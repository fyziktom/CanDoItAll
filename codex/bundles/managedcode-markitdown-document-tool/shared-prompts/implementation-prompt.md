# Implementation Prompt

Implement this bundle end to end.

Use the C# architecture skills. Keep ManagedCode.MarkItDown isolated in `CanDoItAll.Tools.Documents`, expose only SDK-free Core contracts to runtime code, preserve the public `workspace_convert_document` tool contract, and add focused tests. Do not add quotation-specific business logic or broaden project-structure permissions in this bundle.

