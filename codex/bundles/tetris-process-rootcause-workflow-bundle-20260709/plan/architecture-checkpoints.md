# Architecture Checkpoints

## Checkpoint After SB01

- Dependency graph review: extracted services do not create new project cycles.
- Partial-class policy review: no new permanent adapter partial as final architecture.
- Testability review: completion gate evaluator tests do not instantiate full MAF runtime.
- Old-class shrink proof: adapter delegates pure gate logic instead of owning it directly.
- Unlock decision: SB02 may start only when behavior-preserving extraction passes existing tests.

## Checkpoint After SB03

- Dependency graph review: receipt rule contracts remain in Contracts/Abstractions or justified existing project.
- Partial-class policy review: product/process receipt gate behavior is not hidden in new adapter partial helpers.
- Testability review: branch filtering and dedup tests are isolated.
- Old-class shrink proof: receipt parsing/evaluation is outside adapter or behind thin delegation.
- Unlock decision: SB04 may start only when branch-aware receipt enforcement is proved for accepted and repair branches.

## Checkpoint After SB04

- Dependency graph review: completion issue router uses metadata, not Workbench/template implementation references.
- Partial-class policy review: route decisions are not embedded in adapter switch statements.
- Testability review: route tests use arbitrary branch names and issue codes.
- Old-class shrink proof: adapter creates branch signals from evaluation/router output, not local issue ordering hacks.
- Unlock decision: SB07 template migration may start only when route metadata works generically.

## Checkpoint After SB05

- Dependency graph review: Workbench recovery provider implementation does not make generic Application/Runtime depend on Workbench.
- Partial-class policy review: no new domain-specific generic helper partial.
- Testability review: provider selection tests and forbidden-token architecture tests pass.
- Old-class shrink proof: `ProcessStepRecoveryInstructionBuilder` no longer owns .NET/QA branch guidance.
- Unlock decision: SB11 can close architecture only if CodeAnalytics confirms no cycles.

## Checkpoint After SB11

- Dependency graph review: refreshed CodeAnalytics snapshot captured.
- Partial-class policy review: temporary migration bridges removed or documented with follow-up blocker.
- Testability review: extracted responsibilities have independent unit tests.
- Old-class shrink proof: adapter and recovery builder are thin enough for future extension without editing generic monoliths.
- Final decision: bundle can close only when all critical proof manifests and semantic invariant contracts exist.

## Corrective Checkpoint A - SB12

- Dependency graph: no new reference and no cycle.
- Partial policy: exactly one non-partial adapter declaration; zero partial files.
- Testability: direct collaborator tests do not construct adapter or host.
- Old-class shrink: adapter allow-list contains constructor, descriptors, and two delegation methods only.
- Unlock: SB13 remains blocked until all checks pass.

## Corrective Checkpoint B - SB13

- Generic boundary scan: no .NET/software-delivery tool or step-key branches in generic adapter completion services.
- Strategy justification: at least generic and domain contributors exist; no service location or silent fallback.
- Testability: contributors and catalog have positive/negative direct tests.
- Unlock: SB14 remains blocked until checks pass.

## Corrective Checkpoint C - SB14

- Refreshed CodeAnalytics snapshot and dependency/cycle proof.
- Package compatibility decision and build/provider tests.
- Autonomous Tetris production evidence or honest external prerequisite blocker.
- Final architecture review and fake-proof red-team audit.
