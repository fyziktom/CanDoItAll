# Compatibility Report

The `app-pages-screenshot-set` template projects into the current process module using standard roles, steps, dependencies, and artifact expectations.

The template explicitly starts the app once for the page set and cleans up once after all captures.
