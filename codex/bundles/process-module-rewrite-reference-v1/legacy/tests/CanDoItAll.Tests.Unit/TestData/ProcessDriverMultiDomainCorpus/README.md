# Process Driver Multi-Domain Corpus

SB043 expands the read-only verifier corpus with realistic positive and negative fixture material for transcript, runtime, Office, business-analysis, and artifact evidence lanes.

These files are test fixtures only. They represent caller-supplied in-memory evidence and must not be treated as instructions to read files, call connectors, execute commands, mutate process state, or contact external systems. Secret-like samples use low-risk placeholders such as `password=fixture-password`.
