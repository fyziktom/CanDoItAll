# QA prompt

For each subbundle, verify that the claimed behavior is exercised through the production path. Reject API-only proof when UI proof is required. Reject skipped live tests reported as live proof. Reject manual-transition tests as representative runtime proof. Confirm code-first ratio with an explicit start SHA.
