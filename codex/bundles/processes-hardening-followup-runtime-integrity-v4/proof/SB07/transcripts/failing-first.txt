# SB07 failing-first.txt

Failing-first test added before the production router guard:

```powershell
dotnet test tests/CanDoItAll.Tests.Integration/CanDoItAll.Tests.Integration.csproj --filter "FullyQualifiedName~ArtifactDispositionRouter_SB07_INV_001" --no-restore -v minimal
```

Failure excerpt:

```text
Failed CanDoItAll.Tests.Integration.ProcessRunAutomationDispatchServiceTests.ArtifactDispositionRouter_SB07_INV_001_blocks_missing_own_required_artifact_even_with_negative_branch
Assert.Null() Failure: Value is not null
Actual: DispatchBranchOutcome { Key = no_go, Title = No-go, Description = Route blocking product defects to no-go. }
```

This demonstrates the old failure mode: a review step with a negative no-go branch could route despite missing its own required evidence artifact.
