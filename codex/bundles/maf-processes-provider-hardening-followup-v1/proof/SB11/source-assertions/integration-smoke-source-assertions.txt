SB11 source assertions

- Process subprocess projection now writes parent-run-scoped Markdown evidence instead of reusing the child subprocess artifact managed path.
- Parent subprocess artifact projection records normalized projection lineage and projection identity hash on the parent artifact.
- Process integration tests map child artifact expectations through the published child definition id, then complete parent and child steps with real workspace-backed required artifacts.
- Baseline runtime seed completion now materializes required artifact expectations before completion and forces Markdown managed paths for Markdown artifact contracts.
- Template seeding imports nested template dependency definitions before publishing dependent templates.
- Project-structure asset writeback detection distinguishes explicit hard-required tools from negated prose and no longer treats "do not call project_structure_asset_create" as a required asset writeback.
- Process artifact expectation resolution now uses the normalized non-null title for nullable correctness.
- No rendered UI route changed; browser validation is N/A.
