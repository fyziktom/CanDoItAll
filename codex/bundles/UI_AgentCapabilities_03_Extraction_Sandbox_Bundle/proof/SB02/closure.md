# SB02 sandbox closure

Status: PASS. SB00 and SB01 passed before implementation. The existing sandbox now has an explicit typed catalog/capabilities discriminator. Absence/unknown tokens preserve catalog defaults, scenario/layout/agent/team normalization and replace-history. The real catalog markup remains in CatalogSpecimen; the route delegates to exactly one specimen owner. No new project or production effect service is introduced.

Capabilities uses deterministic identities, explicit named scenario tokens, immutable samples and the actual moved surface/list/avatar/tree/tooltips/CSS. The baseline JSON is byte-identical to the SB00 captured rendering snapshot; its inert endpoint/configuration strings are never executed. Missing/malformed targets fail closed, and retry keeps the target. Sample intents only change presentation state and the output log. Application recovery/outcome types remain in Module.

Meaningful RED: the exact public capabilities-query test rendered the catalog before implementation. GREEN: 60 Unit (including existing catalog/query/asset contracts) and 38 Component cases, zero failures/skips. Every execution follows direct production/test builds and current exact discovery. The first fixture-loader failure (named JSON enums), an xUnit API setup error, and value-versus-ImmutableArray-storage assertion correction are retained separately from the initial semantic RED.

The first real browser pass covered all 29 scenarios in both modes. Visual inspection then found the new controls moved the matched top frame. A frozen y-coordinate browser assertion failed; using the existing Grid wrapper corrected this. A subsequent premature tree-attribute assertion was changed to await the actual Blazor render, with its failed evidence preserved.

Final browser-SB02-final2 passes all 29 scenarios in Parity and Fast at 1600 x 1000, with zero page errors. It verifies x=125 and y=222.59375 frame coordinates; kind/proof coverage; pending disabled controls; raw access draft/search preservation; tag/type/assignment filtering and reset; tree expansion; focusable endpoint title/aria text; recovery/acknowledgement actions; reload and replace-history; catalog compatibility; actual theme HTTP delivery; owned process shutdown. Normal/Fast and corrected Parity warning screenshots were visually inspected. Remaining failed/long/acknowledgement images and DOM are retained for closure review.

Fast keeps exactly the existing UI, UiSandbox and Conversations source roots plus BaseLib compiled assets. No Module/broad Components root was added. Parity links production output; separate bin/obj and compiled/runtime mode guards remain. Asset hashes and sizes are recorded, without inferring speed.

Complete proposed-source secrets: 245 existing matches, zero added/removed. Portability: 81 reviewed occurrences (80 distinct fingerprints) for inert captured descriptions/configuration and explicit ordinal query comparisons; final no-write enforcement passes with 14383 findings. No scanner/exclusion changed. Broader stable checkpoint is running once for the shared avatar assembly/reference invalidation; it gates program closure and precedes timing. Historical documentation debt remains explicit.

SB03 may proceed sequentially after broad validation has settled; no competing build/test process may run during comparable watch measurements. Overview remains gated on program closure.

Final visual follow-up: [long-content correction](long-content-review.md). The two CSS-only fixes, meaningful browser RED/GREEN, byte-identical DLLs and refreshed pre-measurement source hashes complete the visual gate. Final authoritative sandbox browser run is browser-SB02-long-green.
