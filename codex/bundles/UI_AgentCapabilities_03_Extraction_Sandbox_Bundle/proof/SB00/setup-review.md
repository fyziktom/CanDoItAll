# SB00 setup and observation calibration

No measurement or source movement is claimed by setup. The current correctness closure is the predecessor gate. The baseline owner builds passed, followed by frozen discovery/execution of 30 Unit and 49 Components cases. The evaluated UI graph has 11 projects, no cycles and no forbidden dependency. Scoped CodeAnalytics independently has no cycles/diagnostics but cannot resolve the transitive project graph or all generated Razor declarations; actual evaluated references and composition builds are authoritative.

The rendering-only fixture uses its own PostgreSQL database and file-store tree. Its public receipt and immutable snapshot are retained; credentials and environment files stay private under ignored task storage. No model, connector, proof diagnostic or chat prompt is invoked. The selected stable target renders three marked capability cards from a representative real catalog (29 agents, 129 capabilities).

Setup failures are preserved separately: an incorrect test-helper delegate signature; an unnecessary agent model override rejected by the existing pricing guard, corrected to the provider default; the database guard expecting the untruncated fixture prefix, corrected to its exact generated database identity; and the kind-badge predicate matching both kind and tag. None is a semantic RED or a timed product failure. The snapshot source, application APIs and production layout were not changed to bypass them.

The first full-app calibration screenshot was inspected at 1600 x 1000. The selected heading, all three access labels, primary kind/proof badges, endpoint and card title are visible inside the viewport; three cards fit side by side and tree/card scroll ownership remains unchanged. Narrow the badge predicate to exclude the independent single tag badge. Every edit remains in the actual future moved renderer/list/CSS source. Generated production and Fast CSS hashes match entry.

The full-app calibration startup is excluded from the cold series because prerequisite compilation and graph inventory were still underway. Once calibration passes, seal the harness, fixture and nine definitions, run three independent process-cold launches, then three forward/reverse repetitions of each edit with no competing build/test process.

## Protocol v1 invalidation

The third attempted cold run exposed delayed startup-database confirmation after the first visible capability DOM. The harness then waited on a disappearing search field and could not service the normal Continue action. Native watch showed no source change or restart. MainLayout.DatabaseProfiles.cs confirms the startup prompt is loaded asynchronously and can replace the route. All v1 cold observations are retained but excluded from the accepted baseline, including the first two completed attempts, because their readiness criterion could accept the pre-confirmation view.

Protocol v2 requires acknowledgement through the real Continue button, observes the existing public browser-state acknowledgement, checks every visible predicate, and applies the frozen navigation settlement before recording readiness. Short bounded input reads allow the loop to service the prompt without forcing a click or changing application state directly. Three fresh cold launches and the entire warm matrix will use this corrected harness. No production fix is required.
