# SB03 semantic follow-up map

| Contract | Frozen public observation | RED | GREEN |
|---|---|---|---|
| Responsive rendered closure | Responsive_filter_layout_matches_full_app_at_large_desktop; real FilterBar Split at 1600 x 1000; Parity and Fast cases | browser-responsive-red: computed column in both modes. browser-responsive-green: still column after layered class generation. | browser-responsive-cascade-green: row, center, space-between in both modes; all 29 scenario checks per mode remain. |
| Production list consumer | Actual AgentDetailsDialog capabilities tab; assignment through the real list saves/reconciles; explicit verify becomes available | No new production defect; initial browser harness incorrectly assumed draft-only assignment and failed. Retain it only as setup evidence. | browser-final-consumers-green; real moved list, contained card content and no automatic diagnostic/chat. |

Case totals are this run's discovery/observation receipts, never architecture invariants. The earlier RED/GREEN map remains in the 02G child; moved-child action and sandbox query/geometry evidence remain in SB01/SB02.
