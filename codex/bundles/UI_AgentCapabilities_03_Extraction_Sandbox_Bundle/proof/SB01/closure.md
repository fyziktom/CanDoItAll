# SB01 extraction closure

Status: PASS. The accepted SB00 baseline was sealed before movement. Entry HEAD remains b540b465cf408dd9c9adeb939538b78ced9bc7bb with owned unstaged changes; no history operation occurred.

Seven capabilities renderer/contract/CSS files were moved, as recorded in movement.json. No compatibility wrapper or old list remains. Application operation and Curator state stay in the module. The real details editor and extracted surface consume the same list.

The first standalone build exposed missing Forms imports and the Curator avatar child outside the allowed graph. The existing public Emits_curator_intent_only_when_ready test failed after the initial move. The service-free avatar action was moved unchanged to the existing Conversations component assembly, retaining its namespace. Its originating AppComponents assembly now references Conversations, with no reverse edge. See avatar-child-review.md. The regression and shared avatar interaction tests pass.

Direct Release builds: Conversations.Components, AppComponents, AgentFramework.UI, broad AgentFramework.Components, Modules.AgentFramework, Web, UiSandbox Parity and Fast, Unit, Components and Integration: all eleven passed. Exact frozen owning execution: 93 Unit, 124 Components, 112 Integration/API, zero failures or skips. Discovery, commands, expanded results and RED/GREEN transcripts are retained here. A final whole stable checkpoint is required once the remaining source settles because the shared avatar assembly identity and AppComponents project graph changed.

The evaluated UI graph has eleven nodes, no cycles and no forbidden reference. Scoped CodeAnalytics reports no diagnostics or blocking errors; its limited generated/reference scope is explicitly recorded. Actual generated UI scoped CSS contains both moved renderer styles and the new display:contents anchor. Tree/card scroll ownership is preserved.

Real Web browser acceptance at 1600 x 1000 passed. Normal, details and recovered-warning screenshots were visually inspected: controls and proof/assignment actions remain legible, details show the real editor, and the acknowledgement warning occupies the detail body. One assignment, two explicit diagnostics and one actual Curator chat were recorded. Acknowledgements perform no diagnostic/chat replay. Zero browser errors. The fixture exited normally; ports 5273 and 17301 are closed.

Complete proposed-source portability required 23 added/23 stale path-sensitive findings, all reviewed relocated MCP labels/filters or explicit ordinal comparisons. After the reviewed baseline refresh, final enforcement without write passed with 14302 unchanged findings. No scanner or exclusion changed. Historical documentation debt remains explicit; this is bounded phase closure, not repository merge readiness.
