# Baseline fixture recipe

Retained executed test-support recipe, not a production project. Reconstruct under the repository .mcp-state/capa03-fixture folder so its relative project reference resolves. It creates only isolated task data. Retained receipt/snapshot are public; credential files remain ignored and private. No diagnostic or model call is performed.
