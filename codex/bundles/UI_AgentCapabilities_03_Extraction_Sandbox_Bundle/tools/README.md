# Reproducing the direct observation

Use an isolated local workspace on the same frozen machine/SDK/live sibling revisions. Do not apply the historical pre-extraction patch over another developer's changes. The SB00 source receipt plus the 02G committed basis and final-source.patch.gz reconstruct the accepted pre-move source; SB00 retained source hashes must match before reproducing that baseline. Current post-extraction probes use the post paths in proof/SB03/planned-edits-post.json.

The safe fixture recipe is in baseline-fixture/README.md. Generate a private local database/environment file with that recipe; never copy credentials into proof. The public baseline snapshot and hash are retained in proof/SB00 and in the sandbox fixture. Install the pinned Tailwind tooling and use the existing package scripts documented by UiSandbox. Keep Parity/Fast source modes and separate bin/obj configuration.

Copy the retained direct-watch.cjs and the accepted plan to a private run directory, install/locate the pinned Playwright version, and set CAPA_PLAYWRIGHT to its local module path. The harness accepts --plan, --host (pre-fullapp, post-fullapp, parity, fast), --phase (calibrate, cold, warm), --repetition and --output. Full-app runs additionally take --environment pointing to the private fixture environment. Use each phase sequentially: calibration, three cold repetitions, then one complete warm matrix. The warm phase itself executes every frozen edit/repetition/forward-reverse observation. No managed refresh tool or manual browser reload belongs in the timed loop.

run-sb03-responsive.py records the exact executed orchestration and prerequisite checks; paths under .mcp-state describe this run's private workspace, not a committed runtime dependency. summarize-sb03.py validates each accepted ledger, all sample identities and source hashes. Their transcripts preserve commands, UTC boundaries and failures. The accepted measurement plan supersedes the earlier incomplete-asset post plan for comparisons; do not silently reuse that excluded series.

After shutdown, regenerate both themes through their normal CLI inputs if necessary and require the frozen source-derived asset hashes. This run's Fast watcher left a transient generated output at shutdown; normal generation restored its exact hash, as recorded in asset-restoration.json. Preserve failed/interrupted attempts and stop only owned processes. Do not delete user data or shared caches.
