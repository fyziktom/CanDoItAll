# SB02 public behavior inventory

Freeze before production implementation: CanDoItAll.Tests.Components.AgentFramework.CapabilitiesSandboxTests.Capabilities_query_renders_the_real_surface_without_feature_services. Navigate the actual sandbox router to its capabilities query; the current catalog-only implementation must fail this visible behavior assertion. A missing type/build/setup error is not semantic RED.

Additional semantic selections, discovered and frozen after compiling their public contracts: typed specimen defaults; every stable capability scenario token round-trip; malformed/missing targets stay explicit; deterministic IDs/kinds/proof matrix; baseline fixture preservation; catalog scenario/layout/selection compatibility; controlled assignment and acknowledgement intents; raw access draft and filters survive presentation changes; recovery/busy action matrix; replace-history query normalization. Browser executes the same matrix in Parity and Fast including scope/scroll/tooltips and asset mode.

The only sample state owner is the capability specimen. Rendering DTOs contain opaque identities, labels and eligibility flags; application outcome state machines remain outside the sandbox. No production service is registered.

Compiled owning names are frozen from CapabilitiesSandboxContextTests and CapabilitiesSandboxTests discovery before execution. The direct query RED is retained in proof/SB02; scenario cases verify public markup/eligibility, raw draft/filter retention, typed sample interactions, missing-target retry and catalog normalization.
