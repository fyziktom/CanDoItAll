# Validation, discovery and invalidation

Read docs/testing.md, CI, engineering and current portability/documentation/security rules before edits. Freeze exact names and discovery before each owning run; counts describe execution, not architecture. Zero discovery or a mismatch is failed setup. Build affected test projects before listing. Retain commands/timestamps/results/skips/failures.

SB00 reads the Capabilities-02 owning-case index as historical selection input, not fresh proof. Run current rendering/state/page/list cases and owning catalog sandbox/query/asset selections for the baseline. Mutation gates need not rerun before assembly/import/contract movement invalidates them. Freeze actual selectors and counts from discovery before execution; do not prescribe a permanent 125-case invariant.

SB01 directly builds UI, broad Components, Module and Web plus any changed contract project. Run exact CapabilitiesSession, Surface, Host, Mutation, RecoveryEffects, Effects, ReadLifecycle, capabilities-only selection-fail-closed, AgentCapabilityList, AgentEditorCapabilityComposition and owning page cases. Run assignment/proof Unit and real file-store/SQL/API Integration selections because moved presentation records are consumed by operations. No private-field reflection, uninitialized objects, file-shape or interface/partial-count tests.

SB02 directly builds UiSandbox Parity and Fast plus changed production projects. Freeze failing-first public test names for specimen/query round-trip; omitted specimen preserving catalog; unknown specimen normalization; valid capabilities target; explicit missing-target failure; browser reload context; typed recovery intents; local filters/raw draft through snapshot changes; scenario transitions; no production service resolution. Preserve catalog scenario/asset-guard tests. Prove actual light composition/dependency direction and use real renderer controls without production registrations.

SB03 inspects full-app Surface and AgentDetailsDialog's actual list, plus sandbox capabilities normal/failure/missing/empty/long-content/kinds/proofs/pending/recovery/Curator states in both modes, catalog regression and reload/query behavior at 1600x1000. Inspect normal and relevant open production dialogs; sandbox records intents instead of opening production dialogs or writing data. Check CSS scope, tooltip/assets and first-viewport/scroll owners. Execute the matched direct-watch matrix and keep failed observations.

Run portability self-tests, scanner and complete proposed-source enforcement; documentation; complete proposed-source/artifact secret scans. Include untracked proposed code. Decode UTF-16/decompress transcripts; no suffix/size truncation hiding source. Fix genuine portability defects, baseline only intentional reviewed deltas and finish enforcement without write-baseline. Preserve inherited historical documentation findings, add no tracked .log/.pid/generated output. Do not weaken gates.

Use scoped CodeAnalytics/dependency proof before/after movement, backed by real project files and composition builds. Record unavailable-tool/partial-resolution limits. One named solution build may be justified after the changed assembly graph stabilizes. Whole stable tests still require a named semantic invalidation; do not repeat every suite from habit. Keep live siblings, no package-snapshot substitution.

Every work unit records Pass/Fail/Blocked, owned files, input outcomes, proof and reopen triggers. Retain compact proof with explicit Git inclusion and full manifest/link checks; .mcp-state-only files do not count. Report semantic route readiness, deterministic rendering, light graph, sandbox behavior, measured watch behavior and implemented bookmarkability separately.
