# Test Impact Inventory

Expected test areas:

- `ProcessRunAutomationDispatchServiceTests` artifact validation and projection tests.
- `ProcessAgentExecutionBoundaryArchitectureTests` architecture/source scans.
- New focused helper tests for each validation rule family.
- Regression tests for required artifact satisfaction statuses.
- Source scans for Process Core/driver-pack absence.
- Source scans for prohibited viewport proof paths.

Codex must update with exact test names and command transcripts during execution.
