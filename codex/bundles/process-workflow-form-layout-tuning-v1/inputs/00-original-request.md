# Original Request

Raw user request from 2026-05-31:

```text
Use [$candoitall-bundle-workflow](C:\\Users\\lucys\\.codex\\skills\\candoitall-bundle-workflow\\SKILL.md) to solve this:
In processes page in Steps tab we have ugly layouts of forms for setup of steps. Analyze it and use [$imagegen](C:\\Users\\lucys\\.codex\\skills\\imagegen\\SKILL.md) to create proposal of UI layout of those forms for each of those forms separaterlly. 
Use Tabs to organize long forms like process step details. It is too long. it is better to split it to tabs like basic info, permissions, etc. 
Then repair also forms in Workflows page in Editor of workflow. It need same tunning as forms in processes. Do not do any special styling. use our components. It is more about correct layout of components, using space effective way and other best practice.
```

## Raw Notes

| Raw note | Exact wording |
| --- | --- |
| `N001` | "In processes page in Steps tab we have ugly layouts of forms for setup of steps." |
| `N002` | "Analyze it and use imagegen to create proposal of UI layout of those forms for each of those forms separaterlly." |
| `N003` | "Use Tabs to organize long forms like process step details. It is too long. it is better to split it to tabs like basic info, permissions, etc." |
| `N004` | "Then repair also forms in Workflows page in Editor of workflow. It need same tunning as forms in processes." |
| `N005` | "Do not do any special styling. use our components. It is more about correct layout of components, using space effective way and other best practice." |
