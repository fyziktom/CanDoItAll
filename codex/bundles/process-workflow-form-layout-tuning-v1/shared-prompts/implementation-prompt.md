# Implementation Prompt

Implement only the current subbundle.

Use existing shared components and keep changes layout-focused. Preserve model bindings, event handlers, test IDs, services, persistence, and runtime behavior. Prefer `Tabs`, `TabsItem`, `Grid`, `Stack`, `FormField`, `FormSection`, `Cluster`, `Split`, `SurfaceCard`, `PanelCard`, `Button`, `StatusBadge`, and `EmptyState`. Do not add a custom style layer or decorative UI.

Before editing, reread the exact source references in the subbundle. After editing, run the required build and source assertions, then update `reviews/01-execution-report.md`.
