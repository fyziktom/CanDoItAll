# Codex guide — GPT-5.6 Sol xHigh

## Start

```powershell
git status --short
git branch --show-current
git rev-parse HEAD
git merge-base development HEAD
git log --oneline --decorate -12
dotnet --info
```

Re-anchor if HEAD differs from `79a6c0d7de353acfae3511e2671baf7daee2b498`.

## Working method

1. Read the active subbundle completely.
2. Inspect callers and ownership through CodeAnalysis MCP.
3. Read relevant C# modular-refactoring, .NET, and architecture-governor skills.
4. Add a failing characterization test.
5. Make the smallest cohesive owner-boundary fix.
6. Run focused tests, neighboring tests, and Release build.
7. Run architecture guards.
8. Write proof and session handoff.
9. Stop if acceptance is not fully evidenced.

## Prohibitions

- No broad runtime facade.
- No source-kind switch in MAF.
- No process semantics in MAF.
- No malformed-authority fallback to no governance.
- No fixed organization-scope cleanup for a project-scoped lease.
- No instance-local CAS claim for shared scoped file storage.
- No ordinary-chat UI/API/features.
- No zero usage when a provider attempt reported tokens.
- No test deletion or weakened assertion to make a gate green.
