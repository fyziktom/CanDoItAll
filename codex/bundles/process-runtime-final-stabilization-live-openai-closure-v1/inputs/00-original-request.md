# Original request

Codex completed the previous bundle. Check whether processes now work like before. If not, identify what refactoring broke and prepare a follow-up bundle. Codex should run a test with OpenAI; OPENAI_API_KEY is in env and other live env vars should be set by Codex or safe defaults should be used. Stabilize process functionality before attempting further separation of process runtime core/dispatcher into separate libraries.
