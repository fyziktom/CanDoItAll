Implement this as a stabilization closure bundle. Do not perform new Process Runtime Core extraction. Keep changes code-first and concise. Run the live OpenAI test with explicit bounded env values. Treat code-first ratio as an anti-churn signal, not as a functional runtime failure. Final output must clearly state whether the branch is stabilization merge-ready, live-provider-blocked, or runtime-blocked.
