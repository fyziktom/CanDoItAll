# QA Prompt

Validate the changed behavior by running targeted builds/tests and browser checks. For UI proof, open the process page, select a process, open `Manager chat`, open the run selector modal, and inspect layout, clipping, layering, and control usability. For process validation, attempt a random small-app delivery scenario and report whether agents can progress without manual code help.
