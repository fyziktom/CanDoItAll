# Normalized Requirements

- R1: Add a `Manager chat` detail tab immediately after `Exchange`.
- R2: Use `ChatWorkspacePanel` or the existing AgentFramework chat contract for the conversation UI.
- R3: Resolve manager chat agent without duplicating manager assignment state.
- R4: Provide a run-selection modal reachable from the manager chat tab.
- R5: Include selected run context in manager prompts and invocation metadata.
- R6: Keep run selection lightweight and avoid loading large runtime trees just to populate the selector.
- R7: Add a default feature/function implementation subprocess template.
- R8: Wire the new subprocess into the main software-development process flow through existing subprocess import semantics.
- R9: Add or update tests proving template import/order and manager chat resolution behavior.
- R10: Validate browser behavior for the new tab and modal.
- R11: Attempt a real small-app process validation and document blockers, manager interventions, and next steps.
