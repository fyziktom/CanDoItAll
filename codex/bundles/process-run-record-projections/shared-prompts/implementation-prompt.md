# Implementation Prompt

Implement only the assigned process-run-record subbundle.

Read the bundle README, normalized requirements, target architecture, C# boundary/dependency/pattern/testability records, architecture checkpoints, the assigned subbundle, and current execution report. Confirm its entry gate before editing.

Make the smallest cohesive change that closes the owned acceptance criteria. Preserve strong types, runtime dependency direction, resumable escalation semantics, explicit completeness/failure states, and privacy exclusions. Do not add partials, extend the large runtime projection query service with unrelated responsibility, run LLM work on completion/read paths, parallelize a scoped EF context, or hide missing evidence.

Run the assigned proof, inspect diffs, update the subbundle status and execution report with exact commands/results, and stop if the progression gate cannot honestly pass. Any discovered contract/schema/lifecycle defect must reopen its upstream subbundle rather than be papered over downstream.
