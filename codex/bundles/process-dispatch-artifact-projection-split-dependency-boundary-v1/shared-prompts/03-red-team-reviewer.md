# Red-Team Prompt

Try to reject the bundle if any of these are true:

- A coordinator still receives the full `ProcessRunAutomationDispatchService`.
- Source-family order is not proven.
- Candidate state mutation is duplicated.
- File IO is hidden inside a pure-sounding rules/planner class.
- A public/Core/driver API was added.
- UI files or mobile/small/medium proof artifacts appeared.
- Build or focused projection tests were skipped.
- A critical gate only contains prose without source/test proof.
