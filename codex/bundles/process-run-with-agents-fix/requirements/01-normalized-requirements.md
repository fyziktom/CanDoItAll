# Normalized Requirements

| ID | Requirement | Source | Owner Subbundle | Validation |
| --- | --- | --- | --- | --- |
| REQ-001 | Stabilize process runtime lifecycle so `StartRunAsync` and outbox automation dispatch cannot leave untracked background work holding SQLite or scoped services after tests/hosts dispose. | Original request, teardown failures | 01 | Focused process-service and outbox tests pass repeatedly without `primary.db` lock failures. |
| REQ-002 | Restore process-template validation test executability after the `AddProcessesModule(IServiceCollection, IConfiguration)` signature change. | Template test compile failure | 01 | `CanDoItAll.Mcp.Processes.Tests` focused template tests compile and run. |
| REQ-003 | Re-align dispatch tests with current production signatures and strict governed completion behavior instead of leaving stale reflection failures. | Dispatch test failures | 01 | Focused `ProcessRunAutomationDispatchServiceTests` subset passes or has explicitly updated expectations. |
| REQ-004 | Define a deterministic calculator process model that includes scope, architecture, implementation, QA rejection, repair, QA approval, and release notes. | Original request | 02 | Process definition/template has explicit branch outcomes and dependencies for reject/repair/approve flow. |
| REQ-005 | Use branch outcome keys compatible with mock QA outputs: `repairs-required` and `approved`. | Mock runtime contract | 02, 04 | Branch transition tests prove selected branch IDs resolve from the mock outcome marker. |
| REQ-006 | Make mock-agent staffing deterministic for process roles and launch plans. | Role mismatch findings | 03 | Launch plan candidate selection binds every required mock role to the intended technical agent. |
| REQ-007 | Keep mock mode settings-gated and prevent real LLM/provider execution during mock process tests. | Original request | 03, 05 | Tests set `AgentFramework:ProcessMockAgents:Enabled=true` and assert mock provider/agent IDs are used. |
| REQ-008 | Ensure mock runtime outputs satisfy dispatcher completion contracts without weakening real governed process validation. | Dispatcher strictness findings | 04 | Dispatcher accepts mock artifacts/outcomes only through explicit deterministic evidence mapping. |
| REQ-009 | Persist required process artifacts for every automated step so `TransitionStepAsync` can complete steps through normal artifact validation. | Artifact projection findings | 04 | Process artifact records exist for expected artifacts and are linked to step runs/expectations. |
| REQ-010 | Add a true E2E integration test that starts a process run, drives durable automation dispatch, executes multiple mock agents, follows QA repair iteration, and finishes the run. | Missing E2E proof | 05 | Test asserts final run status, step statuses, branch selection, artifact records, execution runs, and no dead-letter outbox records. |
| REQ-011 | Capture actionable diagnostics when a process cannot finish, including missing agent binding, missing branch outcome, missing artifact, missing required tool evidence, and outbox dead-letter state. | Original request, observed failures | 04, 05 | Tests exercise at least one negative path and assert the failure reason is specific. |
