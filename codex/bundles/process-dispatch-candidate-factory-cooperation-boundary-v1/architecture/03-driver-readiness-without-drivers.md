# Driver Readiness Without Drivers

This bundle should prepare the future driver story by naming candidate/evidence intent, not by creating driver code.

Documentation-only map:

| Runtime concept | Future driver relevance | Production API now? |
| --- | --- | --- |
| `AgentWorkspaceToolProfileKind.SoftwareDevelopment` | Future SW-dev and .NET/Rust driver pack selection | No |
| `QualityValidation` | Future validation/proof helper drivers | No |
| `BusinessAnalysis` | Future document/spreadsheet/business-analysis drivers | No |
| `ProcessArtifactHandoff` | Future evidence input driver read mode | No |
| `A2A/Handoff/Hybrid` | Future manager verification / cooperation tools | No |
| `Subprocess` route | Future nested process driver boundary | No |
| `Workflow` route | Future workflow-driver bridge | No |

The only allowed production effect is clearer internal naming around candidate route facts. Do not introduce `IProcessDriverPack`, `IProcessHelperDriver`, driver registry, or driver implementation.
