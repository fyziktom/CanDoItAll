interface:
  display_name: "CanDoItAll Bundle Workflow"
  short_description: "Coordinate bundle preparation, execution, gates, and closure"
  default_prompt: "Deliver the requested outcome through the CanDoItAll bundle workflow: prepare or repair the bundle only as needed, execute each gated subbundle, record proof, and close only when evidence matches the original inputs."
