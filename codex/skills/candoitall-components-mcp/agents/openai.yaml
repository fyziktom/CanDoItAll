interface:
  display_name: "CanDoItAll Components MCP"
  short_description: "Choose shared components and correct library setup"
  default_prompt: "Use $candoitall-components-mcp to choose supported components, assets, and layout patterns for the target desktop viewport before adding custom structure."
