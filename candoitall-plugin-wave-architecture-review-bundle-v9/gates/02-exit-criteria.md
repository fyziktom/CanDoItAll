# Exit criteria
The architecture is considered phase9-ready only when:
- all HG-* automated gates pass,
- MG-01 manual gate is explicitly signed off,
- runtime build/test evidence is attached from a real .NET environment,
- QA confirms that no repeated blocker was “fixed” only by moving symbols around.
