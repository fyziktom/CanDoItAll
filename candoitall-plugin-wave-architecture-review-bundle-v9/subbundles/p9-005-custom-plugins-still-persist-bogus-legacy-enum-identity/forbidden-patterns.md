# Forbidden patterns
- Save flows still assign provider/resource legacy enums through fallback logic
- EnsureLegacyResourceKind / ResolveLegacyResourceKind remain active
- Custom plugins require fake legacy enum identity
