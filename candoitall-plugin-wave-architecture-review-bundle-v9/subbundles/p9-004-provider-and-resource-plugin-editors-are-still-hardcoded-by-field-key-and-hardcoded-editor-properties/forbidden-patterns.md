# Forbidden patterns
- @switch(field.Key) remains in shared resource/provider editors
- Unknown connector fields require page/model edits to render or persist
- Shared editor models remain only a hardcoded property bag for current plugins
