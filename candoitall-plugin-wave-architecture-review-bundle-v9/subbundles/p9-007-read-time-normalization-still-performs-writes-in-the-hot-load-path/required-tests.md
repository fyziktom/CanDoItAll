# Required tests
- load-path test proving no DB writes occur during graph load
- one-shot repair/migration tests for remaining compatibility data
- concurrency smoke proving repeated load calls are read-only
