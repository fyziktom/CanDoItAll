Senior QA review:
The repeated blockers are real and the prior false-green result justifies stronger hard gates. The next iteration must not accept compatibility shims in active load/save paths as closure. Custom plugin scenarios need explicit proof tests before the plugin wave can start.
