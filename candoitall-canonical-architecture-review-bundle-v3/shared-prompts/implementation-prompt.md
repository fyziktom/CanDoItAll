# Implementation Prompt

Implement only the current subbundle.

- Follow the exact source references before editing code.
- Prefer the smallest correct change.
- Keep `ProjectPartyAssignment` as the canonical owner for node-scoped party links.
- Keep Workbench on project-facing contracts instead of direct CRM/HR persistence types.
- Update proof and bundle reporting immediately after each completed phase.
