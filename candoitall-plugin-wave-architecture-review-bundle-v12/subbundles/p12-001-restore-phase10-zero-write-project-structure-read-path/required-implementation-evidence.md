# Required implementation evidence

- `ProjectStructureAssemblyService.LoadAsync(...)` is read-only,
- `ProjectStructureProjectionMaintenanceService` exists again,
- DI registers the maintenance service,
- the zero-write proof tests are present.
