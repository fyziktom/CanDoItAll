# Required implementation evidence

- generic provider field ids exist again (for example `provider-config-{field.Key}`),
- generic resource field ids exist again (for example `resource-config-{field.Key}`),
- Settings page passes `Secrets="secrets"` into the shared editor,
- shared editor checkbox branch carries `data-testid`,
- unknown-manifest proof tests are present again.
