# Acceptance

- Restore the provider/resource unknown-manifest proof tests.
- Restore deterministic generic test ids for unknown provider/resource manifest fields.
- Restore secret option wiring from Settings page into the shared connector field editor.
- Ensure the shared checkbox branch exposes the same generic `data-testid` hook as other field types.
- Keep all shared connector field types exercised in the proof tests.
