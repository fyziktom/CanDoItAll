# Original Request

## Raw Request

> great job.
> now analyze other nodes in process canvas. most of them must have options like participants roles, so they need also multiple connections in/out. improve them same way as branch. Our main goal is to have full possibility to edit all processes via canvas primarily. Start with analysis and identifying on all of them what inputs outputs they must have. what inputs and ouptus are many2many and what are just single2many or many2single. Tehn you can do subbundles to improve each of them.

## Preserved Literal Signals

- `other nodes in process canvas`
- `most of them must have options like participants roles`
- `multiple connections in/out`
- `same way as branch`
- `full possibility to edit all processes via canvas primarily`
- `Start with analysis`
- `identifying on all of them what inputs outputs they must have`
- `many2many`
- `single2many`
- `many2single`

## Continuation Context

- This request builds on the already-created branching-router work and explicitly broadens the scope from one advanced node family to the remaining process-canvas node families.
- The user is asking for bundle repair and expansion first, not immediate implementation in this turn.
