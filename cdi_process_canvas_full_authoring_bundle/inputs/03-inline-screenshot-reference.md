# Inline Screenshot Reference

## Referenced Thread Screenshots

- `Thread screenshot: branch-router port circles drifting off their badges under zoom`
  - Purpose: proves that badge-relative anchors must remain stable under zoom and should be computed from the badge geometry, not from a fixed edge slot.
- `Thread screenshot: role node output circle used as the source for branch-router input`
  - Purpose: proves that role nodes need explicit participant and authority ports, not only a generic node body.
- `Thread screenshot set from the earlier branching work`
  - Purpose: shows the already-shipped advanced node style that must become generalized rather than re-invented per node family.

## Planning Use

- These screenshots are design constraints and closure references, not final proof for this bundle.
- Execution must replace them with fresh Playwright screenshots captured from the live `/processes` route after each UI-bearing subbundle lands.
