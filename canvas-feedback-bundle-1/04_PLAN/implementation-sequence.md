# Execution sequence

## Completed sequence

1. Extract and normalize the testing notes from the uploaded `.docx`.
2. Verify the current implementations for:
   - floating canvas windows
   - shared create composer
   - project structure toolbox
   - existing component and Playwright tests
3. Implement shared icon token mappings and icon-only floating window controls.
4. Implement the sectioned wizard-style composer shell with internal scrolling and a persistent action row.
5. Apply the local toolbox layout correction in the project structure page.
6. Add focused component coverage for the shared floating window chrome.
7. Validate with targeted component and Playwright test runs.
