# Extracted notes

## Sources

- `E:\CanDoItAll-notes-1.docx`
- `C:\Users\lucys\OneDrive - TechnicInsider\Produkty\CanDoItAll\feedbacks\feedback1-post-implementation-review.docx`

Non-empty extracted notes: `14`

## Initial feedback notes

- `N001` Some forms to add nodes are so long I cannot see save button. Better create generic Steps/Wizard canvas window and split form into logical steps.
- `N002` Standard blocks toolbox is not possible to scroll inside; layout should be more practical, more like Visual Studio toolbox.
- `N003` Those buttons must be icons only.
- `N004` Min = `minimize` and Max = `expand`.
- `N005` Reset = `restart_alt`.
- `N006` Hide = `visibility_off`.

## Post-implementation review notes

- `N007` Icons in those buttons must be with black text. You must validate with screenshot that you can see the icons in those buttons.
- `N008` Toolbox:
- `N009` Use just only that dark part as whole window. We are doubling the texts in the white background window.
- `N010` When I click on some section it does not open items under it. It should act as accordeon.
- `N011` Search is working, but I cannot scroll in results, so if there is multiple of them I cannot see others out of the scope.
- `N012` Icons in searched items do not display as icon, but as text. It must be icon.
- `N013` Nodes with files still do not have proper background colors.
- `N014` When I double click on pdf it still opens it behind the canvas (you must test it with maximized canvas).
