# QA Prompt

Validate only the current subbundle from `cdi_process_workspace_containment_bundle`.

Required questions:

- Does the list pane scroll internally without pushing the whole page or modal taller?
- Does the detail pane keep the selected tab content inside its own scrollable region?
- When the templates dialog is open, do both panes remain readable and independently scrollable?
- After Mermaid zoom or pan, is any part of the rendered graph visible outside its preview surface?
- Are any texts clipped, overlapped, or pushed behind neighboring chrome?
