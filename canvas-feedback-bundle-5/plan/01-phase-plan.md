# Phase Plan

1. Restore the shared floating-window chrome for the blocks explorer.
   - enable the standard header and actions
   - retune the toolbox body so the dark explorer still fits the shared shell
   - make accordion state intentional instead of passive
2. Validate search scrolling and screenshot proof.
   - confirm the browser-visible section bodies and search results remain readable
   - capture screenshots for the default explorer and the filtered or scrolled explorer
   - update the execution report with exact test commands and screenshot artifact paths
