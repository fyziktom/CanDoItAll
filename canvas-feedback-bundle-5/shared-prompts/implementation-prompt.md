# Implementation Prompt

Implement the current feedback5 subbundle only.

Keep the change small and compatible with the shared canvas workbench patterns. Reuse `CanvasFloatingWindow` for window chrome and drag behavior instead of inventing toolbox-local controls. Preserve create action wiring, selected-source placement, and the dark toolbox item language while making the explorer behave like a real accordion and a real scrollable window.
