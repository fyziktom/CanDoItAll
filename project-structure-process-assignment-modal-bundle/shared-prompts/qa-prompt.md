# QA Prompt

Validate the implemented process assignment modal against the attached screenshot and raw notes.

Questions to answer in the execution report:

- Does the staffing modal occupy the full viewport with no narrow centered-dialog feel?
- Are the header, progress, actions, left role rail, role-card grid, and bottom selected-agent panel all visible and aligned at large desktop width?
- Can unresolved roles be identified quickly, and do `Assign agent` actions open the manual agent picker?
- Does the agent picker show search, tag filtering, favorite toggles, and favorites-first ordering?
- At narrower width, does the modal remain readable without overlapping text or clipped actions?
- Does `Review and start` remain gated until required roles are resolved and assignments are reviewed?
