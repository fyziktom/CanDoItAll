# ProjectStructurePage Follow-Up Request

Date captured: `2026-03-31`

Raw request:

```text
great.
now do the refactor of [ProjectStructurePage.razor](src/CanDoItAll.Modules.Workbench/Pages/ProjectStructurePage.razor) .
- you must replace divs/buttons/spans/p...etc. with our components and prepared tailwind styles. If something is very specific add proper shared style/component for it.
- there is one special part that must be rebuild at all. The "Project Structure Toolbox" must work as kind of treeview. each item must be just oneline/row (icon + text + tooltip). First create common treeview component. Then you can apply it in this case.
- then split this page to logical subparts/components.
- preview od node data should be improved (probably some component factory style, based on type of node). We can use some power of C# and thanks to reflection create better content of detail window of node based on node props (we can display props that has some specific attribute in model).

analyze all and with [$candoitall-bundle-workflow](C:\\Users\\lucys\\.codex\\skills\\candoitall-bundle-workflow\\SKILL.md) prepare and execute and validate bundle to do all those changes.
```
