# Normalized Requirements

- `REQ-01`: Create a real initiative bundle in `CanDoItAll` that captures the Zyphonote findings, parity scope, dependency order, and proof rules before code changes continue.
- `REQ-02`: Add an explicit project and solution navigation surface to CodeAnalytics so direct project-reference questions do not depend on usage-weighted dependency views.
- `REQ-03`: Reuse the sibling `CanDoItAll.CodeAnalsis` libraries and extend them where required instead of copying analysis logic into the host repo.
- `REQ-04`: Add the missing SharpTools-style analysis tools that are still absent from the current MCP flow and that are necessary to replace SharpTools for the benchmark scenarios.
- `REQ-05`: Stabilize or bypass the failing member-focused summary path so the updated MCP can answer realistic method-behavior questions without generic invocation failure.
- `REQ-06`: Update the host MCP reinstall and registration flow so Codex and VS Code receive the new CodeAnalytics tool surface.
- `REQ-07`: Add repo-managed skill guidance or equivalent Codex-facing documentation so the new tool flow is used correctly for architecture questions versus method-behavior questions.
- `REQ-08`: Re-run the same five Zyphonote scenarios against the updated MCP and record the result as the final closure proof.
- `REQ-09`: Capture any remaining trouble as new findings under the bundle so the next iteration starts from explicit evidence instead of chat memory.
