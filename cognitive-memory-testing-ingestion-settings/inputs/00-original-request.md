# Original Request

The follow-up request from the Codex thread on 2026-05-17:

> Great. this worth to use [$candoitall-bundle-workflow](C:\Users\lucys\.codex\skills\candoitall-bundle-workflow\SKILL.md) to solve this as followup bundle.
> I will have to test it, so I need you to load some testing data. do not place those test into code as I said. Load them just via APIs.
> Let the instance running so I can test it and setup same db in visual studio so I can start it from there with same db too.
> I think we are missing api for setting up the db sources, add them first.
> Add on UI of cognitive memory some settings tab, where I can setup when memory will do automatic sorting (if just during night, or if app is not active for some time, or excactly scheduled moments), then there must be buttons that I can use to start ingest data from projects, or processes, based on my option.
> We must have also tab there to ingest another sources. For example I just need to drop there some file, or add link to website and it must ingest it. During that it must show some status and progress bar, so I can see that somethings is truly happening.
