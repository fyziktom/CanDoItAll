# Normalized Requirements

- Create a brand-new isolated SQLite-backed test environment under repo artifacts and keep the run separate from existing working data.
- Treat `CanDoItAll_CrmHr_CodexBundle_Final` as the planning source artifact only; do not rerun its implementation bundle.
- Reconstruct the delivered initiative into a CanDoItAll project hierarchy using the project-structure path, with a clear umbrella project and useful subprojects.
- Cover the full delivered scope strongly enough that a senior project manager could control execution from the resulting plan.
- Add AI-agent participants and assign them to the tasks where automated or agentic ownership is appropriate.
- Preserve enough dependency, priority, and execution information that the plan is not only archival but operational.
- Review the resulting canvas with Playwright MCP and screenshots at least on the umbrella project and on detail surfaces when needed.
- Improve the plan if the first visual or control pass is weak.
- Record MCP-specific and general findings in the requested findings folders.
