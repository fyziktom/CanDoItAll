// Notes: Canvas widget registry sketch.
// Keep comments in English.

export function buildWidgets(payload) {
  // payload.widgets: [{ id, title, items: [...] }]
  // Compute rects, store in renderer.widgets for hit testing.
}
