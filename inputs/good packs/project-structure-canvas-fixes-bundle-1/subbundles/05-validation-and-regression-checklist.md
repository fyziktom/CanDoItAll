# Sub-Bundle 5: Validation And Regression Checklist

- [ ] validate full-width canvas behavior against the running watch session
- [ ] validate toolbar reachability in default and moved-window states
- [ ] validate drag, resize, minimize, normalize, hide, and show for each moved window
- [ ] validate persisted window state after reload
- [ ] validate empty selection, single selection, and multi-selection states
- [ ] validate multi-select common actions for status, progress, marker, and priority
- [ ] validate file/media preview and local-open behavior by file type
- [ ] perform keyboard and accessibility spot checks
- [ ] write evidence into the bundle `artifacts` folder

Acceptance result:

- the bundle contains enough evidence to prove the structure canvas problems were fixed without breaking existing behavior
