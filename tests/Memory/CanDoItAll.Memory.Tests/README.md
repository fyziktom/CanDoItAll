# CanDoItAll.Memory.Tests

Tests Memory operations, provider drivers, response limits, source ingestion, security,
retention, persistence, workers, observability, and AgentFramework integration.

```powershell
dotnet test .\tests\Memory\CanDoItAll.Memory.Tests\CanDoItAll.Memory.Tests.csproj --configuration Release
```

Live external-provider tests are separate from the deterministic routine gate.
