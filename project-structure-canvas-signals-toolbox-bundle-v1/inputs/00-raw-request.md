# Raw Request

## Source

- Chat request on April 1, 2026 in the active Codex thread.

## Exact User Notes

1. `Please increase size of the markers glyph in second layer of right click menu. they are tiny as you can see on screenshot. The glyph can be 3 times larger than now. just assure that it will not increse size of that circle around glyph. that must stay same size.`
2. `then add new toolbox window for markers/progress and maybe few more things.`
3. `I like how they have it in xmind. look at the second screenshot. something like that we must add as floating window over canvas (add button to show it to top canvas toolbar).`
4. `It works that I must select some node, and when I click on marker or priority icon, etc it adds it to node.`
5. `Assure we can add multiple markers to node.`

## Artifact Notes

- Screenshot A showed the current hive context menu with marker submenu glyphs that read too small inside the fixed circular marker badge.
- Screenshot B showed an XMind-style marker palette with grouped sections such as recent items, priority, progress, faces, people, arrows, and symbols. The user asked for composition inspiration, not a literal style copy.

## Scope Signals

- The request is UI-heavy and includes a data-behavior change because the current project-structure node model only stores one marker.
