# Implementation Prompt

Implement the project-structure canvas signals bundle in subbundle order. Keep marker storage additive through metadata, keep the legacy single-marker fields synchronized to a primary marker, enlarge only the marker glyph in second-layer menu presets, and add a grouped floating toolbox for markers, progress, priority, and clear/reset helpers opened from the top toolbar. Do not move on from a UI phase without real browser proof.
