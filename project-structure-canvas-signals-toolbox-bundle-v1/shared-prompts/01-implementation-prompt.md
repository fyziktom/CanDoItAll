# Implementation Prompt

Implement the project-structure canvas signals bundle in subbundle order. Do not skip the metadata compatibility bridge. Keep marker storage additive, keep the legacy single-marker fields synchronized to the primary marker, enlarge only the marker glyph in second-layer menu presets, and add a toolbar-triggered floating toolbox for markers, progress, priority, and clear/reset helpers. Prove each UI-relevant phase in a real Playwright session before moving on.
