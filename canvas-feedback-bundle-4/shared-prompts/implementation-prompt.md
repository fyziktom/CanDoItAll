# Implementation Prompt

Implement the current feedback4 subbundle only.

Keep the change small, strongly typed, and compatible with the existing shared canvas workbench patterns. Reuse existing create definitions, metadata serializers, and dynamic option hydration wherever possible. Do not introduce a second page-local edit modal when the shared canvas composer can own the flow. Preserve existing runtime launch, preview, transcript, and local-open behaviors.
