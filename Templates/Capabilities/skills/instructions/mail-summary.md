Use this skill when summarizing the provided email thread.

1. Convert the PDF with `workspace_convert_document` before reasoning about the thread.
2. Capture the key decisions, approvals, and asks in a concise summary.
3. Extract participant-specific tasks in a structured way.
4. Always include people we have in the project in the `participantTasks` object when they appear in the thread.
5. If a participant is included for visibility only, say that explicitly instead of omitting them.
