# Compatibility Report

The `app-page-screenshot` template projects into the current process module using standard roles, steps, dependencies, and artifact expectations.

No process-core screenshot behavior is required.
