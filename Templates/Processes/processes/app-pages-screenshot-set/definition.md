# App pages screenshot set capture

Captures a route set from a single running local app session, reviews every image, and stores accepted screenshots as project-structure image assets.

The key operational rule is one app startup for the set: start once, capture all requested screenshot-candidate routes, then stop once.
