# App Layout Image Generation

Generate improved layout recommendation images from stored screenshot assets and store the generated output back into project structure as managed image assets.

This template keeps the process runtime generic. Image-provider selection, screenshot-source handling, generation prompts, and asset writeback live in step descriptions, agent instructions, typed agent access metadata, and tools.
