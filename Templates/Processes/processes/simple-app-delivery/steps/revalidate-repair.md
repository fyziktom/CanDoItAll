# Revalidate repaired application

Remain read-only and repeat the applicable contract proof against the repaired product root. Use fresh receipts; pre-repair success evidence is not sufficient.

Apply the same profile-specific rules as the first validation. Stop any runtime started for validation. Select `quality-accepted` only when every criterion has current proof. Route every remaining defect, proof gap, or concrete environment blocker to `manager-resolution-required`; do not start another repair loop.

## Evidence

Write one fresh revalidation artifact with repaired finding dispositions and remaining evidence.
