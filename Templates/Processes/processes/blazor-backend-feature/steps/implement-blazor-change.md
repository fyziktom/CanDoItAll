# Implement Blazor backend feature

Implement backend/domain/API behavior, data contracts, services, persistence, and tests as required by project structure. Use workspace file and command tools only inside the approved product root; if that product root is absent, create it there. Never scaffold, restore, build, or write product files under backup, agent-evidence, observation, process-definition, launch-plan, or other evidence-only folders. Run dotnet restore before build-sensitive changes when dependencies changed. Run dotnet build after implementation. Do not mark complete if explicit acceptance criteria are omitted, if output is written outside the approved product root, or if project-structure constraints are narrowed without a decision record.

## Evidence

Record commands, files, URLs, screenshots, console messages, errors, assumptions, and project-structure writeback references as applicable.

