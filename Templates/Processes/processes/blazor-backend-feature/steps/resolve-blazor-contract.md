# Resolve Blazor delivery contract

Read project structure and run prompt. Resolve Blazor mode (SSR, WASM, or WASM PWA), product root, requested external output root or current-run managed output root, run folder, acceptance criteria, routes/screens, backend/API expectations, exclusions, and required evidence writeback targets. Keep the app description generic and do not add sample-specific assumptions.

## Evidence

Record commands, files, URLs, screenshots, console messages, errors, assumptions, and project-structure writeback references as applicable.


