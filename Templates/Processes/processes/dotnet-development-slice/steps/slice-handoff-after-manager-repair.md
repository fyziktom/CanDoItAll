# Hand off manager-repaired implementation slice

Produce a parent-ready handoff only from an accepted manager-assisted quality-repair child handoff.

Re-read the authoritative `slice-scope-packet` and include a compact criterion coverage table. For every core interaction, state transition, calculation, persistence/recovery behavior, and required output, name the owning production surface and the current independent proof.

Include the manager diagnosis, changed product surfaces, restore/build/test results, runtime/browser proof when applicable, bughunt evidence when used, remaining non-blocking risks, and the parent next action.

Also cite the exact managed artifact ref for the required `dotnet-solution-context`; downstream work must use that approved topology or inspect the grounded product root, never guess a conventional layout.

Do not close from an intermediate escalation packet, a green unchanged build, test counts without owning-source coverage, or a quality-repair no-go.
