# Validate first build and test discovery

Run restore/build and a targeted test command or discovery command. Capture the command, exit code, warnings, and any remaining setup risks.

Keep this as setup validation. Do not start a web app, run browser proof, edit feature behavior, or replace generated starter UI/content unless the parent process explicitly requires runtime proof as part of solution setup.
