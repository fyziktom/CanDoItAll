# Fresh Validation Request

- Reinstall and reload the MCP stack if needed.
- Create a new managed SQLite profile in `artifacts` for the run.
- Use `candoitall-canonical-architecture-review-bundle-v2` as input for a project-structure backfill, but do not execute that source bundle.
- Reconstruct the source bundle as an umbrella project plus properly identified subprojects, with AI agents, task ownership, and best-guess timespans.
- Validate umbrella and subproject mindmaps with Playwright MCP screenshots.
- Judge whether a senior project manager can control execution from the resulting plans.
- If the plan is not manager-usable, improve it first and record the remaining issues as the next phase of this bundle.
