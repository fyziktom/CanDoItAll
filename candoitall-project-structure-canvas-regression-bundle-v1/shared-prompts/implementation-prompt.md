# Implementation Prompt

Implement only the scoped repair proven necessary by the current failing canvas regression evidence. Do not widen into speculative refactors. Reproduce the failing behavior first, fix the smallest correct surface, then rerun the exact MCP proof that failed.
