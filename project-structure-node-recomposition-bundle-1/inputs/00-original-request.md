# Original Request

The user request below is copied from the thread, with the inline image called out separately in `01-source-artifacts.md`.

> use [$candoitall-bundle-workflow](C:\\Users\\lucys\\.codex\\skills\\candoitall-bundle-workflow\\SKILL.md) to prepare, validate and execute and validate bundle that will improve composition of the nodes in the mindmap.
> there must be new button in the toolbar, that will start automatic recomposition of the nodes across the available space.
> Sometimes when I load lots of information via mcp then they are added as you can see on the image. It is correct, but mindmpap goes in practically just one direction and there is lots of not used space around the root node. It should go more in the circle to allow zoom in and still see maximum of information without moving around.
> I would not do it fully automated, because it can disturb some structure, that people will adjust by themselfs. But sometimes they might need it. I think it makes sense to add it as button, that can call that reorganisation. user must select node reorganisation will be called for all nodes bellow it. reorganisation does not mean reconnection of the nodes. It is just their positioning to be shaped to use space more efficient way.
> analyze in preparation stage possible approaches and known algorithms that are widely used for solving those problems like we have.
> system must be sure there are no colisions of the nodes on the canvas.
> Then propose best architecture and based on [$candoitall-bundle-workflow](C:\\Users\\lucys\\.codex\\skills\\candoitall-bundle-workflow\\SKILL.md) prepare and execute it until it is fully done.
