# Normalized Requirements

- `R001` The canvas help overlay must open centered in the visible stage area instead of top-aligned.
- `R002` The markdown file create flow must allow either typed markdown content or an uploaded markdown file.
- `R003` File nodes must use clearer subtype-colored backgrounds that align with their file-type cues while keeping text and badges readable.
- `R004` PDF preview dialogs opened from the project structure canvas must render inside the canvas shell, above the canvas content.
- `R005` The implementation must preserve existing project structure preview and local-open behaviors.
- `R006` The fixes must be proven with focused component tests and bundle execution reporting.
