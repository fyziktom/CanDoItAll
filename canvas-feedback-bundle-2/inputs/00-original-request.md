# Original Request

Prepare `Feedback2.docx` as a real CanDoItAll bundle and execute it.

The source feedback is:

- help window must be centered to the visible canvas area
- create markdown must allow direct text entry or file upload
- file nodes such as Excel, PDF, and Word must use stronger subtype-colored backgrounds
- PDF preview must open inside the canvas instead of behind it
