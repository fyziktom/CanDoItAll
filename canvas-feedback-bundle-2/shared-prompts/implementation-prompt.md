# Implementation Prompt

Implement `canvas-feedback-bundle-2` with the smallest shared change set that closes all four feedback items. Keep the work inside the shared project structure workbench and shared canvas layers. Do not invent page-local hacks for overlay placement, do not weaken the typed create-definition pipeline, and do not replace the file palette system with subtype-specific one-off CSS selectors. Update the bundle execution report with the proof you actually ran.
