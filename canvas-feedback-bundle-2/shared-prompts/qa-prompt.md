# QA Prompt

Validate that:

- the help overlay opens centered inside the visible canvas area
- markdown creation still accepts typed content and now also exposes file upload
- PDF, Excel, and Word-like file nodes read as distinct file types through their backgrounds
- badges and node text remain readable after the palette changes
- PDF preview opens above the canvas rather than behind it
- preview dialogs still support close, open in new tab, and local-open behavior where available
- spacing, hierarchy, and alignment still look intentional for a new user
