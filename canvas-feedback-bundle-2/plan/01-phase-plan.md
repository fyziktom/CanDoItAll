# Phase Plan

1. Fix the shared help overlay positioning because it is isolated to shared canvas CSS and low-risk.
2. Update the markdown create definition so the requested upload capability exists before any broader browser validation.
3. Tune the shared file-node palette gradients to make subtype color cues visible.
4. Move preview-style dialogs into the canvas overlay slot and verify PDF preview behavior still opens without navigation.
5. Run focused component validation and update the execution report.
