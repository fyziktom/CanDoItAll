# Normalized requirements

- Reconcile live proof with checked-in artifacts before trusting prior closure prose.
- Close true canonical dependency modeling, DB integrity, lifecycle, and durable side-effect gaps.
- Keep optimistic concurrency, differential persistence, and the existing decomposition improvements.
- Require real build, test, migration, and browser artifacts for closure.
