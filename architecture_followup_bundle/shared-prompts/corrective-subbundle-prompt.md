# Corrective subbundle prompt

A review gate failed. Create a corrective subbundle before continuing.

The corrective subbundle must include:
- the failed gate and exact trigger;
- the blocking issue stated as a concrete architectural defect;
- the minimum change set needed to unblock the queue safely;
- explicit proof required to rerun the failed gate;
- an updated dependency chain that blocks downstream work until the correction passes.
