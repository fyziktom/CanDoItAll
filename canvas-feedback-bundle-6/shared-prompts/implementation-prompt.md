# Shared Implementation Prompt

```text
Implement the current feedback6 subbundle only.

Keep the work inside the shared project-structure context menu stack. Do not invent a new menu system. Update the existing CanvasLib menu rendering, sizing, and browser proof so the result closes the raw screenshot feedback note by note.

Guardrails:
- preserve action ids and metadata behavior
- keep progress, marker, and priority semantics intact
- if a submenu proof expectation cannot be met, update the bundle first instead of silently narrowing the scope
- final proof must include browser screenshots and exact dotnet test commands
```
