# Shared QA Prompt

```text
QA this feedback6 implementation against the raw notes, not against assumptions.

Check:
- every progress preset icon shows the intended in-icon text or intentional blank state for Start
- enlarged progress and marker submenu items stay non-overlapping
- nested submenu layers stay out of the toolbar band
- submenu loading indicator appears before the layer opens and cancels cleanly
- screenshots show the revised hive-style composition rather than a simple radial ring

If anything is only partially proven, mark it as a gap instead of accepting it.
```
