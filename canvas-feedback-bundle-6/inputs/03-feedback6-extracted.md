# Extracted Feedback

Source: `C:\Users\lucys\OneDrive - TechnicInsider\Produkty\CanDoItAll\feedbacks\feedback6.docx`

## Notes

- `N001` Improve progress right click menu. Put text in item (10%, 20%, N/A, etc) except Start where we can remove text at all, inside of the circle icon. It will save space and we can increase size of icon of progress (green filling circle with white center).
- `N002` Increase diameter so hexagons do not overlaying each other
- `N003` Increase size of markers right click menu. Items must not overlaying each other.
- `N004` Assure that second layer menu will never underlay the toolbar. It must be draw that it will be draw on available space of the canvas and all items can be visible.
- `N005` Add small delay before opening second/third layers of right click menu. When user has mouse on some item, it will start displaying small loading circle (similar as our progress icon set) and if they will wait about 500ms (when circle get full) then it opens second layer menu.
- `N006` Tune hexagons composition, so they will be properly next to each other like in bees hive. On this image you can see example how does similar menu looks like in one game. As you can see the menu is also organized based on categories. Use this as inspiration for layout of right click menu. Do not use same visual style. Just sizing of hexagons and their proper organization.

## Extracted Media

- `C:\repositories\CanDoItAll\canvas-feedback-bundle-6\inputs\feedback6-media\image1.png`
- `C:\repositories\CanDoItAll\canvas-feedback-bundle-6\inputs\feedback6-media\image2.png`
- `C:\repositories\CanDoItAll\canvas-feedback-bundle-6\inputs\feedback6-media\image3.png`
