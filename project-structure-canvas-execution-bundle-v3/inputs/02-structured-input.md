# Structured Input

- Scope:
  Move the active shared workbench runtime to canvas-owned scene rendering, preserve HTML overlays and accessibility layers, and keep ProjectStructure and PromptFactory green.
- Required product outcomes:
  Write-behind view-state persistence, delta move adoption without unconditional reload, centralized CanvasLib asset includes, canvas-owned export, benchmark evidence, and browser proof.
- Closure requirement:
  Bundle documentation, execution report, raw-note closure, and validator gates must match the shipped code and the final regression pack.
- Explicit non-goal:
  Rewrite the original legacy bundle content or remove intentional non-runtime compatibility surfaces that still serve preview and support lanes.
