# Original Request

The user supplied a follow-up screenshot and these tuning notes:

1. Content of the fullscreen assignment dialog must use the whole width.
2. Clicking a specific required role must show the actual main candidate first, then other candidates for that role ordered by score. The last card must be a large plus/icon card that opens the all-agents directory picker.
3. The first item in the left role list must be `All`; selecting it shows a summary overview similar to the current modal, separating assignment work from summary review.
4. Agent cards in recommended/summary views must show badges for `model`, `tools`, `skills`, and `details`. The first three use tooltip service content. `details` opens a readonly details dialog similar to agent settings.

The user asked to solve the larger change with a bundle and subbundles.
