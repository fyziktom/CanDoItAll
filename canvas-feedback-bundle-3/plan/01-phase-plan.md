# Phase Plan

1. Add the runtime launch-plan builder and Windows PowerShell launcher service for supported script and environment nodes.
2. Wire the selection panel to surface normal and elevated PowerShell actions only when a selected node is launchable.
3. Add focused regression coverage for launch-action visibility and command derivation.
4. Run targeted validation and update the execution report with the proof that actually ran.
