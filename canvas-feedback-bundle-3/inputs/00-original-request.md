# Original Request

Prepare `feedback3.docx` as a real CanDoItAll bundle and execute it.

The source feedback is:

- dotnet watch nodes must expose two selection-panel buttons that open PowerShell in the configured project path and run the configured dotnet watch command
- the same launch flow must work for python scripts and other runtime nodes
