# Implementation Prompt

Implement `canvas-feedback-bundle-3` with the smallest shared change set that adds predictable PowerShell launch actions for supported runtime nodes. Keep launch-plan resolution in a dedicated workbench service, keep the page focused on rendering and feedback, and fail explicitly when a node cannot be launched safely. Update the bundle execution report with the proof you actually ran.
