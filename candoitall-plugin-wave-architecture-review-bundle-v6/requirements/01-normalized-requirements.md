# Normalized Requirements

- `R-001` Keep node as the universal carrier and preserve semantic X/Y coordinates and markers as canonical project data.
- `R-002` Restore a single clear canonical owner for every concern before the plugin wave proceeds.
- `R-003` Make node kind, lifecycle, and assignment semantics extensible without enum/switch scatter.
- `R-004` Build the next integration wave on a real plugin/connector platform, not more provider/resource enums.
- `R-005` Strengthen cross-module mutation boundaries and runtime recovery before more integrations join the system.
- `R-006` Add architecture guardrails and a final gate review so the same canonical-model problems do not return.
