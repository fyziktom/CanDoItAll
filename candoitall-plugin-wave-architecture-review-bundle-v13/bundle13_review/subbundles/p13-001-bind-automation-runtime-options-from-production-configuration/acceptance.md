# Acceptance

- Bind `AutomationRuntimeOptions` from a real production configuration section.
- Ensure poll intervals, batch sizes, and MQTT settings are configurable without test-only service overrides.
- Add a documented example configuration section.
- Keep defaults sane when the section is absent.
