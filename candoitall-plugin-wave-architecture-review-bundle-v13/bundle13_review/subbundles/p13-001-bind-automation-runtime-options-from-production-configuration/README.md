# P13-001 — Bind automation runtime options from production configuration

The automation runtime introduced configurable behavior, but the current DI registration does not bind those options from configuration in production code.
