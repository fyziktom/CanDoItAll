
# Acceptance criteria

This item passes only when **all** criteria below are satisfied.

- [ ] Existing project structure data still loads and renders.
- [ ] At least one sample record for each new node family round-trips through persistence with metadata intact.
- [ ] No downstream item needs to invent its own ad-hoc JSON format outside the agreed metadata strategy.
- [ ] Tests prove compatibility, serialization, and default behavior.

## Additional quality gates

- [ ] The implementation respects the normalized decisions for this item.
- [ ] Relevant tests were added or updated.
- [ ] Required screenshots were captured and semantically reviewed.
- [ ] No conflicting parallel architecture was introduced.
