review_kind: canonical-model-review
scope: "canonical-model-refactor branch after CRM/HR + workbench canonical invariant changes"
scores:
  source_of_truth_integrity: 2
  boundary_clarity: 3
  invariant_enforcement: 3
  projection_discipline: 3
  integration_isolation: 2
  runtime_state_separation: 4
  ai_policy_separation: 4
  testable_architecture: 3
  change_safety: 2
  overall_stability: 2
findings:
  critical:
    - "Node-local party truth is dual-written into Workbench metadata and CRM/HR project-party assignments."
  high:
    - "Delete and subtree-transfer flows mutate Workbench structure without reconciling node-scoped assignments."
    - "The universal Workbench node model keeps accumulating unrelated semantics."
  medium:
    - "`NodeKey` is still a raw string bridge instead of a strong cross-module reference."
    - "No integration test spans node-scoped assignments across delete or subtree transfer."
  low: []
  open_questions:
    - "Should node-local party links be canonical in Workbench or in CRM/HR?"
    - "Is ProjectPartyAssignment meant to own node-local relations or only project-level staffing context?"
    - "Is ProjectObjectRecord the intentional long-term universal primitive?"
